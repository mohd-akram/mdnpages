.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "Map.\[at]\[at]species" "JS" "April 10, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Map.\[at]\[at]species - Map[\[at]\[at]species]
.SH SYNOPSIS
The \f[B]\f[CB]Map[\[at]\[at]species]\f[B]\f[R] static accessor property
is an unused accessor property specifying how to copy \f[CR]Map\f[R]
objects.
.SH SYNTAX
.IP
.EX
Map[Symbol.species]
.EE
.SS Return value
The value of the constructor (\f[CR]this\f[R]) on which
\f[CR]get \[at]\[at]species\f[R] was called.
The return value is used to construct copied \f[CR]Map\f[R] instances.
.SH DESCRIPTION
The \f[CR]\[at]\[at]species\f[R] accessor property returns the default
constructor for \f[CR]Map\f[R] objects.
Subclass constructors may override it to change the constructor
assignment.
.RS
.PP
\f[B]Note:\f[R] This property is currently unused by all \f[CR]Map\f[R]
methods.
.RE
.SH EXAMPLES
.SS Species in ordinary objects
The \f[CR]\[at]\[at]species\f[R] property returns the default
constructor function, which is the \f[CR]Map\f[R] constructor for
\f[CR]Map\f[R].
.IP
.EX
Map[Symbol.species]; // function Map()
.EE
.SS Species in derived objects
In an instance of a custom \f[CR]Map\f[R] subclass, such as
\f[CR]MyMap\f[R], the \f[CR]MyMap\f[R] species is the \f[CR]MyMap\f[R]
constructor.
However, you might want to overwrite this, in order to return parent
\f[CR]Map\f[R] objects in your derived class methods:
.IP
.EX
class MyMap extends Map {
  // Overwrite MyMap species to the parent Map constructor
  static get [Symbol.species]() {
    return Map;
  }
}
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Map\f[R]
.IP \[bu] 2
\f[CR]Symbol.species\f[R]
