.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Map.\[at]\[at]species" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]Map[\[at]\[at]species]\f[B]\f[R] static accessor property
is an unused accessor property specifying how to copy \f[V]Map\f[R]
objects.
.SH Syntax
.IP
.nf
\f[C]
Map[Symbol.species]
\f[R]
.fi
.SS Return value
.PP
The value of the constructor (\f[V]this\f[R]) on which
\f[V]get \[at]\[at]species\f[R] was called.
The return value is used to construct copied \f[V]Map\f[R] instances.
.SH Description
.PP
The \f[V]\[at]\[at]species\f[R] accessor property returns the default
constructor for \f[V]Map\f[R] objects.
Subclass constructors may override it to change the constructor
assignment.
.RS
.PP
\f[B]Note:\f[R] This property is currently unused by all \f[V]Map\f[R]
methods.
.RE
.SH Examples
.SS Species in ordinary objects
.PP
The \f[V]\[at]\[at]species\f[R] property returns the default constructor
function, which is the \f[V]Map\f[R] constructor for \f[V]Map\f[R].
.IP
.nf
\f[C]
Map[Symbol.species]; // function Map()
\f[R]
.fi
.SS Species in derived objects
.PP
In an instance of a custom \f[V]Map\f[R] subclass, such as
\f[V]MyMap\f[R], the \f[V]MyMap\f[R] species is the \f[V]MyMap\f[R]
constructor.
However, you might want to overwrite this, in order to return parent
\f[V]Map\f[R] objects in your derived class methods:
.IP
.nf
\f[C]
class MyMap extends Map {
  // Overwrite MyMap species to the parent Map constructor
  static get [Symbol.species]() {
    return Map;
  }
}
\f[R]
.fi
.SH See also
.IP \[bu] 2
\f[V]Map\f[R]
.IP \[bu] 2
\f[V]Symbol.species\f[R]
