.\" Automatically generated by Pandoc 3.10
.\"
.TH "CSPVIOLATIONREPORT" "3JS" "March 23, 2026" "JavaScript" "JavaScript Reference Manual"
.SH NAME
CSPViolationReport \- CSPViolationReport
.SH SYNOPSIS
The \f[CR]CSPViolationReport\f[R] dictionary of the Reporting API
represents a report that is generated when a document violates its
Content Security Policy (CSP).
.PP
Reports of this type can be observed from within a page using a
\f[CR]ReportingObserver\f[R], and a serialized version can be sent to
reporting server endpoints.
.SH INSTANCE PROPERTIES
.TP
\f[B]body\f[R]
The body of the report.
This is an object with the following properties:
.RS
.TP
\f[B]blockedURL\f[R]
A string representing either the type or the URL of the resource that
was blocked because it violates the CSP.
.TP
\f[B]columnNumber\f[R]
The character position in the line of the script at which the violation
occurred.
.TP
\f[B]disposition\f[R]
A string indicating whether the violation was enforced or only reported.
This can have the value \f[CR]\(dqenforce\(dq\f[R] for violations of
policies set with \f[CR]Content\-Security\-Policy\f[R], or
\f[CR]\(dqreporting\(dq\f[R] for policies set with
\f[CR]Content\-Security\-Policy\-Report\-Only\f[R].
.TP
\f[B]documentURL\f[R]
A string representing the URL of the document or worker in which the
violation was found.
.TP
\f[B]effectiveDirective\f[R]
A string representing the directive whose enforcement uncovered the
violation.
.TP
\f[B]lineNumber\f[R]
The line number in the script at which the violation occurred.
.TP
\f[B]originalPolicy\f[R]
A string containing the policy whose enforcement uncovered the
violation.
.TP
\f[B]referrer\f[R]
A string representing the URL for the referrer of the resources whose
policy was violated, or \f[CR]null\f[R].
.TP
\f[B]sample\f[R]
A string representing a sample of the resource that caused the
violation, usually the first 40 characters.
This will only be populated if the resource is an inline script, event
handler, or style \(em external resources causing a violation will not
generate a sample.
.TP
\f[B]sourceFile\f[R]
If the violation occurred as a result of a script, this will be the URL
of the script; otherwise, it will be \f[CR]null\f[R].
Both \f[CR]columnNumber\f[R] and \f[CR]lineNumber\f[R] should have
non\-null values if this property is not \f[CR]null\f[R].
.TP
\f[B]statusCode\f[R]
A number representing the HTTP status code of the document or worker in
which the violation occurred.
.RE
.TP
\f[B]type\f[R]
The string \f[CR]\(dqcsp\-violation\(dq\f[R], indicating that this is a
CSP violation report.
.TP
\f[B]url\f[R]
A string representing the URL of the document that generated the report.
.SH DESCRIPTION
CSP violation reports may be created when a webpage attempts to load a
resource that violates a CSP set using the
\f[CR]Content\-Security\-Policy\f[R] or
\f[CR]Content\-Security\-Policy\-Report\-Only\f[R] HTTP headers.
.PP
You can monitor for CSP violation reports within the page that sets the
policy using the Reporting API.
To do this you create a \f[CR]ReportingObserver\f[R] object to listen
for reports, passing a callback method and an (optional)
\f[CR]options\f[R] property specifying the types of reports that you
want to report on.
The callback method is then called with reports of the requested types,
passing a report object.
For CSP violations, the object will be a \f[CR]CSPViolationReport\f[R]
instance (which has the \f[CR]type\f[R] property set to
\f[CR]\(dqcsp\-violation\(dq\f[R]).
.PP
The structure of a typical report is shown below.
Note that we can see the URL of both the page that had its policy
violated (\f[CR]url\f[R]), the document that attempted to load the
resource (\f[CR]body.documentURL\f[R]) and the resource that was blocked
from loading (\f[CR]body.blockedURL\f[R]).
We can also see that the violation was caused by the page attempting to
load a script element with a source from another origin, which violated
the \f[CR]body.originalPolicy\f[R], and that the violation was enforced
(and not just reported).
.IP
.EX
{
  \(dqtype\(dq: \(dqcsp\-violation\(dq,
  \(dqurl\(dq: \(dqhttps://url\-of\-page\-enforcing\-policy\(dq,
  \(dqbody\(dq: {
    \(dqsourceFile\(dq: \f[B]null\f[R],
    \(dqlineNumber\(dq: \f[B]null\f[R],
    \(dqcolumnNumber\(dq: \f[B]null\f[R],
    \(dqdocumentURL\(dq: \(dqhttps://url\-of\-document\-attempting\-to\-load\-resource\-in\-violation\(dq,
    \(dqreferrer\(dq: \(dq\(dq,
    \(dqblockedURL\(dq: \(dqhttps://url\-of\-blocked\-resource.js\(dq,
    \(dqeffectiveDirective\(dq: \(dqscript\-src\-elem\(dq,
    \(dqoriginalPolicy\(dq: \(dqdefault\-src \(aqself\(aq;\(dq,
    \(dqsample\(dq: \(dq\(dq,
    \(dqdisposition\(dq: \(dqenforce\(dq,
    \(dqstatusCode\(dq: 200
  }
}
.EE
.PP
Violation reports may also sent as a JSON object in a \f[CR]POST\f[R]
request to one or more configured reporting server endpoints.
Reporting server endpoint names are specified in the
\f[CR]report\-to\f[R] policy directive of the
\f[CR]Content\-Security\-Policy\f[R] or
\f[CR]Content\-Security\-Policy\-Report\-Only\f[R] header.
Valid endpoint names and their mapping to a particular URL are defined
using the \f[CR]Reporting\-Endpoints\f[R] header.
.RS
.PP
\f[B]Note:\f[R] CSP violation reports sent by the Reporting API, when an
endpoint is specified using the CSP \f[CR]report\-to\f[R] directive, are
similar (but not identical) to the \(lqCSP report\(rq JSON objects sent
when endpoints are specified using the \f[CR]report\-uri\f[R] directive.
The Reporting API and \f[CR]report\-to\f[R] directive are intended to
replace the older report format and the \f[CR]report\-uri\f[R]
directive.
.RE
.PP
The structure of the server report is almost exactly the same as
\f[CR]CSPViolationReport\f[R], except that it additionally includes
\f[CR]age\f[R] and \f[CR]user_agent\f[R] fields.
.IP
.EX
{
  \(dqage\(dq: \(dq176279\(dq,
  \(dqtype\(dq: \(dqcsp\-violation\(dq,
  \(dqurl\(dq: \(dqhttps://url\-of\-page\-enforcing\-policy\(dq,
  \(dqbody\(dq: {
    \(dqsourceFile\(dq: \f[B]null\f[R],
    \(dqlineNumber\(dq: \f[B]null\f[R],
    \(dqcolumnNumber\(dq: \f[B]null\f[R],
    \(dqdocumentURL\(dq: \(dqhttps://url\-of\-document\-attempting\-to\-load\-resource\-in\-violation\(dq,
    \(dqreferrer\(dq: \(dq\(dq,
    \(dqblockedURL\(dq: \(dqhttps://url\-of\-blocked\-resource.js\(dq,
    \(dqeffectiveDirective\(dq: \(dqscript\-src\-elem\(dq,
    \(dqoriginalPolicy\(dq: \(dqdefault\-src \(aqself\(aq;\(dq,
    \(dqsample\(dq: \(dq\(dq,
    \(dqdisposition\(dq: \(dqenforce\(dq,
    \(dqstatusCode\(dq: 200
  },
  \(dquser_agent\(dq: \(dqMozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/145.0.0.0 Safari/537.36\(dq
}
.EE
.SH EXAMPLES
.SS Using the \f[CR]ReportingObserver\f[R] interface
You can obtain a \f[CR]CSPViolationReport\f[R] object by configuring
your page so that a CSP violation will occur.
In this example, we will set our CSP to only allow content from the
site\(cqs own origin, and then attempt to load a script from
\f[CR]apis.google.com\f[R], which is an external origin.
.PP
First, we will set our \f[CR]Content\-Security\-Policy\f[R] header in
the HTTP response:
.IP
.EX
Content\-Security\-Policy: default\-src \(aqself\(aq;
.EE
.PP
or in the HTML \f[CR]<meta>\f[R] element:
.IP
.EX
<\f[B]meta\f[R] http\-equiv=\(dqContent\-Security\-Policy\(dq content=\(dqdefault\-src \(aqself\(aq\(dq />
.EE
.PP
Then, we will attempt to load an external script:
.IP
.EX
\f[I]<!\-\- This should generate a CSP violation \-\->\f[R]
<\f[B]script\f[R] src=\(dqhttps://apis.google.com/js/platform.js\(dq></\f[B]script\f[R]>
.EE
.PP
Finally, we will create a new \f[CR]ReportingObserver\f[R] object to
listen for CSP violations (this will need to be loaded from the same
location, before the script that causes the violation).
.IP
.EX
\f[B]const\f[R] observer = \f[B]new\f[R] ReportingObserver(
  (reports, observer) \f[B]=>\f[R] {
    reports.forEach((violation) \f[B]=>\f[R] {
      console.log(violation);
      console.log(JSON.stringify(violation));
    });
  },
  {
    types: [\(dqcsp\-violation\(dq],
    buffered: \f[B]true\f[R],
  },
);

observer.observe();
.EE
.PP
Above we log each violation report object and a JSON\-string version of
the object, which might look similar to the object below.
Note that the report is an instance of the \f[CR]CSPViolationReport\f[R]
and the \f[CR]type\f[R] is \f[CR]\(dqcsp\-violation\(dq\f[R].
.IP
.EX
{
  \(dqtype\(dq: \(dqcsp\-violation\(dq,
  \(dqurl\(dq: \(dqhttp://127.0.0.1:9999/\(dq,
  \(dqbody\(dq: {
    \(dqsourceFile\(dq: \f[B]null\f[R],
    \(dqlineNumber\(dq: \f[B]null\f[R],
    \(dqcolumnNumber\(dq: \f[B]null\f[R],
    \(dqdocumentURL\(dq: \(dqhttp://127.0.0.1:9999/\(dq,
    \(dqreferrer\(dq: \(dq\(dq,
    \(dqblockedURL\(dq: \(dqhttps://apis.google.com/js/platform.js\(dq,
    \(dqeffectiveDirective\(dq: \(dqscript\-src\-elem\(dq,
    \(dqoriginalPolicy\(dq: \(dqdefault\-src \(aqself\(aq;\(dq,
    \(dqsample\(dq: \(dq\(dq,
    \(dqdisposition\(dq: \(dqenforce\(dq,
    \(dqstatusCode\(dq: 200
  }
}
.EE
.SS Sending a CSP violation report
Configuring a web page to send a CSP violation report is similar to the
previous example.
As before, you need to configure your page so that there is a violation.
.PP
In addition, you also need to specify the endpoint(s) where the report
will be sent.
A server specifies endpoints using the \f[CR]Reporting\-Endpoints\f[R]
response header: these must be secure URLs (HTTPS).
The CSP \f[CR]report\-to\f[R] directive is then used to specify that a
particular endpoint is used for reporting CSP violations:
.IP
.EX
Reporting\-Endpoints: csp\-endpoint=\(dqhttps://example.com/csp\-report\-to\(dq
Content\-Security\-Policy: default\-src \(aqself\(aq; report\-to csp\-endpoint
.EE
.PP
As before, we can trigger a violation by loading an external script from
a location that is not allowed by our CSP header:
.IP
.EX
\f[I]<!\-\- This should generate a CSP violation \-\->\f[R]
<\f[B]script\f[R] src=\(dqhttps://apis.google.com/js/platform.js\(dq></\f[B]script\f[R]>
.EE
.PP
The violation report will then be sent to the indicated endpoint as a
JSON file.
As you can see from the example below, the \f[CR]type\f[R] is
\f[CR]\(dqcsp\-violation\(dq\f[R] and the \f[CR]body\f[R] property is a
serialization of the \f[CR]CSPViolationReport\f[R] object:
.IP
.EX
[
  {
    \(dqage\(dq: 53531,
    \(dqbody\(dq: {
      \(dqblockedURL\(dq: \(dqinline\(dq,
      \(dqcolumnNumber\(dq: 59,
      \(dqdisposition\(dq: \(dqenforce\(dq,
      \(dqdocumentURL\(dq: \(dqhttps://example.com/csp\-report\-to\(dq,
      \(dqeffectiveDirective\(dq: \(dqscript\-src\-elem\(dq,
      \(dqlineNumber\(dq: 1441,
      \(dqoriginalPolicy\(dq: \(dqdefault\-src \(aqself\(aq; report\-to csp\-endpoint\(dq,
      \(dqreferrer\(dq: \(dqhttps://www.google.com/\(dq,
      \(dqsample\(dq: \(dq\(dq,
      \(dqsourceFile\(dq: \(dqhttps://example.com/csp\-report\-to\(dq,
      \(dqstatusCode\(dq: 200
    },
    \(dqtype\(dq: \(dqcsp\-violation\(dq,
    \(dqurl\(dq: \(dqhttps://example.com/csp\-report\-to\(dq,
    \(dquser_agent\(dq: \(dqMozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36\(dq
  }
]
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]ReportingObserver\f[R]
.IP \(bu 2
\f[CR]Content\-Security\-Policy\f[R]
.IP \(bu 2
\f[CR]Content\-Security\-Policy\-Report\-Only\f[R]
.IP \(bu 2
\f[CR]SecurityPolicyViolationEvent\f[R]
.IP \(bu 2
\f[CR]Reporting\-Endpoints\f[R]
.IP \(bu 2
Reporting API
