.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "RTCPeerConnection.sctp" "JS" "March 25, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
RTCPeerConnection.sctp \- RTCPeerConnection: sctp property
.SH SYNOPSIS
The \f[B]\f[CB]sctp\f[B]\f[R] read\-only property of the
\f[CR]RTCPeerConnection\f[R] interface returns an
\f[CR]RTCSctpTransport\f[R] describing the SCTP transport over which
SCTP data is being sent and received.
If SCTP hasn\[cq]t been negotiated, this value is \f[CR]null\f[R].
.PP
The SCTP transport is used for transmitting and receiving data for any
and all \f[CR]RTCDataChannel\f[R]s on the peer connection.
.SH VALUE
A \f[CR]RTCSctpTransport\f[R] object describing the SCTP transport being
used by the \f[CR]RTCPeerConnection\f[R] for transmitting and receiving
on its data channels, or \f[CR]null\f[R] if SCTP negotiation hasn\[cq]t
happened.
.SH EXAMPLE
.IP
.EX
\f[B]const\f[R] peerConnection = \f[B]new\f[R] RTCPeerConnection();

\f[B]const\f[R] channel = peerConnection.createDataChannel(\[dq]Mydata\[dq]);
channel.onopen = (event) \f[B]=>\f[R] {
  channel.send(\[dq]sending a message\[dq]);
};
channel.onmessage = (event) \f[B]=>\f[R] {
  console.log(event.data);
};

\f[I]// Determine the largest message size that can be sent\f[R]

\f[B]const\f[R] sctp = peerConnection.sctp;
\f[B]const\f[R] maxMessageSize = sctp.maxMessageSize;
.EE
.SH SEE ALSO
.IP \[bu] 2
WebRTC
.IP \[bu] 2
\f[CR]RTCPeerConnection\f[R]
.IP \[bu] 2
\f[CR]RTCDataChannel\f[R]
.IP \[bu] 2
SCTP
