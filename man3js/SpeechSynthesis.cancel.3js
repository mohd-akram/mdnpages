.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "SPEECHSYNTHESIS.CANCEL" "3JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SpeechSynthesis.cancel \- SpeechSynthesis: cancel() method
.SH SYNOPSIS
The \f[B]\f[CB]cancel()\f[B]\f[R] method of the
\f[CR]SpeechSynthesis\f[R] interface removes all utterances from the
utterance queue.
.PP
If an utterance is currently being spoken, speaking will stop
immediately.
.SH SYNTAX
.IP
.EX
cancel()
.EE
.SS Parameters
None.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] synth = window.speechSynthesis;

\f[B]const\f[R] utterance1 = \f[B]new\f[R] SpeechSynthesisUtterance(
  \(dqHow about we say this now? This is quite a long sentence to say.\(dq,
);
\f[B]const\f[R] utterance2 = \f[B]new\f[R] SpeechSynthesisUtterance(
  \(dqWe should say another sentence too, just to be on the safe side.\(dq,
);

synth.speak(utterance1);
synth.speak(utterance2);

synth.cancel(); \f[I]// utterance1 stops being spoken immediately, and both are removed from the queue\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
Web Speech API
