.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "SourceBufferList" "JS" "March 30, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SourceBufferList \- SourceBufferList
.SH SYNOPSIS
The \f[B]\f[CB]SourceBufferList\f[B]\f[R] interface represents a simple
container list for multiple \f[CR]SourceBuffer\f[R] objects.
.PP
The source buffer list containing the \f[CR]SourceBuffer\f[R]s appended
to a particular \f[CR]MediaSource\f[R] can be retrieved using the
\f[CR]MediaSource.sourceBuffers\f[R] property.
.PP
The individual source buffers can be accessed using the bracket notation
\f[CR][]\f[R].
.SH INSTANCE PROPERTIES
.TP
\f[B]SourceBufferList.length\f[R] \f[I](read\-only)\f[R]
Returns the number of \f[CR]SourceBuffer\f[R] objects in the list.
.SH INSTANCE METHODS
\f[I]Inherits methods from its parent interface,
\f[CI]EventTarget\f[I].\f[R]
.SH EVENTS
.TP
\f[B]addsourcebuffer\f[R]
Fired when a \f[CR]SourceBuffer\f[R] is added to the list.
.TP
\f[B]removesourcebuffer\f[R]
Fired when a \f[CR]SourceBuffer\f[R] is removed from the list.
.SH EXAMPLES
This example shows how to access the active source buffers of the
\f[CR]MediaSource\f[R] connected to an already playing
\f[CR]HTMLVideoElement\f[R].
.IP
.EX
// Video is an already playing video using a MediaSource srcObject
const video = document.querySelector(\[dq]video\[dq]);
const mediaSource = video.srcObject;
const sourceBufferList = mediaSource.activeSourceBuffers;
for (const sourceBuffer of sourceBufferList) {
  // Do something with each SourceBuffer, such as call abort()
  sourceBuffer.abort();
}
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]MediaSource\f[R]
.IP \[bu] 2
\f[CR]SourceBuffer\f[R]
