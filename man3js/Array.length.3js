'\" t
.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Array.length" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Array.length - Array: length
.SH SYNOPSIS
.PP
The \f[B]\f[VB]length\f[B]\f[R] data property of an \f[V]Array\f[R]
instance represents the number of elements in that array.
The value is an unsigned, 32-bit integer that is always numerically
greater than the highest index in the array.
.SH VALUE
.PP
A non-negative integer less than 232.
.PP
.TS
tab(@);
l l.
T{
Writable
T}@T{
yes
T}
T{
Enumerable
T}@T{
no
T}
T{
Configurable
T}@T{
no
T}
.TE
.SH DESCRIPTION
.PP
The value of the \f[V]length\f[R] property is a non-negative integer
with a value less than 232.
.IP
.nf
\f[C]
const listA = [1, 2, 3];
const listB = new Array(6);

console.log(listA.length);
// 3

console.log(listB.length);
// 6

listB.length = 2 ** 32; // 4294967296
// RangeError: Invalid array length

const listC = new Array(-100); // Negative numbers are not allowed
// RangeError: Invalid array length
\f[R]
.fi
.PP
The array object observes the \f[V]length\f[R] property, and
automatically syncs the \f[V]length\f[R] value with the array\[cq]s
content.
This means:
.IP \[bu] 2
Setting \f[V]length\f[R] to a value smaller than the current length
truncates the array \[em] elements beyond the new \f[V]length\f[R] are
deleted.
.IP \[bu] 2
Setting any array index (a non-negative integer smaller than 232) beyond
the current \f[V]length\f[R] extends the array \[em] the
\f[V]length\f[R] property is increased to reflect the new highest index.
.IP \[bu] 2
Setting \f[V]length\f[R] to an invalid value (e.g.\ a negative number or
a non-integer) throws a \f[V]RangeError\f[R] exception.
.PP
When \f[V]length\f[R] is set to a bigger value than the current length,
the array is extended by adding empty slots, not actual
\f[V]undefined\f[R] values.
Empty slots have some special interactions with array methods; see array
methods and empty slots.
.IP
.nf
\f[C]
const arr = [1, 2];
console.log(arr);
// [ 1, 2 ]

arr.length = 5; // set array length to 5 while currently 2.
console.log(arr);
// [ 1, 2, <3 empty items> ]

arr.forEach((element) => console.log(element));
// 1
// 2
\f[R]
.fi
.PP
See also Relationship between \f[V]length\f[R] and numerical properties.
.SH EXAMPLES
.SS Iterating over an array
.PP
In the following example, the array \f[V]numbers\f[R] is iterated
through by looking at the \f[V]length\f[R] property.
The value in each element is then doubled.
.IP
.nf
\f[C]
const numbers = [1, 2, 3, 4, 5];
const length = numbers.length;
for (let i = 0; i < length; i++) {
  numbers[i] *= 2;
}
// numbers is now [2, 4, 6, 8, 10]
\f[R]
.fi
.SS Shortening an array
.PP
The following example shortens the array \f[V]numbers\f[R] to a length
of 3 if the current length is greater than 3.
.IP
.nf
\f[C]
const numbers = [1, 2, 3, 4, 5];

if (numbers.length > 3) {
  numbers.length = 3;
}

console.log(numbers); // [1, 2, 3]
console.log(numbers.length); // 3
console.log(numbers[3]); // undefined; the extra elements are deleted
\f[R]
.fi
.SS Create empty array of fixed length
.PP
Setting \f[V]length\f[R] to a value greater than the current length
creates a sparse array.
.IP
.nf
\f[C]
const numbers = [];
numbers.length = 3;
console.log(numbers); // [empty x 3]
\f[R]
.fi
.SS Array with non-writable length
.PP
The \f[V]length\f[R] property is automatically updated by the array when
elements are added beyond the current length.
If the \f[V]length\f[R] property is made non-writable, the array will
not be able to update it.
This causes an error in strict mode.
.IP
.nf
\f[C]
\[dq]use strict\[dq];

const numbers = [1, 2, 3, 4, 5];
Object.defineProperty(numbers, \[dq]length\[dq], { writable: false });
numbers[5] = 6; // TypeError: Cannot assign to read only property \[aq]length\[aq] of object \[aq][object Array]\[aq]
numbers.push(5); // // TypeError: Cannot assign to read only property \[aq]length\[aq] of object \[aq][object Array]\[aq]
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
Indexed collections
.IP \[bu] 2
\f[V]Array\f[R]
.IP \[bu] 2
\f[V]TypedArray\f[R]: \f[V]length\f[R]
.IP \[bu] 2
\f[V]String\f[R]: \f[V]length\f[R]
.IP \[bu] 2
RangeError: invalid array length
