.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "SpeechSynthesisUtterance.voice" "JS" "April 8, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SpeechSynthesisUtterance.voice \- SpeechSynthesisUtterance: voice
property
.SH SYNOPSIS
The \f[B]\f[CB]voice\f[B]\f[R] property of the
\f[CR]SpeechSynthesisUtterance\f[R] interface gets and sets the voice
that will be used to speak the utterance.
.PP
This should be set to one of the \f[CR]SpeechSynthesisVoice\f[R] objects
returned by \f[CR]SpeechSynthesis.getVoices()\f[R].
If not set by the time the utterance is spoken, the voice used will be
the most suitable default voice available for the utterance\[cq]s
\f[CR]lang\f[R] setting.
.SH VALUE
A \f[CR]SpeechSynthesisVoice\f[R] object.
.SH EXAMPLES
.IP
.EX
const synth = window.speechSynthesis;

const inputForm = document.querySelector(\[dq]form\[dq]);
const inputTxt = document.querySelector(\[dq]input\[dq]);
const voiceSelect = document.querySelector(\[dq]select\[dq]);

const voices = synth.getVoices();

// ...

inputForm.onsubmit = (event) => {
  event.preventDefault();

  const utterThis = new SpeechSynthesisUtterance(inputTxt.value);
  const selectedOption =
    voiceSelect.selectedOptions[0].getAttribute(\[dq]data\-name\[dq]);
  for (let i = 0; i < voices.length; i++) {
    if (voices[i].name === selectedOption) {
      utterThis.voice = voices[i];
    }
  }
  synth.speak(utterThis);
  inputTxt.blur();
};
.EE
.SH SEE ALSO
.IP \[bu] 2
Web Speech API
