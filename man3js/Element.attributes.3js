.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Element.attributes" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.attributes \- Element: attributes property
.SH SYNOPSIS
The \f[B]\f[CB]Element.attributes\f[B]\f[R] property returns a live
collection of all attribute nodes registered to the specified node.
It is a \f[CR]NamedNodeMap\f[R], not an \f[CR]Array\f[R], so it has no
\f[CR]Array\f[R] methods and the \f[CR]Attr\f[R] nodes\[cq] indexes may
differ among browsers.
To be more specific, \f[CR]attributes\f[R] is a key/value pair of
strings that represents any information regarding that attribute.
.SH VALUE
A \f[CR]NamedNodeMap\f[R] object.
.SH EXAMPLES
.SS Basic examples
.IP
.EX
// Get the first <p> element in the document
const paragraph = document.querySelector(\[dq]p\[dq]);
const attributes = paragraph.attributes;
.EE
.SS Enumerating elements attributes
You can enumerate through an element\[cq]s attributes using
\f[CR]for...of\f[R].
The following example runs through the attribute nodes for the element
in the document with id \[lq]paragraph\[rq], and prints each
attribute\[cq]s value.
.IP
.EX
<!DOCTYPE html>
<html lang=\[dq]en\-US\[dq]>
  <head>
    <title>Attributes example</title>
    <script>
    function listAttributes() {
       const paragraph = document.getElementById(\[dq]paragraph\[dq]);
       const result = document.getElementById(\[dq]result\[dq]);

       // First, let\[aq]s verify that the paragraph has some attributes
       if (paragraph.hasAttributes()) {
         let output = \[dq]Attributes of first paragraph:\[rs]n\[dq];
         for (const attr of paragraph.attributes) {
           output += \[ga]${attr.name} \-> ${attr.value}\[rs]n\[ga];
         }
         result.textContent = output;
       } else {
         result.textContent = \[dq]No attributes to show\[dq];
       }
    }
    </script>
  </head>

  <body>
    <p id=\[dq]paragraph\[dq] style=\[dq]color: green;\[dq]>Sample Paragraph</p>
    <form action=\[dq]\[dq]>
      <p>
        <input type=\[dq]button\[dq] value=\[dq]Show first attribute name and value\[dq]
          onclick=\[dq]listAttributes();\[dq]>
        <pre id=\[dq]result\[dq]></pre>
      </p>
    </form>
  </body>
</html>
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]NamedNodeMap\f[R], the interface of the returned object
.IP \[bu] 2
Cross\-browser compatibility considerations: on \c
.UR https://quirksmode.org/dom/core/#attributes
quirksmode
.UE \c
