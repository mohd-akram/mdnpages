.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "HTMLElement.popover" "JS" "December 22, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLElement.popover \- HTMLElement: popover property
.SH SYNOPSIS
The \f[B]\f[CB]popover\f[B]\f[R] property of the \f[CR]HTMLElement\f[R]
interface gets and sets an element\[cq]s popover state via JavaScript
(\f[CR]\[dq]auto\[dq]\f[R] or \f[CR]\[dq]manual\[dq]\f[R]), and can be
used for feature detection.
.PP
It reflects the value of the \f[CR]popover\f[R] global HTML attribute.
.SH VALUE
An enumerated value; possible values are:
.IP \[bu] 2
\f[CR]\[dq]auto\[dq]\f[R]: In auto state:
.RS 2
.IP \[bu] 2
The popover can be \[lq]light dismissed\[rq] \[em] this means that you
can hide the popover by clicking outside it or pressing the Esc key.
.IP \[bu] 2
Usually, only one popover can be shown at a time \[em] showing a second
popover when one is already shown will hide the first one.
The exception to this rule is when you have nested auto popovers.
See Nested popovers for more details.
.RE
.IP \[bu] 2
\f[CR]\[dq]manual\[dq]\f[R]: In manual state:
.RS 2
.IP \[bu] 2
The popover cannot be \[lq]light dismissed\[rq], although declarative
show/hide/toggle buttons will still work.
.IP \[bu] 2
Multiple independent popovers can be shown at a time.
.RE
.SH EXAMPLES
.SS Feature detection
You can use the \f[CR]popover\f[R] attribute to feature detect the
Popover API:
.IP
.EX
function supportsPopover() {
  return HTMLElement.prototype.hasOwnProperty(\[dq]popover\[dq]);
}
.EE
.SS Setting up a popover programmatically
.IP
.EX
const popover = document.getElementById(\[dq]mypopover\[dq]);
const toggleBtn = document.getElementById(\[dq]toggleBtn\[dq]);

const popoverSupported = supportsPopover();

if (popoverSupported) {
  popover.popover = \[dq]auto\[dq];
  toggleBtn.popoverTargetElement = popover;
  toggleBtn.popoverTargetAction = \[dq]toggle\[dq];
} else {
  console.log(\[dq]Popover API not supported.\[dq]);
}
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]popover\f[R] HTML global attribute
.IP \[bu] 2
Popover API
