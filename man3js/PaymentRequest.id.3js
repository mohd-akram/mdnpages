.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "PaymentRequest.id" "JS" "December 30, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
PaymentRequest.id \- PaymentRequest: id property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]id\f[B]\f[R] read\-only attribute of the
\f[CR]PaymentRequest\f[R] interface returns a unique identifier for a
particular \f[CR]PaymentRequest\f[R] instance.
.PP
When constructing an instance of the \f[CR]PaymentRequest\f[R], you are
able to supply an custom id.
If none is provided, the browser automatically sets the id value to a
UUID.
.SH EXAMPLE
This example shows how to give a \f[CR]PaymentRequest\f[R] instance a
custom id.
.IP
.EX
\f[B]const\f[R] details = {
  id: \[dq]super\-store\-order\-123\-12312\[dq],
  total: {
    label: \[dq]Total due\[dq],
    amount: { currency: \[dq]USD\[dq], value: \[dq]65.00\[dq] },
  },
};
\f[B]const\f[R] request = \f[B]new\f[R] PaymentRequest(methodData, details);
console.log(request.id); \f[I]// super\-store\-order\-123\-12312\f[R]
.EE
.PP
The \f[CR]id\f[R] is then also available in the
\f[CR]PaymentResponse\f[R] returned from the \f[CR]show()\f[R] method,
but under the \f[CR]requestId\f[R] attribute.
.IP
.EX
\f[B]const\f[R] response = \f[B]await\f[R] request.show();
console.log(response.requestId === request.id);

\f[I]// And in serialized form too\f[R]
\f[B]const\f[R] json = response.toJSON();
console.log(json.requestId, response.requestId, request.id);
.EE
.SH VALUE
A string.
