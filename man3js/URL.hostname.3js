.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "URL.HOSTNAME" "3JS" "February 13, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
URL.hostname \- URL: hostname property
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]hostname\f[B]\f[R] property of the \f[CR]URL\f[R]
interface is a string containing either the domain name or IP address of
the URL.
If the URL does not have a hostname, this property contains an empty
string, \f[CR]\(dq\(dq\f[R].
IPv4 and IPv6 addresses are normalized, such as stripping leading zeros,
and domain names are converted to \c
.UR https://en.wikipedia.org/wiki/Internationalized_domain_name
IDN
.UE \c
\&.
.PP
This property can be set to change the hostname of the URL.
If the URL\(cqs scheme is not \c
.UR https://www.rfc-editor.org/rfc/rfc3986#section-1.2.3
hierarchical
.UE \c
\ (which the URL standard calls \(lq\c
.UR https://url.spec.whatwg.org/#special-scheme
special schemes
.UE \c
\(rq), then it has no concept of a host and setting this property has no
effect.
.PP
The hostname is percent\-encoded when setting but not percent\-decoded
when reading.
.SH VALUE
A string.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] url = \f[B]new\f[R] URL(
  \(dqhttps://developer.mozilla.org/en\-US/docs/Web/API/URL/hostname\(dq,
);
console.log(url.hostname); \f[I]// Logs: \(aqdeveloper.mozilla.org\(aq\f[R]

url.hostname = \(dq你好.com\(dq;
console.log(url.hostname); \f[I]// Logs: \(aqxn\-\-6qq79v.com\(aq\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
The \f[CR]URL\f[R] interface it belongs to.
