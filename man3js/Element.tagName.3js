.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "Element.tagName" "JS" "September 1, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.tagName \- Element: tagName property
.SH SYNOPSIS
The \f[B]\f[CB]tagName\f[B]\f[R] read\-only property of the
\f[CR]Element\f[R] interface returns the tag name of the element on
which it\[cq]s called.
.PP
For example, if the element is an \f[CR]<img>\f[R], its
\f[CR]tagName\f[R] property is \f[CR]IMG\f[R] (for HTML documents; it
may be cased differently for XML/XHTML documents).
Note: You can use the \f[CR]localName\f[R] property to access the
Element\[cq]s local name \[em] which for the case in the example is
\f[CR]img\f[R] (lowercase).
.SH VALUE
A string indicating the element\[cq]s tag name.
This string\[cq]s capitalization depends on the document type:
.IP \[bu] 2
For DOM trees which represent HTML documents, the returned tag name is
always in the canonical upper\-case form.
For example, \f[CR]tagName\f[R] called on a \f[CR]<div>\f[R] element
returns \f[CR]\[dq]DIV\[dq]\f[R].
.IP \[bu] 2
The tag names of elements in an XML DOM tree are returned in the same
case in which they\[cq]re written in the original XML file.
If an XML document includes a tag \f[CR]\[dq]<SomeTag>\[dq]\f[R], then
the \f[CR]tagName\f[R] property\[cq]s value is
\f[CR]\[dq]SomeTag\[dq]\f[R].
.PP
For \f[CR]Element\f[R] objects, the value of \f[CR]tagName\f[R] is the
same as the value of the \f[CR]nodeName\f[R] property the element object
inherits from \f[CR]Node\f[R].
.SH EXAMPLES
.SS HTML
.IP
.EX
<\f[B]span\f[R] id=\[dq]born\[dq]>When I was born\&...</\f[B]span\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] span = document.getElementById(\[dq]born\[dq]);
console.log(span.tagName);
.EE
.PP
In XHTML (or any other XML format), the original case will be
maintained, so \f[CR]\[dq]span\[dq]\f[R] would be output in case the
original tag name was created lowercase.
In HTML, \f[CR]\[dq]SPAN\[dq]\f[R] would be output instead regardless of
the case used while creating the original document.
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Element.localName\f[R]
