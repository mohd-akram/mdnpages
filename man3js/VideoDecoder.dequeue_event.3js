.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "VideoDecoder.dequeue_event" "JS" "November 29, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
VideoDecoder.dequeue_event \- VideoDecoder: dequeue event
.SH SYNOPSIS
\f[B]Secure context\f[R]: This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]dequeue\f[B]\f[R] event of the \f[CR]VideoDecoder\f[R]
interface fires to signal a decrease in
\f[CR]VideoDecoder.decodeQueueSize\f[R].
.PP
This eliminates the need for developers to use a \f[CR]setTimeout()\f[R]
poll to determine when the queue has decreased, and more work should be
queued up.
.SH SYNTAX
Use the event name in methods like \f[CR]addEventListener()\f[R], or set
an event handler property.
.IP
.EX
addEventListener(\[dq]dequeue\[dq], (event) => {});

ondequeue = (event) => {};
.EE
.SH EXAMPLE
.IP
.EX
videoDecoder.addEventListener(\[dq]dequeue\[dq], (event) => {
  // Queue up more encoding work
});
.EE
