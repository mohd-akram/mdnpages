.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Math.atan" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Math.atan - Math.atan()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]Math.atan()\f[B]\f[R] static method returns the inverse
tangent (in radians) of a number, that is
.PP
𝙼𝚊𝚝𝚑.𝚊𝚝𝚊𝚗(𝚡)=arctan(x)=the unique\ y∊[−π2,π2]\ such that\ tan(y)=x = (x)
= y (y) = x
.SH SYNTAX
.IP
.nf
\f[C]
Math.atan(x)
\f[R]
.fi
.SS Parameters
.TP
\f[B]x\f[R]
A number.
.SS Return value
.PP
The inverse tangent (angle in radians between -π2- and π2, inclusive) of
\f[V]x\f[R].
If \f[V]x\f[R] is \f[V]Infinity\f[R], it returns π2.
If \f[V]x\f[R] is \f[V]-Infinity\f[R], it returns -π2-.
.SH DESCRIPTION
.PP
Because \f[V]atan()\f[R] is a static method of \f[V]Math\f[R], you
always use it as \f[V]Math.atan()\f[R], rather than as a method of a
\f[V]Math\f[R] object you created (\f[V]Math\f[R] is not a constructor).
.SH EXAMPLES
.SS Using Math.atan()
.IP
.nf
\f[C]
Math.atan(-Infinity); // -1.5707963267948966 (-π/2)
Math.atan(-0); // -0
Math.atan(0); // 0
Math.atan(1); // 0.7853981633974483  (π/4)
Math.atan(Infinity); // 1.5707963267948966  (π/2)

// The angle that the line (0,0) -- (x,y) forms with the x-axis in a Cartesian coordinate system
const theta = (x, y) => Math.atan(y / x);
\f[R]
.fi
.PP
Note that you may want to avoid the \f[V]theta\f[R] function and use
\f[V]Math.atan2()\f[R] instead, which has a wider range (between -π and
π) and avoids outputting \f[V]NaN\f[R] for cases such as when
\f[V]x\f[R] is \f[V]0\f[R].
.SH SEE ALSO
.IP \[bu] 2
\f[V]Math.acos()\f[R]
.IP \[bu] 2
\f[V]Math.asin()\f[R]
.IP \[bu] 2
\f[V]Math.atan2()\f[R]
.IP \[bu] 2
\f[V]Math.cos()\f[R]
.IP \[bu] 2
\f[V]Math.sin()\f[R]
.IP \[bu] 2
\f[V]Math.tan()\f[R]
