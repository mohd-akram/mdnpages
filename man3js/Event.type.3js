.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Event.type" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Event.type \- Event: type property
.SH SYNOPSIS
The \f[B]\f[CB]type\f[B]\f[R] read\-only property of the
\f[CR]Event\f[R] interface returns a string containing the event\[cq]s
type.
It is set when the event is constructed and is the name commonly used to
refer to the specific event, such as \f[CR]click\f[R], \f[CR]load\f[R],
or \f[CR]error\f[R].
.SH VALUE
A string containing the type of \f[CR]Event\f[R].
.SH EXAMPLE
This example logs the event type whenever you press a keyboard key or
click a mouse button.
.SS HTML
.IP
.EX
<p>Press any key or click the mouse to get the event type.</p>
<p id=\[dq]log\[dq]></p>
.EE
.SS JavaScript
.IP
.EX
function getEventType(event) {
  const log = document.getElementById(\[dq]log\[dq]);
  log.innerText = \[ga]${event.type}\[rs]n${log.innerText}\[ga];
}

// Keyboard events
document.addEventListener(\[dq]keydown\[dq], getEventType, false); // first
document.addEventListener(\[dq]keypress\[dq], getEventType, false); // second
document.addEventListener(\[dq]keyup\[dq], getEventType, false); // third

// Mouse events
document.addEventListener(\[dq]mousedown\[dq], getEventType, false); // first
document.addEventListener(\[dq]mouseup\[dq], getEventType, false); // second
document.addEventListener(\[dq]click\[dq], getEventType, false); // third
.EE
.SS Result
.SH SEE ALSO
.IP \[bu] 2
\f[CR]EventTarget.addEventListener()\f[R]
.IP \[bu] 2
\f[CR]EventTarget.removeEventListener()\f[R]
