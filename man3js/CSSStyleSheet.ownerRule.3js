.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "CSSSTYLESHEET.OWNERRULE" "3JS" "May 31, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
CSSStyleSheet.ownerRule \- CSSStyleSheet: ownerRule property
.SH SYNOPSIS
The read\-only \f[CR]CSSStyleSheet\f[R] property
\f[B]\f[CB]ownerRule\f[B]\f[R] returns the \f[CR]CSSImportRule\f[R]
corresponding to the \f[CR]\(atimport\f[R] at\-rule which imported the
stylesheet into the document.
If the stylesheet wasn\(cqt imported into the document using
\f[CR]\(atimport\f[R], the returned value is \f[CR]null\f[R].
.SH VALUE
A \f[CR]CSSImportRule\f[R] corresponding to the \f[CR]\(atimport\f[R]
rule which imported the stylesheet into the document.
If the stylesheet wasn\(cqt imported into the document using
\f[CR]\(atimport\f[R], the returned value is \f[CR]null\f[R].
.SH EXAMPLES
This snippet of code looks for rules which were not imported into the
document using an \f[CR]\(atimport\f[R] at\-rule.
.IP
.EX
\f[B]const\f[R] ruleList = document.styleSheets[0].cssRules;

\f[B]for\f[R] (\f[B]const\f[R] rule \f[B]of\f[R] ruleList) {
  \f[B]if\f[R] (!rule.ownerRule) {
    \f[I]/* rule is not imported */\f[R]
  }
}
.EE
.PP
This snippet obtains a reference to the stylesheet associated with the
\f[CR]\(atimport\f[R] and processes it in some manner:
.IP
.EX
\f[B]const\f[R] ruleList = document.styleSheets[0].cssRules;

\f[B]for\f[R] (\f[B]const\f[R] rule \f[B]of\f[R] ruleList) {
  \f[B]if\f[R] (rule.ownerRule) {
    checkStylesheet(rule.ownerRule.styleSheet);
  }
}
.EE
.SH SEE ALSO
.IP \(bu 2
CSS Object Model
.IP \(bu 2
Using dynamic styling information
