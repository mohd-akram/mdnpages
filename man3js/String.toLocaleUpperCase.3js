.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "STRING.TOLOCALEUPPERCASE" "3JS" "March 14, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
String.toLocaleUpperCase \- String.prototype.toLocaleUpperCase()
.SH SYNOPSIS
The \f[B]\f[CB]toLocaleUpperCase()\f[B]\f[R] method of \f[CR]String\f[R]
values returns this string converted to upper case, according to any
locale\-specific case mappings.
.IP
.EX
\f[B]const\f[R] city = \(dqistanbul\(dq;

console.log(city.toLocaleUpperCase(\(dqen\-US\(dq));
\f[I]// Expected output: \(dqISTANBUL\(dq\f[R]

console.log(city.toLocaleUpperCase(\(dqTR\(dq));
\f[I]// Expected output: \(dqİSTANBUL\(dq\f[R]
.EE
.SH SYNTAX
.IP
.EX
toLocaleUpperCase()
toLocaleUpperCase(locales)
.EE
.SS Parameters
.TP
\f[B]locales\f[R] \f[I](optional)\f[R]
A string with a BCP 47 language tag, or an array of such strings.
Indicates the locale to be used to convert to upper case according to
any locale\-specific case mappings.
For the general form and interpretation of the \f[CR]locales\f[R]
argument, see the parameter description on the \f[CR]Intl\f[R] main
page.
.RS
.PP
Unlike other methods that use the \f[CR]locales\f[R] argument,
\f[CR]toLocaleUpperCase()\f[R] does not allow locale matching.
Therefore, after checking the validity of the \f[CR]locales\f[R]
argument, \f[CR]toLocaleUpperCase()\f[R] always uses the first locale in
the list (or the default locale if the list is empty), even if this
locale is not supported by the implementation.
.RE
.SS Return value
A new string representing the calling string converted to upper case,
according to any locale\-specific case mappings.
.SH DESCRIPTION
The \f[CR]toLocaleUpperCase()\f[R] method returns the value of the
string converted to upper case according to any locale\-specific case
mappings.
\f[CR]toLocaleUpperCase()\f[R] does not affect the value of the string
itself.
In most cases, this will produce the same result as
\f[CR]toUpperCase()\f[R], but for some locales, such as Turkish, whose
case mappings do not follow the default case mappings in Unicode, there
may be a different result.
.PP
Also notice that conversion is not necessarily a 1:1 character mapping,
as some characters might result in two (or even more) characters when
transformed to upper\-case.
Therefore the length of the result string can differ from the input
length.
This also implies that the conversion is not stable, so for example the
following can return \f[CR]false\f[R]:
\f[CR]x.toLocaleLowerCase() === x.toLocaleUpperCase().toLocaleLowerCase()\f[R]
.SH EXAMPLES
.SS Using toLocaleUpperCase()
.IP
.EX
\(dqalphabet\(dq.toLocaleUpperCase(); \f[I]// \(aqALPHABET\(aq\f[R]

\(dqGesäß\(dq.toLocaleUpperCase(); \f[I]// \(aqGESÄSS\(aq\f[R]

\(dqi\(rsu0307\(dq.toLocaleUpperCase(\(dqlt\-LT\(dq); \f[I]// \(aqI\(aq\f[R]

\f[B]const\f[R] locales = [\(dqlt\(dq, \(dqLT\(dq, \(dqlt\-LT\(dq, \(dqlt\-u\-co\-phonebk\(dq, \(dqlt\-x\-lietuva\(dq];
\(dqi\(rsu0307\(dq.toLocaleUpperCase(locales); \f[I]// \(aqI\(aq\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]String.prototype.toLocaleLowerCase()\f[R]
.IP \(bu 2
\f[CR]String.prototype.toLowerCase()\f[R]
.IP \(bu 2
\f[CR]String.prototype.toUpperCase()\f[R]
