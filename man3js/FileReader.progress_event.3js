.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "FILEREADER.PROGRESS_EVENT" "3JS" "May 2, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
FileReader.progress_event \- FileReader: progress event
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]progress\f[B]\f[R] event of the \f[CR]FileReader\f[R]
interface is fired periodically as the \f[CR]FileReader\f[R] reads data.
.PP
This event is not cancelable and does not bubble.
.SH SYNTAX
Use the event name in methods like \f[CR]addEventListener()\f[R], or set
an event handler property.
.IP
.EX
addEventListener(\(dqprogress\(dq, (event) => { })

onprogress = (event) => { }
.EE
.SH EVENT TYPE
A \f[CR]ProgressEvent\f[R].
Inherits from \f[CR]Event\f[R].
.SH EVENT PROPERTIES
\f[I]Also inherits properties from its parent \f[CI]Event\f[I]\f[R].
.TP
\f[B]ProgressEvent.lengthComputable\f[R] \f[I](read\-only)\f[R]
A boolean flag indicating if the total work to be done, and the amount
of work already done, by the underlying process is calculable.
In other words, it tells if the progress is measurable or not.
.TP
\f[B]ProgressEvent.loaded\f[R] \f[I](read\-only)\f[R]
A 64\-bit unsigned integer value indicating the amount of work already
performed by the underlying process.
The ratio of work done can be calculated by dividing the value of this
property by \f[CR]total\f[R].
When downloading a resource using HTTP, this only counts the body of the
HTTP message, and doesn\(cqt include headers and other overhead.
.TP
\f[B]ProgressEvent.total\f[R] \f[I](read\-only)\f[R]
A 64\-bit unsigned integer representing the total amount of work that
the underlying process is in the progress of performing.
When downloading a resource using HTTP, this is the
\f[CR]Content\-Length\f[R] (the size of the body of the message), and
doesn\(cqt include the headers and other overhead.
.SH EXAMPLES
.SS Live example
.SS HTML
.IP
.EX
<\f[B]div\f[R] class=\(dqexample\(dq>
  <\f[B]div\f[R] class=\(dqfile\-select\(dq>
    <\f[B]label\f[R] for=\(dqavatar\(dq>Choose a profile picture:</\f[B]label\f[R]>
    <\f[B]input\f[R]
      type=\(dqfile\(dq
      id=\(dqavatar\(dq
      name=\(dqavatar\(dq
      accept=\(dqimage/png, image/jpeg\(dq />
  </\f[B]div\f[R]>

  <\f[B]img\f[R] src=\(dq\(dq class=\(dqpreview\(dq height=\(dq200\(dq alt=\(dqImage preview\(dq />

  <\f[B]div\f[R] class=\(dqevent\-log\(dq>
    <\f[B]label\f[R] for=\(dqeventLog\(dq>Event log:</\f[B]label\f[R]>
    <\f[B]textarea\f[R] readonly class=\(dqevent\-log\-contents\(dq id=\(dqeventLog\(dq></\f[B]textarea\f[R]>
  </\f[B]div\f[R]>
</\f[B]div\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] fileInput = document.querySelector(\(aqinput[type=\(dqfile\(dq]\(aq);
\f[B]const\f[R] preview = document.querySelector(\(dqimg.preview\(dq);
\f[B]const\f[R] eventLog = document.querySelector(\(dq.event\-log\-contents\(dq);
\f[B]const\f[R] reader = \f[B]new\f[R] FileReader();

\f[B]function\f[R] handleEvent(event) {
  eventLog.textContent += \(ga${event.type}: ${event.loaded} bytes transferred\(rsn\(ga;

  \f[B]if\f[R] (event.type === \(dqload\(dq) {
    preview.src = reader.result;
  }
}

\f[B]function\f[R] addListeners(reader) {
  reader.addEventListener(\(dqloadstart\(dq, handleEvent);
  reader.addEventListener(\(dqload\(dq, handleEvent);
  reader.addEventListener(\(dqloadend\(dq, handleEvent);
  reader.addEventListener(\(dqprogress\(dq, handleEvent);
  reader.addEventListener(\(dqerror\(dq, handleEvent);
  reader.addEventListener(\(dqabort\(dq, handleEvent);
}

\f[B]function\f[R] handleSelected(e) {
  eventLog.textContent = \(dq\(dq;
  \f[B]const\f[R] selectedFile = fileInput.files[0];
  \f[B]if\f[R] (selectedFile) {
    addListeners(reader);
    reader.readAsDataURL(selectedFile);
  }
}

fileInput.addEventListener(\(dqchange\(dq, handleSelected);
.EE
.SS Result
.SH SEE ALSO
.IP \(bu 2
Related events: \f[CR]loadstart\f[R], \f[CR]loadend\f[R],
\f[CR]load\f[R], \f[CR]error\f[R], \f[CR]abort\f[R]
