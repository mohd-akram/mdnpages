.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "MouseEvent.metaKey" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
MouseEvent.metaKey \- MouseEvent: metaKey property
.SH SYNOPSIS
The \f[B]\f[CB]MouseEvent.metaKey\f[B]\f[R] read\-only property is a
boolean value that indicates whether the meta key was pressed or not
when a given mouse event occurs.
.PP
Be aware that many operating systems bind special functionality to the
meta key, so this property may be \f[CR]false\f[R] even when the key is
actually pressed.
On Windows, for example, this key may open the Start menu.
.RS
.PP
\f[B]Note:\f[R] On Macintosh keyboards, this key is the command key (⌘).
On Windows keyboards, this key is the Windows key (⊞).
.RE
.SH VALUE
A boolean value, where \f[CR]true\f[R] indicates that the key is
pressed, and \f[CR]false\f[R] indicates that the key is \f[I]not\f[R]
pressed.
.SH EXAMPLES
This example logs the \f[CR]metaKey\f[R] property when you trigger a
\f[CR]click\f[R] event.
.SS HTML
.IP
.EX
<p>Click anywhere to test the <code>metaKey</code> property.</p>
<p id=\[dq]log\[dq]></p>
.EE
.SS JavaScript
.IP
.EX
let log = document.querySelector(\[dq]#log\[dq]);
document.addEventListener(\[dq]click\[dq], logKey);

function logKey(e) {
  log.textContent = \[ga]The meta key is pressed: ${e.metaKey}\[ga];
}
.EE
.SS Result
.SH SEE ALSO
.IP \[bu] 2
\f[CR]MouseEvent\f[R]
