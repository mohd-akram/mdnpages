.\" Automatically generated by Pandoc 3.4
.\"
.TH "Request.json" "JS" "October 8, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Request.json \- Request: json() method
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]json()\f[B]\f[R] method of the \f[CR]Request\f[R]
interface reads the request body and returns it as a promise that
resolves with the result of parsing the body text as \f[CR]JSON\f[R].
.PP
Note that despite the method being named \f[CR]json()\f[R], the result
is not JSON but is instead the result of taking JSON as input and
parsing it to produce a JavaScript object.
.SH SYNTAX
.IP
.EX
json()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]Promise\f[R] that resolves to a JavaScript object.
This object could be anything that can be represented by JSON \[em] an
object, an array, a string, a number\&...
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] obj = { hello: \[dq]world\[dq] };

\f[B]const\f[R] request = \f[B]new\f[R] Request(\[dq]/myEndpoint\[dq], {
  method: \[dq]POST\[dq],
  body: JSON.stringify(obj),
});

request.json().then((data) \f[B]=>\f[R] {
  \f[I]// do something with the data sent in the request\f[R]
});
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Response.json()\f[R]
