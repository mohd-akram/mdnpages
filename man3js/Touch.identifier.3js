.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "TOUCH.IDENTIFIER" "3JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Touch.identifier \- Touch: identifier property
.SH SYNOPSIS
The \f[B]\f[CB]Touch.identifier\f[B]\f[R] returns a value uniquely
identifying this point of contact with the touch surface.
This value remains consistent for every event involving this finger\(cqs
(or stylus\(cqs) movement on the surface until it is lifted off the
surface.
.SH VALUE
A \f[CR]long\f[R] that represents the unique ID of the \f[CR]Touch\f[R]
object.
.SH EXAMPLES
.IP
.EX
someElement.addEventListener(
  \(dqtouchmove\(dq,
  (e) \f[B]=>\f[R] {
    \f[I]// Iterate through the list of touch points that changed\f[R]
    \f[I]// since the last event and print each touch point\(aqs identifier.\f[R]
    \f[B]for\f[R] (\f[B]let\f[R] i = 0; i < e.changedTouches.length; i++) {
      console.log(
        \(gachangedTouches[${i}].identifier = ${e.changedTouches[i].identifier}\(ga,
      );
    }
  },
  \f[B]false\f[R],
);
.EE
