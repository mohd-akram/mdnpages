.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "HEADERS.HEADERS" "3JS" "March 13, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Headers.Headers \- Headers: Headers() constructor
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]Headers()\f[B]\f[R] constructor creates a new
\f[CR]Headers\f[R] object.
.SH SYNTAX
.IP
.EX
new Headers()
new Headers(init)
.EE
.SS Parameters
.TP
\f[B]init\f[R] \f[I](optional)\f[R]
An object containing any HTTP headers that you want to pre\-populate
your \f[CR]Headers\f[R] object with.
This can be a simple object literal with \f[CR]String\f[R] values, an
array of name\-value pairs, where each pair is a 2\-element string
array; or an existing \f[CR]Headers\f[R] object.
In the last case, the new \f[CR]Headers\f[R] object copies its data from
the existing \f[CR]Headers\f[R] object.
.SH EXAMPLES
Creating an empty \f[CR]Headers\f[R] object is simple:
.IP
.EX
\f[B]const\f[R] myHeaders = \f[B]new\f[R] Headers(); \f[I]// Currently empty\f[R]
.EE
.PP
You could add a header to this using \f[CR]Headers.append\f[R]:
.IP
.EX
myHeaders.append(\(dqContent\-Type\(dq, \(dqimage/jpeg\(dq);
myHeaders.get(\(dqContent\-Type\(dq); \f[I]// Returns \(aqimage/jpeg\(aq\f[R]
.EE
.PP
Or you can add the headers you want as the \f[CR]Headers\f[R] object is
created.
In the following snippet we create a new \f[CR]Headers\f[R] object,
adding some headers by passing the constructor an init object as an
argument:
.IP
.EX
\f[B]const\f[R] httpHeaders = {
  \(dqContent\-Type\(dq: \(dqimage/jpeg\(dq,
  \(dqX\-My\-Custom\-Header\(dq: \(dqZeke are cool\(dq,
};
\f[B]const\f[R] myHeaders = \f[B]new\f[R] Headers(httpHeaders);
.EE
.PP
You can now create another \f[CR]Headers\f[R] object, passing it the
first \f[CR]Headers\f[R] object as its init object:
.IP
.EX
\f[B]const\f[R] secondHeadersObj = \f[B]new\f[R] Headers(myHeaders);
secondHeadersObj.get(\(dqContent\-Type\(dq); \f[I]// Would return \(aqimage/jpeg\(aq \(em it inherits it from the first headers object\f[R]
.EE
.PP
You can also add the headers you want as the \f[CR]Headers\f[R] object
is created by using a two\-dimensional array to add multiple headers
with the same values.
In the following snippet we create a new \f[CR]Headers\f[R] object with
multiple \f[CR]Set\-Cookie\f[R] headers by passing the constructor an
init array as an argument:
.IP
.EX
\f[B]const\f[R] headers = [
  [\(dqSet\-Cookie\(dq, \(dqgreeting=hello\(dq],
  [\(dqSet\-Cookie\(dq, \(dqname=world\(dq],
];
\f[B]const\f[R] myHeaders = \f[B]new\f[R] Headers(headers);
.EE
.SH SEE ALSO
.IP \(bu 2
ServiceWorker API
.IP \(bu 2
HTTP access control (CORS)
.IP \(bu 2
HTTP
