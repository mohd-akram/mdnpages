.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "AsyncIterator" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
AsyncIterator - AsyncIterator
.SH SYNOPSIS
.PP
An \f[B]\f[VB]AsyncIterator\f[B]\f[R] object is an object that conforms
to the async iterator protocol by providing a \f[V]next()\f[R] method
that returns a promise fulfilling to an iterator result object.
The \f[V]AsyncIterator.prototype\f[R] object is a hidden global object
that all built-in async iterators inherit from.
It provides an \f[V]\[at]\[at]asyncIterator\f[R] method that returns the
async iterator object itself, making the async iterator also async
iterable.
.PP
Note that \f[V]AsyncIterator\f[R] is \f[I]not\f[R] a global object,
although it will be in the future with the async iterator helpers
proposal (https://github.com/tc39/proposal-async-iterator-helpers).
The \f[V]AsyncIterator.prototype\f[R] object shared by all built-in
async iterators can be obtained with the following code:
.IP
.nf
\f[C]
const AsyncIteratorPrototype = Object.getPrototypeOf(
  Object.getPrototypeOf(Object.getPrototypeOf((async function* () {})())),
);
\f[R]
.fi
.SH DESCRIPTION
.PP
Currently, the only built-in JavaScript async iterator is the
\f[V]AsyncGenerator\f[R] object returned by async generator functions.
There are some other built-in async iterators in web API, such as the
one of a \f[V]ReadableStream\f[R].
.PP
Each of these async iterators have a distinct prototype object, which
defines the \f[V]next()\f[R] method used by the particular async
iterator.
All of these prototype objects inherit from
\f[V]AsyncIterator.prototype\f[R], which provides am
\f[V]\[at]\[at]asyncIterator\f[R] method that returns the async iterator
object itself, making the async iterator also async iterable.
.RS
.PP
\f[B]Note:\f[R] \f[V]AsyncIterator.prototype\f[R] does not implement
\f[V]\[at]\[at]iterator\f[R], so async iterators are not sync iterable
by default.
.RE
.SH INSTANCE METHODS
.TP
\f[B]AsyncIterator.prototype[\[at]\[at]asyncIterator]()\f[R]
Returns the async iterator object itself.
This allows async iterator objects to also be async iterable.
.SH EXAMPLES
.SS Using an async iterator as an async iterable
.PP
All built-in async iterators are also async iterable, so you can use
them in a \f[V]for await...of\f[R] loop:
.IP
.nf
\f[C]
const asyncIterator = (async function* () {
  yield 1;
  yield 2;
  yield 3;
})();
(async () => {
  for await (const value of asyncIterator) {
    console.log(value);
  }
})();
// Logs: 1, 2, 3
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]async function*\f[R]
.IP \[bu] 2
The Iterator protocol
