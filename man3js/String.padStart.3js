.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "String.padStart" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]padStart()\f[B]\f[R] method pads the current string with
another string (multiple times, if needed) until the resulting string
reaches the given length.
The padding is applied from the start of the current string.
.SH Syntax
.IP
.nf
\f[C]
padStart(targetLength)
padStart(targetLength, padString)
\f[R]
.fi
.SS Parameters
.IP \[bu] 2
\f[V]targetLength\f[R]
.RS 2
.IP \[bu] 2
The length of the resulting string once the current \f[V]str\f[R] has
been padded.
If the value is less than or equal to \f[V]str.length\f[R], then
\f[V]str\f[R] is returned as-is.
.RE
.IP \[bu] 2
\f[V]padString\f[R] \f[I](optional)\f[R]
.RS 2
.IP \[bu] 2
The string to pad the current \f[V]str\f[R] with.
If \f[V]padString\f[R] is too long to stay within the
\f[V]targetLength\f[R], it will be truncated from the end.
The default value is the unicode \[lq]space\[rq] character (U+0020).
.RE
.SS Return value
.PP
A \f[V]String\f[R] of the specified \f[V]targetLength\f[R] with
\f[V]padString\f[R] applied from the start.
.SH Examples
.SS Basic examples
.IP
.nf
\f[C]
\[dq]abc\[dq].padStart(10); // \[dq]       abc\[dq]
\[dq]abc\[dq].padStart(10, \[dq]foo\[dq]); // \[dq]foofoofabc\[dq]
\[dq]abc\[dq].padStart(6, \[dq]123465\[dq]); // \[dq]123abc\[dq]
\[dq]abc\[dq].padStart(8, \[dq]0\[dq]); // \[dq]00000abc\[dq]
\[dq]abc\[dq].padStart(1); // \[dq]abc\[dq]
\f[R]
.fi
.SS Fixed width string number conversion
.IP
.nf
\f[C]
// JavaScript version of: (unsigned)
// printf \[dq]%0*d\[dq] width num
function leftFillNum(num, targetLength) {
  return num.toString().padStart(targetLength, \[dq]0\[dq]);
}

const num = 123;
console.log(leftFillNum(num, 5)); // \[dq]00123\[dq]
\f[R]
.fi
.SH See also
.IP \[bu] 2
Polyfill of \f[V]String.prototype.padStart\f[R] in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-string-and-regexp)
.IP \[bu] 2
\f[V]String.prototype.padEnd()\f[R]
