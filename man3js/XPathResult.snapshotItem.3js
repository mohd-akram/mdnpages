.\" Automatically generated by Pandoc 3.4
.\"
.TH "XPathResult.snapshotItem" "JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
XPathResult.snapshotItem \- XPathResult: snapshotItem() method
.SH SYNOPSIS
The \f[B]\f[CB]snapshotItem()\f[B]\f[R] method of the
\f[CR]XPathResult\f[R] interface returns an item of the snapshot
collection or \f[CR]null\f[R] in case the index is not within the range
of nodes.
Unlike the iterator result, the snapshot does not become invalid, but
may not correspond to the current document if it is mutated.
.SH SYNTAX
.IP
.EX
snapshotItem(i)
.EE
.SS Parameters
.TP
\f[B]i\f[R]
A number, the index of the item.
.SS Return value
The \f[CR]Node\f[R] at the given index within the node set of the
\f[CR]XPathResult\f[R].
.SS Exceptions
.SS TYPE_ERR
In case \f[CR]XPathResult.resultType\f[R] is not
\f[CR]UNORDERED_NODE_SNAPSHOT_TYPE\f[R] or
\f[CR]ORDERED_NODE_SNAPSHOT_TYPE\f[R], an \f[CR]XPathException\f[R] of
type \f[CR]TYPE_ERR\f[R] is thrown.
.SH EXAMPLES
The following example shows the use of the \f[CR]snapshotItem()\f[R]
method.
.SS HTML
.IP
.EX
<\f[B]div\f[R]>XPath example</\f[B]div\f[R]>
<\f[B]div\f[R]>Tag names of the matched nodes: <\f[B]output\f[R]></\f[B]output\f[R]></\f[B]div\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] xpath = \[dq]//div\[dq];
\f[B]const\f[R] result = document.evaluate(
  xpath,
  document,
  \f[B]null\f[R],
  XPathResult.ORDERED_NODE_SNAPSHOT_TYPE,
  \f[B]null\f[R],
);
\f[B]let\f[R] node = \f[B]null\f[R];
\f[B]const\f[R] tagNames = [];
\f[B]for\f[R] (\f[B]let\f[R] i = 0; i < result.snapshotLength; i++) {
  node = result.snapshotItem(i);
  tagNames.push(node.localName);
}
document.querySelector(\[dq]output\[dq]).textContent = tagNames.join(\[dq], \[dq]);
.EE
.SS Result
