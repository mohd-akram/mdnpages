.\" Automatically generated by Pandoc 3.4
.\"
.TH "Element.insertAdjacentText" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.insertAdjacentText \- Element: insertAdjacentText() method
.SH SYNOPSIS
The \f[B]\f[CB]insertAdjacentText()\f[B]\f[R] method of the
\f[CR]Element\f[R] interface, given a relative position and a string,
inserts a new text node at the given position relative to the element it
is called from.
.SH SYNTAX
.IP
.EX
insertAdjacentText(where, data)
.EE
.SS Parameters
.TP
\f[B]where\f[R]
A string representing the position relative to the element the method is
called from; must be one of the following strings:
.RS
.IP \[bu] 2
\f[CR]\[aq]beforebegin\[aq]\f[R]: Before the \f[CR]element\f[R] itself.
.IP \[bu] 2
\f[CR]\[aq]afterbegin\[aq]\f[R]: Just inside the \f[CR]element\f[R],
before its first child.
.IP \[bu] 2
\f[CR]\[aq]beforeend\[aq]\f[R]: Just inside the \f[CR]element\f[R],
after its last child.
.IP \[bu] 2
\f[CR]\[aq]afterend\[aq]\f[R]: After the \f[CR]element\f[R] itself.
.RE
.TP
\f[B]data\f[R]
A string from which to create a new text node to insert at the given
position \f[CR]where\f[R] relative to the element the method is called
from.
.SS Return value
None (\f[CR]undefined\f[R]).
.SS Exceptions
.TP
\f[B]SyntaxError\f[R] \f[B]DOMException\f[R]
Thrown if \f[CR]where\f[R] is not a recognized value.
.SS Visualization of position names
.IP
.EX
\f[I]<!\-\- beforebegin \-\->\f[R]
<\f[B]p\f[R]>
  \f[I]<!\-\- afterbegin \-\->\f[R]
  foo
  \f[I]<!\-\- beforeend \-\->\f[R]
</\f[B]p\f[R]>
\f[I]<!\-\- afterend \-\->\f[R]
.EE
.RS
.PP
\f[B]Note:\f[R] The \f[CR]beforebegin\f[R] and \f[CR]afterend\f[R]
positions work only if the node is in a tree and has an element parent.
.RE
.SH EXAMPLES
.IP
.EX
beforeBtn.addEventListener(\[dq]click\[dq], () \f[B]=>\f[R] {
  para.insertAdjacentText(\[dq]afterbegin\[dq], textInput.value);
});

afterBtn.addEventListener(\[dq]click\[dq], () \f[B]=>\f[R] {
  para.insertAdjacentText(\[dq]beforeend\[dq], textInput.value);
});
.EE
.PP
Have a look at our \c
.UR https://mdn.github.io/dom-examples/insert-adjacent/insertAdjacentText.html
insertAdjacentText.html
.UE \c
\ demo on GitHub (see the \c
.UR https://github.com/mdn/dom-examples/blob/main/insert-adjacent/insertAdjacentText.html
source code
.UE \c
\ too.)
Here we have a simple paragraph.
You can enter some text into the form element, then press the
\f[I]Insert before\f[R] and \f[I]Insert after\f[R] buttons to insert it
before or after the existing paragraph text using
\f[CR]insertAdjacentText()\f[R].
Note that the existing text node is not added to \[em] further text
nodes are created containing the new additions.
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Element.insertAdjacentElement()\f[R]
.IP \[bu] 2
\f[CR]Element.insertAdjacentHTML()\f[R]
