.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Clients.matchAll" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Clients.matchAll \- Clients: matchAll() method
.SH SYNOPSIS
The \f[B]\f[CB]matchAll()\f[B]\f[R] method of the \f[CR]Clients\f[R]
interface returns a \f[CR]Promise\f[R] for a list of service worker
\f[CR]Client\f[R] objects.
Include the \f[CR]options\f[R] parameter to return all service worker
clients whose origin is the same as the associated service worker\[cq]s
origin.
If options are not included, the method returns only the service worker
clients controlled by the service worker.
.SH SYNTAX
.IP
.EX
matchAll()
matchAll(options)
.EE
.SS Parameters
.TP
\f[B]options\f[R] \f[I](optional)\f[R]
An options object allowing you to set options for the matching
operation.
Available options are:
.RS
.TP
\f[B]includeUncontrolled\f[R]
A boolean value \[em] if set to \f[CR]true\f[R], the matching operation
will return all service worker clients who share the same origin as the
current service worker.
Otherwise, it returns only the service worker clients controlled by the
current service worker.
The default is \f[CR]false\f[R].
.TP
\f[B]type\f[R]
Sets the type of clients you want matched.
Available values are \f[CR]\[dq]window\[dq]\f[R],
\f[CR]\[dq]worker\[dq]\f[R], \f[CR]\[dq]sharedworker\[dq]\f[R], and
\f[CR]\[dq]all\[dq]\f[R].
The default is \f[CR]\[dq]window\[dq]\f[R].
.RE
.SS Return value
A \f[CR]Promise\f[R] that resolves to an array of \f[CR]Client\f[R]
objects.
In Chrome 46/Firefox 54 and later, this method returns clients in most
recently focused order, correct as per spec.
.SH EXAMPLES
.IP
.EX
clients.matchAll(options).then((clientList) => {
  for (const client of clientList) {
    if (client.url === \[dq]index.html\[dq]) {
      clients.openWindow(client);
      // or do something else involving the matching client
    }
  }
});
.EE
