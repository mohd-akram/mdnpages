.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "String.fixed" "JS" "September 15, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
String.fixed - String.prototype.fixed()
.SH SYNOPSIS
\f[I](deprecated)\f[R]
.PP
The \f[B]\f[CB]fixed()\f[B]\f[R] method of \f[CR]String\f[R] values
creates a string that embeds this string in a \f[CR]<tt>\f[R] element
(\f[CR]<tt>str</tt>\f[R]), which causes this string to be displayed in a
fixed-width font.
.RS
.PP
\f[B]Note:\f[R] All HTML wrapper methods are deprecated and only
standardized for compatibility purposes.
For the case of \f[CR]fixed()\f[R], the \f[CR]<tt>\f[R] element itself
has been removed from the HTML specification and shouldn\[cq]t be used
anymore.
Web developers should use CSS properties instead.
.RE
.SH SYNTAX
.IP
.EX
fixed()
.EE
.SS Parameters
None.
.SS Return value
A string beginning with a \f[CR]<tt>\f[R] start tag, then the text
\f[CR]str\f[R], and then a \f[CR]</tt>\f[R] end tag.
.SH EXAMPLES
.SS Using fixed()
The code below creates an HTML string and then replaces the
document\[cq]s body with it:
.IP
.EX
const contentString = \[dq]Hello, world\[dq];

document.body.innerHTML = contentString.fixed();
.EE
.PP
This will create the following HTML:
.IP
.EX
<tt>Hello, world</tt>
.EE
.RS
.PP
\f[B]Warning:\f[R] This markup is invalid, because \f[CR]tt\f[R] is no
longer a valid element.
.RE
.PP
Instead of using \f[CR]fixed()\f[R] and creating HTML text directly, you
should use CSS to manipulate fonts.
For example, you can manipulate {{cssxref(\[lq]font-family\[rq])}}
through the \f[CR]element.style\f[R] attribute:
.IP
.EX
document.getElementById(\[dq]yourElemId\[dq]).style.fontFamily = \[dq]monospace\[dq];
.EE
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[CR]String.prototype.fixed\f[R] in
\f[CR]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-string-and-regexp)
.IP \[bu] 2
HTML wrapper methods
.IP \[bu] 2
\f[CR]<tt>\f[R]
