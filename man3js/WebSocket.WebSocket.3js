.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "WebSocket.WebSocket" "JS" "April 8, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
WebSocket.WebSocket \- WebSocket: WebSocket() constructor
.SH SYNOPSIS
The \f[B]\f[CB]WebSocket()\f[B]\f[R] constructor returns a new
\f[CR]WebSocket\f[R] object.
.SH SYNTAX
.IP
.EX
new WebSocket(url)
new WebSocket(url, protocols)
.EE
.SS Parameters
.TP
\f[B]url\f[R]
The URL to which to connect; this should be the URL to which the
WebSocket server will respond.
.TP
\f[B]protocols\f[R] \f[I](optional)\f[R]
Either a single protocol string or an array of protocol strings.
These strings are used to indicate sub\-protocols, so that a single
server can implement multiple WebSocket sub\-protocols (for example, you
might want one server to be able to handle different types of
interactions depending on the specified \f[CR]protocol\f[R]).
.RS
.PP
If it is omitted, an empty array is used by default,
i.e.\ \f[CR][]\f[R].
.RE
.SS Exceptions
.TP
\f[B]SyntaxError\f[R] \f[B]DOMException\f[R]
Thrown if:
.RS
.IP \[bu] 2
parsing of \f[CR]url\f[R] fails
.IP \[bu] 2
\f[CR]url\f[R] has a scheme other than \f[CR]ws\f[R] or \f[CR]wss\f[R]
.IP \[bu] 2
\f[CR]url\f[R] has a fragment
.IP \[bu] 2
any of the values in \f[CR]protocols\f[R] occur more than once, or
otherwise fail to match the requirements for elements that comprise the
value of \f[CR]Sec\-WebSocket\-Protocol\f[R] fields as defined by the
WebSocket Protocol specification
.RE
.SH SEE ALSO
.IP \[bu] 2
\c
.UR https://www.rfc-editor.org/rfc/rfc6455.html
RFC 6455
.UE \c
\ (the WebSocket Protocol specification)
