.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "NAVIGATOR.CREDENTIALS" "3JS" "April 27, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Navigator.credentials \- Navigator: credentials property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]credentials\f[B]\f[R] read\-only property of the
\f[CR]Navigator\f[R] interface returns the
\f[CR]CredentialsContainer\f[R] object associated with the current
document, which exposes methods to request credentials.
The \f[CR]CredentialsContainer\f[R] interface also notifies the user
agent when an interesting event occurs, such as a successful sign\-in or
sign\-out.
This interface can be used for feature detection.
.SH VALUE
A \f[CR]CredentialsContainer\f[R] object.
.SH EXAMPLES
.IP
.EX
\f[B]if\f[R] (\(dqcredentials\(dq \f[B]in\f[R] navigator) {
  navigator.credentials.get({ password: \f[B]true\f[R] }).then((creds) \f[B]=>\f[R] {
    \f[I]// Do something with the credentials.\f[R]
  });
} \f[B]else\f[R] {
  \f[I]// Handle sign\-in the way you did before.\f[R]
}
.EE
