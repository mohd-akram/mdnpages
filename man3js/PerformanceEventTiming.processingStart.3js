.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "PerformanceEventTiming.processingStart" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
PerformanceEventTiming.processingStart \- PerformanceEventTiming:
processingStart property
.SH SYNOPSIS
The read\-only \f[B]\f[CB]processingStart\f[B]\f[R] property returns the
time at which event dispatch started.
This is when event handlers are about to be executed.
.SH VALUE
A \f[CR]DOMHighResTimeStamp\f[R] timestamp.
.SH EXAMPLES
.SS Using the processingStart property
The \f[CR]processingStart\f[R] property can be used when observing event
timing entries (\f[CR]PerformanceEventTiming\f[R]).
For example, to calculate input delay or event processing times.
.IP
.EX
const observer = new PerformanceObserver((list) => {
  list.getEntries().forEach((entry) => {
    // Full duration
    const duration = entry.duration;
    // Input delay (before processing event)
    const delay = entry.processingStart \- entry.startTime;
    // Synchronous event processing time
    // (between start and end dispatch)
    const time = entry.processingEnd \- entry.processingStart;
  });
});
// Register the observer for events
observer.observe({ type: \[dq]event\[dq], buffered: true });
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]PerformanceEventTiming.processingEnd\f[R]
