.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Object.lookupGetter" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Object.lookupGetter - Object.prototype.__lookupGetter__()
.SH SYNOPSIS
.PP
\f[I](deprecated)\f[R]
.RS
.PP
\f[B]Note:\f[R] This feature is deprecated in favor of the
\f[V]Object.getOwnPropertyDescriptor()\f[R] API.
This method\[cq]s behavior is only specified for web compatibility, and
is not required to be implemented in any platform.
It may not work everywhere.
.RE
.PP
The \f[B]\f[VB]__lookupGetter__()\f[B]\f[R] method returns the function
bound as a getter to the specified property.
.SH SYNTAX
.IP
.nf
\f[C]
__lookupGetter__(prop)
\f[R]
.fi
.SS Parameters
.TP
\f[B]prop\f[R]
A string containing the name of the property whose getter should be
returned.
.SS Return value
.PP
The function bound as a getter to the specified property.
Returns \f[V]undefined\f[R] if no such property is found, or the
property is a data property.
.SH DESCRIPTION
.PP
All objects that inherit from \f[V]Object.prototype\f[R] (that is, all
except \f[V]null\f[R]-prototype objects) inherit the
\f[V]__lookupGetter__()\f[R] method.
If a getter has been defined for an object\[cq]s property, it\[cq]s not
possible to reference the getter function through that property, because
that property refers to the return value of that function.
\f[V]__lookupGetter__()\f[R] can be used to obtain a reference to the
getter function.
.PP
\f[V]__lookupGetter__()\f[R] walks up the prototype chain to find the
specified property.
If any object along the prototype chain has the specified own property,
the \f[V]get\f[R] attribute of the property descriptor for that property
is returned.
If that property is a data property, \f[V]undefined\f[R] is returned.
If the property is not found along the entire prototype chain,
\f[V]undefined\f[R] is also returned.
.PP
\f[V]__lookupGetter__()\f[R] is defined in the spec as \[lq]normative
optional\[rq], which means no implementation is required to implement
this.
However, all major browsers implement it, and due to its continued
usage, it\[cq]s unlikely to be removed.
If a browser implements \f[V]__lookupGetter__()\f[R], it also needs to
implement the \f[V]__lookupSetter__()\f[R],
\f[V]__defineGetter__()\f[R], and \f[V]__defineSetter__()\f[R] methods.
.SH EXAMPLES
.SS Using __lookupGetter__()
.IP
.nf
\f[C]
const obj = {
  get foo() {
    return Math.random() > 0.5 ? \[dq]foo\[dq] : \[dq]bar\[dq];
  },
};

obj.__lookupGetter__(\[dq]foo\[dq]);
// [Function: get foo]
\f[R]
.fi
.SS Looking up a property\[cq]s getter in the standard way
.PP
You should use the \f[V]Object.getOwnPropertyDescriptor()\f[R] API to
look up a property\[cq]s getter.
Compared to \f[V]__lookupGetter__()\f[R], this method allows looking up
symbol properties.
The \f[V]Object.getOwnPropertyDescriptor()\f[R] method also works with
\f[V]null\f[R]-prototype objects, which don\[cq]t inherit from
\f[V]Object.prototype\f[R] and therefore don\[cq]t have the
\f[V]__lookupGetter__()\f[R] method.
If \f[V]__lookupGetter__()\f[R]\[cq]s behavior of walking up the
prototype chain is important, you may implement it yourself with
\f[V]Object.getPrototypeOf()\f[R].
.IP
.nf
\f[C]
const obj = {
  get foo() {
    return Math.random() > 0.5 ? \[dq]foo\[dq] : \[dq]bar\[dq];
  },
};

Object.getOwnPropertyDescriptor(obj, \[dq]foo\[dq]).get;
// [Function: get foo]
\f[R]
.fi
.IP
.nf
\f[C]
const obj2 = {
  __proto__: {
    get foo() {
      return Math.random() > 0.5 ? \[dq]foo\[dq] : \[dq]bar\[dq];
    },
  },
};

function findGetter(obj, prop) {
  while (obj) {
    const desc = Object.getOwnPropertyDescriptor(obj, \[dq]foo\[dq]);
    if (desc) {
      return desc.get;
    }
    obj = Object.getPrototypeOf(obj);
  }
}

console.log(findGetter(obj2, \[dq]foo\[dq])); // [Function: get foo]
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[V]Object.prototype.__lookupGetter__\f[R] in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-object)
.IP \[bu] 2
\f[V]Object.prototype.__lookupSetter__()\f[R]
.IP \[bu] 2
\f[V]get\f[R] syntax
.IP \[bu] 2
\f[V]Object.getOwnPropertyDescriptor()\f[R] and
\f[V]Object.getPrototypeOf()\f[R]
.IP \[bu] 2
\f[V]Object.prototype.__defineGetter__()\f[R]
.IP \[bu] 2
\f[V]Object.prototype.__defineSetter__()\f[R]
.IP \[bu] 2
JS Guide: Defining Getters and Setters
