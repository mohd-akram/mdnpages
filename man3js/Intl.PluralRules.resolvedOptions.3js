.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "Intl.PluralRules.resolvedOptions" "JS" "July 2, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Intl.PluralRules.resolvedOptions \-
Intl.PluralRules.prototype.resolvedOptions()
.SH SYNOPSIS
The \f[B]\f[CB]resolvedOptions()\f[B]\f[R] method of
\f[CR]Intl.PluralRules\f[R] instances returns a new object with
properties reflecting the locale and plural formatting options computed
during initialization of this \f[CR]Intl.PluralRules\f[R] object.
.SH SYNTAX
.IP
.EX
resolvedOptions()
.EE
.SS Parameters
None.
.SS Return value
A new object with properties reflecting the locale and plural formatting
options computed during the initialization of the given
\f[CR]Intl.PluralRules\f[R] object.
.PP
The object has the following properties:
.TP
\f[B]locale\f[R]
The BCP 47 language tag for the locale actually used.
If any Unicode extension values were requested in the input BCP 47
language tag that led to this locale, the key\-value pairs that were
requested and are supported for this locale are included in
\f[CR]locale\f[R].
.TP
\f[B]pluralCategories\f[R]
An \f[CR]Array\f[R] of plural categories used by the given locale,
selected from the list \f[CR]\[dq]zero\[dq]\f[R],
\f[CR]\[dq]one\[dq]\f[R], \f[CR]\[dq]two\[dq]\f[R],
\f[CR]\[dq]few\[dq]\f[R], \f[CR]\[dq]many\[dq]\f[R] and
\f[CR]\[dq]other\[dq]\f[R].
.TP
\f[B]type\f[R]
The type used (\f[CR]cardinal\f[R] or \f[CR]ordinal\f[R]).
.TP
\f[B]roundingIncrement\f[R] \f[I](experimental)\f[R]
The rounding\-increment precision (the increment used when rounding
numbers).
This is the value specified in the \f[CR]options.roundingIncrement\f[R]
argument in the constructor.
.TP
\f[B]roundingMode\f[R] \f[I](experimental)\f[R]
The rounding mode.
This is the value provided for the \f[CR]options.roundingMode\f[R]
argument in the constructor, or the default value:
\f[CR]halfExpand\f[R].
.TP
\f[B]roundingPriority\f[R] \f[I](experimental)\f[R]
The priority for resolving rounding conflicts if both
\[lq]FractionDigits\[rq] and \[lq]SignificantDigits\[rq] are specified.
This is the value provided for the \f[CR]options.roundingPriority\f[R]
argument in the constructor, or the default value: \f[CR]auto\f[R].
.TP
\f[B]trailingZeroDisplay\f[R] \f[I](experimental)\f[R]
The strategy for displaying trailing zeros on whole numbers.
This is the value specified in the
\f[CR]options.trailingZeroDisplay\f[R] argument in the constructor, or
the default value: \f[CR]\[dq]auto\[dq]\f[R].
.PP
Only one of the following two groups of properties is included:
.TP
\f[B]minimumIntegerDigits\f[R], \f[B]minimumFractionDigits\f[R], \f[B]maximumFractionDigits\f[R]
The values provided for these properties in the \f[CR]options\f[R]
argument or filled in as defaults.
These properties are present only if neither
\f[CR]minimumSignificantDigits\f[R] nor
\f[CR]maximumSignificantDigits\f[R] was provided in the
\f[CR]options\f[R] argument.
.TP
\f[B]minimumSignificantDigits\f[R], \f[B]maximumSignificantDigits\f[R]
The values provided for these properties in the \f[CR]options\f[R]
argument or filled in as defaults.
These properties are present only if at least one of them was provided
in the \f[CR]options\f[R] argument.
.SH EXAMPLES
.SS Using the resolvedOptions() method
The code below shows the construction of a \f[CR]PluralRules\f[R]
object, followed by logging of each of the resolved options.
.IP
.EX
\f[I]// Create a PluralRules instance\f[R]
\f[B]const\f[R] de = \f[B]new\f[R] Intl.PluralRules(\[dq]de\-DE\[dq], {
  maximumSignificantDigits: 2,
  trailingZeroDisplay: \[dq]auto\[dq],
});

\f[I]// Resolve the options\f[R]
\f[B]const\f[R] usedOptions = de.resolvedOptions();
console.log(usedOptions.locale); \f[I]// \[dq]de\-DE\[dq]\f[R]
console.log(usedOptions.pluralCategories); \f[I]// Array [\[dq]one\[dq], \[dq]other\[dq]]\f[R]
console.log(usedOptions.type); \f[I]// \[dq]cardinal\[dq]\f[R]
console.log(usedOptions.minimumIntegerDigits); \f[I]// 1\f[R]
console.log(usedOptions.minimumFractionDigits); \f[I]// undefined (maximumSignificantDigits is set)\f[R]
console.log(usedOptions.maximumFractionDigits); \f[I]//undefined (maximumSignificantDigits is set)\f[R]
console.log(usedOptions.minimumSignificantDigits); \f[I]// 1\f[R]
console.log(usedOptions.maximumSignificantDigits); \f[I]//2\f[R]
console.log(usedOptions.roundingIncrement); \f[I]// 1\f[R]
console.log(usedOptions.roundingMode); \f[I]// \[dq]halfExpand\[dq]\f[R]
console.log(usedOptions.roundingPriority); \f[I]// \[dq]auto\[dq]\f[R]
console.log(usedOptions.trailingZeroDisplay); \f[I]// \[dq]auto\[dq]\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Intl.PluralRules\f[R]
