.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "IDBCursor.request" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
IDBCursor.request \- IDBCursor: request property
.SH SYNOPSIS
The \f[B]\f[CB]request\f[B]\f[R] read\-only property of the
\f[CR]IDBCursor\f[R] interface returns the \f[CR]IDBRequest\f[R] used to
obtain the cursor.
.PP
This feature is available in Web Workers.
.SH VALUE
An \f[CR]IDBRequest\f[R] object instance.
.SH EXAMPLES
When you open a cursor, the \f[CR]request\f[R] property is then
available on that cursor object, to tell you what request object the
cursor originated from.
For example:
.IP
.EX
function displayData() {
  list.textContent = \[dq]\[dq];
  const transaction = db.transaction([\[dq]rushAlbumList\[dq]], \[dq]readonly\[dq]);
  const objectStore = transaction.objectStore(\[dq]rushAlbumList\[dq]);

  const request = objectStore.openCursor();

  request.onsuccess = (event) => {
    const cursor = event.target.result;
    if (cursor) {
      const listItem = document.createElement(\[dq]li\[dq]);
      listItem.textContent = \[ga]${cursor.value.albumTitle}, ${cursor.value.year}\[ga];
      list.appendChild(listItem);
      console.log(cursor.request);
      cursor.continue();
    } else {
      console.log(\[dq]Entries all displayed.\[dq]);
    }
  };
}
.EE
.SH SEE ALSO
.IP \[bu] 2
Using IndexedDB
.IP \[bu] 2
Starting transactions: \f[CR]IDBDatabase\f[R]
.IP \[bu] 2
Using transactions: \f[CR]IDBTransaction\f[R]
.IP \[bu] 2
Setting a range of keys: \f[CR]IDBKeyRange\f[R]
.IP \[bu] 2
Retrieving and making changes to your data: \f[CR]IDBObjectStore\f[R]
.IP \[bu] 2
Using cursors: \f[CR]IDBCursor\f[R]
.IP \[bu] 2
Reference example: \c
.UR https://github.com/mdn/dom-examples/tree/main/to-do-notifications
To\-do Notifications
.UE \c
\ (\c
.UR https://mdn.github.io/dom-examples/to-do-notifications/
View the example live
.UE \c
).
