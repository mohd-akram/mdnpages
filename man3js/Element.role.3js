.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "ELEMENT.ROLE" "3JS" "April 2, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.role \- Element: role property
.SH SYNOPSIS
The \f[B]\f[CB]role\f[B]\f[R] property of the \f[CR]Element\f[R]
interface returns the explicitly set WAI\-ARIA role for the element.
.PP
All HTML elements have an implicit ARIA role, even if that role is
\f[CR]generic\f[R].
This semantic association allows tools to present and support
interaction with the object in a manner that is consistent with user
expectations about other objects of that type.
The \f[CR]role\f[R] attribute is used to explicitly set the element\(cqs
ARIA role, overriding the implicit role.
For example, a \f[CR]<ul>\f[R], which has an implicit \f[CR]list\f[R]
role, might have \f[CR]role=\(dqtreegrid\(dq\f[R] explicitly set.
The \f[CR]role\f[R] property reflects the explicitly set value of the
\f[CR]role\f[R] attribute\(emin this case \f[CR]treegrid\f[R]; it does
not return the element\(cqs implicit \f[CR]list\f[R] role unless
explicitly set.
.PP
The full list of defined ARIA roles can be found on the ARIA roles
reference page.
.SH VALUE
A string; the value of the \f[CR]role\f[R] attribute or \f[CR]null\f[R]
if not explicitly set.
.SH EXAMPLES
In this example, images with empty or missing \f[CR]alt\f[R] attributes
are given a \f[CR]role\f[R] of \f[CR]presentation\f[R]:
.IP
.EX
\f[B]const\f[R] images = document.querySelectorAll(\(dqimg\(dq);
images.forEach((image) \f[B]=>\f[R] {
  \f[B]if\f[R] (!image.alt) {
    image.role = \(dqpresentation\(dq;
  }
});
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]ElementInternals.role\f[R]
.IP \(bu 2
ARIA attributes
