.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "OffscreenCanvasRenderingContext2D.commit" "JS" "April 25, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
OffscreenCanvasRenderingContext2D.commit \-
OffscreenCanvasRenderingContext2D: commit() method
.SH SYNOPSIS
This feature is available in Web Workers.
.PP
The \f[B]\f[CB]OffscreenCanvasRenderingContext2D.commit()\f[B]\f[R]
method of the Canvas 2D API was intended to copy the rendering
context\[cq]s bitmap to the bitmap of the placeholder
\f[CR]<canvas>\f[R] element of the associated \f[CR]OffscreenCanvas\f[R]
object.
The copy operation is synchronous.
Calling this method is not needed for the transfer, since it happens
automatically during the event\-loop execution.
.SH SYNTAX
.IP
.EX
commit()
.EE
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] placeholder = document.createElement(\[dq]canvas\[dq]);
\f[B]const\f[R] offscreen = placeholder.transferControlToOffscreen();
\f[B]const\f[R] ctx = offscreenCanvas.getContext(\[dq]2d\[dq]);

\f[I]// Perform some drawing using the 2d context\f[R]
ctx.fillStyle = \[dq]blue\[dq];
ctx.fillRect(0, 0, 10, 10);

\f[I]// Push placeholder to the canvas element\f[R]
ctx.commit();
.EE
