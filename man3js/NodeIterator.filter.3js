.\" Automatically generated by Pandoc 3.4
.\"
.TH "NodeIterator.filter" "JS" "August 4, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NodeIterator.filter \- NodeIterator: filter property
.SH SYNOPSIS
The \f[B]\f[CB]NodeIterator.filter\f[B]\f[R] read\-only property returns
a \f[CR]NodeFilter\f[R] object, that is an object which implements an
\f[CR]acceptNode(node)\f[R] method, used to screen nodes.
.PP
When creating the \f[CR]NodeIterator\f[R], the filter object is passed
in as the third parameter, and the object method
\f[CR]acceptNode(node)\f[R] is called on every single node to determine
whether or not to accept it.
This function should return the constant
\f[CR]NodeFilter.FILTER_ACCEPT\f[R] for cases when the node should be
accepted and \f[CR]NodeFilter.FILTER_REJECT\f[R] for cases when the node
should be rejected.
.SH VALUE
A \f[CR]NodeFilter\f[R] object.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] nodeIterator = document.createNodeIterator(
  document.body,
  NodeFilter.SHOW_ELEMENT,
  {
    acceptNode(node) {
      \f[B]return\f[R] NodeFilter.FILTER_ACCEPT;
    },
  },
);
nodeFilter = nodeIterator.filter;
.EE
.SH SEE ALSO
.IP \[bu] 2
The interface this property belongs to: \f[CR]NodeIterator\f[R].
