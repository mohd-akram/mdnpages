.\" Automatically generated by Pandoc 3.4
.\"
.TH "PerformanceNavigationTiming.loadEventEnd" "JS" "August 13, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
PerformanceNavigationTiming.loadEventEnd \- PerformanceNavigationTiming:
loadEventEnd property
.SH SYNOPSIS
The \f[B]\f[CB]loadEventEnd\f[B]\f[R] read\-only property returns a
\f[CR]DOMHighResTimeStamp\f[R] representing the time immediately after
the current document\[cq]s \f[CR]load\f[R] event handler completes.
.SH VALUE
A \f[CR]DOMHighResTimeStamp\f[R] representing the time immediately after
the current document\[cq]s \f[CR]load\f[R] event handler completes.
.SH EXAMPLES
.SS Measuring \f[CR]load\f[R] event handler time
The \f[CR]loadEventEnd\f[R] property can be used to measure how long it
takes to process the \f[CR]load\f[R] event handler.
.PP
This is useful to measure the time of long running \f[CR]load\f[R] event
handlers.
.IP
.EX
window.addEventListener(\[dq]load\[dq], (event) \f[B]=>\f[R] {
  \f[I]// Some long running code\f[R]
});
.EE
.PP
Example using a \f[CR]PerformanceObserver\f[R], which notifies of new
\f[CR]navigation\f[R] performance entries as they are recorded in the
browser\[cq]s performance timeline.
Use the \f[CR]buffered\f[R] option to access entries from before the
observer creation.
.IP
.EX
\f[B]const\f[R] observer = \f[B]new\f[R] PerformanceObserver((list) \f[B]=>\f[R] {
  list.getEntries().forEach((entry) \f[B]=>\f[R] {
    \f[B]const\f[R] loadEventTime = entry.loadEventEnd \- entry.loadEventStart;
    \f[B]if\f[R] (loadEventTime > 0) {
      console.log(\[ga]${entry.name}: load event handler time: ${loadEventTime}ms\[ga]);
    }
  });
});

observer.observe({ type: \[dq]navigation\[dq], buffered: \f[B]true\f[R] });
.EE
.PP
Example using \f[CR]Performance.getEntriesByType()\f[R], which only
shows \f[CR]navigation\f[R] performance entries present in the
browser\[cq]s performance timeline at the time you call this method:
.IP
.EX
\f[B]const\f[R] entries = performance.getEntriesByType(\[dq]navigation\[dq]);
entries.forEach((entry) \f[B]=>\f[R] {
  \f[B]const\f[R] loadEventTime = entry.loadEventEnd \- entry.loadEventStart;
  \f[B]if\f[R] (loadEventTime > 0) {
    console.log(\[ga]${entry.name}:
      load event handler time: ${loadEventTime}ms\[ga]);
  }
});
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]load\f[R] event
