.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "MATH.F16ROUND" "3JS" "March 5, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Math.f16round \- Math.f16round()
.SH SYNOPSIS
The \f[B]\f[CB]Math.f16round()\f[B]\f[R] static method returns the
nearest \c
.UR https://en.wikipedia.org/wiki/Half-precision_floating-point_format
16\-bit half precision
.UE \c
\ float representation of a number.
.IP
.EX
console.log(Math.f16round(5.5));
\f[I]// Expected output: 5.5\f[R]

console.log(Math.f16round(5.05));
\f[I]// Expected output: 5.05078125\f[R]

console.log(Math.f16round(5));
\f[I]// Expected output: 5\f[R]

console.log(Math.f16round(\-5.05));
\f[I]// Expected output: \-5.05078125\f[R]
.EE
.SH SYNTAX
.IP
.EX
Math.f16round(doubleFloat)
.EE
.SS Parameters
.TP
\f[B]doubleFloat\f[R]
A number.
.SS Return value
The nearest \c
.UR https://en.wikipedia.org/wiki/Half-precision_floating-point_format
16\-bit half precision
.UE \c
\ float representation of \f[CR]doubleFloat\f[R].
.SH DESCRIPTION
\f[CR]Math.f16round\f[R] is the 16\-bit counterpart of
\f[CR]Math.fround()\f[R].
It is intended to smooth some rough edges when interacting with float16
numbers, such as when reading from a \f[CR]Float16Array\f[R].
Internally, JavaScript continues to treat the number as a 64\-bit float,
it just performs a \(lqround to even\(rq on the 10th bit of the
mantissa, and sets all following mantissa bits to \f[CR]0\f[R].
If the number is outside the range of a 16\-bit float,
\f[CR]Infinity\f[R] or \f[CR]\-Infinity\f[R] is returned.
.PP
Because \f[CR]f16round()\f[R] is a static method of \f[CR]Math\f[R], you
always use it as \f[CR]Math.f16round()\f[R], rather than as a method of
a \f[CR]Math\f[R] object you created (\f[CR]Math\f[R] is not a
constructor).
.SH EXAMPLES
.SS Using Math.f16round()
The number 1.5 can be precisely represented in the binary numeral
system, and is identical in 16\-bit and 64\-bit:
.IP
.EX
Math.f16round(1.5); \f[I]// 1.5\f[R]
Math.f16round(1.5) === 1.5; \f[I]// true\f[R]
.EE
.PP
However, the number 1.337 cannot be precisely represented in the binary
numeral system, so it differs in 16\-bit and 64\-bit:
.IP
.EX
Math.f16round(1.337); \f[I]// 1.3369140625\f[R]
Math.f16round(1.337) === 1.337; \f[I]// false\f[R]
.EE
.PP
100000 is too big for a 16\-bit float, so \f[CR]Infinity\f[R] is
returned:
.IP
.EX
Math.f16round(100000); \f[I]// Infinity\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
\c
.UR https://github.com/zloirock/core-js#float16-methods
Polyfill of \f[CR]Math.f16round\f[R] in \f[CR]core\-js\f[R]
.UE \c
.IP \(bu 2
\c
.UR https://www.npmjs.com/package/math.f16round
es\-shims polyfill of \f[CR]Math.f16round\f[R]
.UE \c
.IP \(bu 2
\f[CR]Math.fround()\f[R]
.IP \(bu 2
\f[CR]Math.round()\f[R]
