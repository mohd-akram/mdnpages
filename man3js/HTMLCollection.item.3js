.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "HTMLCollection.item" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLCollection.item \- HTMLCollection: item() method
.SH SYNOPSIS
The \f[CR]HTMLCollection\f[R] method \f[CR]item()\f[R] returns the
element located at the specified offset into the collection.
.RS
.PP
\f[B]Note:\f[R] Because the contents of an \f[CR]HTMLCollection\f[R] are
live, changes to the underlying DOM can and will cause the position of
individual elements in the collection to change, so the index value will
not necessarily remain constant for a given element.
.RE
.SH SYNTAX
.IP
.EX
item(index)
.EE
.SS Parameters
.TP
\f[B]index\f[R]
The position of the \f[CR]Element\f[R] to be returned.
Elements appear in an \f[CR]HTMLCollection\f[R] in the same order in
which they appear in the document\[cq]s source.
.SS Return value
The \f[CR]Element\f[R] at the specified index, or \f[CR]null\f[R] if
\f[CR]index\f[R] is less than zero or greater than or equal to the
length property.
.SH USAGE NOTES
The \f[CR]item()\f[R] method returns a numbered element from an
\f[CR]HTMLCollection\f[R].
In JavaScript, it is easier to treat the \f[CR]HTMLCollection\f[R] as an
array and to index it using array notation.
See the example below.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] images = document.images; \f[I]// This is an HTMLCollection\f[R]
\f[B]const\f[R] img0 = images.item(0); \f[I]// You can use the item() method this way\f[R]
\f[B]const\f[R] img1 = images[1]; \f[I]// But this notation is easier and more common\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]NodeList.item()\f[R]
