.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "PERFORMANCERESOURCETIMING.WORKERSTART" "3JS" "October 12, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
PerformanceResourceTiming.workerStart \- PerformanceResourceTiming:
workerStart property
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]workerStart\f[B]\f[R] read\-only property of the
\f[CR]PerformanceResourceTiming\f[R] interface returns a
\f[CR]DOMHighResTimeStamp\f[R] immediately before dispatching the
\f[CR]FetchEvent\f[R] if a Service Worker thread is already running, or
immediately before starting the Service Worker thread if it is not
already running.
If the resource is not intercepted by a Service Worker the property will
always return 0.
.SH VALUE
The \f[CR]workerStart\f[R] property can have the following values:
.IP \(bu 2
A \f[CR]DOMHighResTimeStamp\f[R].
.IP \(bu 2
\f[CR]0\f[R] if no service worker is used.
.IP \(bu 2
\f[CR]0\f[R] if the resource is a cross\-origin request and no
\f[CR]Timing\-Allow\-Origin\f[R] HTTP response header is used.
.SH EXAMPLES
.SS Measuring ServiceWorker processing time
The \f[CR]workerStart\f[R] and \f[CR]fetchStart\f[R] properties can be
used to measure the processing time of a \f[CR]ServiceWorker\f[R].
.IP
.EX
\f[B]const\f[R] workerProcessingTime = entry.fetchStart \- entry.workerStart;
.EE
.PP
Example using a \f[CR]PerformanceObserver\f[R], which notifies of new
\f[CR]resource\f[R] performance entries as they are recorded in the
browser\(cqs performance timeline.
Use the \f[CR]buffered\f[R] option to access entries from before the
observer creation.
.IP
.EX
\f[B]const\f[R] observer = \f[B]new\f[R] PerformanceObserver((list) \f[B]=>\f[R] {
  list.getEntries().forEach((entry) \f[B]=>\f[R] {
    \f[B]const\f[R] workerProcessingTime = entry.fetchStart \- entry.workerStart;
    \f[B]if\f[R] (workerProcessingTime > 0) {
      console.log(
        \(ga${entry.name}: Worker processing time: ${workerProcessingTime}ms\(ga,
      );
    }
  });
});

observer.observe({ type: \(dqresource\(dq, buffered: \f[B]true\f[R] });
.EE
.PP
Example using \f[CR]Performance.getEntriesByType()\f[R], which only
shows \f[CR]resource\f[R] performance entries present in the
browser\(cqs performance timeline at the time you call this method:
.IP
.EX
\f[B]const\f[R] resources = performance.getEntriesByType(\(dqresource\(dq);
resources.forEach((entry) \f[B]=>\f[R] {
  \f[B]const\f[R] workerProcessingTime = entry.fetchStart \- entry.workerStart;
  \f[B]if\f[R] (workerProcessingTime > 0) {
    console.log(
      \(ga${entry.name}: Worker processing time: ${workerProcessingTime}ms\(ga,
    );
  }
});
.EE
.SS Cross\-origin timing information
If the value of the \f[CR]workerStart\f[R] property is \f[CR]0\f[R], the
resource might be a cross\-origin request.
To allow seeing cross\-origin timing information, the
\f[CR]Timing\-Allow\-Origin\f[R] HTTP response header needs to be set.
.PP
For example, to allow \f[CR]https://developer.mozilla.org\f[R] to see
timing resources, the cross\-origin resource should send:
.IP
.EX
Timing\-Allow\-Origin: https://developer.mozilla.org
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]Timing\-Allow\-Origin\f[R]
