.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "BatteryManager.level" "JS" "December 1, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
BatteryManager.level \- BatteryManager: level property
.SH SYNOPSIS
\f[B]Secure context\f[R]: This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]BatteryManager.level\f[B]\f[R] property indicates the
current battery charge level as a value between \f[CR]0.0\f[R] and
\f[CR]1.0\f[R].
A value of \f[CR]0.0\f[R] means the battery is empty and the system is
about to be suspended.
A value of \f[CR]1.0\f[R] means the battery is full or the user agent is
unable to report the battery status information.
When its value changes, the \f[CR]levelchange\f[R] event is fired.
.SH VALUE
A number.
.SH EXAMPLES
.SS Getting the battery level
.SS HTML
.IP
.EX
<button id=\[dq]get\-level\[dq]>Get battery level</button>
<div id=\[dq]output\[dq]></div>
.EE
.SS JavaScript
.IP
.EX
const getLevel = document.querySelector(\[dq]#get\-level\[dq]);
const output = document.querySelector(\[dq]#output\[dq]);

getLevel.addEventListener(\[dq]click\[dq], async () => {
  if (!navigator.getBattery) {
    output.textContent = \[dq]Battery manager is unsupported\[dq];
  } else {
    const manager = await navigator.getBattery();
    const level = manager.level;
    output.textContent = \[ga]Battery level: ${level}\[ga];
  }
});
.EE
.SS Result
.SH SEE ALSO
.IP \[bu] 2
\f[CR]BatteryManager\f[R]
.IP \[bu] 2
\f[CR]Navigator.getBattery()\f[R]
