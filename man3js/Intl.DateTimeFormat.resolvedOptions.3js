.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "Intl.DateTimeFormat.resolvedOptions" "JS" "June 11, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Intl.DateTimeFormat.resolvedOptions \-
Intl.DateTimeFormat.prototype.resolvedOptions()
.SH SYNOPSIS
The \f[B]\f[CB]resolvedOptions()\f[B]\f[R] method of
\f[CR]Intl.DateTimeFormat\f[R] instances returns a new object with
properties reflecting the locale and date and time formatting options
computed during initialization of this \f[CR]Intl.DateTimeFormat\f[R]
object.
.SH SYNTAX
.IP
.EX
resolvedOptions()
.EE
.SS Parameters
None.
.SS Return value
A new object with properties reflecting the options computed during the
initialization of the given \f[CR]Intl.DateTimeFormat\f[R] object.
The object has the following properties, in the order they are listed:
.TP
\f[B]locale\f[R]
The BCP 47 language tag for the locale actually used.
Only the \f[CR]ca\f[R], \f[CR]hc\f[R], and \f[CR]nu\f[R] Unicode
extension keys may be included in the output.
.TP
\f[B]calendar\f[R]
One of the supported calendar types, reflecting the value provided for
this property in the \f[CR]options\f[R] argument or the \f[CR]ca\f[R]
Unicode extension key.
The default is locale dependent.
.TP
\f[B]numberingSystem\f[R]
One of the supported numbering system types, reflecting the value
provided for this property in the \f[CR]options\f[R] argument or the
\f[CR]nu\f[R] Unicode extension key.
The default is locale dependent.
.TP
\f[B]timeZone\f[R]
One of the IANA time zone names, reflecting the value provided for this
property in the \f[CR]options\f[R] argument.
The default is the runtime\[cq]s default time zone; should never be
\f[CR]undefined\f[R].
.TP
\f[B]hourCycle\f[R]
The value provided for this property in the \f[CR]options\f[R] argument,
or provided in the Unicode extension key \f[CR]\[dq]hc\[dq]\f[R], with
default filled in as needed.
Only present if the \f[CR]options\f[R] argument included \f[CR]hour\f[R]
or \f[CR]timeStyle\f[R].
.TP
\f[B]hour12\f[R]
The value provided for this property in the \f[CR]options\f[R] argument,
or computed from the \f[CR]hourCycle\f[R] property.
Only present if the \f[CR]options\f[R] argument included \f[CR]hour\f[R]
or \f[CR]timeStyle\f[R].
.TP
\f[B]weekday\f[R], \f[B]era\f[R], \f[B]year\f[R], \f[B]month\f[R], \f[B]day\f[R], \f[B]dayPeriod\f[R], \f[B]hour\f[R], \f[B]minute\f[R], \f[B]second\f[R], \f[B]fractionalSecondDigits\f[R], \f[B]timeZoneName\f[R]
The values resulting from format matching between the corresponding
properties in the \f[CR]options\f[R] argument and the available
combinations and representations for date\-time formatting in the
selected locale.
Some of these properties may not be present, indicating that the
corresponding components will not be represented in formatted output.
If the \f[CR]dateStyle\f[R] or \f[CR]timeStyle\f[R] shortcuts were used
in \f[CR]options\f[R], these individual component properties will never
be present.
.TP
\f[B]dateStyle\f[R], \f[B]timeStyle\f[R]
The values provided for these properties in the \f[CR]options\f[R]
argument, if any.
.SH DESCRIPTION
Although \f[CR]dateStyle\f[R] and \f[CR]timeStyle\f[R] are shortcuts for
individual date and time component styles, the exact (locale dependent)
component styles they resolve to are not included in the resolved
options.
This ensures the result of \f[CR]resolvedOptions()\f[R] can be passed
directly to the \f[CR]Intl.DateTimeFormat()\f[R] constructor (because an
\f[CR]options\f[R] object with both \f[CR]dateStyle\f[R] or
\f[CR]timeStyle\f[R] and individual date or time component styles is not
valid).
.SH EXAMPLES
.SS Using the resolvedOptions method
.IP
.EX
\f[B]const\f[R] germanFakeRegion = \f[B]new\f[R] Intl.DateTimeFormat(\[dq]de\-XX\[dq], { timeZone: \[dq]UTC\[dq] });
\f[B]const\f[R] usedOptions = germanFakeRegion.resolvedOptions();

usedOptions.locale; \f[I]// \[dq]de\[dq] (because \[dq]de\-XX\[dq] does not exist)\f[R]
usedOptions.calendar; \f[I]// \[dq]gregory\[dq]\f[R]
usedOptions.numberingSystem; \f[I]// \[dq]latn\[dq]\f[R]
usedOptions.timeZone; \f[I]// \[dq]UTC\[dq]\f[R]
usedOptions.month; \f[I]// \[dq]numeric\[dq]\f[R]
.EE
.SS Getting the user\[cq]s time zone and locale preferences
The \f[CR]Intl.DateTimeFormat\f[R] constructor without any options uses
the current system settings.
You can use \f[CR]resolvedOptions()\f[R] to get the user\[cq]s current
time zone and locale\[cq]s preferred calendar and numbering system:
.IP
.EX
\f[B]const\f[R] systemOptions = \f[B]new\f[R] Intl.DateTimeFormat().resolvedOptions();
systemOptions.timeZone; \f[I]// e.g., \[dq]Europe/Brussels\[dq] or \[dq]Asia/Riyadh\[dq]\f[R]
systemOptions.calendar; \f[I]// e.g., \[dq]gregory\[dq] or \[dq]islamic\-umalqura\[dq]\f[R]
systemOptions.numberingSystem; \f[I]// e.g., \[dq]latn\[dq] or \[dq]arab\[dq]\f[R]
systemOptions.locale; \f[I]// e.g., \[dq]nl\-BE\[dq] or \[dq]ar\-SA\[dq]\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Intl.DateTimeFormat\f[R]
