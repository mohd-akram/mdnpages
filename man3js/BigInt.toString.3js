.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "BigInt.toString" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
BigInt.toString - BigInt.prototype.toString()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]toString()\f[B]\f[R] method returns a string representing
the specified \f[V]BigInt\f[R] value.
The trailing \[lq]n\[rq] is not part of the string.
.SH SYNTAX
.IP
.nf
\f[C]
toString()
toString(radix)
\f[R]
.fi
.SS Parameters
.TP
\f[B]radix\f[R] \f[I](optional)\f[R]
An integer in the range 2 through 36 specifying the base to use for
representing the BigInt value.
Defaults to 10.
.SS Return value
.PP
A string representing the specified \f[V]BigInt\f[R] value.
.SS Exceptions
.TP
\f[B]RangeError\f[R]
Thrown if \f[V]radix\f[R] is less than 2 or greater than 36.
.SH DESCRIPTION
.PP
The \f[V]BigInt\f[R] object overrides the \f[V]toString\f[R] method of
\f[V]Object\f[R]; it does not inherit
\f[V]Object.prototype.toString()\f[R].
For \f[V]BigInt\f[R] values, the \f[V]toString()\f[R] method returns a
string representation of the value in the specified radix.
.PP
For radixes above 10, the letters of the alphabet indicate digits
greater than 9.
For example, for hexadecimal numbers (base 16) \f[V]a\f[R] through
\f[V]f\f[R] are used.
.PP
If the specified BigInt value is negative, the sign is preserved.
This is the case even if the radix is 2; the string returned is the
positive binary representation of the BigInt value preceded by a
\f[V]-\f[R] sign, \f[B]not\f[R] the two\[cq]s complement of the BigInt
value.
.PP
The \f[V]toString()\f[R] method requires its \f[V]this\f[R] value to be
a \f[V]BigInt\f[R] primitive or wrapper object.
It throws a \f[V]TypeError\f[R] for other \f[V]this\f[R] values without
attempting to coerce them to BigInt values.
.PP
Because \f[V]BigInt\f[R] doesn\[cq]t have a
\f[V][\[at]\[at]toPrimitive]()\f[R] method, JavaScript calls the
\f[V]toString()\f[R] method automatically when a \f[V]BigInt\f[R]
\f[I]object\f[R] is used in a context expecting a string, such as in a
template literal.
However, BigInt \f[I]primitive\f[R] values do not consult the
\f[V]toString()\f[R] method to be coerced to strings \[em] rather, they
are directly converted using the same algorithm as the initial
\f[V]toString()\f[R] implementation.
.IP
.nf
\f[C]
BigInt.prototype.toString = () => \[dq]Overridden\[dq];
console.log(\[ga]${1n}\[ga]); // \[dq]1\[dq]
console.log(\[ga]${Object(1n)}\[ga]); // \[dq]Overridden\[dq]
\f[R]
.fi
.SH EXAMPLES
.SS Using toString()
.IP
.nf
\f[C]
17n.toString(); // \[dq]17\[dq]
66n.toString(2); // \[dq]1000010\[dq]
254n.toString(16); // \[dq]fe\[dq]
(-10n).toString(2); // \[dq]-1010\[dq]
(-0xffn).toString(2); // \[dq]-11111111\[dq]
\f[R]
.fi
.SS Negative-zero BigInt
.PP
There is no negative-zero \f[V]BigInt\f[R] as there are no negative
zeros in integers.
\f[V]-0.0\f[R] is an IEEE floating-point concept that only appears in
the JavaScript \f[V]Number\f[R] type.
.IP
.nf
\f[C]
(-0n).toString(); // \[dq]0\[dq]
BigInt(-0).toString(); // \[dq]0\[dq]
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]BigInt.prototype.toLocaleString()\f[R]
.IP \[bu] 2
\f[V]BigInt.prototype.valueOf()\f[R]
.IP \[bu] 2
\f[V]Number.prototype.toString()\f[R]
