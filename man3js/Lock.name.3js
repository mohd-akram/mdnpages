.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "Lock.name" "JS" "March 6, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Lock.name \- Lock: name property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]name\f[B]\f[R] read\-only property of the \f[CR]Lock\f[R]
interface returns the \f[I]name\f[R] passed to
\f[CR]LockManager.request\f[R] selected when the lock was requested.
.PP
The name of a lock is passed by script when the lock is requested.
The name is selected by the developer to represent an abstract resource
for which use is being coordinated across multiple tabs, workers, or
other code within the origin.
For example, if only one tab of a web application should be
synchronizing network resources with an offline database, it could use a
lock name such as \f[CR]\[dq]net_db_sync\[dq]\f[R].
.SH VALUE
A string.
.SH EXAMPLES
The following examples show how the name property passed in the call to
\f[CR]LockManager.request()\f[R].
\f[CR]LockManager\f[R] is the object returned by
\f[CR]navigator.locks\f[R].
.IP
.EX
navigator.locks.request(\[dq]net_db_sync\[dq], show_lock_properties);

\f[B]function\f[R] show_lock_properties(lock) {
  console.log(\[ga]The lock name is: ${lock.name}\[ga]);
  console.log(\[ga]The lock mode is: ${lock.mode}\[ga]);
}
.EE
