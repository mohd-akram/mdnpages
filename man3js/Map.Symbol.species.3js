.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "Map.Symbol.species" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Map.Symbol.species \- Map[Symbol.species]
.SH SYNOPSIS
The \f[B]\f[CB]Map[Symbol.species]\f[B]\f[R] static accessor property is
an unused accessor property specifying how to copy \f[CR]Map\f[R]
objects.
.SH SYNTAX
.IP
.EX
Map[Symbol.species]
.EE
.SS Return value
The value of the constructor (\f[CR]this\f[R]) on which
\f[CR]get [Symbol.species]\f[R] was called.
The return value is used to construct copied \f[CR]Map\f[R] instances.
.SH DESCRIPTION
The \f[CR][Symbol.species]\f[R] accessor property returns the default
constructor for \f[CR]Map\f[R] objects.
Subclass constructors may override it to change the constructor
assignment.
.RS
.PP
\f[B]Note:\f[R] This property is currently unused by all \f[CR]Map\f[R]
methods.
.RE
.SH EXAMPLES
.SS Species in ordinary objects
The \f[CR][Symbol.species]\f[R] property returns the default constructor
function, which is the \f[CR]Map\f[R] constructor for \f[CR]Map\f[R].
.IP
.EX
Map[Symbol.species]; \f[I]// function Map()\f[R]
.EE
.SS Species in derived objects
In an instance of a custom \f[CR]Map\f[R] subclass, such as
\f[CR]MyMap\f[R], the \f[CR]MyMap\f[R] species is the \f[CR]MyMap\f[R]
constructor.
However, you might want to overwrite this, in order to return parent
\f[CR]Map\f[R] objects in your derived class methods:
.IP
.EX
\f[B]class\f[R] MyMap \f[B]extends\f[R] Map {
  \f[I]// Overwrite MyMap species to the parent Map constructor\f[R]
  \f[B]static\f[R] \f[B]get\f[R] [Symbol.species]() {
    \f[B]return\f[R] Map;
  }
}
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Map\f[R]
.IP \[bu] 2
\f[CR]Symbol.species\f[R]
