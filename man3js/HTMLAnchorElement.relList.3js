.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "HTMLAnchorElement.relList" "JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLAnchorElement.relList \- HTMLAnchorElement: relList property
.SH SYNOPSIS
The \f[B]\f[CB]HTMLAnchorElement.relList\f[B]\f[R] read\-only property
reflects the \f[CR]rel\f[R] attribute.
It is a live \f[CR]DOMTokenList\f[R] containing the set of link types
indicating the relationship between the resource represented by the
\f[CR]<a>\f[R] element and the current document.
.PP
The property itself is read\-only, meaning you can\[cq]t substitute the
\f[CR]DOMTokenList\f[R] with another one, but its contents can still be
changed.
.SH VALUE
A live \f[CR]DOMTokenList\f[R] of strings.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] anchors = document.getElementsByTagName(\[dq]a\[dq]);
\f[B]for\f[R] (\f[B]const\f[R] anchor \f[B]of\f[R] anchors) {
  \f[B]const\f[R] list = anchor.relList;
  console.log(
    \[ga]New anchor node found with ${list.length} link types in relList.\[ga],
  );
  list.forEach((relValue) \f[B]=>\f[R] {
    console.log(relValue);
  });
}
.EE
.SH SEE ALSO
.IP \[bu] 2
The equivalent property on \f[CR]<area>\f[R] and \f[CR]<link>\f[R],
\f[CR]HTMLAreaElement.relList\f[R] and
\f[CR]HTMLLinkElement.relList\f[R].
.IP \[bu] 2
The very same list but as a space\-separated tokens in a string:
\f[CR]HTMLAnchorElement.rel\f[R]
