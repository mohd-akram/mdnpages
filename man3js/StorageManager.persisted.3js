.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "StorageManager.persisted" "JS" "December 30, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
StorageManager.persisted \- StorageManager: persisted() method
.SH SYNOPSIS
\f[B]Secure context\f[R]: This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]persisted()\f[B]\f[R] method of the
\f[CR]StorageManager\f[R] interface returns a \f[CR]Promise\f[R] that
resolves to \f[CR]true\f[R] if your site\[cq]s storage bucket is
persistent.
.PP
This feature is available in Web Workers.
.SH SYNTAX
.IP
.EX
persisted()
.EE
.SS Parameters
None.
.SS Returns
A \f[CR]Promise\f[R] that resolves to a \f[CR]Boolean\f[R].
.SS Exceptions
.TP
\f[B]TypeError\f[R]
Thrown if obtaining a local storage shelf failed.
For example, if the current origin is an opaque origin or if the user
has disabled storage.
.SH EXAMPLE
.IP
.EX
if (navigator.storage && navigator.storage.persist) {
  navigator.storage.persisted().then((persistent) => {
    if (persistent) {
      console.log(\[dq]Storage will not be cleared except by explicit user action\[dq]);
    } else {
      console.log(\[dq]Storage may be cleared by the UA under storage pressure.\[dq]);
    }
  });
}
.EE
