.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "LOCATION.SEARCH" "3JS" "February 13, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Location.search \- Location: search property
.SH SYNOPSIS
The \f[B]\f[CB]search\f[B]\f[R] property of the \f[CR]Location\f[R]
interface is a search string, also called a \f[I]query string\f[R], that
is a string containing a \f[CR]\(dq?\(dq\f[R] followed by the parameters
of the location\(cqs URL.
If the URL does not have a search query, this property contains an empty
string, \f[CR]\(dq\(dq\f[R].
.PP
This property can be set to change the query string of the URL.
When setting, a single \f[CR]\(dq?\(dq\f[R] prefix is added to the
provided value, if not already present.
Setting it to \f[CR]\(dq\(dq\f[R] removes the query string.
.PP
The query is percent\-encoded when setting but not percent\-decoded when
reading.
.PP
Modern browsers provide \f[CR]URLSearchParams\f[R] and
\f[CR]URL.searchParams\f[R] to make it easy to parse out the parameters
from the querystring.
.PP
See \f[CR]URL.search\f[R] for more information.
.SH VALUE
A string.
.SH EXAMPLES
.IP
.EX
\f[I]// Let an <a id=\(dqmyAnchor\(dq href=\(dq/en\-US/docs/Location.search?q=123\(dq> element be in the document\f[R]
\f[B]const\f[R] anchor = document.getElementById(\(dqmyAnchor\(dq);
\f[B]const\f[R] queryString = anchor.search; \f[I]// Returns:\(aq?q=123\(aq\f[R]

\f[I]// Further parsing:\f[R]
\f[B]const\f[R] params = \f[B]new\f[R] URLSearchParams(queryString);
\f[B]const\f[R] q = parseInt(params.get(\(dqq\(dq)); \f[I]// is the number 123\f[R]
.EE
