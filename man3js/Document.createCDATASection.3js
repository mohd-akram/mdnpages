.\" Automatically generated by Pandoc 3.4
.\"
.TH "Document.createCDATASection" "JS" "October 16, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Document.createCDATASection \- Document: createCDATASection() method
.SH SYNOPSIS
\f[B]\f[CB]createCDATASection()\f[B]\f[R] creates a new CDATA section
node, and returns it.
.SH SYNTAX
.IP
.EX
createCDATASection(data)
.EE
.SS Parameters
.TP
\f[B]data\f[R]
A string containing the data to be added to the CDATA Section.
.SS Return value
A CDATA Section node.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] doc = \f[B]new\f[R] DOMParser().parseFromString(\[dq]<xml></xml>\[dq], \[dq]application/xml\[dq]);
\f[B]const\f[R] cdata = doc.createCDATASection(\[dq]Some <CDATA> data & then some\[dq]);
doc.querySelector(\[dq]xml\[dq]).appendChild(cdata);
console.log(\f[B]new\f[R] XMLSerializer().serializeToString(doc));
\f[I]// Displays: <xml><![CDATA[Some <CDATA> data & then some]]></xml>\f[R]
.EE
.SH NOTES
.IP \[bu] 2
This will only work with XML, not HTML documents (as HTML documents do
not support CDATA sections); attempting it on an HTML document will
throw \f[CR]NOT_SUPPORTED_ERR\f[R].
.IP \[bu] 2
Will throw a \f[CR]NS_ERROR_DOM_INVALID_CHARACTER_ERR\f[R] exception if
one tries to submit the closing CDATA sequence (\f[CR]]]>\f[R]) as part
of the data, so unescaped user\-provided data cannot be safely used
without this method getting this exception (\f[CR]createTextNode()\f[R]
can often be used in its place).
