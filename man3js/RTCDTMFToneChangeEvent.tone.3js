.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "RTCDTMFToneChangeEvent.tone" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
RTCDTMFToneChangeEvent.tone \- RTCDTMFToneChangeEvent: tone property
.SH SYNOPSIS
The read\-only property \f[B]\f[CB]RTCDTMFToneChangeEvent.tone\f[B]\f[R]
returns the DTMF character which has just begun to play, or an empty
string (\f[CR]\[dq]\[dq]\f[R]).
if all queued tones have finished playing (that is,
\f[CR]RTCDTMFSender.toneBuffer\f[R] is empty).
.SH VALUE
A string with the DTML character playing, or the empty string.
.SH EXAMPLE
This example establishes a handler for the \f[CR]tonechange\f[R] event
which updates an element to display the currently playing tone in its
content, or, if all tones have played, the string \[lq]<none>\[rq].
.IP
.EX
dtmfSender.ontonechange = (ev) \f[B]=>\f[R] {
  \f[B]let\f[R] tone = ev.tone;
  \f[B]if\f[R] (tone === \[dq]\[dq]) {
    tone = \[dq]&lt;none&gt;\[dq];
  }

  document.getElementById(\[dq]playingTone\[dq]).innerText = tone;
};
.EE
.SH SEE ALSO
.IP \[bu] 2
WebRTC
.IP \[bu] 2
Using DTMF with WebRTC
.IP \[bu] 2
\f[CR]tonechange\f[R]
.IP \[bu] 2
\f[CR]RTCDTMFToneChangeEvent\f[R]
