.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "NAVIGATIONPRELOADMANAGER.ENABLE" "3JS" "June 7, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NavigationPreloadManager.enable \- NavigationPreloadManager: enable()
method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]enable()\f[B]\f[R] method of the
\f[CR]NavigationPreloadManager\f[R] interface is used to enable
preloading of resources managed by the service worker.
It returns a promise that resolves with \f[CR]undefined\f[R].
.PP
The method should be called in the service worker\(cqs
\f[CR]activate\f[R] event handler, which ensures it is called before any
\f[CR]fetch\f[R] event handler can fire.
.SH SYNTAX
.IP
.EX
enable()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]Promise\f[R] that resolves with \f[CR]undefined\f[R].
.SS Exceptions
.TP
\f[B]InvalidStateError\f[R] \f[B]DOMException\f[R]
There is no active worker associated with the registration to which this
\f[CR]NavigationPreloadManager\f[R] belongs.
.SH EXAMPLES
The code below shows how to enable preloading, after first using
\f[CR]ServiceWorkerRegistration.navigationPreload\f[R] to test that it
is supported.
.IP
.EX
addEventListener(\(dqactivate\(dq, (event) \f[B]=>\f[R] {
  event.waitUntil(
    (\f[B]async\f[R] () \f[B]=>\f[R] {
      \f[B]if\f[R] (self.registration.navigationPreload) {
        \f[I]// Enable navigation preloads!\f[R]
        \f[B]await\f[R] self.registration.navigationPreload.enable();
      }
    })(),
  );
});
.EE
.SH SEE ALSO
\f[CR]NavigationPreloadManager.disable()\f[R]
