.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "TypedArray.some" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
TypedArray.some - TypedArray.prototype.some()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]some()\f[B]\f[R] method tests whether some element in the
typed array passes the test implemented by the provided function.
This method has the same algorithm as \f[V]Array.prototype.some()\f[R].
\f[I]TypedArray\f[R] is one of the typed array types here.
.SH SYNTAX
.IP
.nf
\f[C]
some(callbackFn)
some(callbackFn, thisArg)
\f[R]
.fi
.SS Parameters
.TP
\f[B]callbackFn\f[R]
A function to execute for each element in the typed array.
It should return a truthy value to indicate the element passes the test,
and a falsy value otherwise.
The function is called with the following arguments:
.RS
.TP
\f[B]element\f[R]
The current element being processed in the typed array.
.TP
\f[B]index\f[R]
The index of the current element being processed in the typed array.
.TP
\f[B]array\f[R]
The typed array \f[V]some()\f[R] was called upon.
.RE
.TP
\f[B]thisArg\f[R] \f[I](optional)\f[R]
A value to use as \f[V]this\f[R] when executing \f[V]callbackFn\f[R].
See iterative methods.
.SS Return value
.PP
\f[B]\f[VB]true\f[B]\f[R] if the callback function returns a truthy
value for any array element; otherwise, \f[B]\f[VB]false\f[B]\f[R].
.SH DESCRIPTION
.PP
The \f[V]some\f[R] method executes the \f[V]callbackFn\f[R] function
once for each element present in the typed array until it finds one
where \f[V]callbackFn\f[R] returns a true value.
If such an element is found, \f[V]some\f[R] immediately returns
\f[V]true\f[R].
Otherwise, \f[V]some\f[R] returns \f[V]false\f[R].
.PP
\f[V]callbackFn\f[R] is invoked with three arguments: the value of the
element, the index of the element, and the array object being traversed.
.PP
If a \f[V]thisArg\f[R] parameter is provided to \f[V]some\f[R], it will
be passed to \f[V]callbackFn\f[R] when invoked, for use as its
\f[V]this\f[R] value.
Otherwise, the value \f[V]undefined\f[R] will be passed for use as its
\f[V]this\f[R] value.
The \f[V]this\f[R] value ultimately observable by \f[V]callbackFn\f[R]
is determined according to the usual rules for determining the
\f[V]this\f[R] seen by a function.
.PP
\f[V]some\f[R] does not mutate the typed array on which it is called.
.SH EXAMPLES
.SS Testing size of all typed array elements
.PP
The following example tests whether any element in the typed array is
bigger than 10.
.IP
.nf
\f[C]
function isBiggerThan10(element, index, array) {
  return element > 10;
}
new Uint8Array([2, 5, 8, 1, 4]).some(isBiggerThan10); // false
new Uint8Array([12, 5, 8, 1, 4]).some(isBiggerThan10); // true
\f[R]
.fi
.SS Testing typed array elements using arrow functions
.PP
Arrow functions provide a shorter syntax for the same test.
.IP
.nf
\f[C]
new Uint8Array([2, 5, 8, 1, 4]).some((elem) => elem > 10); // false
new Uint8Array([12, 5, 8, 1, 4]).some((elem) => elem > 10); // true
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[V]TypedArray.prototype.some\f[R] in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-typed-arrays)
.IP \[bu] 2
\f[V]TypedArray.prototype.every()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.some()\f[R]
