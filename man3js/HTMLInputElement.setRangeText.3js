.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "HTMLINPUTELEMENT.SETRANGETEXT" "3JS" "June 11, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLInputElement.setRangeText \- HTMLInputElement: setRangeText() method
.SH SYNOPSIS
The \f[B]\f[CB]HTMLInputElement.setRangeText()\f[B]\f[R] method replaces
a range of text in an \f[CR]<input>\f[R] or \f[CR]<textarea>\f[R]
element with a new string.
.SH SYNTAX
.IP
.EX
setRangeText(replacement)
setRangeText(replacement, start)
setRangeText(replacement, start, end)
setRangeText(replacement, start, end, selectMode)
.EE
.SS Parameters
.TP
\f[B]replacement\f[R]
The string to insert.
.TP
\f[B]start\f[R] \f[I](optional)\f[R]
The 0\-based index of the first character to replace.
Defaults to the current \f[CR]selectionStart\f[R] value (the start of
the user\(cqs current selection).
.TP
\f[B]end\f[R] \f[I](optional)\f[R]
The 0\-based index of the character \f[I]after\f[R] the last character
to replace.
Defaults to the current \f[CR]selectionEnd\f[R] value (the end of the
user\(cqs current selection).
.TP
\f[B]selectMode\f[R] \f[I](optional)\f[R]
A string defining how the selection should be set after the text has
been replaced.
Possible values:
.RS
.IP \(bu 2
\f[CR]\(dqselect\(dq\f[R] selects the newly inserted text.
.IP \(bu 2
\f[CR]\(dqstart\(dq\f[R] moves the selection to just before the inserted
text.
.IP \(bu 2
\f[CR]\(dqend\(dq\f[R] moves the selection to just after the inserted
text.
.IP \(bu 2
\f[CR]\(dqpreserve\(dq\f[R] attempts to preserve the selection.
This is the default.
.RE
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
Click the button in this example to replace part of the text in the text
box.
The newly inserted text will be highlighted (selected) afterwards.
.SS HTML
.IP
.EX
<\f[B]input\f[R]
  type=\(dqtext\(dq
  id=\(dqtext\-box\(dq
  size=\(dq30\(dq
  value=\(dqThis text has NOT been updated.\(dq />
<\f[B]button\f[R]>Update text</\f[B]button\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]function\f[R] selectText() {
  \f[B]const\f[R] input = document.getElementById(\(dqtext\-box\(dq);
  input.focus();
  input.setRangeText(\(dqALREADY\(dq, 14, 17, \(dqselect\(dq);
}

document.querySelector(\(dqbutton\(dq).addEventListener(\(dqclick\(dq, selectText);
.EE
.SS Result
.SH SEE ALSO
.IP \(bu 2
\f[CR]<input>\f[R]
.IP \(bu 2
\f[CR]<textarea>\f[R]
.IP \(bu 2
\f[CR]HTMLInputElement\f[R]
.IP \(bu 2
\f[CR]Selection\f[R]
