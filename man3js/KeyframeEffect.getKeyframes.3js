.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "KEYFRAMEEFFECT.GETKEYFRAMES" "3JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
KeyframeEffect.getKeyframes \- KeyframeEffect: getKeyframes() method
.SH SYNOPSIS
The \f[B]\f[CB]getKeyframes()\f[B]\f[R] method of a
\f[CR]KeyframeEffect\f[R] returns an Array of the computed keyframes
that make up this animation along with their computed offsets.
.SH SYNTAX
.IP
.EX
getKeyframes()
.EE
.SS Parameters
None.
.SS Return value
Returns a sequence of objects with the following format:
.TP
property value pairs
As many property value pairs as are contained in each keyframe of the
animation.
.TP
\f[B]offset\f[R]
The offset of the keyframe specified as a number between \f[CR]0.0\f[R]
and \f[CR]1.0\f[R] inclusive or \f[CR]null\f[R].
This is equivalent to specifying start and end states in percentages in
CSS stylesheets using \f[CR]\(atkeyframes\f[R].
This will be \f[CR]null\f[R] if the keyframe is automatically spaced.
.TP
\f[B]computedOffset\f[R]
The computed offset for this keyframe, calculated when the list of
computed keyframes was produced.
Unlike \f[B]\f[CB]offset\f[B],\f[R] above, the
\f[B]\f[CB]computedOffset\f[B]\f[R] is never \f[CR]null\f[R].
.TP
\f[B]easing\f[R]
The easing function used from this keyframe until the next keyframe in
the series.
.TP
\f[B]composite\f[R]
The \f[CR]KeyframeEffect.composite\f[R] operation used to combine the
values specified in this keyframe with the underlying value.
This will be absent if the composite operation specified on the effect
is being used.
.SH EXAMPLES
In the following example, we can inspect the rolling animation to see
its keyframes using the \f[CR]getKeyframes()\f[R] method:
.IP
.EX
\f[B]const\f[R] emoji = document.querySelector(\(dqdiv\(dq); \f[I]// element to animate\f[R]

\f[B]const\f[R] rollingKeyframes = \f[B]new\f[R] KeyframeEffect(
  emoji,
  [
    { transform: \(dqtranslateX(0) rotate(0)\(dq }, \f[I]// keyframe\f[R]
    { transform: \(dqtranslateX(200px) rotate(1.3turn)\(dq }, \f[I]// keyframe\f[R]
  ],
  {
    \f[I]// keyframe options\f[R]
    duration: 2000,
    direction: \(dqalternate\(dq,
    easing: \(dqease\-in\-out\(dq,
    iterations: \(dqInfinity\(dq,
  },
);

\f[B]const\f[R] rollingAnimation = \f[B]new\f[R] Animation(rollingKeyframes, document.timeline);
rollingAnimation.play();

\f[I]// Array [ {\&...}, {\&...} ]\f[R]
console.log(rollingAnimation.effect.getKeyframes());
.EE
.IP
.EX
<\f[B]div\f[R]>🤣</\f[B]div\f[R]>
.EE
.SH SEE ALSO
.IP \(bu 2
Web Animations API
.IP \(bu 2
Method of \f[CR]KeyframeEffect\f[R] objects.
