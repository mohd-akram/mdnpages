.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "MessagePort.start" "JS" "March 6, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
MessagePort.start \- MessagePort: start() method
.SH SYNOPSIS
This feature is available in Web Workers.
.PP
The \f[B]\f[CB]start()\f[B]\f[R] method of the \f[CR]MessagePort\f[R]
interface starts the sending of messages queued on the port.
This method is only needed when using
\f[CR]EventTarget.addEventListener\f[R]; it is implied when using
\f[CR]onmessage\f[R].
.SH SYNTAX
.IP
.EX
start()
.EE
.SS Parameters
None.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
In the following code block, you can see a \f[CR]handleMessage\f[R]
handler function, run when a message is sent back to this document using
\f[CR]onmessage\f[R]:
.IP
.EX
channel.port1.onmessage = handleMessage;
\f[B]function\f[R] handleMessage(e) {
  para.innerHTML = e.data;
}
.EE
.PP
Another option would be to do this using
\f[CR]EventTarget.addEventListener\f[R], however, when this method is
used, you need to explicitly call \f[CR]start()\f[R] to begin the flow
of messages to this document:
.IP
.EX
channel.port1.addEventListener(\[dq]message\[dq], handleMessage, \f[B]false\f[R]);
\f[B]function\f[R] handleMessage(e) {
  para.innerHTML = e.data;
  textInput.value = \[dq]\[dq];
}

channel.port1.start();
.EE
.SH SEE ALSO
.IP \[bu] 2
Using channel messaging
