.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "HTMLLINKELEMENT.REFERRERPOLICY" "3JS" "April 10, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLLinkElement.referrerPolicy \- HTMLLinkElement: referrerPolicy
property
.SH SYNOPSIS
The \f[B]\f[CB]referrerPolicy\f[B]\f[R] property of the
\f[CR]HTMLLinkElement\f[R] interface reflects the HTML
\f[CR]referrerpolicy\f[R] attribute of the \f[CR]<link>\f[R] element
defining which referrer is sent when fetching the resource.
.PP
See the HTTP \f[CR]Referrer\-Policy\f[R] header for details.
.SH VALUE
A string; one of the following:
.TP
\f[B]no\-referrer\f[R]
The \f[CR]Referer\f[R] header will be omitted entirely.
No referrer information is sent along with requests.
.TP
\f[B]no\-referrer\-when\-downgrade\f[R]
The URL is sent as a referrer when the protocol security level stays the
same (e.g.HTTP→HTTP, HTTPS→HTTPS), but isn\(cqt sent to a less secure
destination (e.g., HTTPS→HTTP).
.TP
\f[B]origin\f[R]
Only send the origin of the document as the referrer in all cases.
The document \f[CR]https://example.com/page.html\f[R] will send the
referrer \f[CR]https://example.com/\f[R].
.TP
\f[B]origin\-when\-cross\-origin\f[R]
Send a full URL when performing a same\-origin request, but only send
the origin of the document for other cases.
.TP
\f[B]same\-origin\f[R]
A referrer will be sent for same\-site origins, but cross\-origin
requests will contain no referrer information.
.TP
\f[B]strict\-origin\f[R]
Only send the origin of the document as the referrer when the protocol
security level stays the same (e.g., HTTPS→HTTPS), but don\(cqt send it
to a less secure destination (e.g., HTTPS→HTTP).
.TP
\f[B]strict\-origin\-when\-cross\-origin\f[R] (default)
This is the user agent\(cqs default behavior if no policy is specified.
Send a full URL when performing a same\-origin request, only send the
origin when the protocol security level stays the same (e.g.,
HTTPS→HTTPS), and send no header to a less secure destination (e.g.,
HTTPS→HTTP).
.TP
\f[B]unsafe\-url\f[R]
Send a full URL when performing a same\-origin or cross\-origin request.
This policy will leak origins and paths from TLS\-protected resources to
insecure origins.
Carefully consider the impact of this setting.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] links = document.getElementsByTagName(\(dqlink\(dq);
links[0].referrerPolicy; \f[I]// \(dqno\-referrer\(dq\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
HTTP header \f[CR]Referrer\-Policy\f[R]
.IP \(bu 2
\f[CR]HTMLAnchorElement.referrerPolicy\f[R]
.IP \(bu 2
\f[CR]HTMLAreaElement.referrerPolicy\f[R]
.IP \(bu 2
\f[CR]HTMLIFrameElement.referrerPolicy\f[R]
.IP \(bu 2
\f[CR]HTMLImageElement.referrerPolicy\f[R]
