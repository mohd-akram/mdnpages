.\" Automatically generated by Pandoc 3.10.1
.\"
.TH "DATATRANSFERITEM.KIND" "3JS" "August 31, 2026" "JavaScript" "JavaScript Reference Manual"
.SH NAME
DataTransferItem.kind \- DataTransferItem: kind property
.SH SYNOPSIS
The \f[B]\f[CB]kind\f[B]\f[R] read\-only property of the
\f[CR]DataTransferItem\f[R] interface returns the kind\(ena string or a
file\(enof the object representing the \f[I]drag data item\f[R].
.PP
During a drag operation, this property can be read in any drag event
handler, even when the drag data store is in protected mode.
The item\(cqs kind remains accessible, but its data can only be read in
the handlers for the \f[CR]dragstart\f[R] and \f[CR]drop\f[R] events.
See Reading the drag data store for details.
.SH VALUE
A string representing the drag data item\(cqs kind.
It must be one of the following values:
.TP
\f[B]\(aqfile\(aq\f[R]
If the drag data item is a file.
.TP
\f[B]\(aqstring\(aq\f[R]
If the kind of drag data item is a \f[I]plain Unicode string\f[R].
.SH EXAMPLES
This example shows the use of the \f[CR]kind\f[R] property.
.IP
.EX
\f[B]function\f[R] dropHandler(ev) {
  console.log(\(dqDrop\(dq);
  ev.preventDefault();
  \f[B]for\f[R] (\f[B]const\f[R] item \f[B]of\f[R] ev.dataTransfer.items) {
    \f[B]if\f[R] (item.kind === \(dqstring\(dq && item.type.match(\(dq\(hatext/plain\(dq)) {
      \f[I]// This item is the target node\f[R]
      item.getAsString((s) \f[B]=>\f[R] {
        ev.target.appendChild(document.getElementById(s));
      });
    } \f[B]else\f[R] \f[B]if\f[R] (item.kind === \(dqstring\(dq && item.type.match(\(dq\(hatext/html\(dq)) {
      \f[I]// Drag data item is HTML\f[R]
      console.log(\(dq\&... Drop: HTML\(dq);
    } \f[B]else\f[R] \f[B]if\f[R] (item.kind === \(dqfile\(dq && item.type.match(\(dq\(haimage/\(dq)) {
      \f[I]// Drag data item is an image file\f[R]
      \f[B]const\f[R] f = item.getAsFile();
      console.log(\(dq\&... Drop: File\(dq);
    }
  }
}
.EE
.SH SEE ALSO
.IP \(bu 2
Drag and drop
.IP \(bu 2
Drag Operations
.IP \(bu 2
Working with the drag data store
