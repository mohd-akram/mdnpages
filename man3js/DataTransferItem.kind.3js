.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "DataTransferItem.kind" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
DataTransferItem.kind \- DataTransferItem: kind property
.SH SYNOPSIS
The read\-only \f[B]\f[CB]DataTransferItem.kind\f[B]\f[R] property
returns a \f[CR]DataTransferItem\f[R] representing the \f[I]drag data
item\f[R] kind: some text or some file.
.SH VALUE
A string representing the drag data item\[cq]s kind.
It must be one of the following values:
.TP
\f[B]\[aq]file\[aq]\f[R]
If the drag data item is a file.
.TP
\f[B]\[aq]string\[aq]\f[R]
If the kind of drag data item is a \f[I]plain Unicode string\f[R].
.SH EXAMPLES
This example shows the use of the \f[CR]kind\f[R] property.
.IP
.EX
function drop_handler(ev) {
  console.log(\[dq]Drop\[dq]);
  ev.preventDefault();
  const data = event.dataTransfer.items;
  for (let i = 0; i < data.length; i += 1) {
    if (data[i].kind === \[dq]string\[dq] && data[i].type.match(\[dq]\[ha]text/plain\[dq])) {
      // This item is the target node
      data[i].getAsString((s) => {
        ev.target.appendChild(document.getElementById(s));
      });
    } else if (data[i].kind === \[dq]string\[dq] && data[i].type.match(\[dq]\[ha]text/html\[dq])) {
      // Drag data item is HTML
      console.log(\[dq]\&... Drop: HTML\[dq]);
    } else if (data[i].kind === \[dq]file\[dq] && data[i].type.match(\[dq]\[ha]image/\[dq])) {
      // Drag data item is an image file
      const f = data[i].getAsFile();
      console.log(\[dq]\&... Drop: File \[dq]);
    }
  }
}
.EE
.SH SEE ALSO
.IP \[bu] 2
Drag and drop
.IP \[bu] 2
Drag Operations
.IP \[bu] 2
Recommended Drag Types
.IP \[bu] 2
\c
.UR https://codepen.io/tech_query/pen/MqGgap
DataTransfer test \- Paste or Drag
.UE \c
