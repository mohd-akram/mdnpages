.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "DOMTokenList.item" "JS" "February 2, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
DOMTokenList.item \- DOMTokenList: item() method
.SH SYNOPSIS
The \f[B]\f[CB]item()\f[B]\f[R] method of the \f[CR]DOMTokenList\f[R]
interface returns an item in the list, determined by its position in the
list, its index.
.RS
.PP
\f[B]Note:\f[R] This method is equivalent as the bracket notation.
So \f[CR]aList.item(i)\f[R] is the same as \f[CR]aList[i]\f[R].
.RE
.SH SYNTAX
.IP
.EX
item(index)
.EE
.SS Parameters
.TP
\f[B]index\f[R]
A number representing the index of the item you want to return.
If it isn\[cq]t an integer, only the integer part is considered.
.SS Return value
A string representing the returned item, or \f[CR]null\f[R] if the
number is greater than or equal to the \f[CR]length\f[R] of the list.
.SS Exceptions
.TP
\f[B]TypeError\f[R]
Thrown if the \f[CR]index\f[R] cannot be converted to an integer.
.SH EXAMPLES
In the following example we retrieve the list of classes set on a
\f[CR]<span>\f[R] element as a \f[CR]DOMTokenList\f[R] using
\f[CR]Element.classList\f[R].
We then retrieve the last item in the list using
\f[CR]item(tokenList.length \- 1)\f[R], and write it into the
\f[CR]<span>\f[R]\[cq]s \f[CR]Node.textContent\f[R].
.PP
First, the HTML:
.IP
.EX
<\f[B]span\f[R] class=\[dq]a b c\[dq]></\f[B]span\f[R]>
.EE
.PP
Now the JavaScript:
.IP
.EX
\f[B]const\f[R] span = document.querySelector(\[dq]span\[dq]);
\f[B]const\f[R] classes = span.classList;
\f[B]const\f[R] item = classes.item(classes.length \- 1);
span.textContent = item;
.EE
.PP
The output looks like this:
