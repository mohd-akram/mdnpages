.\" Automatically generated by Pandoc 3.4
.\"
.TH "SVGNumberList.insertItemBefore" "JS" "January 2, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SVGNumberList.insertItemBefore \- SVGNumberList: insertItemBefore()
method
.SH SYNOPSIS
The \f[CR]insertItemBefore()\f[R] method of the \f[CR]SVGNumberList\f[R]
interface inserts a new item into the list at the specified position.
.PP
The first item is indexed at \f[CR]0\f[R].
The inserted item is the item itself and not a copy.
.IP \[bu] 2
If \f[CR]newItem\f[R] is already in a list, it is removed from its
previous list before it is inserted into this list.
.IP \[bu] 2
If the item is already in this list, note that the \f[CR]index\f[R] of
the item to insert before is before the removal of the item.
.IP \[bu] 2
If the \f[CR]index\f[R] is equal to \f[CR]0\f[R], then the new item is
inserted at the front of the list.
.IP \[bu] 2
If the \f[CR]index\f[R] is greater than or equal to
\f[CR]numberOfItems\f[R], then the new item is appended to the end of
the list.
.SH SYNTAX
.IP
.EX
SVGNumberList.insertItemBefore(newItem, index)
.EE
.SS Parameters
.TP
\f[B]newItem\f[R]
An \f[CR]SVGNumber\f[R] item that is inserted into the list.
.TP
\f[B]index\f[R]
An integer; the index where the new item should be inserted as an
unsigned long.
.SS Return value
An \f[CR]SVGNumber\f[R] object; the inserted item from the list.
.SS Exceptions
.TP
\f[B]NoModificationAllowedError\f[R] \f[B]DOMException\f[R]
Thrown if \f[CR]SVGNumberList\f[R] corresponds to a read\-only attribute
or when the object itself is read\-only.
.SH SEE ALSO
.IP \[bu] 2
\f[CR]SVGNumber\f[R]
