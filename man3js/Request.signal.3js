.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Request.signal" "JS" "July 25, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Request.signal \- Request: signal property
.SH SYNOPSIS
The read\-only \f[B]\f[CB]signal\f[B]\f[R] property of the
\f[CR]Request\f[R] interface returns the \f[CR]AbortSignal\f[R]
associated with the request.
.SH VALUE
An \f[CR]AbortSignal\f[R] object.
.SH EXAMPLES
.IP
.EX
// Create a new abort controller
const controller = new AbortController();

// Create a request with this controller\[aq]s AbortSignal object
const req = new Request(\[dq]/\[dq], { signal: controller.signal });

// Add an event handler logging a message in case of abort
req.signal.addEventListener(\[dq]abort\[dq], () => {
  console.log(\[dq]abort\[dq]);
});

// In case of abort, log the AbortSignal reason, if any
fetch(req).catch(() => {
  if (signal.aborted) {
    if (signal.reason) {
      console.log(\[ga]Request aborted with reason: ${signal.reason}\[ga]);
    } else {
      console.log(\[dq]Request aborted but no reason was given.\[dq]);
    }
  } else {
    console.log(\[dq]Request not aborted, but terminated abnormally.\[dq]);
  }
});

// Actually abort the request
controller.abort();
.EE
