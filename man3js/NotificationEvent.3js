.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "NotificationEvent" "JS" "December 10, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NotificationEvent \- NotificationEvent
.SH SYNOPSIS
The \f[B]\f[CB]NotificationEvent\f[B]\f[R] interface of the
Notifications API represents a notification event dispatched on the
\f[CR]ServiceWorkerGlobalScope\f[R] of a \f[CR]ServiceWorker\f[R].
.PP
This interface inherits from the \f[CR]ExtendableEvent\f[R] interface.
.RS
.PP
\f[B]Note\f[R]: Only persistent notification events, fired at the
\f[CR]ServiceWorkerGlobalScope\f[R] object, implement the
\f[CR]NotificationEvent\f[R] interface.
Non\-persistent notification events, fired at the
\f[CR]Notification\f[R] object, implement the \f[CR]Event\f[R]
interface.
.RE
.SH CONSTRUCTOR
.TP
\f[B]NotificationEvent()\f[R]
Creates a new \f[CR]NotificationEvent\f[R] object.
.SH INSTANCE PROPERTIES
\f[I]Also inherits properties from its parent interface,
\f[CI]ExtendableEvent\f[I]\f[R].
.TP
\f[B]NotificationEvent.notification\f[R] \f[I](read\-only)\f[R]
Returns a \f[CR]Notification\f[R] object representing the notification
that was clicked to fire the event.
.TP
\f[B]NotificationEvent.action\f[R] \f[I](read\-only)\f[R]
Returns the string ID of the notification button the user clicked.
This value returns an empty string if the user clicked the notification
somewhere other than an action button, or the notification does not have
a button.
.SH INSTANCE METHODS
\f[I]Also inherits methods from its parent interface,
\f[CI]ExtendableEvent\f[I]\f[R].
.SH EXAMPLE
.IP
.EX
self.addEventListener(\[dq]notificationclick\[dq], (event) => {
  console.log(\[ga]On notification click: ${event.notification.tag}\[ga]);
  event.notification.close();

  // This looks to see if the current is already open and
  // focuses if it is
  event.waitUntil(
    clients
      .matchAll({
        type: \[dq]window\[dq],
      })
      .then((clientList) => {
        for (const client of clientList) {
          if (client.url === \[dq]/\[dq] && \[dq]focus\[dq] in client) return client.focus();
        }
        if (clients.openWindow) return clients.openWindow(\[dq]/\[dq]);
      }),
  );
});
.EE
.RS
.PP
\f[B]Note:\f[R] This interface is specified in the Notifications API,
but accessed through \f[CR]ServiceWorkerGlobalScope\f[R].
.RE
