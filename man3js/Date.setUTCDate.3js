.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Date.setUTCDate" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Date.setUTCDate - Date.prototype.setUTCDate()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]setUTCDate()\f[B]\f[R] method of \f[V]Date\f[R] instances
changes the day of the month for this date according to universal time.
.SH SYNTAX
.IP
.nf
\f[C]
setUTCDate(dateValue)
\f[R]
.fi
.SS Parameters
.TP
\f[B]dateValue\f[R]
An integer from 1 to 31 representing the day of the month.
.SS Return value
.PP
Changes the \f[V]Date\f[R] object in place, and returns its new
timestamp.
If \f[V]dateValue\f[R] is \f[V]NaN\f[R] (or other values that get
coerced to \f[V]NaN\f[R], such as \f[V]undefined\f[R]), the date is set
to Invalid Date and \f[V]NaN\f[R] is returned.
.SH DESCRIPTION
.PP
If the \f[V]dateValue\f[R] is outside of the range of date values for
the month, \f[V]setDate()\f[R] will update the \f[V]Date\f[R] object
accordingly.
.PP
For example, if 0 is provided for \f[V]dateValue\f[R], the date will be
set to the last day of the previous month.
If you use 40 for \f[V]dateValue\f[R], and the month stored in the
\f[V]Date\f[R] object is June, the day will be changed to 10 and the
month will be incremented to July.
.PP
If a negative number is provided for \f[V]dateValue\f[R], the date will
be set counting backwards from the last day of the previous month.
-1 would result in the date being set to 1 day before the last day of
the previous month.
.SH EXAMPLES
.SS Using setUTCDate()
.IP
.nf
\f[C]
const theBigDay = new Date();
theBigDay.setUTCDate(20);
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]Date.prototype.getUTCDate()\f[R]
.IP \[bu] 2
\f[V]Date.prototype.setDate()\f[R]
