.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Document.append" "JS" "August 4, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Document.append \- Document: append() method
.SH SYNOPSIS
The \f[B]\f[CB]Document.append()\f[B]\f[R] method inserts a set of
\f[CR]Node\f[R] objects or string objects after the last child of the
document.
String objects are inserted as equivalent \f[CR]Text\f[R] nodes.
.PP
This method appends a child to a \f[CR]Document\f[R].
To append to an arbitrary element in the tree, see
\f[CR]Element.append()\f[R].
.SH SYNTAX
.IP
.EX
append(param1)
append(param1, param2)
append(param1, param2, /* \&..., */ paramN)
.EE
.SS Parameters
.TP
\f[B]param1\f[R], \&..., \f[B]paramN\f[R]
A set of \f[CR]Node\f[R] or string objects to insert.
.SS Return value
None (\f[CR]undefined\f[R]).
.SS Exceptions
.TP
\f[B]HierarchyRequestError\f[R] \f[B]DOMException\f[R]
Thrown when the node cannot be inserted at the specified point in the
hierarchy.
.SH EXAMPLES
.SS Appending a root element to a document
If you try to append an element to an existing HTML document, it might
throw a \f[CR]HierarchyRequestError\f[R] \f[CR]DOMException\f[R] given a
\f[CR]<html>\f[R] element already exists.
.IP
.EX
let html = document.createElement(\[dq]html\[dq]);
document.append(html);
// HierarchyRequestError: The operation would yield an incorrect node tree.
.EE
.PP
If you are creating a new document without any existing element, you can
append a root HTML element (or a root SVG element):
.IP
.EX
let doc = new Document();
let html = document.createElement(\[dq]html\[dq]);
doc.append(html);

doc.children; // HTMLCollection [<html>]
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Document.prepend()\f[R]
.IP \[bu] 2
\f[CR]Element.append()\f[R]
