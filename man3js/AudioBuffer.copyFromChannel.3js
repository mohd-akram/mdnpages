.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "AUDIOBUFFER.COPYFROMCHANNEL" "3JS" "April 6, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
AudioBuffer.copyFromChannel \- AudioBuffer: copyFromChannel() method
.SH SYNOPSIS
The \f[B]\f[CB]copyFromChannel()\f[B]\f[R] method of the
\f[CR]AudioBuffer\f[R] interface copies the audio sample data from the
specified channel of the \f[CR]AudioBuffer\f[R] to a specified
\f[CR]Float32Array\f[R].
.SH SYNTAX
.IP
.EX
copyFromChannel(destination, channelNumber, startInChannel)
.EE
.SS Parameters
.TP
\f[B]destination\f[R]
A \f[CR]Float32Array\f[R] to copy the channel\(cqs samples to.
.TP
\f[B]channelNumber\f[R]
The channel number of the current \f[CR]AudioBuffer\f[R] to copy the
channel data from.
.TP
\f[B]startInChannel\f[R] \f[I](optional)\f[R]
An optional offset into the source channel\(cqs buffer from which to
begin copying samples.
If not specified, a value of 0 (the beginning of the buffer) is assumed
by default.
.SS Return value
None (\f[CR]undefined\f[R]).
.SS Exceptions
.TP
\f[B]indexSizeError\f[R]
One of the input parameters has a value that is outside the accepted
range:
.RS
.IP \(bu 2
The value of \f[CR]channelNumber\f[R] specifies a channel number which
doesn\(cqt exist (that is, it\(cqs greater than or equal to the value of
\f[CR]numberOfChannels\f[R] on the channel).
.IP \(bu 2
The value of \f[CR]startInChannel\f[R] is outside the current range of
samples that already exist in the source buffer; that is, it\(cqs
greater than its current \f[CR]length\f[R].
.RE
.SH EXAMPLES
This example creates a new audio buffer, then copies the samples from
another channel into it.
.IP
.EX
\f[B]const\f[R] myArrayBuffer = audioCtx.createBuffer(2, frameCount, audioCtx.sampleRate);
\f[B]const\f[R] anotherArray = \f[B]new\f[R] Float32Array(length);
myArrayBuffer.copyFromChannel(anotherArray, 1, 0);
.EE
.SH SEE ALSO
.IP \(bu 2
Using the Web Audio API
