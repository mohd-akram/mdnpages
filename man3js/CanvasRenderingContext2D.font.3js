.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "CANVASRENDERINGCONTEXT2D.FONT" "3JS" "April 21, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
CanvasRenderingContext2D.font \- CanvasRenderingContext2D: font property
.SH SYNOPSIS
The \f[B]\f[CB]CanvasRenderingContext2D.font\f[B]\f[R] property of the
Canvas 2D API specifies the current text style to use when drawing text.
This string uses the same syntax as the CSS font specifier.
.SH VALUE
A string parsed as CSS \f[CR]font\f[R] value.
The default font is 10px sans\-serif.
.SH EXAMPLES
.SS Using a custom font
In this example we use the \f[CR]font\f[R] property to specify a custom
font weight, size, and family.
.SS HTML
.IP
.EX
<\f[B]canvas\f[R] id=\(dqcanvas\(dq></\f[B]canvas\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] canvas = document.getElementById(\(dqcanvas\(dq);
\f[B]const\f[R] ctx = canvas.getContext(\(dq2d\(dq);

ctx.font = \(dqbold 48px serif\(dq;
ctx.strokeText(\(dqHello world\(dq, 50, 100);
.EE
.SS Result
.SS Loading fonts with the CSS Font Loading API
With the help of the \f[CR]FontFace\f[R] API, you can explicitly load
fonts before using them in a canvas.
.IP
.EX
\f[B]let\f[R] f = \f[B]new\f[R] FontFace(\(dqtest\(dq, \(dqurl(x)\(dq);

f.load().then(() \f[B]=>\f[R] {
  \f[I]// Ready to use the font in a canvas context\f[R]
});
.EE
.SH SEE ALSO
.IP \(bu 2
The interface defining this property:
\f[CR]CanvasRenderingContext2D\f[R]
