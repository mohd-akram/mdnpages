.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "DOMTOKENLIST.REPLACE" "3JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
DOMTokenList.replace \- DOMTokenList: replace() method
.SH SYNOPSIS
The \f[B]\f[CB]replace()\f[B]\f[R] method of the \f[CR]DOMTokenList\f[R]
interface replaces an existing token with a new token.
If the first token doesn\(cqt exist, \f[CR]replace()\f[R] returns
\f[CR]false\f[R] immediately, without adding the new token to the token
list.
.SH SYNTAX
.IP
.EX
replace(oldToken, newToken)
.EE
.SS Parameters
.TP
\f[B]oldToken\f[R]
A string representing the token you want to replace.
.TP
\f[B]newToken\f[R]
A string representing the token you want to replace \f[CR]oldToken\f[R]
with.
.SS Return value
A boolean value, which is \f[CR]true\f[R] if \f[CR]oldToken\f[R] was
successfully replaced, or \f[CR]false\f[R] if not.
.SH EXAMPLES
In the following example we retrieve the list of classes set on a
\f[CR]<span>\f[R] element as a \f[CR]DOMTokenList\f[R] using
\f[CR]Element.classList\f[R].
We then replace a token in the list, and write the list into the
\f[CR]<span>\f[R]\(cqs \f[CR]Node.textContent\f[R].
.PP
First, the HTML:
.IP
.EX
<\f[B]span\f[R] class=\(dqa b c\(dq></\f[B]span\f[R]>
.EE
.PP
Now the JavaScript:
.IP
.EX
\f[B]const\f[R] span = document.querySelector(\(dqspan\(dq);
\f[B]const\f[R] classes = span.classList;

\f[B]const\f[R] result = classes.replace(\(dqc\(dq, \(dqz\(dq);

span.textContent = result ? classes : \(dqtoken not replaced successfully\(dq;
.EE
.PP
The output looks like this:
