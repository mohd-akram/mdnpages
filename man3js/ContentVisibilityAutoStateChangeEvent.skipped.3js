.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "CONTENTVISIBILITYAUTOSTATECHANGEEVENT.SKIPPED" "3JS" "July 4, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
ContentVisibilityAutoStateChangeEvent.skipped \-
ContentVisibilityAutoStateChangeEvent: skipped property
.SH SYNOPSIS
The \f[CR]skipped\f[R] read\-only property of the
\f[CR]ContentVisibilityAutoStateChangeEvent\f[R] interface returns
\f[CR]true\f[R] if the user agent skips the element\(cqs contents, or
\f[CR]false\f[R] otherwise.
.SH VALUE
A boolean.
Returns \f[CR]true\f[R] if the user agent skips the element\(cqs
contents, or \f[CR]false\f[R] otherwise.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] canvasElem = document.querySelector(\(dqcanvas\(dq);

canvasElem.addEventListener(\(dqcontentvisibilityautostatechange\(dq, stateChanged);
canvasElem.style.contentVisibility = \(dqauto\(dq;

\f[B]function\f[R] stateChanged(event) {
  \f[B]if\f[R] (event.skipped) {
    stopCanvasUpdates(canvasElem);
  } \f[B]else\f[R] {
    startCanvasUpdates(canvasElem);
  }
}

\f[I]// Call this when the canvas updates need to start.\f[R]
\f[B]function\f[R] startCanvasUpdates(canvas) {
  \f[I]// \&...\f[R]
}

\f[I]// Call this when the canvas updates need to stop.\f[R]
\f[B]function\f[R] stopCanvasUpdates(canvas) {
  \f[I]// \&...\f[R]
}
.EE
.SH SEE ALSO
.IP \(bu 2
The \f[CR]contentvisibilityautostatechange\f[R] event
.IP \(bu 2
CSS Containment
.IP \(bu 2
The \f[CR]content\-visibility\f[R] property
.IP \(bu 2
The \f[CR]contain\f[R] property
