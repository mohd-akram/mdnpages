.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Performance.eventCounts" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Performance.eventCounts \- Performance: eventCounts property
.SH SYNOPSIS
The read\-only \f[CR]performance.eventCounts\f[R] property is an
\f[CR]EventCounts\f[R] map containing the number of events which have
been dispatched per event type.
.PP
Not all event types are exposed.
You can only get counts for event types supported by the
\f[CR]PerformanceEventTiming\f[R] interface.
.SH VALUE
An \f[CR]EventCounts\f[R] map.
(A read\-only \f[CR]Map\f[R] without the \f[CR]clear()\f[R],
\f[CR]delete()\f[R], and \f[CR]set()\f[R] methods).
.SH EXAMPLES
.SS Reporting event types and their counts
If you like to send event counts to your analytics, you may want to
implement a function like \f[CR]sendToEventAnalytics\f[R] which takes
the event counts from the \f[CR]performance.eventCounts\f[R] map and
then uses the Fetch API to post the data to your endpoint.
.IP
.EX
// Report all exposed events
for (entry of performance.eventCounts.entries()) {
  const type = entry[0];
  const count = entry[1];
  // sendToEventAnalytics(type, count);
}

// Report a specific event
const clickCount = performance.eventCounts.get(\[dq]click\[dq]);
// sendToEventAnalytics(\[dq]click\[dq], clickCount);

// Check if an event count is exposed for a type
const isExposed = performance.eventCounts.has(\[dq]mousemove\[dq]); // false
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]EventCounts\f[R]
.IP \[bu] 2
\f[CR]PerformanceEventTiming\f[R]
.IP \[bu] 2
\f[CR]Map\f[R]
