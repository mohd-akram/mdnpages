.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "TIMERANGES.START" "3JS" "October 19, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
TimeRanges.start \- TimeRanges: start() method
.SH SYNOPSIS
The \f[B]\f[CB]start()\f[B]\f[R] method of the \f[CR]TimeRanges\f[R]
interface returns the time offset (in seconds) at which a specified time
range begins.
.SH SYNTAX
.IP
.EX
start(index)
.EE
.SS Parameters
.TP
\f[B]index\f[R]
The range number to return the starting time for.
.SS Return value
A number.
.SS Exceptions
.TP
\f[B]IndexSizeError\f[R] \f[B]DOMException\f[R]
Thrown if the specified index doesn\(cqt correspond to an existing
range.
.SH EXAMPLES
Given a video element with the ID \(lqmyVideo\(rq:
.IP
.EX
\f[B]const\f[R] v = document.getElementById(\(dqmyVideo\(dq);

\f[B]const\f[R] buf = v.buffered;

\f[B]const\f[R] numRanges = buf.length;

\f[B]if\f[R] (buf.length === 1) {
  \f[I]// only one range\f[R]
  \f[B]if\f[R] (buf.start(0) === 0 && buf.end(0) === v.duration) {
    \f[I]// The one range starts at the beginning and ends at\f[R]
    \f[I]// the end of the video, so the whole thing is loaded\f[R]
  }
}
.EE
.PP
This example looks at the time ranges and looks to see if the entire
video has been loaded.
