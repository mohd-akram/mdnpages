.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "String.fontsize" "JS" "September 15, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
String.fontsize - String.prototype.fontsize()
.SH SYNOPSIS
\f[I](deprecated)\f[R]
.PP
The \f[B]\f[CB]fontsize()\f[B]\f[R] method of \f[CR]String\f[R] values
creates a string that embeds this string in a \f[CR]<font>\f[R] element
(\f[CR]<font size=\[dq]...\[dq]>str</font>\f[R]), which causes this
string to be displayed in the specified font size.
.RS
.PP
\f[B]Note:\f[R] All HTML wrapper methods are deprecated and only
standardized for compatibility purposes.
For the case of \f[CR]fontsize()\f[R], the \f[CR]<font>\f[R] element
itself has been removed from the HTML specification and shouldn\[cq]t be
used anymore.
Web developers should use CSS properties instead.
.RE
.SH SYNTAX
.IP
.EX
fontsize(size)
.EE
.SS Parameters
.TP
\f[B]size\f[R]
An integer between 1 and 7, or a string representing a signed integer
between 1 and 7.
.SS Return value
A string beginning with a \f[CR]<font size=\[dq]size\[dq]>\f[R] start
tag (double quotes in \f[CR]size\f[R] are replaced with
\f[CR]&quot;\f[R]), then the text \f[CR]str\f[R], and then a
\f[CR]</font>\f[R] end tag.
.SH DESCRIPTION
The \f[CR]fontsize()\f[R] method itself simply joins the string parts
together without any validation or normalization.
However, to create valid \f[CR]<font>\f[R] elements, When you specify
size as an integer, you set the font size of \f[CR]str\f[R] to one of
the 7 defined sizes.
You can specify \f[CR]size\f[R] as a string such as
\f[CR]\[dq]-2\[dq]\f[R] or \f[CR]\[dq]+3\[dq]\f[R] to adjust the font
size of \f[CR]str\f[R] relative to 3, the default value.
.SH EXAMPLES
.SS Using fontsize()
The code below creates an HTML string and then replaces the
document\[cq]s body with it:
.IP
.EX
const contentString = \[dq]Hello, world\[dq];

document.body.innerHTML = contentString.fontsize(7);
.EE
.PP
This will create the following HTML:
.IP
.EX
<font size=\[dq]7\[dq]>Hello, world</font>
.EE
.RS
.PP
\f[B]Warning:\f[R] This markup is invalid, because \f[CR]font\f[R] is no
longer a valid element.
.RE
.PP
Instead of using \f[CR]fontsize()\f[R] and creating HTML text directly,
you should use CSS to manipulate fonts.
For example, you can manipulate {{cssxref(\[lq]font-size\[rq])}} through
the \f[CR]element.style\f[R] attribute:
.IP
.EX
document.getElementById(\[dq]yourElemId\[dq]).style.fontSize = \[dq]7pt\[dq];
.EE
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[CR]String.prototype.fontsize\f[R] in
\f[CR]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-string-and-regexp)
.IP \[bu] 2
HTML wrapper methods
.IP \[bu] 2
\f[CR]<font>\f[R]
