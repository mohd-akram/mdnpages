.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "ELEMENT.ARIAACTIVEDESCENDANTELEMENT" "3JS" "May 3, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.ariaActiveDescendantElement \- Element:
ariaActiveDescendantElement property
.SH SYNOPSIS
The \f[B]\f[CB]ariaActiveDescendantElement\f[B]\f[R] property of the
\f[CR]Element\f[R] interface represents the current active element when
focus is on a \f[CR]composite\f[R] widget, \f[CR]combobox\f[R],
\f[CR]textbox\f[R], \f[CR]group\f[R], or \f[CR]application\f[R].
.PP
The \f[CR]aria\-activedescendant\f[R] topic contains additional
information about how the attribute and property should be used.
.SH VALUE
An subclass of \f[CR]HTMLElement\f[R] that represents the active
descendant, or \f[CR]null\f[R] if there is no active descendant.
.SH DESCRIPTION
The property is a flexible alternative to using the
\f[CR]aria\-activedescendant\f[R] attribute.
Unlike \f[CR]aria\-activedescendant\f[R], the element assigned to this
property does not have to have an \f[CR]id\f[R] attribute.
.PP
The property reflects the element\(cqs \f[CR]aria\-activedescendant\f[R]
attribute when it is defined, but only for reference \f[CR]id\f[R]
values that match valid in\-scope elements.
If the property is set, then the corresponding attribute is cleared.
For more information about reflected element references and scope see
Reflected element references in the \f[I]Reflected attributes\f[R]
guide.
.SH EXAMPLES
.SS Get the active descendant
This example shows how \f[CR]ariaActiveDescendantElement\f[R] can be
used to get the current active descendant.
.SS HTML
The HTML defines a listbox for selecting different kinds of streets,
consisting of a \f[CR]<div>\f[R] element with the \f[CR]listbox\f[R]
role and nested \f[CR]<div>\f[R] items for each of the options.
The active descendant is initially set to the element with \f[CR]id\f[R]
of \f[CR]avenue\f[R] using \f[CR]aria\-activedescendant\f[R].
.IP
.EX
<\f[B]div\f[R] id=\(dqstreetType\(dq role=\(dqlistbox\(dq aria\-activedescendant=\(dqavenue\(dq>
  <\f[B]div\f[R]>Street</\f[B]div\f[R]>
  <\f[B]div\f[R] id=\(dqavenue\(dq>Avenue</\f[B]div\f[R]>
  <\f[B]div\f[R]>Lane</\f[B]div\f[R]>
</\f[B]div\f[R]>
.EE
.SS JavaScript
The code below first checks whether the
\f[CR]ariaActiveDescendantElement\f[R] is supported.
It the property is supported the code then logs the value of
\f[CR]aria\-activedescendant\f[R] (the \f[CR]id\f[R] of the referenced
element) using \f[CR]Element.getAttribute()\f[R], the property element,
and the element\(cqs text content.
.IP
.EX
\f[I]// Feature test for ariaActiveDescendantElement\f[R]
\f[B]if\f[R] (\(dqariaActiveDescendantElement\(dq \f[B]in\f[R] Element.prototype) {
  log(\(gagetAttribute(): ${streetType.getAttribute(\(dqaria\-activedescendant\(dq)}\(ga);
  log(\(gaariaActiveDescendantElement: ${streetType.ariaActiveDescendantElement}\(ga);
  log(\(gatext: ${streetType.ariaActiveDescendantElement?.textContent.trim()}\(ga);
} \f[B]else\f[R] {
  log(\(dqariaActiveDescendantElement not supported by browser\(dq);
}
.EE
.SS Result
The log below shows the output of the above code.
The value returned from the \f[CR]aria\-activedescendant\f[R] property
should be \f[CR]\(dqavenue\(dq\f[R], the associated element should be a
\f[CR]HTMLDivElement\f[R] element, and the text in that element should
be \f[CR]Avenue\f[R].
.SH SEE ALSO
.IP \(bu 2
\f[CR]aria\-activedescendant\f[R] attribute
.IP \(bu 2
\f[CR]ElementInternals.ariaActiveDescendantElement\f[R]
.IP \(bu 2
Reflected element references in the \f[I]Attribute reflection\f[R]
guide.
