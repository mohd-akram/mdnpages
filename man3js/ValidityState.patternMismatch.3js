.\" Automatically generated by Pandoc 3.4
.\"
.TH "ValidityState.patternMismatch" "JS" "December 19, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
ValidityState.patternMismatch \- ValidityState: patternMismatch property
.SH SYNOPSIS
The read\-only \f[B]\f[CB]patternMismatch\f[B]\f[R] property of the
\f[CR]ValidityState\f[R] interface indicates if the value of an
\f[CR]<input>\f[R], after having been edited by the user, does not
conform to the constraints set by the element\[cq]s \f[CR]pattern\f[R]
attribute.
.PP
The \f[CR]patternMismatch\f[R] property will be true if and only if the
following conditions are all true:
.IP \[bu] 2
the field supports the \f[CR]pattern\f[R] attribute \[em] which means
the \f[CR]<input>\f[R] is of \f[CR]type\f[R] \f[CR]text\f[R],
\f[CR]tel\f[R], \f[CR]email\f[R], \f[CR]url\f[R], \f[CR]password\f[R],
or \f[CR]search\f[R]
.IP \[bu] 2
the \f[CR]pattern\f[R] attribute contains a valid regular expression
.IP \[bu] 2
the \f[CR]<input>\f[R] value doesn\[cq]t conform to the constraints set
by the \f[CR]pattern\f[R] value.
.SH VALUE
A boolean that is \f[CR]true\f[R] if the \f[CR]ValidityState\f[R] object
does not conform to the constraints.
.SH EXAMPLES
Given the following:
.IP
.EX
<\f[B]p\f[R]>
  <\f[B]label\f[R]
    >Enter your phone number in the format (123)456\-7890 (<\f[B]input\f[R]
      name=\[dq]tel1\[dq]
      type=\[dq]tel\[dq]
      pattern=\[dq][0\-9]{3}\[dq]
      placeholder=\[dq]###\[dq]
      aria\-label=\[dq]3\-digit area code\[dq]
      size=\[dq]2\[dq] />)\-
    <\f[B]input\f[R]
      name=\[dq]tel2\[dq]
      type=\[dq]tel\[dq]
      pattern=\[dq][0\-9]{3}\[dq]
      placeholder=\[dq]###\[dq]
      aria\-label=\[dq]3\-digit prefix\[dq]
      size=\[dq]2\[dq] />
    \-
    <\f[B]input\f[R]
      name=\[dq]tel3\[dq]
      type=\[dq]tel\[dq]
      pattern=\[dq][0\-9]{4}\[dq]
      placeholder=\[dq]####\[dq]
      aria\-label=\[dq]4\-digit number\[dq]
      size=\[dq]3\[dq] />
  </\f[B]label\f[R]>
</\f[B]p\f[R]>
.EE
.PP
Here we have 3 sections for a North American phone number with an
implicit label encompassing all three components of the phone number,
expecting 3\-digits, 3\-digits and 4\-digits respectively, as defined by
the \f[CR]pattern\f[R] attribute set on each.
.PP
If the values are too long or too short, or contain characters that
aren\[cq]t digits, \f[CR]patternMismatch\f[R] will be true.
When \f[CR]true\f[R], the element matches the \f[CR]:invalid\f[R] CSS
pseudo\-classes.
.IP
.EX
input\f[BI]:invalid\f[R] {
  \f[B]border\f[R]: red solid 3px;
}
.EE
.PP
Note, in this case, we get a \f[CR]patternMismatch\f[R] not a
\f[CR]validityState.tooLong\f[R] or \f[CR]validityState.tooShort\f[R] if
the values are too long or too short because it is the pattern that is
dictating the length of the value.
Had we used \f[CR]minlength\f[R] and \f[CR]maxlength\f[R] attributes
instead, we may have seen \f[CR]validityState.tooLong\f[R] or
\f[CR]validityState.tooShort\f[R] being true.
.RS
.PP
\f[B]Note:\f[R] The \f[CR]email\f[R] input type requires, at minimum, a
match of \f[CR]x\[at]y\f[R] and the \f[CR]url\f[R] type requires, at
minimum, a match to x:, with no pattern attribute present.
When invalid, the \f[CR]validityState.typeMismatch\f[R] will be true, if
there is no pattern attribute (or if the pattern attribute is not valid
for that input type).
.RE
.SH SEE ALSO
.IP \[bu] 2
Constraint validation
.IP \[bu] 2
Forms: Data form validation
.IP \[bu] 2
Regular Expressions
