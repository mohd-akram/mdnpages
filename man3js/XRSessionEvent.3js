.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "XRSessionEvent" "JS" "March 4, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
XRSessionEvent \- XRSessionEvent
.SH SYNOPSIS
\f[B]Secure context\f[R]: This feature is available only in secure
contexts (HTTPS).
.PP
The WebXR Device API\[cq]s \f[B]\f[CB]XRSessionEvent\f[B]\f[R] interface
describes an event which indicates the change of the state of an
\f[CR]XRSession\f[R].
These events occur, for example, when the session ends or the visibility
of its context changes.
.SH CONSTRUCTOR
.TP
\f[B]XRSessionEvent()\f[R]
Creates and returns a new \f[CR]XRSessionEvent\f[R] object.
.SH INSTANCE PROPERTIES
\f[I]In addition to properties inherited from its parent interface,
\f[CI]Event\f[I], \f[CI]XRSessionEvent\f[I] provides the following:\f[R]
.TP
\f[B]session\f[R] \f[I](read\-only)\f[R]
The \f[CR]XRSession\f[R] to which the event refers.
.SH INSTANCE METHODS
\f[I]While \f[CI]XRSessionEvent\f[I] defines no methods, it inherits
methods from its parent interface, \f[CI]Event\f[I].\f[R]
.SH SESSION EVENT TYPES
\f[I]The following events are represented using the
\f[CI]XRSessionEvent\f[I] interface, and are permitted values for its
\f[CI]type\f[I] property.\f[R]
.TP
\f[B]end\f[R] \f[I](secure context)\f[R]
Fired at the session when it has ended, after being terminated by the
application or the user agent.
.TP
\f[B]visibilitychange\f[R] \f[I](secure context)\f[R]
Fired at the session whenever its visibility state changes.
.SH EXAMPLES
This example creates a listener that watches for the visibility state of
the session to change.
It reacts by calling a function \f[CR]mySessionVisible()\f[R] with a
Boolean indicating whether or not the session is visible; this function
might, for instance, spin up or reconfigure a worker that handles
rendering the scene.
.IP
.EX
xrSession.addEventListener(\[dq]visibilitystate\[dq], (e) \f[B]=>\f[R] {
  \f[B]switch\f[R] (e.session.visibilitystate) {
    \f[B]case\f[R] \[dq]visible\[dq]:
    \f[B]case\f[R] \[dq]visible\-blurred\[dq]:
      mySessionVisible(\f[B]true\f[R]);
      \f[B]break\f[R];
    \f[B]case\f[R] \[dq]hidden\[dq]:
      mySessionVisible(\f[B]false\f[R]);
      \f[B]break\f[R];
  }
});
.EE
