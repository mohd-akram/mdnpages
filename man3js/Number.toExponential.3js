.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "Number.toExponential" "JS" "September 25, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Number.toExponential \- Number.prototype.toExponential()
.SH SYNOPSIS
The \f[B]\f[CB]toExponential()\f[B]\f[R] method of \f[CR]Number\f[R]
values returns a string representing this number in exponential
notation.
.SH SYNTAX
.IP
.EX
toExponential()
toExponential(fractionDigits)
.EE
.SS Parameters
.TP
\f[B]fractionDigits\f[R] \f[I](optional)\f[R]
Optional.
An integer specifying the number of digits after the decimal point.
Defaults to as many digits as necessary to specify the number.
.SS Return value
A string representing the given \f[CR]Number\f[R] object in exponential
notation with one digit before the decimal point, rounded to
\f[CR]fractionDigits\f[R] digits after the decimal point.
.SS Exceptions
.TP
\f[B]RangeError\f[R]
Thrown if \f[CR]fractionDigits\f[R] is not between \f[CR]0\f[R] and
\f[CR]100\f[R] (inclusive).
.TP
\f[B]TypeError\f[R]
Thrown if this method is invoked on an object that is not a
\f[CR]Number\f[R].
.SH DESCRIPTION
If the \f[CR]fractionDigits\f[R] argument is omitted, the number of
digits after the decimal point defaults to the number of digits
necessary to represent the value uniquely.
.PP
If you use the \f[CR]toExponential()\f[R] method for a numeric literal
and the numeric literal has no exponent and no decimal point, leave
whitespace(s) before the dot that precedes the method call to prevent
the dot from being interpreted as a decimal point.
.PP
If a number has more digits than requested by the
\f[CR]fractionDigits\f[R] parameter, the number is rounded to the
nearest number represented by \f[CR]fractionDigits\f[R] digits.
See the discussion of rounding in the description of the
\f[CR]toFixed()\f[R] method, which also applies to
\f[CR]toExponential()\f[R].
.SH EXAMPLES
.SS Using toExponential
.IP
.EX
\f[B]const\f[R] numObj = 77.1234;

console.log(numObj.toExponential()); \f[I]// 7.71234e+1\f[R]
console.log(numObj.toExponential(4)); \f[I]// 7.7123e+1\f[R]
console.log(numObj.toExponential(2)); \f[I]// 7.71e+1\f[R]
console.log((77.1234).toExponential()); \f[I]// 7.71234e+1\f[R]
console.log((77).toExponential()); \f[I]// 7.7e+1\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
\c
.UR https://github.com/zloirock/core-js#ecmascript-number
Polyfill of \f[CR]Number.prototype.toExponential\f[R] with many bug
fixes in \f[CR]core\-js\f[R]
.UE \c
.IP \[bu] 2
\f[CR]Number.prototype.toFixed()\f[R]
.IP \[bu] 2
\f[CR]Number.prototype.toPrecision()\f[R]
.IP \[bu] 2
\f[CR]Number.prototype.toString()\f[R]
