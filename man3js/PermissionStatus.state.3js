.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "PERMISSIONSTATUS.STATE" "3JS" "April 19, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
PermissionStatus.state \- PermissionStatus: state property
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]state\f[B]\f[R] read\-only property of the
\f[CR]PermissionStatus\f[R] interface returns the state of a requested
permission.
This property returns one of \f[CR]\(aqgranted\(aq\f[R],
\f[CR]\(aqdenied\(aq\f[R], or \f[CR]\(aqprompt\(aq\f[R].
.SH VALUE
One of the following:
.TP
\f[B]\(aqgranted\(aq\f[R]
The user, or the user agent on the user\(cqs behalf, has given express
permission to use a \c
.UR https://w3c.github.io/permissions/#dfn-powerful-feature
powerful feature
.UE \c
\&.
The caller can use the feature possibly without having the user agent
ask the user\(cqs permission.
.TP
\f[B]\(aqdenied\(aq\f[R]
The user, or the user agent on the user\(cqs behalf, has denied access
to this \c
.UR https://w3c.github.io/permissions/#dfn-powerful-feature
powerful feature
.UE \c
\&.
The caller can\(cqt use the feature.
.TP
\f[B]\(aqprompt\(aq\f[R]
The user has not given express permission to use the feature (\f[I]i.e.,
it\(cqs the same as denied\f[R]).
It also means that if a caller attempts to use the feature, the user
agent will either be prompting the user for permission or access to the
feature will be denied.
.SH EXAMPLES
.IP
.EX
navigator.permissions
  .query({ name: \(dqgeolocation\(dq })
  .then((permissionStatus) \f[B]=>\f[R] {
    console.log(\(gageolocation permission state is ${permissionStatus.state}\(ga);
    permissionStatus.onchange = () \f[B]=>\f[R] {
      console.log(
        \(gageolocation permission status has changed to ${permissionStatus.state}\(ga,
      );
    };
  });
.EE
