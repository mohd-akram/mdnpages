.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Generator" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Generator - Generator
.SH SYNOPSIS
.PP
The \f[B]\f[VB]Generator\f[B]\f[R] object is returned by a
\f[V]generator function\f[R] and it conforms to both the iterable
protocol and the iterator protocol.
.PP
\f[V]Generator\f[R] is a subclass of the hidden \f[V]Iterator\f[R]
class.
.SH CONSTRUCTOR
.PP
The \f[V]Generator\f[R] constructor is not available globally.
Instances of \f[V]Generator\f[R] must be returned from generator
functions:
.IP
.nf
\f[C]
function* generator() {
  yield 1;
  yield 2;
  yield 3;
}

const gen = generator(); // \[dq]Generator { }\[dq]

console.log(gen.next().value); // 1
console.log(gen.next().value); // 2
console.log(gen.next().value); // 3
\f[R]
.fi
.PP
In fact, there\[cq]s no JavaScript entity that corresponds to the
\f[V]Generator\f[R] constructor.
There\[cq]s only a hidden object which is the prototype object shared by
all objects created by generator functions.
This object is often stylized as \f[V]Generator.prototype\f[R] to make
it look like a class, but it should be more appropriately called
\f[V]GeneratorFunction.prototype.prototype\f[R], because
\f[V]GeneratorFunction\f[R] is an actual JavaScript entity.
.SH INSTANCE PROPERTIES
.PP
These properties are defined on \f[V]Generator.prototype\f[R] and shared
by all \f[V]Generator\f[R] instances.
.TP
\f[B]Generator.prototype.constructor\f[R]
The constructor function that created the instance object.
For \f[V]Generator\f[R] instances, the initial value is
\f[V]GeneratorFunction.prototype\f[R].
.RS
.RS
.PP
\f[B]Note:\f[R] \f[V]Generator\f[R] objects do not store a reference to
the generator function that created them.
.RE
.RE
.TP
\f[B]Generator.prototype[\[at]\[at]toStringTag]\f[R]
The initial value of the \f[V]\[at]\[at]toStringTag\f[R] property is the
string \f[V]\[dq]Generator\[dq]\f[R].
This property is used in \f[V]Object.prototype.toString()\f[R].
.SH INSTANCE METHODS
.PP
\f[I]Also inherits instance methods from its parent
\f[VI]Iterator\f[I]\f[R].
.TP
\f[B]Generator.prototype.next()\f[R]
Returns a value yielded by the \f[V]yield\f[R] expression.
.TP
\f[B]Generator.prototype.return()\f[R]
Acts as if a \f[V]return\f[R] statement is inserted in the
generator\[cq]s body at the current suspended position, which finishes
the generator and allows the generator to perform any cleanup tasks when
combined with a \f[V]try...finally\f[R] block.
.TP
\f[B]Generator.prototype.throw()\f[R]
Acts as if a \f[V]throw\f[R] statement is inserted in the
generator\[cq]s body at the current suspended position, which informs
the generator of an error condition and allows it to handle the error,
or perform cleanup and close itself.
.SH EXAMPLES
.SS An infinite iterator
.PP
With a generator function, values are not evaluated until they are
needed.
Therefore a generator allows us to define a potentially infinite data
structure.
.IP
.nf
\f[C]
function* infinite() {
  let index = 0;

  while (true) {
    yield index++;
  }
}

const generator = infinite(); // \[dq]Generator { }\[dq]

console.log(generator.next().value); // 0
console.log(generator.next().value); // 1
console.log(generator.next().value); // 2
// \&...
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]function*\f[R]
.IP \[bu] 2
\f[V]function*\f[R] expression
.IP \[bu] 2
\f[V]GeneratorFunction\f[R]
.IP \[bu] 2
The Iterator protocol
