.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Event.target" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Event.target \- Event: target property
.SH SYNOPSIS
The read\-only \f[B]\f[CB]target\f[B]\f[R] property of the
\f[CR]Event\f[R] interface is a reference to the object onto which the
event was dispatched.
It is different from \f[CR]Event.currentTarget\f[R] when the event
handler is called during the bubbling or capturing phase of the event.
.SH VALUE
The associated \f[CR]EventTarget\f[R].
.SH EXAMPLE
The \f[CR]event.target\f[R] property can be used in order to implement
\f[B]event delegation\f[R].
.IP
.EX
// Make a list
const ul = document.createElement(\[dq]ul\[dq]);
document.body.appendChild(ul);

const li1 = document.createElement(\[dq]li\[dq]);
const li2 = document.createElement(\[dq]li\[dq]);
ul.appendChild(li1);
ul.appendChild(li2);

function hide(evt) {
  // evt.target refers to the clicked <li> element
  // This is different than evt.currentTarget, which would refer to the parent <ul> in this context
  evt.target.style.visibility = \[dq]hidden\[dq];
}

// Attach the listener to the list
// It will fire when each <li> is clicked
ul.addEventListener(\[dq]click\[dq], hide, false);
.EE
.SH SEE ALSO
.IP \[bu] 2
Comparison of Event Targets
