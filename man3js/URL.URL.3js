'\" t
.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "URL.URL" "JS" "April 8, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
URL.URL \- URL: URL() constructor
.SH SYNOPSIS
The \f[B]\f[CB]URL()\f[B]\f[R] constructor returns a newly created
\f[CR]URL\f[R] object representing the URL defined by the parameters.
.PP
If the given base URL or the resulting URL are not valid URLs, the
JavaScript \f[CR]TypeError\f[R] exception is thrown.
.PP
This feature is available in Web Workers.
.SH SYNTAX
.IP
.EX
new URL(url)
new URL(url, base)
.EE
.SS Parameters
.TP
\f[B]url\f[R]
A string or any other object with a stringifier \[em] including, for
example, an \f[CR]<a>\f[R] or \f[CR]<area>\f[R] element \[em] that
represents an absolute or relative URL.
If \f[CR]url\f[R] is a relative URL, \f[CR]base\f[R] is required, and
will be used as the base URL.
If \f[CR]url\f[R] is an absolute URL, a given \f[CR]base\f[R] will be
ignored.
.TP
\f[B]base\f[R] \f[I](optional)\f[R]
A string representing the base URL to use in cases where \f[CR]url\f[R]
is a relative URL.
If not specified, it defaults to \f[CR]undefined\f[R].
.RS
.PP
\f[B]Note:\f[R] The \f[CR]url\f[R] and \f[CR]base\f[R] arguments will
each be stringified from whatever value you pass, just like with other
Web APIs that accept a string.
In particular, you can use an existing \f[CR]URL\f[R] object for either
argument, and it will stringify to the object\[cq]s \f[CR]href\f[R]
property.
.RE
.SS Exceptions
.PP
.TS
tab(@);
lw(12.6n) lw(57.4n).
T{
Exception
T}@T{
Explanation
T}
_
T{
\f[CR]TypeError\f[R]
T}@T{
\f[CR]url\f[R] (in the case of absolute URLs) or \f[CR]base\f[R] +
\f[CR]url\f[R] (in the case of relative URLs) is not a valid URL.
T}
.TE
.SH EXAMPLES
.IP
.EX
// Base URLs:
let baseUrl = \[dq]https://developer.mozilla.org\[dq];

let A = new URL(\[dq]/\[dq], baseUrl);
// => \[aq]https://developer.mozilla.org/\[aq]

let B = new URL(baseUrl);
// => \[aq]https://developer.mozilla.org/\[aq]

new URL(\[dq]en\-US/docs\[dq], B);
// => \[aq]https://developer.mozilla.org/en\-US/docs\[aq]

let D = new URL(\[dq]/en\-US/docs\[dq], B);
// => \[aq]https://developer.mozilla.org/en\-US/docs\[aq]

new URL(\[dq]/en\-US/docs\[dq], D);
// => \[aq]https://developer.mozilla.org/en\-US/docs\[aq]

new URL(\[dq]/en\-US/docs\[dq], A);
// => \[aq]https://developer.mozilla.org/en\-US/docs\[aq]

new URL(\[dq]/en\-US/docs\[dq], \[dq]https://developer.mozilla.org/fr\-FR/toto\[dq]);
// => \[aq]https://developer.mozilla.org/en\-US/docs\[aq]

// Invalid URLs:

new URL(\[dq]/en\-US/docs\[dq], \[dq]\[dq]);
// Raises a TypeError exception as \[aq]\[aq] is not a valid URL

new URL(\[dq]/en\-US/docs\[dq]);
// Raises a TypeError exception as \[aq]/en\-US/docs\[aq] is not a valid URL

// Other cases:

new URL(\[dq]http://www.example.com\[dq]);
// => \[aq]http://www.example.com/\[aq]

new URL(\[dq]http://www.example.com\[dq], B);
// => \[aq]http://www.example.com/\[aq]

new URL(\[dq]\[dq], \[dq]https://example.com/?query=1\[dq]);
// => \[aq]https://example.com/?query=1\[aq] (Edge before 79 removes query arguments)

new URL(\[dq]/a\[dq], \[dq]https://example.com/?query=1\[dq]);
// => \[aq]https://example.com/a\[aq] (see relative URLs)

new URL(\[dq]//foo.com\[dq], \[dq]https://example.com\[dq]);
// => \[aq]https://foo.com/\[aq] (see relative URLs)
.EE
.SH SEE ALSO
.IP \[bu] 2
\c
.UR https://github.com/zloirock/core-js#url-and-urlsearchparams
Polyfill of \f[CR]URL\f[R] in \f[CR]core\-js\f[R]
.UE \c
.IP \[bu] 2
The interface it belongs to: \f[CR]URL\f[R].
