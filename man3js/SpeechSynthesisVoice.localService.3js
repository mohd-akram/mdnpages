.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "SPEECHSYNTHESISVOICE.LOCALSERVICE" "3JS" "April 8, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SpeechSynthesisVoice.localService \- SpeechSynthesisVoice: localService
property
.SH SYNOPSIS
The \f[B]\f[CB]localService\f[B]\f[R] read\-only property of the
\f[CR]SpeechSynthesisVoice\f[R] interface returns a boolean value
indicating whether the voice is supplied by a local speech synthesizer
service (\f[CR]true\f[R]), or a remote speech synthesizer service
(\f[CR]false\f[R].)
.PP
This property is provided to allow differentiation in the case that some
voice options are provided by a remote service; it is possible that
remote voices might have extra latency, bandwidth or cost associated
with them, so such distinction may be useful.
.SH VALUE
A boolean value.
.SH EXAMPLES
.IP
.EX
\f[B]for\f[R] (\f[B]let\f[R] i = 0; i < voices.length; i++) {
  \f[B]const\f[R] option = document.createElement(\(dqoption\(dq);
  option.textContent = \(ga${voices[i].name} (${voices[i].lang})\(ga;

  \f[B]if\f[R] (voices[i].default) {
    option.textContent += \(dq \(em DEFAULT\(dq;
  }

  console.log(voices[i].localService);

  option.setAttribute(\(dqdata\-lang\(dq, voices[i].lang);
  option.setAttribute(\(dqdata\-name\(dq, voices[i].name);
  voiceSelect.appendChild(option);
}
.EE
.SH SEE ALSO
.IP \(bu 2
Web Speech API
