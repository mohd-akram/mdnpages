.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "CanvasRenderingContext2D.lineDashOffset" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
CanvasRenderingContext2D.lineDashOffset \- CanvasRenderingContext2D:
lineDashOffset property
.SH SYNOPSIS
The \f[B]\f[CB]CanvasRenderingContext2D.lineDashOffset\f[B]\f[R]
property of the Canvas 2D API sets the line dash offset, or
\[lq]phase.\[rq]
.RS
.PP
\f[B]Note:\f[R] Lines are drawn by calling the \f[CR]stroke()\f[R]
method.
.RE
.SH VALUE
A float specifying the amount of the line dash offset.
The default value is \f[CR]0.0\f[R].
.SH EXAMPLES
.SS Offsetting a line dash
This example draws two dashed lines.
The first has no dash offset.
The second has a dash offset of 4.
.SS HTML
.IP
.EX
<\f[B]canvas\f[R] id=\[dq]canvas\[dq]></\f[B]canvas\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] canvas = document.getElementById(\[dq]canvas\[dq]);
\f[B]const\f[R] ctx = canvas.getContext(\[dq]2d\[dq]);

ctx.setLineDash([4, 16]);

\f[I]// Dashed line with no offset\f[R]
ctx.beginPath();
ctx.moveTo(0, 50);
ctx.lineTo(300, 50);
ctx.stroke();

\f[I]// Dashed line with offset of 4\f[R]
ctx.beginPath();
ctx.strokeStyle = \[dq]red\[dq];
ctx.lineDashOffset = 4;
ctx.moveTo(0, 100);
ctx.lineTo(300, 100);
ctx.stroke();
.EE
.SS Result
The line with a dash offset is drawn in red.
.SS Marching ants
The \c
.UR https://en.wikipedia.org/wiki/Marching_ants
marching ants
.UE \c
\ effect is an animation technique often found in selection tools of
computer graphics programs.
It helps the user to distinguish the selection border from the image
background by animating the border.
.IP
.EX
\f[B]const\f[R] canvas = document.getElementById(\[dq]canvas\[dq]);
\f[B]const\f[R] ctx = canvas.getContext(\[dq]2d\[dq]);
\f[B]let\f[R] offset = 0;

\f[B]function\f[R] draw() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  ctx.setLineDash([4, 2]);
  ctx.lineDashOffset = offset;
  ctx.strokeRect(10, 10, 100, 100);
}

\f[B]function\f[R] march() {
  offset++;
  \f[B]if\f[R] (offset > 5) {
    offset = 0;
  }
  draw();
  setTimeout(march, 20);
}

march();
.EE
.SH SEE ALSO
.IP \[bu] 2
The interface defining this property:
\f[CR]CanvasRenderingContext2D\f[R]
.IP \[bu] 2
\f[CR]CanvasRenderingContext2D.getLineDash()\f[R]
.IP \[bu] 2
\f[CR]CanvasRenderingContext2D.setLineDash()\f[R]
.IP \[bu] 2
Applying styles and color
