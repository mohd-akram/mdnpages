.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "RTCVideoSourceStats.framesPerSecond" "JS" "March 23, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
RTCVideoSourceStats.framesPerSecond \- RTCVideoSourceStats:
framesPerSecond property
.SH SYNOPSIS
The \f[B]\f[CB]framesPerSecond\f[B]\f[R] property of the
\f[CR]RTCVideoSourceStats\f[R] dictionary indicates the number of frames
originating from this video source in the last second.
.PP
The property is not defined on the stats object for the first second of
its lifetime.
.SH VALUE
A number indicating the frames originating from this source in the last
second.
.SH EXAMPLES
This example shows how you might iterate the stats object returned from
\f[CR]RTCRtpSender.getStats()\f[R] to get the video source stats, and
then extract the \f[CR]framesPerSecond\f[R].
.IP
.EX
\f[I]// where sender is an RTCRtpSender\f[R]
\f[B]const\f[R] stats = \f[B]await\f[R] sender.getStats();
\f[B]let\f[R] videoSourceStats = \f[B]null\f[R];

stats.forEach((report) \f[B]=>\f[R] {
  \f[B]if\f[R] (report.type === \[dq]media\-source\[dq] && report.kind===\[dq]video\[dq]) {
    videoSourceStats = report;
    \f[B]break\f[R];
  }
});

\f[I]// Note, test is conditional in case the stats object\f[R]
\f[I]// does not include video source stats\f[R]
\f[B]const\f[R] fps = videoSourceStats?.framesPerSecond;
.EE
