.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "AsyncIterator.\[at]\[at]asyncIterator" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
AsyncIterator.\[at]\[at]asyncIterator -
AsyncIterator.prototype\[at]\[at]asyncIterator
.SH SYNOPSIS
.PP
The \f[B]\f[VB][\[at]\[at]asyncIterator]()\f[B]\f[R] method of
\f[V]AsyncIterator\f[R] instances implements the async iterable protocol
and allows built-in async iterators to be consumed by most syntaxes
expecting async iterables, such as \f[V]for await...of\f[R] loops.
It returns the value of \f[V]this\f[R], which is the async iterator
object itself.
.SH SYNTAX
.IP
.nf
\f[C]
asyncIterator[Symbol.asyncIterator]()
\f[R]
.fi
.SS Return value
.PP
The value of \f[V]this\f[R], which is the async iterator object itself.
.SH EXAMPLES
.SS Iteration using for await\&...of loop
.PP
Note that you seldom need to call this method directly.
The existence of the \f[V]\[at]\[at]asyncIterator\f[R] method makes all
built-in async iterators async iterable, and iterating syntaxes like the
\f[V]for await...of\f[R] loop automatically calls this method to obtain
the async iterator to loop over.
.IP
.nf
\f[C]
const asyncIterator = (async function* () {
  yield 1;
  yield 2;
  yield 3;
})();
(async () => {
  for await (const value of asyncIterator) {
    console.log(value);
  }
})();
// Logs: 1, 2, 3
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]for await...of\f[R]
