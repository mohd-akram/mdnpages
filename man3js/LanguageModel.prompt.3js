.\" Automatically generated by Pandoc 3.10.1
.\"
.TH "LANGUAGEMODEL.PROMPT" "3JS" "August 27, 2026" "JavaScript" "JavaScript Reference Manual"
.SH NAME
LanguageModel.prompt \- LanguageModel: prompt() method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]prompt()\f[B]\f[R] method of the \f[CR]LanguageModel\f[R]
interface sends input to the language model and returns a
\f[CR]Promise\f[R] that resolves with the model\(cqs complete response
as a string.
.SH SYNTAX
.IP
.EX
prompt(input)
prompt(input, options)
.EE
.SS Parameters
.TP
\f[B]input\f[R]
The content to prompt the model with.
This is either:
.RS
.IP \(bu 2
A string \(em Shorthand for a single textual message.
.IP \(bu 2
An array of objects, each representing a single message in a
conversation with a language model.
Objects may have the following properties:
.RS 2
.TP
\f[B]role\f[R]
A string indicating the point of view the message is phrased from.
Must be one of:
.RS
.TP
\f[B]system\f[R]
A system\-level instruction that guides the model\(cqs overall behavior.
This must be the first instruction passed to the model.
.TP
\f[B]user\f[R]
A message from the user, which the API should respond to.
.TP
\f[B]assistant\f[R]
An input that provides context for the AI assistant, such as its persona
or the format of its responses.
Such messages mainly serve to provide context/history, and further shape
how the model responds.
.RE
.TP
\f[B]content\f[R]
A string representing a textual prompt, or an array of objects.
Each object includes the following properties:
.RS
.TP
\f[B]type\f[R]
An enumerated value representing the type of content.
This can be one of:
.RS
.TP
\f[B]audio\f[R]
Audio content.
.TP
\f[B]image\f[R]
Image content.
.TP
\f[B]text\f[R]
Textual content.
.TP
\f[B]tool\-call\f[R]
A tool invocation issued by the model.
.TP
\f[B]tool\-response\f[R]
The result of a tool invocation.
.RE
.TP
\f[B]value\f[R]
The content of the message.
If the \f[CR]type\f[R] is \f[CR]text\f[R], this is always a string.
If the \f[CR]type\f[R] is \f[CR]audio\f[R] or \f[CR]image\f[R], the
\f[CR]value\f[R] can be one of several different object types; see What
data types are accepted?.
.RE
.TP
\f[B]prefix\f[R] \f[I](optional)\f[R]
A boolean, defaulting to \f[CR]false\f[R].
When \f[CR]true\f[R], the message is treated as a prefix for the
model\(cqs next generated response rather than a complete turn.
.RE
.RE
.TP
\f[B]options\f[R] \f[I](optional)\f[R]
Options for creating a prompt.
Properties include:
.RS
.TP
\f[B]responseConstraint\f[R]
An object following the structure defined by \c
.UR https://json-schema.org/
JSON Schema
.UE \c
\ defining the precise format the model\(cqs output should be delivered
in.
When provided and \f[CR]omitResponseConstraintInput\f[R] is
\f[CR]false\f[R], any implementation\-defined constraint\-description
message is included in the measurement.
.TP
\f[B]omitResponseConstraintInput\f[R]
A boolean; when \f[CR]true\f[R], the automatic constraint\-description
message is excluded from the measurement.
.TP
\f[B]signal\f[R]
An \f[CR]AbortSignal\f[R] to cancel the operation.
.RE
.SS Return value
A \f[CR]Promise\f[R] that resolves with a \f[CR]String\f[R] containing
the model\(cqs complete response.
.SS Exceptions
.TP
\f[B]AbortError\f[R] \f[B]DOMException\f[R]
Thrown if the operation was canceled via the \f[CR]signal\f[R] option.
.TP
\f[B]NotAllowedError\f[R] \f[B]DOMException\f[R]
Thrown if usage of the method is blocked by a \f[CR]language\-model\f[R]
\f[CR]Permissions\-Policy\f[R].
.TP
\f[B]NotSupportedError\f[R] \f[B]DOMException\f[R]
Thrown if:
.RS
.IP \(bu 2
A message\(cqs \f[CR]role\f[R] is \f[CR]assistant\f[R] and its
\f[CR]type\f[R] is anything other than \f[CR]text\f[R].
.IP \(bu 2
A message\(cqs \f[CR]type\f[R] is \f[CR]text\f[R] and its
\f[CR]value\f[R] is not a string.
.IP \(bu 2
The input or output text is in a language the user agent doesn\(cqt
support for prompting.
.IP \(bu 2
A message\(cqs \f[CR]type\f[R] is \f[CR]image\f[R] or \f[CR]audio\f[R]
but the type was not listed in \f[CR]expectedInputs\f[R], or the
\f[CR]value\f[R] is not an accepted data type.
.RE
.TP
\f[B]OperationError\f[R] \f[B]DOMException\f[R]
Thrown if the prompt fails for any other reason not listed in the other
exception types.
.TP
\f[B]QuotaExceededError\f[R] \f[B]DOMException\f[R]
Thrown if the prompt would cause the session\(cqs context usage to
exceed the model\(cqs \f[CR]LanguageModel.contextWindow\f[R].
.TP
\f[B]SyntaxError\f[R] \f[B]DOMException\f[R]
Thrown if:
.RS
.IP \(bu 2
No messages are included in the messages array.
.IP \(bu 2
A message\(cqs \f[CR]prefix\f[R] property is set to \f[CR]true\f[R] and:
.RS 2
.IP \(bu 2
The message\(cqs \f[CR]role\f[R] is not \f[CR]assistant\f[R].
.IP \(bu 2
The message is not the last item in the messages array.
.RE
.RE
.TP
\f[B]TypeError\f[R]
Thrown if:
.RS
.IP \(bu 2
\f[CR]omitResponseConstraintInput\f[R] is \f[CR]true\f[R] but
\f[CR]responseConstraint\f[R] is not provided.
.IP \(bu 2
A message\(cqs \f[CR]role\f[R] is \f[CR]system\f[R] but it was not the
first message passed to the context.
.RE
.SH DESCRIPTION
The \f[CR]prompt()\f[R] method is the primary mechanism for interacting
with a language model session.
It adds the provided input to the context window and generates a
response.
The entire response is buffered and returned as a single string when
generation completes.
.PP
For long responses or streaming use cases, use
\f[CR]LanguageModel.promptStreaming()\f[R] instead to receive the
response incrementally.
To add content to the context window without generating a response, use
\f[CR]LanguageModel.append()\f[R].
.PP
Each call to \f[CR]prompt()\f[R] adds to the session\(cqs context.
To branch from a given state without affecting the original session,
call \f[CR]LanguageModel.clone()\f[R].
.SH EXAMPLES
.SS Basic text prompt
This example shows basic \f[CR]prompt()\f[R] usage with a single user
text input.
.IP
.EX
\f[B]const\f[R] session = \f[B]await\f[R] LanguageModel.create();
\f[B]const\f[R] response = \f[B]await\f[R] session.prompt(
  \(dqSummarize the water cycle in one paragraph.\(dq,
);
console.log(response);
.EE
.PP
See also Using the Prompt API > Prompting the model.
.SS Multi\-turn conversation
.IP
.EX
\f[B]const\f[R] session = \f[B]await\f[R] LanguageModel.create();

\f[B]const\f[R] reply1 = \f[B]await\f[R] session.prompt(\(dqMy name is Alex.\(dq);
console.log(reply1); \f[I]// \(dqNice to meet you, Alex!\(dq\f[R]

\f[B]const\f[R] reply2 = \f[B]await\f[R] session.prompt(\(dqWhat\(aqs my name?\(dq);
console.log(reply2); \f[I]// \(dqYour name is Alex.\(dq\f[R]
.EE
.SS Constrained JSON output
The following example shows how to pass JSON to the
\f[CR]responseConstraint\f[R] option to specify that you want an array
returned by the call to \f[CR]prompt()\f[R].
.IP
.EX
\f[B]const\f[R] session = \f[B]await\f[R] LanguageModel.create();
\f[B]const\f[R] raw = \f[B]await\f[R] session.prompt(\(dqName three planets in our solar system.\(dq, {
  responseConstraint: {
    type: \(dqobject\(dq,
    properties: {
      planets: {
        type: \(dqarray\(dq,
        items: { type: \(dqstring\(dq },
      },
    },
    required: [\(dqplanets\(dq],
  },
});

\f[B]const\f[R] { planets } = JSON.parse(raw);
console.log(planets); \f[I]// [\(dqMercury\(dq, \(dqVenus\(dq, \(dqEarth\(dq]\f[R]
.EE
.PP
See also Adding context with initial and ongoing prompt inputs > Adding
response constraints.
.SS Canceling a prompt
The following example shows how to enable a user to cancel a prompt with
a button.
It does this by creating an \f[CR]AbortController\f[R].
Its \f[CR]abort()\f[R] is callable from a button\(cqs \f[CR]click\f[R]
handler.
For this to work, a reference to the controller\(cqs \f[CR]signal\f[R]
property must be passed to \f[CR]prompt()\f[R].
.IP
.EX
\f[B]const\f[R] controller = \f[B]new\f[R] AbortController();

\f[I]// Select your cancel button from the DOM\f[R]
\f[B]const\f[R] cancelButton = document.querySelector(\(dq#btn\-cancel\(dq);

\f[I]// Trigger the abort when the user clicks the button\f[R]
cancelButton.addEventListener(\(dqclick\(dq, () \f[B]=>\f[R] {
  controller.abort();
});

\f[B]try\f[R] {
  \f[B]const\f[R] response = \f[B]await\f[R] session.prompt(\(dqwrite a very long story.\(dq, {
    signal: controller.signal,
  });
  console.log(response);
} \f[B]catch\f[R] (err) {
  \f[B]if\f[R] (err.name === \(dqAbortError\(dq) {
    console.log(\(dqprompt was canceled.\(dq);
  } \f[B]else\f[R] {
    console.error(\(dqAn unexpected error occurred:\(dq, err);
  }
}
.EE
.PP
See also Using the Prompt API > Canceling operations and destroying
instances.
.SH SEE ALSO
.IP \(bu 2
\f[CR]LanguageModel.promptStreaming()\f[R]
.IP \(bu 2
\f[CR]LanguageModel.append()\f[R]
.IP \(bu 2
Prompt API
.IP \(bu 2
Using the Prompt API
.IP \(bu 2
Adding context with initial and ongoing prompt inputs
