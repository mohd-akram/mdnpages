.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "Window.cancelAnimationFrame" "JS" "September 16, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Window.cancelAnimationFrame \- Window: cancelAnimationFrame() method
.SH SYNOPSIS
The \f[B]\f[CB]window.cancelAnimationFrame()\f[B]\f[R] method cancels an
animation frame request previously scheduled through a call to
\f[CR]window.requestAnimationFrame()\f[R].
.SH SYNTAX
.IP
.EX
cancelAnimationFrame(requestID)
.EE
.SS Parameters
.TP
\f[B]requestID\f[R]
The ID value returned by the call to
\f[CR]window.requestAnimationFrame()\f[R] that requested the callback.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] start = document.timeline.currentTime;

\f[B]let\f[R] myReq;

\f[B]function\f[R] step(timestamp) {
  \f[B]const\f[R] progress = timestamp \- start;
  d.style.left = \[ga]${Math.min(progress / 10, 200)}px\[ga];
  \f[B]if\f[R] (progress < 2000) {
    \f[I]// it\[aq]s important to update the requestId each time you\[aq]re calling requestAnimationFrame\f[R]
    myReq = requestAnimationFrame(step);
  }
}
myReq = requestAnimationFrame(step);
\f[I]// the cancellation uses the last requestId\f[R]
cancelAnimationFrame(myReq);
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Window.requestAnimationFrame()\f[R]
.IP \[bu] 2
\f[CR]DedicatedWorkerGlobalScope.cancelAnimationFrame()\f[R]
