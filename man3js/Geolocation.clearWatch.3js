.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "GEOLOCATION.CLEARWATCH" "3JS" "December 1, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Geolocation.clearWatch \- Geolocation: clearWatch() method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]clearWatch()\f[B]\f[R] method of the
\f[CR]Geolocation\f[R] interface is used to unregister location/error
monitoring handlers previously installed using
\f[CR]Geolocation.watchPosition()\f[R].
.SH SYNTAX
.IP
.EX
clearWatch(id)
.EE
.SS Parameters
.TP
\f[B]id\f[R]
The ID number returned by the \f[CR]Geolocation.watchPosition()\f[R]
method when installing the handler you wish to remove.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.IP
.EX
\f[B]let\f[R] id;
\f[B]let\f[R] target;
\f[B]let\f[R] options;

\f[B]function\f[R] success(pos) {
  \f[B]const\f[R] crd = pos.coords;

  \f[B]if\f[R] (target.latitude === crd.latitude && target.longitude === crd.longitude) {
    console.log(\(dqCongratulations, you\(aqve reached the target!\(dq);
    navigator.geolocation.clearWatch(id);
  }
}

\f[B]function\f[R] error(err) {
  console.error(\(gaERROR(${err.code}): ${err.message}\(ga);
}

target = {
  latitude: 0,
  longitude: 0,
};

options = {
  enableHighAccuracy: \f[B]false\f[R],
  timeout: 5000,
  maximumAge: 0,
};

id = navigator.geolocation.watchPosition(success, error, options);
.EE
.SH SEE ALSO
.IP \(bu 2
Using geolocation
.IP \(bu 2
\f[CR]Geolocation\f[R]
.IP \(bu 2
\f[CR]Geolocation.watchPosition()\f[R]
.IP \(bu 2
\f[CR]Geolocation.getCurrentPosition()\f[R]
