.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "HTMLLinkElement.fetchPriority" "JS" "December 2, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLLinkElement.fetchPriority \- HTMLLinkElement: fetchPriority property
.SH SYNOPSIS
The \f[B]\f[CB]fetchPriority\f[B]\f[R] property of the
\f[CR]HTMLLinkElement\f[R] interface represents a hint given to the
browser on how it should prioritize the preload of the given resource
relative to other resources of the same type.
.SH VALUE
A string representing the priority hint.
Possible values are:
.TP
\f[B]high\f[R]
Fetch the preload at a high priority relative to other resources of the
same type.
.TP
\f[B]low\f[R]
Fetch the image at a low priority relative to other resources of the
same type.
.TP
\f[B]auto\f[R]
Default mode, which indicates no preference for the fetch priority.
The browser decides what is best for the user.
.PP
The \f[CR]fetchPriority\f[R] property allows you to signal high or low
priority preload fetches.
This can be useful when applied to \f[CR]<link>\f[R] elements to signal
preloads that are more or less important to the user experience early in
the loading process.
.PP
The effects of the hint on resource loading is browser\-specific so make
sure to test on multiple browser engines.
.PP
Use it sparingly for exceptional cases where the browser may not be able
to infer the best way to load the resource automatically.
Over use can result in degrading performance.
.SH EXAMPLES
.IP
.EX
const preloadLink = document.createElement(\[dq]link\[dq]);
preloadLink.href = \[dq]myimage.jpg\[dq];
preloadLink.rel = \[dq]preload\[dq];
preloadLink.as = \[dq]image\[dq];
preloadLink.fetchPriority = \[dq]high\[dq];
document.head.appendChild(preloadLink);
.EE
