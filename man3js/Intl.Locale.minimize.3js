.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "Intl.Locale.minimize" "JS" "September 25, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Intl.Locale.minimize - Intl.Locale.prototype.minimize()
.SH SYNOPSIS
The \f[B]\f[CB]minimize()\f[B]\f[R] method of \f[CR]Intl.Locale\f[R]
instances attempts to remove information about this locale that would be
added by calling \f[CR]maximize()\f[R].
.SH SYNTAX
.IP
.EX
minimize()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]Intl.Locale\f[R] instance whose \f[CR]baseName\f[R] property
returns the result of the Remove Likely
Subtags (https://www.unicode.org/reports/tr35/#Likely_Subtags) algorithm
executed against \f[I]\f[CI]locale.baseName\f[I]\f[R].
.SH DESCRIPTION
This method carries out the reverse of \f[CR]maximize()\f[R], removing
any language, script, or region subtags from the locale language
identifier (essentially the contents of \f[CR]baseName\f[R]).
This is useful when there are superfluous subtags in the language
identifier; for instance, \[lq]en-Latn\[rq] can be simplified to
\[lq]en\[rq], since \[lq]Latn\[rq] is the only script used to write
English.
\f[CR]minimize()\f[R] only affects the main subtags that comprise the
language
identifier (https://www.unicode.org/reports/tr35/#Language_Locale_Field_Definitions):
language, script, and region subtags.
Other subtags after the \[lq]-u\[rq] in the locale identifier are called
extension subtags and are not affected by the \f[CR]minimize()\f[R]
method.
Examples of these subtags include \f[CR]hourCycle\f[R],
\f[CR]calendar\f[R], and \f[CR]numeric\f[R].
.SH EXAMPLES
.SS Using minimize
.IP
.EX
const myLocale = new Intl.Locale(\[dq]fr-Latn-FR\[dq], {
  hourCycle: \[dq]h12\[dq],
  calendar: \[dq]gregory\[dq],
});
console.log(myLocale.baseName); // Prints \[dq]fr-Latn-FR\[dq]
console.log(myLocale.toString()); // Prints \[dq]fr-Latn-FR-u-ca-gregory-hc-h12\[dq]

const myLocMinimized = myLocale.minimize();

// Prints \[dq]fr\[dq], since French is only written in the Latin script
// and is most likely to be spoken in France.
console.log(myLocMinimized.baseName);

// Prints \[dq]fr-u-ca-gregory-hc-h12\[dq].
// Note that the extension tags (after \[dq]-u\[dq]) remain unchanged.
console.log(myLocMinimized.toString());
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Intl.Locale\f[R]
.IP \[bu] 2
\f[CR]baseName\f[R]
