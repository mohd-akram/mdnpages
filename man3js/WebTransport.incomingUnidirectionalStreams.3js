.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "WebTransport.incomingUnidirectionalStreams" "JS" "March 6, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
WebTransport.incomingUnidirectionalStreams \- WebTransport:
incomingUnidirectionalStreams property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]incomingUnidirectionalStreams\f[B]\f[R] read\-only
property of the \f[CR]WebTransport\f[R] interface represents one or more
unidirectional streams opened by the server.
Returns a \f[CR]ReadableStream\f[R] of
\f[CR]WebTransportReceiveStream\f[R] objects.
Each one can be used to reliably read data from the server.
.PP
\[lq]Reliable\[rq] means that transmission and order of data are
guaranteed.
This provides slower delivery (albeit faster than with WebSockets) than
\f[CR]datagrams\f[R], but is needed in situations where reliability and
ordering are important, like chat applications.
.SH VALUE
A \f[CR]ReadableStream\f[R] of \f[CR]WebTransportReceiveStream\f[R]
objects.
.SH EXAMPLES
An initial function is used to read the
\f[CR]WebTransportReceiveStream\f[R] objects from the
\f[CR]ReadableStream\f[R].
Each object is then passed to another function to read from those
streams.
.IP
.EX
\f[B]async\f[R] \f[B]function\f[R] receiveUnidirectional() {
  \f[B]const\f[R] uds = transport.incomingUnidirectionalStreams;
  \f[B]const\f[R] reader = uds.getReader();
  \f[B]while\f[R] (\f[B]true\f[R]) {
    \f[B]const\f[R] { done, value } = \f[B]await\f[R] reader.read();
    \f[B]if\f[R] (done) {
      \f[B]break\f[R];
    }
    \f[I]// value is an instance of WebTransportReceiveStream\f[R]
    \f[B]await\f[R] readData(value);
  }
}

\f[B]async\f[R] \f[B]function\f[R] readData(receiveStream) {
  \f[B]const\f[R] reader = receiveStream.getReader();
  \f[B]while\f[R] (\f[B]true\f[R]) {
    \f[B]const\f[R] { done, value } = \f[B]await\f[R] reader.read();
    \f[B]if\f[R] (done) {
      \f[B]break\f[R];
    }
    \f[I]// value is a Uint8Array\f[R]
    console.log(value);
  }
}
.EE
.SH SEE ALSO
.IP \[bu] 2
\c
.UR https://developer.chrome.com/docs/capabilities/web-apis/webtransport
Using WebTransport
.UE \c
.IP \[bu] 2
WebSockets API
.IP \[bu] 2
Streams API
.IP \[bu] 2
\c
.UR https://datatracker.ietf.org/doc/html/draft-ietf-webtrans-http3/
WebTransport over HTTP/3
.UE \c
