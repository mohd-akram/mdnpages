.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "RTCIceCandidate.priority" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
RTCIceCandidate.priority \- RTCIceCandidate: priority property
.SH SYNOPSIS
The \f[B]\f[CB]RTCIceCandidate\f[B]\f[R] interface\[cq]s read\-only
\f[B]\f[CB]priority\f[B]\f[R] property specifies the candidate\[cq]s
priority according to the remote peer; the higher this value is, the
better the remote peer considers the candidate to be.
.PP
The \f[CR]priority\f[R] field\[cq]s value is set from the
\f[CR]candidateInfo\f[R] options object passed to the
\f[CR]RTCIceCandidate()\f[R] constructor.
You can\[cq]t specify the value of \f[CR]priority\f[R] directly in the
options object, but its value is automatically extracted from the
object\[cq]s \f[CR]candidate\f[R] a\-line, if it\[cq]s formatted
properly.
.SH VALUE
A long, unsigned integer value indicating the priority of the candidate
according to the remote peer.
The larger this value is, the more preferable the remote peer considers
this candidate to be.
.PP
\f[CR]priority\f[R] is initialized to \f[CR]null\f[R] if it is not
specified in the \f[CR]candidate\f[R], or if the \f[CR]candidate\f[R]
string can\[cq]t be parsed properly.
.RS
.PP
\f[B]Note:\f[R] If \f[CR]priority\f[R] is \f[CR]null\f[R], passing the
candidate to \f[CR]addIceCandidate()\f[R] will fail, throwing an
\f[CR]OperationError\f[R] exception.
This applies only if the candidate implements the \f[CR]priority\f[R]
property.
.RE
.SH USAGE NOTES
Consider this SDP attribute line (a\-line) which describes an ICE
candidate:
.IP
.EX
a=candidate:4234997325 1 udp 2043278322 192.0.2.172 44323 typ host
.EE
.PP
The priority is the number after the protocol, so it\[cq]s the fourth
field in the candidate string.
In this example, the priority is 2043278322.
.SH EXAMPLES
This candidate examines the \f[CR]priority\f[R] of the candidate and, if
it\[cq]s greater than the priority of a previously\-seen candidate,
remembers the candidate for later use.
.IP
.EX
\f[B]let\f[R] bestCandidate = {
  candidate: \[dq]\[dq],
  sdpMid: \f[B]null\f[R],
  sdpMLineIndex: \f[B]null\f[R],
  priority: 0,
};

\f[B]function\f[R] handleCandidate(candidateString) {
  \f[B]const\f[R] candidate = \f[B]new\f[R] RTCIceCandidate(candidateString);

  \f[B]if\f[R] (candidate.priority > bestCandidate.priority) {
    bestCandidate = candidate;
  }
}
.EE
