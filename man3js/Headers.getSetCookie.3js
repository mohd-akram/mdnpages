.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "Headers.getSetCookie" "JS" "March 6, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Headers.getSetCookie \- Headers: getSetCookie() method
.SH SYNOPSIS
This feature is available in Web Workers.
.PP
The \f[B]\f[CB]getSetCookie()\f[B]\f[R] method of the \f[CR]Headers\f[R]
interface returns an array containing the values of all
\f[CR]Set\-Cookie\f[R] headers associated with a response.
This allows \f[CR]Headers\f[R] objects to handle having multiple
\f[CR]Set\-Cookie\f[R] headers, which wasn\[cq]t possible prior to its
implementation.
.PP
This method is intended for use on server environments (for example
Node.js).
Browsers block frontend JavaScript code from accessing the
\f[CR]Set\-Cookie\f[R] header, as required by the Fetch spec, which
defines \f[CR]Set\-Cookie\f[R] as a \c
.UR https://fetch.spec.whatwg.org/#forbidden-response-header-name
forbidden response\-header name
.UE \c
\ that \c
.UR https://fetch.spec.whatwg.org/#ref-for-forbidden-response-header-name%E2%91%A0
must be filtered out
.UE \c
\ from any response exposed to frontend code.
.SH SYNTAX
.IP
.EX
getSetCookie()
.EE
.SS Parameters
None.
.SS Return value
An array of strings representing the values of all the different
\f[CR]Set\-Cookie\f[R] headers associated with a response.
.PP
If no \f[CR]Set\-Cookie\f[R] headers are set, the method will return an
empty array (\f[CR][ ]\f[R]).
.SH EXAMPLES
As alluded to above, running code like the following on the client
won\[cq]t return any results \[em] \f[CR]Set\-Cookie\f[R] is filtered
out from \f[CR]Headers\f[R] retrieved over the network.
.IP
.EX
fetch(\[dq]https://example.com\[dq]).then((response) \f[B]=>\f[R] {
  console.log(response.headers.getSetCookie());
  \f[I]// No header values returned\f[R]
});
.EE
.PP
However, the following could be used to query multiple
\f[CR]Set\-Cookie\f[R] values.
This is much more useful on the server, but it would also work on the
client.
.IP
.EX
\f[B]const\f[R] headers = \f[B]new\f[R] Headers({
  \[dq]Set\-Cookie\[dq]: \[dq]name1=value1\[dq],
});

headers.append(\[dq]Set\-Cookie\[dq], \[dq]name2=value2\[dq]);

headers.getSetCookie();
\f[I]// Returns [\[dq]name1=value1\[dq], \[dq]name2=value2\[dq]]\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
HTTP
.IP \[bu] 2
\f[CR]Set\-Cookie\f[R]
