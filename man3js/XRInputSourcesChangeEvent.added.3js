.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "XRINPUTSOURCESCHANGEEVENT.ADDED" "3JS" "July 25, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
XRInputSourcesChangeEvent.added \- XRInputSourcesChangeEvent: added
property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The read\-only \f[CR]XRInputSourcesChangeEvent\f[R] property
\f[CR]added\f[R] is a list of zero or more input sources, each
identified using an \f[CR]XRInputSource\f[R] object, which have been
newly made available for use.
.SH VALUE
An \f[CR]Array\f[R] of zero or more \f[CR]XRInputSource\f[R] objects,
each representing one input device added to the XR system.
.SH EXAMPLES
The example below creates a handler for the
\f[CR]inputsourceschange\f[R] event that processes the lists of added
and removed from the WebXR system.
It looks for new and removed devices whose \f[CR]targetRayMode\f[R] is
\f[CR]tracked\-pointer\f[R].
.IP
.EX
xrSession.oninputsourcescchange = (event) \f[B]=>\f[R] {
  \f[B]for\f[R] (\f[B]const\f[R] input \f[B]of\f[R] event.added) {
    \f[B]if\f[R] (input.targetRayMode === \(dqtracked\-pointer\(dq) {
      addedPointerDevice(input);
    }
  }
  \f[B]for\f[R] (\f[B]const\f[R] input \f[B]of\f[R] event.removed) {
    \f[B]if\f[R] (input.targetRayMode === \(dqtracked\-pointer\(dq) {
      removedPointerDevice(input);
    }
  }
};
.EE
