.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "String.split" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]split()\f[B]\f[R] method takes a pattern and divides a
\f[V]String\f[R] into an ordered list of substrings by searching for the
pattern, puts these substrings into an array, and returns the array.
.SH Syntax
.IP
.nf
\f[C]
split(separator)
split(separator, limit)
\f[R]
.fi
.SS Parameters
.IP \[bu] 2
\f[V]separator\f[R]
.RS 2
.IP \[bu] 2
The pattern describing where each split should occur.
Can be \f[V]undefined\f[R], a string, or an object with a
\f[V]Symbol.split\f[R] method \[em] the typical example being a
\f[V]regular expression\f[R].
Omitting \f[V]separator\f[R] or passing \f[V]undefined\f[R] causes
\f[V]split()\f[R] to return an array with the calling string as a single
element.
All values that are not \f[V]undefined\f[R] or objects with a
\f[V]\[at]\[at]split\f[R] method are coerced to strings.
.RE
.IP \[bu] 2
\f[V]limit\f[R] \f[I](optional)\f[R]
.RS 2
.IP \[bu] 2
A non-negative integer specifying a limit on the number of substrings to
be included in the array.
If provided, splits the string at each occurrence of the specified
\f[V]separator\f[R], but stops when \f[V]limit\f[R] entries have been
placed in the array.
Any leftover text is not included in the array at all.
.RS 2
.IP \[bu] 2
The array may contain fewer entries than \f[V]limit\f[R] if the end of
the string is reached before the limit is reached.
.IP \[bu] 2
If \f[V]limit\f[R] is \f[V]0\f[R], \f[V][]\f[R] is returned.
.RE
.RE
.SS Return value
.PP
An \f[V]Array\f[R] of strings, split at each point where the
\f[V]separator\f[R] occurs in the given string.
.SH Description
.PP
If \f[V]separator\f[R] is a non-empty string, the target string is split
by all matches of the \f[V]separator\f[R] without including
\f[V]separator\f[R] in the results.
For example, a string containing tab separated values (TSV) could be
parsed by passing a tab character as the separator, like
\f[V]myString.split(\[dq]\[rs]t\[dq])\f[R].
If \f[V]separator\f[R] contains multiple characters, that entire
character sequence must be found in order to split.
If \f[V]separator\f[R] appears at the beginning (or end) of the string,
it still has the effect of splitting, resulting in an empty (i.e.\ zero
length) string appearing at the first (or last) position of the returned
array.
If \f[V]separator\f[R] does not occur in \f[V]str\f[R], the returned
array contains one element consisting of the entire string.
.PP
If \f[V]separator\f[R] is an empty string (\f[V]\[dq]\[dq]\f[R]),
\f[V]str\f[R] is converted to an array of each of its UTF-16
\[lq]characters\[rq], without empty strings on either ends of the
resulting string.
.RS
.PP
\f[B]Note:\f[R] \f[V]\[dq]\[dq].split(\[dq]\[dq])\f[R] is therefore the
only way to produce an empty array when a string is passed as
\f[V]separator\f[R].
.RE
.RS
.PP
\f[B]Warning:\f[R] When the empty string (\f[V]\[dq]\[dq]\f[R]) is used
as a separator, the string is \f[B]not\f[R] split by \f[I]user-perceived
characters\f[R] (grapheme
clusters (https://unicode.org/reports/tr29/#Grapheme_Cluster_Boundaries))
or unicode characters (code points), but by UTF-16 code units.
This destroys surrogate
pairs (https://unicode.org/faq/utf_bom.html#utf16-2).
See \[lq]How do you get a string to a character array in
JavaScript?\[rq] on
StackOverflow (https://stackoverflow.com/questions/4547609/how-to-get-character-array-from-a-string/34717402#34717402).
.RE
.PP
If \f[V]separator\f[R] is a regexp that matches empty strings, whether
the match is split by UTF-16 code units or Unicode code points depends
on if the \f[V]u\f[R] flag is set.
.IP
.nf
\f[C]
\[dq]😄😄\[dq].split(/(?:)/); // [ \[dq]\[rs]ud83d\[dq], \[dq]\[rs]ude04\[dq], \[dq]\[rs]ud83d\[dq], \[dq]\[rs]ude04\[dq] ]
\[dq]😄😄\[dq].split(/(?:)/u); // [ \[dq]😄\[dq], \[dq]😄\[dq] ]
\f[R]
.fi
.PP
If \f[V]separator\f[R] is a regular expression with capturing groups,
then each time \f[V]separator\f[R] matches, the captured groups
(including any \f[V]undefined\f[R] results) are spliced into the output
array.
This behavior is specified by the regexp\[cq]s \f[V]Symbol.split\f[R]
method.
.PP
If \f[V]separator\f[R] is an object with a \f[V]Symbol.split\f[R]
method, that method is called with the target string and \f[V]limit\f[R]
as arguments, and \f[V]this\f[R] set to the object.
Its return value becomes the return value of \f[V]split\f[R].
.PP
Any other value will be coerced to a string before being used as
separator.
.SH Examples
.SS Using split()
.PP
When the string is empty and a non-empty separator is specified,
\f[V]split()\f[R] returns \f[V][\[dq]\[dq]]\f[R].
If the string and separator are both empty strings, an empty array is
returned.
.IP
.nf
\f[C]
const emptyString = \[dq]\[dq];

// string is empty and separator is non-empty
console.log(emptyString.split(\[dq]a\[dq]));
// [\[dq]\[dq]]

// string and separator are both empty strings
console.log(emptyString.split(emptyString));
// []
\f[R]
.fi
.PP
The following example defines a function that splits a string into an
array of strings using \f[V]separator\f[R].
After splitting the string, the function logs messages indicating the
original string (before the split), the separator used, the number of
elements in the array, and the individual array elements.
.IP
.nf
\f[C]
function splitString(stringToSplit, separator) {
  const arrayOfStrings = stringToSplit.split(separator);

  console.log(\[dq]The original string is:\[dq], stringToSplit);
  console.log(\[dq]The separator is:\[dq], separator);
  console.log(
    \[dq]The array has\[dq],
    arrayOfStrings.length,
    \[dq]elements:\[dq],
    arrayOfStrings.join(\[dq] / \[dq]),
  );
}

const tempestString = \[dq]Oh brave new world that has such people in it.\[dq];
const monthString = \[dq]Jan,Feb,Mar,Apr,May,Jun,Jul,Aug,Sep,Oct,Nov,Dec\[dq];

const space = \[dq] \[dq];
const comma = \[dq],\[dq];

splitString(tempestString, space);
splitString(tempestString);
splitString(monthString, comma);
\f[R]
.fi
.PP
This example produces the following output:
.IP
.nf
\f[C]
The original string is: \[dq]Oh brave new world that has such people in it.\[dq]
The separator is: \[dq] \[dq]
The array has 10 elements: Oh / brave / new / world / that / has / such / people / in / it.

The original string is: \[dq]Oh brave new world that has such people in it.\[dq]
The separator is: \[dq]undefined\[dq]
The array has 1 elements: Oh brave new world that has such people in it.

The original string is: \[dq]Jan,Feb,Mar,Apr,May,Jun,Jul,Aug,Sep,Oct,Nov,Dec\[dq]
The separator is: \[dq],\[dq]
The array has 12 elements: Jan / Feb / Mar / Apr / May / Jun / Jul / Aug / Sep / Oct / Nov / Dec
\f[R]
.fi
.SS Removing spaces from a string
.PP
In the following example, \f[V]split()\f[R] looks for zero or more
spaces, followed by a semicolon, followed by zero or more
spaces\[em]and, when found, removes the spaces and the semicolon from
the string.
\f[V]nameList\f[R] is the array returned as a result of
\f[V]split()\f[R].
.IP
.nf
\f[C]
const names = \[dq]Harry Trump ;Fred Barney; Helen Rigby ; Bill Abel ;Chris Hand \[dq];

console.log(names);

const re = /\[rs]s*(?:;|$)\[rs]s*/;
const nameList = names.split(re);

console.log(nameList);
\f[R]
.fi
.PP
This logs two lines; the first line logs the original string, and the
second line logs the resulting array.
.IP
.nf
\f[C]
Harry Trump ;Fred Barney; Helen Rigby ; Bill Abel ;Chris Hand
[ \[dq]Harry Trump\[dq], \[dq]Fred Barney\[dq], \[dq]Helen Rigby\[dq], \[dq]Bill Abel\[dq], \[dq]Chris Hand\[dq], \[dq]\[dq] ]
\f[R]
.fi
.SS Returning a limited number of splits
.PP
In the following example, \f[V]split()\f[R] looks for spaces in a string
and returns the first 3 splits that it finds.
.IP
.nf
\f[C]
const myString = \[dq]Hello World. How are you doing?\[dq];
const splits = myString.split(\[dq] \[dq], 3);

console.log(splits); // [ \[dq]Hello\[dq], \[dq]World.\[dq], \[dq]How\[dq] ]
\f[R]
.fi
.SS Splitting with a \f[V]RegExp\f[R] to include parts of the separator in the result
.PP
If \f[V]separator\f[R] is a regular expression that contains capturing
parentheses \f[V]( )\f[R], matched results are included in the array.
.IP
.nf
\f[C]
const myString = \[dq]Hello 1 word. Sentence number 2.\[dq];
const splits = myString.split(/(\[rs]d)/);

console.log(splits);
// [ \[dq]Hello \[dq], \[dq]1\[dq], \[dq] word. Sentence number \[dq], \[dq]2\[dq], \[dq].\[dq] ]
\f[R]
.fi
.RS
.PP
\f[B]Note:\f[R] \f[V]\[rs]d\f[R] matches the character class for digits
between 0 and 9.
.RE
.SS Using a custom splitter
.PP
An object with a \f[V]Symbol.split\f[R] method can be used as a splitter
with custom behavior.
.PP
The following example splits a string using an internal state consisting
of an incrementing number:
.IP
.nf
\f[C]
const splitByNumber = {
  [Symbol.split](str) {
    let num = 1;
    let pos = 0;
    const result = [];
    while (pos < str.length) {
      const matchPos = str.indexOf(num, pos);
      if (matchPos === -1) {
        result.push(str.substring(pos));
        break;
      }
      result.push(str.substring(pos, matchPos));
      pos = matchPos + String(num).length;
      num++;
    }
    return result;
  },
};

const myString = \[dq]a1bc2c5d3e4f\[dq];
console.log(myString.split(splitByNumber)); // [ \[dq]a\[dq], \[dq]bc\[dq], \[dq]c5d\[dq], \[dq]e\[dq], \[dq]f\[dq] ]
\f[R]
.fi
.PP
The following example uses an internal state to enforce certain
behavior, and to ensure a \[lq]valid\[rq] result is produced.
.IP
.nf
\f[C]
const DELIMITER = \[dq];\[dq];

// Split the commands, but remove any invalid or unnecessary values.
const splitCommands = {
  [Symbol.split](str, lim) {
    const results = [];
    const state = {
      on: false,
      brightness: {
        current: 2,
        min: 1,
        max: 3,
      },
    };
    let pos = 0;
    let matchPos = str.indexOf(DELIMITER, pos);

    while (matchPos !== -1) {
      const subString = str.slice(pos, matchPos).trim();

      switch (subString) {
        case \[dq]light on\[dq]:
          // If the \[ga]on\[ga] state is already true, do nothing.
          if (!state.on) {
            state.on = true;
            results.push(subString);
          }
          break;

        case \[dq]light off\[dq]:
          // If the \[ga]on\[ga] state is already false, do nothing.
          if (state.on) {
            state.on = false;
            results.push(subString);
          }
          break;

        case \[dq]brightness up\[dq]:
          // Enforce a brightness maximum.
          if (state.brightness.current < state.brightness.max) {
            state.brightness.current += 1;
            results.push(subString);
          }
          break;

        case \[dq]brightness down\[dq]:
          // Enforce a brightness minimum.
          if (state.brightness.current > state.brightness.min) {
            state.brightness.current -= 1;
            results.push(subString);
          }
          break;
      }

      if (results.length === lim) {
        break;
      }

      pos = matchPos + DELIMITER.length;
      matchPos = str.indexOf(DELIMITER, pos);
    }

    // If we broke early due to reaching the split \[ga]lim\[ga], don\[aq]t add the remaining commands.
    if (results.length < lim) {
      results.push(str.slice(pos).trim());
    }

    return results;
  },
};

const commands =
  \[dq]light on; brightness up; brightness up; brightness up; light on; brightness down; brightness down; light off\[dq];
console.log(commands.split(splitCommands, 3)); // [\[dq]light on\[dq], \[dq]brightness up\[dq], \[dq]brightness down\[dq]]
\f[R]
.fi
.SH See also
.IP \[bu] 2
Polyfill of \f[V]String.prototype.split\f[R] in \f[V]core-js\f[R] with
fixes and implementation of modern behavior like \f[V]Symbol.split\f[R]
support (https://github.com/zloirock/core-js#ecmascript-string-and-regexp)
.IP \[bu] 2
\f[V]String.prototype.charAt()\f[R]
.IP \[bu] 2
\f[V]String.prototype.indexOf()\f[R]
.IP \[bu] 2
\f[V]String.prototype.lastIndexOf()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.join()\f[R]
.IP \[bu] 2
Using regular expressions in JavaScript
