.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "String.blink" "JS" "September 15, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
String.blink - String.prototype.blink()
.SH SYNOPSIS
\f[I](deprecated)\f[R]
.PP
The \f[B]\f[CB]blink()\f[B]\f[R] method of \f[CR]String\f[R] values
creates a string that embeds this string in a \f[CR]<blink>\f[R] element
(\f[CR]<blink>str</blink>\f[R]), which used to cause a string to blink
in old browsers.
.RS
.PP
\f[B]Note:\f[R] All HTML wrapper methods are deprecated and only
standardized for compatibility purposes.
For the case of \f[CR]blink()\f[R], the \f[CR]<blink>\f[R] element
itself is removed from modern browsers, and blinking text is frowned
upon by several accessibility standards.
Avoid using the element in any way.
.RE
.SH SYNTAX
.IP
.EX
blink()
.EE
.SS Parameters
None.
.SS Return value
A string beginning with a \f[CR]<blink>\f[R] start tag, then the text
\f[CR]str\f[R], and then a \f[CR]</blink>\f[R] end tag.
.SH EXAMPLES
.SS Using blink()
The code below creates an HTML string and then replaces the
document\[cq]s body with it:
.IP
.EX
const contentString = \[dq]Hello, world\[dq];

document.body.innerHTML = contentString.blink();
.EE
.PP
This will create the following HTML:
.IP
.EX
<blink>Hello, world</blink>
.EE
.RS
.PP
\f[B]Warning:\f[R] This markup is invalid, because \f[CR]blink\f[R] is
no longer a valid element.
.RE
.PP
You should avoid blinking elements altogether.
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[CR]String.prototype.blink\f[R] in
\f[CR]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-string-and-regexp)
.IP \[bu] 2
HTML wrapper methods
