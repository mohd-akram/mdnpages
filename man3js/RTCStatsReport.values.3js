.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "RTCSTATSREPORT.VALUES" "3JS" "May 27, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
RTCStatsReport.values \- RTCStatsReport: values() method
.SH SYNOPSIS
The \f[B]\f[CB]values()\f[B]\f[R] method of the
\f[CR]RTCStatsReport\f[R] interface returns a new \f[I]iterator\f[R]
object that can be used to iterate through the values for each element
in the \f[CR]RTCStatsReport\f[R] object, in insertion order.
.PP
The values are statistics dictionary objects.
.PP
The method is otherwise the same as \f[CR]Map.prototype.values()\f[R].
.SH SYNTAX
.IP
.EX
values()
.EE
.SS Return value
A new iterable iterator object.
.SH EXAMPLES
This example shows how to iterate through a \f[CR]RTCStatsReport\f[R]
using the iterator returned by \f[CR]values()\f[R].
.PP
Given a variable \f[CR]myPeerConnection\f[R], which is an instance of
\f[CR]RTCPeerConnection\f[R], the code calls \f[CR]getStats()\f[R] with
\f[CR]await\f[R] to wait for the statistics report.
It then uses a for\&...of loop, with the iterator returned by
\f[CR]values()\f[R], to iterate through the dictionary objects in the
report.
The properties of statistics objects with the \f[CR]type\f[R] of
\f[CR]outbound\-rtp\f[R] are logged to the console (other objects are
discarded).
.IP
.EX
\f[B]const\f[R] stats = \f[B]await\f[R] myPeerConnection.getStats();

\f[B]for\f[R] (\f[B]const\f[R] stat \f[B]of\f[R] stats.values()) {
  \f[B]if\f[R] (stat.type !== \(dqoutbound\-rtp\(dq) \f[B]continue\f[R];
  Object.keys(stat).forEach((statName) \f[B]=>\f[R] {
    console.log(\(ga${statName}: ${report[statName]}\(ga);
  });
}
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]Map.prototype.values()\f[R]
.IP \(bu 2
\f[CR]RTCStatsReport.keys()\f[R]
.IP \(bu 2
\f[CR]RTCStatsReport.entries()\f[R]
