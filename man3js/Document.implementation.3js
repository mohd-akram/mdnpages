.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "Document.implementation" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Document.implementation \- Document: implementation property
.SH SYNOPSIS
The \f[B]\f[CB]Document.implementation\f[B]\f[R] property returns a
\f[CR]DOMImplementation\f[R] object associated with the current
document.
.SH VALUE
A \f[CR]DOMImplementation\f[R] object.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] modName = \[dq]HTML\[dq];
\f[B]const\f[R] modVer = \[dq]2.0\[dq];
\f[B]const\f[R] conformTest = document.implementation.hasFeature(modName, modVer);

console.log(\[ga]DOM ${modName} ${modVer} supported?: ${conformTest}\[ga]);

\f[I]// Log: \[dq]DOM HTML 2.0 supported?: true\[dq] (hasFeature always returns true)\f[R]
.EE
.RS
.PP
\f[B]Warning:\f[R] Do not use this for feature detection.
The \f[CR]hasFeature()\f[R] method always returns true.
.RE
.SH NOTES
The W3C\[cq]s DOM Level 1 Recommendation only specified the
\f[CR]hasFeature\f[R] method, which is one way to determine if a DOM
module is supported by a browser (see example above and \c
.UR https://www.w3.org/2003/02/06-dom-support.html
What does your user agent claim to support?
.UE \c
).
If available, other \f[CR]DOMImplementation\f[R] methods provide
services for controlling things outside of a single document.
For example, the \f[CR]DOMImplementation\f[R] interface includes a
\f[CR]createDocumentType\f[R] method with which DTDs can be created for
one or more documents managed by the implementation.
