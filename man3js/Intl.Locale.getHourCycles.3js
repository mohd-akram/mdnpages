.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "INTL.LOCALE.GETHOURCYCLES" "3JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Intl.Locale.getHourCycles \- Intl.Locale.prototype.getHourCycles()
.SH SYNOPSIS
The \f[B]\f[CB]getHourCycles()\f[B]\f[R] method of
\f[CR]Intl.Locale\f[R] instances returns a list of one or more unique
hour cycle identifiers for this locale.
.RS
.PP
\f[B]Note:\f[R] In some versions of some browsers, this method was
implemented as an accessor property called \f[CR]hourCycles\f[R].
However, because it returns a new array on each access, it is now
implemented as a method to prevent the situation of
\f[CR]locale.hourCycles === locale.hourCycles\f[R] returning
\f[CR]false\f[R].
Check the browser compatibility table for details.
.RE
.SH SYNTAX
.IP
.EX
getHourCycles()
.EE
.SS Parameters
None.
.SS Return value
An array of strings representing all hour cycle types commonly used for
the \f[CR]Locale\f[R], sorted in descending preference.
If the \f[CR]Locale\f[R] already has an \f[CR]hourCycle\f[R], then the
returned array contains that single value.
.PP
Below is a list of supported hour cycle types.
.SS Supported hour cycle types
.TP
\f[B]h12\f[R]
Hour system using 1\(en12; corresponds to `h' in patterns.
The 12 hour clock, with midnight starting at 12:00 am.
As used, for example, in the United States.
.TP
\f[B]h23\f[R]
Hour system using 0\(en23; corresponds to `H' in patterns.
The 24 hour clock, with midnight starting at 0:00.
.TP
\f[B]h11\f[R]
Hour system using 0\(en11; corresponds to `K' in patterns.
The 12 hour clock, with midnight starting at 0:00 am.
Mostly used in Japan.
.TP
\f[B]h24\f[R]
Hour system using 1\(en24; corresponds to `k' in pattern.
The 24 hour clock, with midnight starting at 24:00.
Not used anywhere.
.SH EXAMPLES
.SS Obtaining supported hour cycles
If the \f[CR]Locale\f[R] object doesn\(cqt have a \f[CR]hourCycle\f[R]
already, \f[CR]getHourCycles()\f[R] lists all commonly\-used hour cycle
identifiers for the given \f[CR]Locale\f[R].
For examples of explicitly setting a \f[CR]hourCycle\f[R], see
\f[CR]hourCycle\f[R] examples.
.IP
.EX
\f[B]const\f[R] arEG = \f[B]new\f[R] Intl.Locale(\(dqar\-EG\(dq);
console.log(arEG.getHourCycles()); \f[I]// [\(dqh12\(dq]\f[R]
.EE
.IP
.EX
\f[B]const\f[R] jaJP = \f[B]new\f[R] Intl.Locale(\(dqja\-JP\(dq);
console.log(jaJP.getHourCycles()); \f[I]// [\(dqh23\(dq]\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]Intl.Locale\f[R]
.IP \(bu 2
\f[CR]Intl.Locale.prototype.hourCycle\f[R]
.IP \(bu 2
\c
.UR https://www.unicode.org/reports/tr35/#UnicodeHourCycleIdentifier
Unicode Hour Cycle Identifier
.UE \c
\ in the Unicode locale data markup language spec
