.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Object.seal" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Object.seal - Object.seal()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]Object.seal()\f[B]\f[R] static method \f[I]seals\f[R] an
object.
Sealing an object prevents extensions and makes existing properties
non-configurable.
A sealed object has a fixed set of properties: new properties cannot be
added, existing properties cannot be removed, their enumerability and
configurability cannot be changed, and its prototype cannot be
re-assigned.
Values of existing properties can still be changed as long as they are
writable.
\f[V]seal()\f[R] returns the same object that was passed in.
.SH SYNTAX
.IP
.nf
\f[C]
Object.seal(obj)
\f[R]
.fi
.SS Parameters
.TP
\f[B]obj\f[R]
The object which should be sealed.
.SS Return value
.PP
The object being sealed.
.SH DESCRIPTION
.PP
Sealing an object is equivalent to preventing extensions and then
changing all existing properties\[cq] descriptors to
\f[V]configurable: false\f[R].
This has the effect of making the set of properties on the object fixed.
Making all properties non-configurable also prevents them from being
converted from data properties to accessor properties and vice versa,
but it does not prevent the values of data properties from being
changed.
Attempting to delete or add properties to a sealed object, or to convert
a data property to accessor or vice versa, will fail, either silently or
by throwing a \f[V]TypeError\f[R] (most commonly, although not
exclusively, when in \f[V]strict mode\f[R] code).
.PP
The prototype chain remains untouched.
However, due to the effect of preventing extensions, the
\f[V][[Prototype]]\f[R] cannot be reassigned.
.PP
Unlike \f[V]Object.freeze()\f[R], objects sealed with
\f[V]Object.seal()\f[R] may have their existing properties changed, as
long as they are writable.
.SH EXAMPLES
.SS Using Object.seal
.IP
.nf
\f[C]
const obj = {
  prop() {},
  foo: \[dq]bar\[dq],
};

// New properties may be added, existing properties
// may be changed or removed.
obj.foo = \[dq]baz\[dq];
obj.lumpy = \[dq]woof\[dq];
delete obj.prop;

const o = Object.seal(obj);

o === obj; // true
Object.isSealed(obj); // true

// Changing property values on a sealed object
// still works.
obj.foo = \[dq]quux\[dq];

// But you can\[aq]t convert data properties to accessors,
// or vice versa.
Object.defineProperty(obj, \[dq]foo\[dq], {
  get() {
    return \[dq]g\[dq];
  },
}); // throws a TypeError

// Now any changes, other than to property values,
// will fail.
obj.quaxxor = \[dq]the friendly duck\[dq];
// silently doesn\[aq]t add the property
delete obj.foo;
// silently doesn\[aq]t delete the property

// ...and in strict mode such attempts
// will throw TypeErrors.
function fail() {
  \[dq]use strict\[dq];
  delete obj.foo; // throws a TypeError
  obj.sparky = \[dq]arf\[dq]; // throws a TypeError
}
fail();

// Attempted additions through
// Object.defineProperty will also throw.
Object.defineProperty(obj, \[dq]ohai\[dq], {
  value: 17,
}); // throws a TypeError
Object.defineProperty(obj, \[dq]foo\[dq], {
  value: \[dq]eit\[dq],
}); // changes existing property value
\f[R]
.fi
.SS Non-object argument
.PP
In ES5, if the argument to this method is not an object (a primitive),
then it will cause a \f[V]TypeError\f[R].
In ES2015, a non-object argument will be returned as-is without any
errors, since primitives are already, by definition, immutable.
.IP
.nf
\f[C]
Object.seal(1);
// TypeError: 1 is not an object (ES5 code)

Object.seal(1);
// 1                             (ES2015 code)
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]Object.isSealed()\f[R]
.IP \[bu] 2
\f[V]Object.preventExtensions()\f[R]
.IP \[bu] 2
\f[V]Object.isExtensible()\f[R]
.IP \[bu] 2
\f[V]Object.freeze()\f[R]
.IP \[bu] 2
\f[V]Object.isFrozen()\f[R]
