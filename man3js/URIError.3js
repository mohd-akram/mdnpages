.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "URIError" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
URIError - URIError
.SH SYNOPSIS
.PP
The \f[B]\f[VB]URIError\f[B]\f[R] object represents an error when a
global URI handling function was used in a wrong way.
.PP
\f[V]URIError\f[R] is a serializable object, so it can be cloned with
\f[V]structuredClone()\f[R] or copied between Workers using
\f[V]postMessage()\f[R].
.PP
\f[V]URIError\f[R] is a subclass of \f[V]Error\f[R].
.SH CONSTRUCTOR
.TP
\f[B]URIError()\f[R]
Creates a new \f[V]URIError\f[R] object.
.SH INSTANCE PROPERTIES
.PP
\f[I]Also inherits instance properties from its parent
\f[VI]Error\f[I]\f[R].
.PP
These properties are defined on \f[V]URIError.prototype\f[R] and shared
by all \f[V]URIError\f[R] instances.
.TP
\f[B]URIError.prototype.constructor\f[R]
The constructor function that created the instance object.
For \f[V]URIError\f[R] instances, the initial value is the
\f[V]URIError\f[R] constructor.
.TP
\f[B]URIError.prototype.name\f[R]
Represents the name for the type of error.
For \f[V]URIError.prototype.name\f[R], the initial value is
\f[V]\[dq]URIError\[dq]\f[R].
.SH INSTANCE METHODS
.PP
\f[I]Inherits instance methods from its parent \f[VI]Error\f[I]\f[R].
.SH EXAMPLES
.SS Catching an URIError
.IP
.nf
\f[C]
try {
  decodeURIComponent(\[dq]%\[dq]);
} catch (e) {
  console.log(e instanceof URIError); // true
  console.log(e.message); // \[dq]malformed URI sequence\[dq]
  console.log(e.name); // \[dq]URIError\[dq]
  console.log(e.stack); // Stack of the error
}
\f[R]
.fi
.SS Creating an URIError
.IP
.nf
\f[C]
try {
  throw new URIError(\[dq]Hello\[dq]);
} catch (e) {
  console.log(e instanceof URIError); // true
  console.log(e.message); // \[dq]Hello\[dq]
  console.log(e.name); // \[dq]URIError\[dq]
  console.log(e.stack); // Stack of the error
}
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]Error\f[R]
.IP \[bu] 2
\f[V]decodeURI()\f[R]
.IP \[bu] 2
\f[V]decodeURIComponent()\f[R]
.IP \[bu] 2
\f[V]encodeURI()\f[R]
.IP \[bu] 2
\f[V]encodeURIComponent()\f[R]
