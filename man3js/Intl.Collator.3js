.\" Automatically generated by Pandoc 3.4
.\"
.TH "Intl.Collator" "JS" "July 25, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Intl.Collator \- Intl.Collator
.SH SYNOPSIS
The \f[B]\f[CB]Intl.Collator\f[B]\f[R] object enables
language\-sensitive string comparison.
.SH CONSTRUCTOR
.TP
\f[B]Intl.Collator()\f[R]
Creates a new \f[CR]Collator\f[R] object.
.SH STATIC METHODS
.TP
\f[B]Intl.Collator.supportedLocalesOf()\f[R]
Returns an array containing those of the provided locales that are
supported without having to fall back to the runtime\[cq]s default
locale.
.SH INSTANCE PROPERTIES
These properties are defined on \f[CR]Intl.Collator.prototype\f[R] and
shared by all \f[CR]Intl.Collator\f[R] instances.
.TP
\f[B]Intl.Collator.prototype.constructor\f[R]
The constructor function that created the instance object.
For \f[CR]Intl.Collator\f[R] instances, the initial value is the
\f[CR]Intl.Collator\f[R] constructor.
.TP
\f[B]Intl.Collator.prototype[Symbol.toStringTag]\f[R]
The initial value of the \f[CR][Symbol.toStringTag]\f[R] property is the
string \f[CR]\[dq]Intl.Collator\[dq]\f[R].
This property is used in \f[CR]Object.prototype.toString()\f[R].
.SH INSTANCE METHODS
.TP
\f[B]Intl.Collator.prototype.compare()\f[R]
Getter function that compares two strings according to the sort order of
this \f[CR]Intl.Collator\f[R] object.
.TP
\f[B]Intl.Collator.prototype.resolvedOptions()\f[R]
Returns a new object with properties reflecting the locale and collation
options computed during initialization of the object.
.SH EXAMPLES
.SS Using Collator
The following example demonstrates the different potential results for a
string occurring before, after, or at the same level as another:
.IP
.EX
console.log(\f[B]new\f[R] Intl.Collator().compare(\[dq]a\[dq], \[dq]c\[dq])); \f[I]// \-1, or some other negative value\f[R]
console.log(\f[B]new\f[R] Intl.Collator().compare(\[dq]c\[dq], \[dq]a\[dq])); \f[I]// 1, or some other positive value\f[R]
console.log(\f[B]new\f[R] Intl.Collator().compare(\[dq]a\[dq], \[dq]a\[dq])); \f[I]// 0\f[R]
.EE
.PP
Note that the results shown in the code above can vary between browsers
and browser versions.
This is because the values are implementation\-specific.
That is, the specification requires only that the before and after
values are negative and positive.
.SS Using locales
The results provided by \f[CR]Intl.Collator.prototype.compare()\f[R]
vary between languages.
In order to get the sort order of the language used in the user
interface of your application, make sure to specify that language (and
possibly some fallback languages) using the \f[CR]locales\f[R] argument:
.IP
.EX
\f[I]// in German, ä sorts with a\f[R]
console.log(\f[B]new\f[R] Intl.Collator(\[dq]de\[dq]).compare(\[dq]ä\[dq], \[dq]z\[dq]));
\f[I]// \-1, or some other negative value\f[R]

\f[I]// in Swedish, ä sorts after z\f[R]
console.log(\f[B]new\f[R] Intl.Collator(\[dq]sv\[dq]).compare(\[dq]ä\[dq], \[dq]z\[dq]));
\f[I]// 1, or some other positive value\f[R]
.EE
.SS Using options
The results provided by \f[CR]Intl.Collator.prototype.compare()\f[R] can
be customized using the \f[CR]options\f[R] argument:
.IP
.EX
\f[I]// in German, ä has a as the base letter\f[R]
console.log(\f[B]new\f[R] Intl.Collator(\[dq]de\[dq], { sensitivity: \[dq]base\[dq] }).compare(\[dq]ä\[dq], \[dq]a\[dq]));
\f[I]// 0\f[R]

\f[I]// in Swedish, ä and a are separate base letters\f[R]
console.log(\f[B]new\f[R] Intl.Collator(\[dq]sv\[dq], { sensitivity: \[dq]base\[dq] }).compare(\[dq]ä\[dq], \[dq]a\[dq]));
\f[I]// 1, or some other positive value\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Intl\f[R]
