.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "DOCUMENT.ELEMENTSFROMPOINT" "3JS" "July 19, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Document.elementsFromPoint \- Document: elementsFromPoint() method
.SH SYNOPSIS
The \f[B]\f[CB]elementsFromPoint()\f[B]\f[R] method of the
\f[CR]Document\f[R] interface returns an array of all elements at the
specified coordinates (relative to the viewport).
The elements are ordered from the topmost to the bottommost box of the
viewport.
.PP
It operates in a similar way to the \f[CR]elementFromPoint()\f[R]
method.
.SH SYNTAX
.IP
.EX
elementsFromPoint(x, y)
.EE
.SS Parameters
.TP
\f[B]x\f[R]
The horizontal coordinate of a point.
.TP
\f[B]y\f[R]
The vertical coordinate of a point.
.SS Return value
An array of \f[CR]Element\f[R] objects, ordered from the topmost to the
bottommost box of the viewport.
.SH EXAMPLES
.SS HTML
.IP
.EX
<\f[B]div\f[R]>
  <\f[B]p\f[R]>Some text</\f[B]p\f[R]>
</\f[B]div\f[R]>
<\f[B]p\f[R]>Elements at point 30, 20:</\f[B]p\f[R]>
<\f[B]div\f[R] id=\(dqoutput\(dq></\f[B]div\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]let\f[R] output = document.getElementById(\(dqoutput\(dq);
\f[B]if\f[R] (document.elementsFromPoint) {
  \f[B]let\f[R] elements = document.elementsFromPoint(30, 20);
  elements.forEach((elt, i) \f[B]=>\f[R] {
    output.textContent += elt.localName;
    \f[B]if\f[R] (i < elements.length \- 1) {
      output.textContent += \(dq < \(dq;
    }
  });
} \f[B]else\f[R] {
  output.innerHTML = \(ga<span style=\(dqcolor: red\(dq>
  Browser does not support
  <code>document.elementsFromPoint()</code>
</span>
\(ga;
}
.EE
.SH SEE ALSO
.IP \(bu 2
\f[CR]Document.elementFromPoint()\f[R]
