.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "Intl.Collator.resolvedOptions" "JS" "August 25, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Intl.Collator.resolvedOptions -
Intl.Collator.prototype.resolvedOptions()
.SH SYNOPSIS
The \f[B]\f[CB]resolvedOptions()\f[B]\f[R] method of
\f[CR]Intl.Collator\f[R] instances returns a new object with properties
reflecting the locale and collation options computed during
initialization of this collator object.
.SH SYNTAX
.IP
.EX
resolvedOptions()
.EE
.SS Parameters
None.
.SS Return value
A new object with properties reflecting the locale and collation options
computed during the initialization of the given \f[CR]Intl.Collator\f[R]
object.
.SH DESCRIPTION
The resulting object has the following properties:
.TP
\f[B]locale\f[R]
The BCP 47 language tag for the locale actually used.
If any Unicode extension values were requested in the input BCP 47
language tag that led to this locale, the key-value pairs that were
requested and are supported for this locale are included in
\f[CR]locale\f[R].
.TP
\f[B]usage\f[R], \f[B]sensitivity\f[R], \f[B]ignorePunctuation\f[R]
The values provided for these properties in the \f[CR]options\f[R]
argument or filled in as defaults.
.TP
\f[B]collation\f[R]
The value requested using the Unicode extension key
\f[CR]\[dq]co\[dq]\f[R], if it is supported for \f[CR]locale\f[R], or
\f[CR]\[dq]default\[dq]\f[R].
.TP
\f[B]numeric\f[R], \f[B]caseFirst\f[R]
The values requested for these properties in the \f[CR]options\f[R]
argument or using the Unicode extension keys \f[CR]\[dq]kn\[dq]\f[R] and
\f[CR]\[dq]kf\[dq]\f[R] or filled in as defaults.
If the implementation does not support these properties, they are
omitted.
.SH EXAMPLES
.SS Using the resolvedOptions method
.IP
.EX
const de = new Intl.Collator(\[dq]de\[dq], { sensitivity: \[dq]base\[dq] });
const usedOptions = de.resolvedOptions();

usedOptions.locale; // \[dq]de\[dq]
usedOptions.usage; // \[dq]sort\[dq]
usedOptions.sensitivity; // \[dq]base\[dq]
usedOptions.ignorePunctuation; // false
usedOptions.collation; // \[dq]default\[dq]
usedOptions.numeric; // false
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Intl.Collator\f[R]
