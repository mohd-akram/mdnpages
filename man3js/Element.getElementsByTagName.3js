.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "Element.getElementsByTagName" "JS" "September 8, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.getElementsByTagName \- Element: getElementsByTagName() method
.SH SYNOPSIS
The \f[B]\f[CB]Element.getElementsByTagName()\f[B]\f[R] method returns a
live \f[CR]HTMLCollection\f[R] of elements with the given tag name.
.PP
All descendants of the specified element are searched, but not the
element itself.
The returned list is \f[I]live\f[R], which means it updates itself with
the DOM tree automatically.
Therefore, there is no need to call
\f[CR]Element.getElementsByTagName()\f[R] with the same element and
arguments repeatedly if the DOM changes in between calls.
.PP
When called on an HTML element in an HTML document,
\f[CR]getElementsByTagName\f[R] lower\-cases the argument before
searching for it.
This is undesirable when trying to match camel\-cased SVG elements (such
as \f[CR]<linearGradient>\f[R]) in an HTML document.
Instead, use \f[CR]Element.getElementsByTagNameNS()\f[R], which
preserves the capitalization of the tag name.
.PP
\f[CR]Element.getElementsByTagName\f[R] is similar to
\f[CR]Document.getElementsByTagName()\f[R], except that it only searches
for elements that are descendants of the specified element.
.SH SYNTAX
.IP
.EX
getElementsByTagName(tagName)
.EE
.SS Parameters
.IP \[bu] 2
\f[CR]tagName\f[R] is the qualified name to look for.
The special string \f[CR]\[dq]*\[dq]\f[R] represents all elements.
For compatibility with XHTML, lower\-case should be used.
.SS Return value
A \f[I]live\f[R] \f[CR]HTMLCollection\f[R] of elements with a matching
tag name, in the order they appear.
If no elements are found, the \f[CR]HTMLCollection\f[R] is empty.
.SH EXAMPLES
.IP
.EX
\f[I]// Check the status of each data cell in a table\f[R]
\f[B]const\f[R] table = document.getElementById(\[dq]forecast\-table\[dq]);
\f[B]const\f[R] cells = table.getElementsByTagName(\[dq]td\[dq]);

\f[B]for\f[R] (\f[B]const\f[R] cell \f[B]of\f[R] cells) {
  \f[B]const\f[R] status = cell.getAttribute(\[dq]data\-status\[dq]);
  \f[B]if\f[R] (status === \[dq]open\[dq]) {
    \f[I]// Grab the data\f[R]
  }
}
.EE
