.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "Geolocation.getCurrentPosition" "JS" "January 1, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Geolocation.getCurrentPosition \- Geolocation: getCurrentPosition()
method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]getCurrentPosition()\f[B]\f[R] method of the
\f[CR]Geolocation\f[R] interface is used to get the current position of
the device.
.SH SYNTAX
.IP
.EX
getCurrentPosition(success)
getCurrentPosition(success, error)
getCurrentPosition(success, error, options)
.EE
.SS Parameters
.TP
\f[B]success\f[R]
A callback function that takes a \f[CR]GeolocationPosition\f[R] object
as its sole input parameter.
.TP
\f[B]error\f[R] \f[I](optional)\f[R]
An optional callback function that takes a
\f[CR]GeolocationPositionError\f[R] object as its sole input parameter.
.TP
\f[B]options\f[R] \f[I](optional)\f[R]
An optional object including the following parameters:
.RS
.TP
\f[B]maximumAge\f[R] \f[I](optional)\f[R]
A positive \f[CR]long\f[R] value indicating the maximum age in
milliseconds of a possible cached position that is acceptable to return.
If set to \f[CR]0\f[R], it means that the device cannot use a cached
position and must attempt to retrieve the real current position.
If set to \f[CR]Infinity\f[R] the device must return a cached position
regardless of its age.
Default: \f[CR]0\f[R].
.TP
\f[B]timeout\f[R] \f[I](optional)\f[R]
A positive \f[CR]long\f[R] value representing the maximum length of time
(in milliseconds) the device is allowed to take in order to return a
position.
The default value is \f[CR]Infinity\f[R], meaning that
\f[CR]getCurrentPosition()\f[R] won\[cq]t return until the position is
available.
.TP
\f[B]enableHighAccuracy\f[R] \f[I](optional)\f[R]
A boolean value that indicates the application would like to receive the
best possible results.
If \f[CR]true\f[R] and if the device is able to provide a more accurate
position, it will do so.
Note that this can result in slower response times or increased power
consumption (with a GPS chip on a mobile device for example).
On the other hand, if \f[CR]false\f[R], the device can take the liberty
to save resources by responding more quickly and/or using less power.
Default: \f[CR]false\f[R].
.RE
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] options = {
  enableHighAccuracy: \f[B]true\f[R],
  timeout: 5000,
  maximumAge: 0,
};

\f[B]function\f[R] success(pos) {
  \f[B]const\f[R] crd = pos.coords;

  console.log(\[dq]Your current position is:\[dq]);
  console.log(\[ga]Latitude : ${crd.latitude}\[ga]);
  console.log(\[ga]Longitude: ${crd.longitude}\[ga]);
  console.log(\[ga]More or less ${crd.accuracy} meters.\[ga]);
}

\f[B]function\f[R] error(err) {
  console.warn(\[ga]ERROR(${err.code}): ${err.message}\[ga]);
}

navigator.geolocation.getCurrentPosition(success, error, options);
.EE
.SH SEE ALSO
.IP \[bu] 2
Using the Geolocation API
.IP \[bu] 2
\f[CR]Navigator.geolocation\f[R]
