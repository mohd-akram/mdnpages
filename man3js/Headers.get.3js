.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Headers.get" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Headers.get \- Headers: get() method
.SH SYNOPSIS
The \f[B]\f[CB]get()\f[B]\f[R] method of the \f[CR]Headers\f[R]
interface returns a byte string of all the values of a header within a
\f[CR]Headers\f[R] object with a given name.
If the requested header doesn\[cq]t exist in the \f[CR]Headers\f[R]
object, it returns \f[CR]null\f[R].
.PP
For security reasons, some headers can only be controlled by the user
agent.
These headers include the forbidden header names and forbidden response
header names.
.SH SYNTAX
.IP
.EX
get(name)
.EE
.SS Parameters
.TP
\f[B]name\f[R]
The name of the HTTP header whose values you want to retrieve from the
\f[CR]Headers\f[R] object.
If the given name is not the name of an HTTP header, this method throws
a \f[CR]TypeError\f[R].
The name is case\-insensitive.
.SS Return value
A \f[CR]String\f[R] sequence representing the values of the retrieved
header or \f[CR]null\f[R] if this header is not set.
.SH EXAMPLES
Creating an empty \f[CR]Headers\f[R] object is simple:
.IP
.EX
const myHeaders = new Headers(); // Currently empty
myHeaders.get(\[dq]Not\-Set\[dq]); // Returns null
.EE
.PP
You could add a header to this using \f[CR]Headers.append\f[R], then
retrieve it using \f[CR]get()\f[R]:
.IP
.EX
myHeaders.append(\[dq]Content\-Type\[dq], \[dq]image/jpeg\[dq]);
myHeaders.get(\[dq]Content\-Type\[dq]); // Returns \[dq]image/jpeg\[dq]
.EE
.PP
If the header has multiple values associated with it, the byte string
will contain all the values, in the order they were added to the Headers
object:
.IP
.EX
myHeaders.append(\[dq]Accept\-Encoding\[dq], \[dq]deflate\[dq]);
myHeaders.append(\[dq]Accept\-Encoding\[dq], \[dq]gzip\[dq]);
myHeaders.get(\[dq]Accept\-Encoding\[dq]); // Returns \[dq]deflate, gzip\[dq]
myHeaders
  .get(\[dq]Accept\-Encoding\[dq])
  .split(\[dq],\[dq])
  .map((v) => v.trimStart()); // Returns [ \[dq]deflate\[dq], \[dq]gzip\[dq] ]
.EE
.SH SEE ALSO
.IP \[bu] 2
ServiceWorker API
.IP \[bu] 2
HTTP access control (CORS)
.IP \[bu] 2
HTTP
