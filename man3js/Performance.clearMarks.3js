.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Performance.clearMarks" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Performance.clearMarks \- Performance: clearMarks() method
.SH SYNOPSIS
The \f[B]\f[CB]clearMarks()\f[B]\f[R] method removes all or specific
\f[CR]PerformanceMark\f[R] objects from the browser\[cq]s performance
timeline.
.SH SYNTAX
.IP
.EX
clearMarks()
clearMarks(name)
.EE
.SS Parameters
.TP
\f[B]name\f[R] \f[I](optional)\f[R]
A string representing the \f[CR]name\f[R] of the
\f[CR]PerformanceMark\f[R] object.
If this argument is omitted, all entries with an \f[CR]entryType\f[R] of
\[lq]\f[CR]mark\f[R]\[rq] will be removed.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.SS Removing markers
To clean up all performance marks, or just specific entries, use the
\f[CR]clearMarks()\f[R] method like this:
.IP
.EX
// Create a bunch of marks
performance.mark(\[dq]login\-started\[dq]);
performance.mark(\[dq]login\-started\[dq]);
performance.mark(\[dq]login\-finished\[dq]);
performance.mark(\[dq]form\-sent\[dq]);
performance.mark(\[dq]video\-loaded\[dq]);
performance.mark(\[dq]video\-loaded\[dq]);

performance.getEntriesByType(\[dq]mark\[dq]).length; // 6

// Delete just the \[dq]login\-started\[dq] mark entries
performance.clearMarks(\[dq]login\-started\[dq]);
performance.getEntriesByType(\[dq]mark\[dq]).length; // 4

// Delete all of the mark entries
performance.clearMarks();
performance.getEntriesByType(\[dq]mark\[dq]).length; // 0
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]PerformanceMark\f[R]
