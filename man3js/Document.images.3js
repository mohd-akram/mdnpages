.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "DOCUMENT.IMAGES" "3JS" "May 20, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Document.images \- Document: images property
.SH SYNOPSIS
The \f[B]\f[CB]images\f[B]\f[R] read\-only property of the
\f[CR]Document\f[R] interface returns a collection of the images in the
current HTML document.
.SH VALUE
An \f[CR]HTMLCollection\f[R] providing a live list of all of the images
contained in the current document.
Each entry in the collection is an \f[CR]HTMLImageElement\f[R]
representing a single image element.
.SH USAGE NOTES
You can use either JavaScript array notation or the \f[CR]item()\f[R]
method on the returned collection to access the items in the collection.
The following are equivalent:
.IP
.EX
firstImage = imageCollection.item(0);

firstImage = imageCollection[0];
.EE
.SH EXAMPLES
This example looks through the list of images and finds those named
\f[CR]\(dqbanner.gif\(dq\f[R].
.IP
.EX
\f[B]for\f[R] (\f[B]const\f[R] image \f[B]of\f[R] document.images) {
  \f[B]if\f[R] (image.src === \(dqbanner.gif\(dq) {
    console.log(\(dqFound the banner\(dq);
  }
}
.EE
