.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "ArrayBuffer.slice" "JS" "February 21, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
ArrayBuffer.slice - ArrayBuffer.prototype.slice()
.SH SYNOPSIS
The \f[B]\f[CB]slice()\f[B]\f[R] method returns a new
\f[CR]ArrayBuffer\f[R] whose contents are a copy of this
\f[CR]ArrayBuffer\f[R]\[cq]s bytes from \f[CR]begin\f[R], inclusive, up
to \f[CR]end\f[R], exclusive.
.SH SYNTAX
.IP
.EX
slice(begin)
slice(begin, end)
.EE
.SS Parameters
.TP
\f[B]begin\f[R]
Zero-based byte index at which to begin slicing.
.TP
\f[B]end\f[R] \f[I](optional)\f[R]
Byte index before which to end slicing.
If end is unspecified, the new \f[CR]ArrayBuffer\f[R] contains all bytes
from begin to the end of this \f[CR]ArrayBuffer\f[R].
If negative, it will make the Byte index begin from the last Byte.
.SS Return value
A new \f[CR]ArrayBuffer\f[R] object.
.SH DESCRIPTION
The \f[CR]slice()\f[R] method copies up to, but not including, the byte
indicated by the \f[CR]end\f[R] parameter.
If either \f[CR]begin\f[R] or \f[CR]end\f[R] is negative, it refers to
an index from the end of the array, as opposed to from the beginning.
.PP
The range specified by the \f[CR]begin\f[R] and \f[CR]end\f[R]
parameters is clamped to the valid index range for the current array.
If the computed length of the new \f[CR]ArrayBuffer\f[R] would be
negative, it is clamped to zero.
.SH EXAMPLES
.SS Copying an ArrayBuffer
.IP
.EX
const buf1 = new ArrayBuffer(8);
const buf2 = buf1.slice(0);
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]ArrayBuffer\f[R]
