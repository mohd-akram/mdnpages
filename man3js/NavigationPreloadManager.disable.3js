.\" Automatically generated by Pandoc 3.4
.\"
.TH "NavigationPreloadManager.disable" "JS" "June 7, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NavigationPreloadManager.disable \- NavigationPreloadManager: disable()
method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]disable()\f[B]\f[R] method of the
\f[CR]NavigationPreloadManager\f[R] interface halts the automatic
preloading of service\-worker\-managed resources previously started
using \f[CR]enable()\f[R] It returns a promise that resolves with
\f[CR]undefined\f[R].
.PP
The method may be called in the service worker\[cq]s \f[CR]activate\f[R]
event handler (before the \f[CR]fetch\f[R] event handler can be called).
.SH SYNTAX
.IP
.EX
disable()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]Promise\f[R] that resolves with \f[CR]undefined\f[R].
.SS Exceptions
.TP
\f[B]InvalidStateError\f[R] \f[B]DOMException\f[R]
There is no active worker associated with the registration to which this
\f[CR]NavigationPreloadManager\f[R] belongs.
.SH EXAMPLES
The code below shows how to disable preloading, after first using
\f[CR]ServiceWorkerRegistration.navigationPreload\f[R] to test that it
is supported.
.IP
.EX
addEventListener(\[dq]activate\[dq], (event) \f[B]=>\f[R] {
  event.waitUntil(
    (\f[B]async\f[R] () \f[B]=>\f[R] {
      \f[B]if\f[R] (self.registration.navigationPreload) {
        \f[I]// Disable navigation preloads!\f[R]
        \f[B]await\f[R] self.registration.navigationPreload.disable();
      }
    })(),
  );
});
.EE
.SH SEE ALSO
\f[CR]NavigationPreloadManager.enable()\f[R]
