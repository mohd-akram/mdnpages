'\" t
.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Proxy" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Proxy - Proxy
.SH SYNOPSIS
.PP
The \f[B]\f[VB]Proxy\f[B]\f[R] object enables you to create a proxy for
another object, which can intercept and redefine fundamental operations
for that object.
.SH DESCRIPTION
.PP
The \f[V]Proxy\f[R] object allows you to create an object that can be
used in place of the original object, but which may redefine fundamental
\f[V]Object\f[R] operations like getting, setting, and defining
properties.
Proxy objects are commonly used to log property accesses, validate,
format, or sanitize inputs, and so on.
.PP
You create a \f[V]Proxy\f[R] with two parameters:
.IP \[bu] 2
\f[V]target\f[R]: the original object which you want to proxy
.IP \[bu] 2
\f[V]handler\f[R]: an object that defines which operations will be
intercepted and how to redefine intercepted operations.
.PP
For example, this code creates a proxy for the \f[V]target\f[R] object.
.IP
.nf
\f[C]
const target = {
  message1: \[dq]hello\[dq],
  message2: \[dq]everyone\[dq],
};

const handler1 = {};

const proxy1 = new Proxy(target, handler1);
\f[R]
.fi
.PP
Because the handler is empty, this proxy behaves just like the original
target:
.IP
.nf
\f[C]
console.log(proxy1.message1); // hello
console.log(proxy1.message2); // everyone
\f[R]
.fi
.PP
To customize the proxy, we define functions on the handler object:
.IP
.nf
\f[C]
const target = {
  message1: \[dq]hello\[dq],
  message2: \[dq]everyone\[dq],
};

const handler2 = {
  get(target, prop, receiver) {
    return \[dq]world\[dq];
  },
};

const proxy2 = new Proxy(target, handler2);
\f[R]
.fi
.PP
Here we\[cq]ve provided an implementation of the \f[V]get()\f[R]
handler, which intercepts attempts to access properties in the target.
.PP
Handler functions are sometimes called \f[I]traps\f[R], presumably
because they trap calls to the target object.
The very simple trap in \f[V]handler2\f[R] above redefines all property
accessors:
.IP
.nf
\f[C]
console.log(proxy2.message1); // world
console.log(proxy2.message2); // world
\f[R]
.fi
.PP
Proxies are often used with the \f[V]Reflect\f[R] object, which provides
some methods with the same names as the \f[V]Proxy\f[R] traps.
The \f[V]Reflect\f[R] methods provide the reflective semantics for
invoking the corresponding object internal methods.
For example, we can call \f[V]Reflect.get\f[R] if we don\[cq]t wish to
redefine the object\[cq]s behavior:
.IP
.nf
\f[C]
const target = {
  message1: \[dq]hello\[dq],
  message2: \[dq]everyone\[dq],
};

const handler3 = {
  get(target, prop, receiver) {
    if (prop === \[dq]message2\[dq]) {
      return \[dq]world\[dq];
    }
    return Reflect.get(...arguments);
  },
};

const proxy3 = new Proxy(target, handler3);

console.log(proxy3.message1); // hello
console.log(proxy3.message2); // world
\f[R]
.fi
.PP
The \f[V]Reflect\f[R] method still interacts with the object through
object internal methods \[em] it doesn\[cq]t \[lq]de-proxify\[rq] the
proxy if it\[cq]s invoked on a proxy.
If you use \f[V]Reflect\f[R] methods within a proxy trap, and the
\f[V]Reflect\f[R] method call gets intercepted by the trap again, there
may be infinite recursion.
.SS Terminology
.PP
The following terms are used when talking about the functionality of
proxies.
.TP
handler
The object passed as the second argument to the \f[V]Proxy\f[R]
constructor.
It contains the traps which define the behavior of the proxy.
.TP
trap
The function that define the behavior for the corresponding object
internal method.
(This is analogous to the concept of \f[I]traps\f[R] in operating
systems.)
.TP
target
Object which the proxy virtualizes.
It is often used as storage backend for the proxy.
Invariants (semantics that remain unchanged) regarding object
non-extensibility or non-configurable properties are verified against
the target.
.TP
invariants
Semantics that remain unchanged when implementing custom operations.
If your trap implementation violates the invariants of a handler, a
\f[V]TypeError\f[R] will be thrown.
.SS Object internal methods
.PP
Objects are collections of properties.
However, the language doesn\[cq]t provide any machinery to
\f[I]directly\f[R] manipulate data stored in the object \[em] rather,
the object defines some internal methods specifying how it can be
interacted with.
For example, when you read \f[V]obj.x\f[R], you may expect the following
to happen:
.IP \[bu] 2
The \f[V]x\f[R] property is searched up the prototype chain until it is
found.
.IP \[bu] 2
If \f[V]x\f[R] is a data property, the property descriptor\[cq]s
\f[V]value\f[R] attribute is returned.
.IP \[bu] 2
If \f[V]x\f[R] is an accessor property, the getter is invoked, and the
return value of the getter is returned.
.PP
There isn\[cq]t anything special about this process in the language
\[em] it\[cq]s just because ordinary objects, by default, have a
\f[V][[Get]]\f[R] internal method that is defined with this behavior.
The \f[V]obj.x\f[R] property access syntax simply invokes the
\f[V][[Get]]\f[R] method on the object, and the object uses its own
internal method implementation to determine what to return.
.PP
As another example, arrays differ from normal objects, because they have
a magic \f[V]length\f[R] property that, when modified, automatically
allocates empty slots or removes elements from the array.
Similarly, adding array elements automatically changes the
\f[V]length\f[R] property.
This is because arrays have a \f[V][[DefineOwnProperty]]\f[R] internal
method that knows to update \f[V]length\f[R] when an integer index is
written to, or update the array contents when \f[V]length\f[R] is
written to.
Such objects whose internal methods have different implementations from
ordinary objects are called \f[I]exotic objects\f[R].
\f[V]Proxy\f[R] enable developers to define their own exotic objects
with full capacity.
.PP
All objects have the following internal methods:
.PP
.TS
tab(@);
lw(15.6n) lw(54.4n).
T{
Internal method
T}@T{
Corresponding trap
T}
_
T{
\f[V][[GetPrototypeOf]]\f[R]
T}@T{
\f[V]getPrototypeOf()\f[R]
T}
T{
\f[V][[SetPrototypeOf]]\f[R]
T}@T{
\f[V]setPrototypeOf()\f[R]
T}
T{
\f[V][[IsExtensible]]\f[R]
T}@T{
\f[V]isExtensible()\f[R]
T}
T{
\f[V][[PreventExtensions]]\f[R]
T}@T{
\f[V]preventExtensions()\f[R]
T}
T{
\f[V][[GetOwnProperty]]\f[R]
T}@T{
\f[V]getOwnPropertyDescriptor()\f[R]
T}
T{
\f[V][[DefineOwnProperty]]\f[R]
T}@T{
\f[V]defineProperty()\f[R]
T}
T{
\f[V][[HasProperty]]\f[R]
T}@T{
\f[V]has()\f[R]
T}
T{
\f[V][[Get]]\f[R]
T}@T{
\f[V]get()\f[R]
T}
T{
\f[V][[Set]]\f[R]
T}@T{
\f[V]set()\f[R]
T}
T{
\f[V][[Delete]]\f[R]
T}@T{
\f[V]deleteProperty()\f[R]
T}
T{
\f[V][[OwnPropertyKeys]]\f[R]
T}@T{
\f[V]ownKeys()\f[R]
T}
.TE
.PP
Function objects also have the following internal methods:
.PP
.TS
tab(@);
l l.
T{
Internal method
T}@T{
Corresponding trap
T}
_
T{
\f[V][[Call]]\f[R]
T}@T{
\f[V]apply()\f[R]
T}
T{
\f[V][[Construct]]\f[R]
T}@T{
\f[V]construct()\f[R]
T}
.TE
.PP
It\[cq]s important to realize that all interactions with an object
eventually boils down to the invocation of one of these internal
methods, and that they are all customizable through proxies.
This means almost no behavior (except certain critical invariants) is
guaranteed in the language \[em] everything is defined by the object
itself.
When you run \f[V]delete obj.x\f[R], there\[cq]s no guarantee that
\f[V]\[dq]x\[dq] in obj\f[R] returns \f[V]false\f[R] afterwards \[em] it
depends on the object\[cq]s implementations of \f[V][[Delete]]\f[R] and
\f[V][[HasProperty]]\f[R].
A \f[V]delete obj.x\f[R] may log things to the console, modify some
global state, or even define a new property instead of deleting the
existing one, although these semantics should be avoided in your own
code.
.PP
All internal methods are called by the language itself, and are not
directly accessible in JavaScript code.
The \f[V]Reflect\f[R] namespace offers methods that do little more than
call the internal methods, besides some input normalization/validation.
In each trap\[cq]s page, we list several typical situations when the
trap is invoked, but these internal methods are called in \f[I]a
lot\f[R] of places.
For example, array methods read and write to array through these
internal methods, so methods like \f[V]push()\f[R] would also invoke
\f[V]get()\f[R] and \f[V]set()\f[R] traps.
.PP
Most of the internal methods are straightforward in what they do.
The only two that may be confusable are \f[V][[Set]]\f[R] and
\f[V][[DefineOwnProperty]]\f[R].
For normal objects, the former invokes setters; the latter doesn\[cq]t.
(And \f[V][[Set]]\f[R] calls \f[V][[DefineOwnProperty]]\f[R] internally
if there\[cq]s no existing property or the property is a data property.)
While you may know that the \f[V]obj.x = 1\f[R] syntax uses
\f[V][[Set]]\f[R], and \f[V]Object.defineProperty()\f[R] uses
\f[V][[DefineOwnProperty]]\f[R], it\[cq]s not immediately apparent what
semantics other built-in methods and syntaxes use.
For example, class fields use the \f[V][[DefineOwnProperty]]\f[R]
semantic, which is why setters defined in the superclass are not invoked
when a field is declared on the derived class.
.SH CONSTRUCTOR
.TP
\f[B]Proxy()\f[R]
Creates a new \f[V]Proxy\f[R] object.
.RS
.PP
\f[B]Note:\f[R] There\[cq]s no \f[V]Proxy.prototype\f[R] property, so
\f[V]Proxy\f[R] instances do not have any special properties or methods.
.RE
.SH STATIC METHODS
.TP
\f[B]Proxy.revocable()\f[R]
Creates a revocable \f[V]Proxy\f[R] object.
.SH EXAMPLES
.SS Basic example
.PP
In this simple example, the number \f[V]37\f[R] gets returned as the
default value when the property name is not in the object.
It is using the \f[V]get()\f[R] handler.
.IP
.nf
\f[C]
const handler = {
  get(obj, prop) {
    return prop in obj ? obj[prop] : 37;
  },
};

const p = new Proxy({}, handler);
p.a = 1;
p.b = undefined;

console.log(p.a, p.b); // 1, undefined

console.log(\[dq]c\[dq] in p, p.c); // false, 37
\f[R]
.fi
.SS No-op forwarding proxy
.PP
In this example, we are using a native JavaScript object to which our
proxy will forward all operations that are applied to it.
.IP
.nf
\f[C]
const target = {};
const p = new Proxy(target, {});

p.a = 37; // Operation forwarded to the target

console.log(target.a); // 37 (The operation has been properly forwarded!)
\f[R]
.fi
.PP
Note that while this \[lq]no-op\[rq] works for plain JavaScript objects,
it does not work for native objects, such as DOM elements, \f[V]Map\f[R]
objects, or anything that has internal slots.
See no private property forwarding for more information.
.SS No private property forwarding
.PP
A proxy is still another object with a different identity \[em] it\[cq]s
a \f[I]proxy\f[R] that operates between the wrapped object and the
outside.
As such, the proxy does not have direct access to the original
object\[cq]s private properties.
.IP
.nf
\f[C]
class Secret {
  #secret;
  constructor(secret) {
    this.#secret = secret;
  }
  get secret() {
    return this.#secret.replace(/\[rs]d+/, \[dq][REDACTED]\[dq]);
  }
}

const aSecret = new Secret(\[dq]123456\[dq]);
console.log(aSecret.secret); // [REDACTED]
// Looks like a no-op forwarding...
const proxy = new Proxy(aSecret, {});
console.log(proxy.secret); // TypeError: Cannot read private member #secret from an object whose class did not declare it
\f[R]
.fi
.PP
This is because when the proxy\[cq]s \f[V]get\f[R] trap is invoked, the
\f[V]this\f[R] value is the \f[V]proxy\f[R] instead of the original
\f[V]secret\f[R], so \f[V]#secret\f[R] is not accessible.
To fix this, use the original \f[V]secret\f[R] as \f[V]this\f[R]:
.IP
.nf
\f[C]
const proxy = new Proxy(aSecret, {
  get(target, prop, receiver) {
    // By default, it looks like Reflect.get(target, prop, receiver)
    // which has a different value of \[ga]this\[ga]
    return target[prop];
  },
});
console.log(proxy.secret);
\f[R]
.fi
.PP
For methods, this means you have to redirect the method\[cq]s
\f[V]this\f[R] value to the original object as well:
.IP
.nf
\f[C]
class Secret {
  #x = 1;
  x() {
    return this.#x;
  }
}

const aSecret = new Secret();
const proxy = new Proxy(aSecret, {
  get(target, prop, receiver) {
    const value = target[prop];
    if (value instanceof Function) {
      return function (...args) {
        return value.apply(this === receiver ? target : this, args);
      };
    }
    return value;
  },
});
console.log(proxy.x());
\f[R]
.fi
.PP
Some native JavaScript objects have properties called \f[I]internal
slots (https://tc39.es/ecma262/#sec-object-internal-methods-and-internal-slots)\f[R],
which are not accessible from JavaScript code.
For example, \f[V]Map\f[R] objects have an internal slot called
\f[V][[MapData]]\f[R], which stores the key-value pairs of the map.
As such, you cannot trivially create a forwarding proxy for a map:
.IP
.nf
\f[C]
const proxy = new Proxy(new Map(), {});
console.log(proxy.size); // TypeError: get size method called on incompatible Proxy
\f[R]
.fi
.PP
You have to use the \[lq]\f[V]this\f[R]-recovering\[rq] proxy
illustrated above to work around this.
.SS Validation
.PP
With a \f[V]Proxy\f[R], you can easily validate the passed value for an
object.
This example uses the \f[V]set()\f[R] handler.
.IP
.nf
\f[C]
const validator = {
  set(obj, prop, value) {
    if (prop === \[dq]age\[dq]) {
      if (!Number.isInteger(value)) {
        throw new TypeError(\[dq]The age is not an integer\[dq]);
      }
      if (value > 200) {
        throw new RangeError(\[dq]The age seems invalid\[dq]);
      }
    }

    // The default behavior to store the value
    obj[prop] = value;

    // Indicate success
    return true;
  },
};

const person = new Proxy({}, validator);

person.age = 100;
console.log(person.age); // 100
person.age = \[dq]young\[dq]; // Throws an exception
person.age = 300; // Throws an exception
\f[R]
.fi
.SS Manipulating DOM nodes
.PP
In this example we use \f[V]Proxy\f[R] to toggle an attribute of two
different elements: so when we set the attribute on one element, the
attribute is unset on the other one.
.PP
We create a \f[V]view\f[R] object which is a proxy for an object with a
\f[V]selected\f[R] property.
The proxy handler defines the \f[V]set()\f[R] handler.
.PP
When we assign an HTML element to \f[V]view.selected\f[R], the
element\[cq]s \f[V]\[aq]aria-selected\[aq]\f[R] attribute is set to
\f[V]true\f[R].
If we then assign a different element to \f[V]view.selected\f[R], this
element\[cq]s \f[V]\[aq]aria-selected\[aq]\f[R] attribute is set to
\f[V]true\f[R] and the previous element\[cq]s
\f[V]\[aq]aria-selected\[aq]\f[R] attribute is automatically set to
\f[V]false\f[R].
.IP
.nf
\f[C]
const view = new Proxy(
  {
    selected: null,
  },
  {
    set(obj, prop, newval) {
      const oldval = obj[prop];

      if (prop === \[dq]selected\[dq]) {
        if (oldval) {
          oldval.setAttribute(\[dq]aria-selected\[dq], \[dq]false\[dq]);
        }
        if (newval) {
          newval.setAttribute(\[dq]aria-selected\[dq], \[dq]true\[dq]);
        }
      }

      // The default behavior to store the value
      obj[prop] = newval;

      // Indicate success
      return true;
    },
  },
);

const item1 = document.getElementById(\[dq]item-1\[dq]);
const item2 = document.getElementById(\[dq]item-2\[dq]);

// select item1:
view.selected = item1;

console.log(\[ga]item1: ${item1.getAttribute(\[dq]aria-selected\[dq])}\[ga]);
// item1: true

// selecting item2 de-selects item1:
view.selected = item2;

console.log(\[ga]item1: ${item1.getAttribute(\[dq]aria-selected\[dq])}\[ga]);
// item1: false

console.log(\[ga]item2: ${item2.getAttribute(\[dq]aria-selected\[dq])}\[ga]);
// item2: true
\f[R]
.fi
.SS Value correction and an extra property
.PP
The \f[V]products\f[R] proxy object evaluates the passed value and
converts it to an array if needed.
The object also supports an extra property called
\f[V]latestBrowser\f[R] both as a getter and a setter.
.IP
.nf
\f[C]
const products = new Proxy(
  {
    browsers: [\[dq]Firefox\[dq], \[dq]Chrome\[dq]],
  },
  {
    get(obj, prop) {
      // An extra property
      if (prop === \[dq]latestBrowser\[dq]) {
        return obj.browsers[obj.browsers.length - 1];
      }

      // The default behavior to return the value
      return obj[prop];
    },
    set(obj, prop, value) {
      // An extra property
      if (prop === \[dq]latestBrowser\[dq]) {
        obj.browsers.push(value);
        return true;
      }

      // Convert the value if it is not an array
      if (typeof value === \[dq]string\[dq]) {
        value = [value];
      }

      // The default behavior to store the value
      obj[prop] = value;

      // Indicate success
      return true;
    },
  },
);

console.log(products.browsers);
//  [\[aq]Firefox\[aq], \[aq]Chrome\[aq]]

products.browsers = \[dq]Safari\[dq];
//  pass a string (by mistake)

console.log(products.browsers);
//  [\[aq]Safari\[aq]] <- no problem, the value is an array

products.latestBrowser = \[dq]Edge\[dq];

console.log(products.browsers);
//  [\[aq]Safari\[aq], \[aq]Edge\[aq]]

console.log(products.latestBrowser);
//  \[aq]Edge\[aq]
\f[R]
.fi
.SS Finding an array item object by its property
.PP
This proxy extends an array with some utility features.
As you see, you can flexibly \[lq]define\[rq] properties without using
\f[V]Object.defineProperties()\f[R].
This example can be adapted to find a table row by its cell.
In that case, the target will be \f[V]table.rows\f[R].
.IP
.nf
\f[C]
const products = new Proxy(
  [
    { name: \[dq]Firefox\[dq], type: \[dq]browser\[dq] },
    { name: \[dq]SeaMonkey\[dq], type: \[dq]browser\[dq] },
    { name: \[dq]Thunderbird\[dq], type: \[dq]mailer\[dq] },
  ],
  {
    get(obj, prop) {
      // The default behavior to return the value; prop is usually an integer
      if (prop in obj) {
        return obj[prop];
      }

      // Get the number of products; an alias of products.length
      if (prop === \[dq]number\[dq]) {
        return obj.length;
      }

      let result;
      const types = {};

      for (const product of obj) {
        if (product.name === prop) {
          result = product;
        }
        if (types[product.type]) {
          types[product.type].push(product);
        } else {
          types[product.type] = [product];
        }
      }

      // Get a product by name
      if (result) {
        return result;
      }

      // Get products by type
      if (prop in types) {
        return types[prop];
      }

      // Get product types
      if (prop === \[dq]types\[dq]) {
        return Object.keys(types);
      }

      return undefined;
    },
  },
);

console.log(products[0]); // { name: \[aq]Firefox\[aq], type: \[aq]browser\[aq] }
console.log(products[\[dq]Firefox\[dq]]); // { name: \[aq]Firefox\[aq], type: \[aq]browser\[aq] }
console.log(products[\[dq]Chrome\[dq]]); // undefined
console.log(products.browser); // [{ name: \[aq]Firefox\[aq], type: \[aq]browser\[aq] }, { name: \[aq]SeaMonkey\[aq], type: \[aq]browser\[aq] }]
console.log(products.types); // [\[aq]browser\[aq], \[aq]mailer\[aq]]
console.log(products.number); // 3
\f[R]
.fi
.SS A complete traps list example
.PP
Now in order to create a complete sample \f[V]traps\f[R] list, for
didactic purposes, we will try to proxify a \f[I]non-native\f[R] object
that is particularly suited to this type of operation: the
\f[V]docCookies\f[R] global object created by a simple cookie
framework (https://reference.codeproject.com/dom/document/cookie/simple_document.cookie_framework).
.IP
.nf
\f[C]
/*
  const docCookies = ... get the \[dq]docCookies\[dq] object here:
  https://reference.codeproject.com/dom/document/cookie/simple_document.cookie_framework
*/

const docCookies = new Proxy(docCookies, {
  get(target, key) {
    return target[key] ?? target.getItem(key) ?? undefined;
  },
  set(target, key, value) {
    if (key in target) {
      return false;
    }
    return target.setItem(key, value);
  },
  deleteProperty(target, key) {
    if (!(key in target)) {
      return false;
    }
    return target.removeItem(key);
  },
  ownKeys(target) {
    return target.keys();
  },
  has(target, key) {
    return key in target || target.hasItem(key);
  },
  defineProperty(target, key, descriptor) {
    if (descriptor && \[dq]value\[dq] in descriptor) {
      target.setItem(key, descriptor.value);
    }
    return target;
  },
  getOwnPropertyDescriptor(target, key) {
    const value = target.getItem(key);
    return value
      ? {
          value,
          writable: true,
          enumerable: true,
          configurable: false,
        }
      : undefined;
  },
});

/* Cookies test */

console.log((docCookies.myCookie1 = \[dq]First value\[dq]));
console.log(docCookies.getItem(\[dq]myCookie1\[dq]));

docCookies.setItem(\[dq]myCookie1\[dq], \[dq]Changed value\[dq]);
console.log(docCookies.myCookie1);
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\[lq]Proxies are awesome\[rq] Brendan Eich presentation at
JSConf (https://www.youtube.com/watch?v=sClk6aB_CPk)
(slides (https://www.slideshare.net/BrendanEich/metaprog-5303821))
.IP \[bu] 2
Tutorial on
proxies (https://web.archive.org/web/20171007221059/https://soft.vub.ac.be/~tvcutsem/proxies/)
