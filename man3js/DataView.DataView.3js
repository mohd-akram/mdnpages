.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "DataView.DataView" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]DataView()\f[B]\f[R] constructor creates
\f[V]DataView\f[R] objects.
.SH Syntax
.IP
.nf
\f[C]
new DataView(buffer)
new DataView(buffer, byteOffset)
new DataView(buffer, byteOffset, byteLength)
\f[R]
.fi
.RS
.PP
\f[B]Note:\f[R] \f[V]DataView()\f[R] can only be constructed with
\f[V]new\f[R].
Attempting to call it without \f[V]new\f[R] throws a
\f[V]TypeError\f[R].
.RE
.SS Parameters
.IP \[bu] 2
\f[V]buffer\f[R]
.RS 2
.IP \[bu] 2
An existing \f[V]ArrayBuffer\f[R] or \f[V]SharedArrayBuffer\f[R] to use
as the storage backing the new \f[V]DataView\f[R] object.
.RE
.IP \[bu] 2
\f[V]byteOffset\f[R] \f[I](optional)\f[R]
.RS 2
.IP \[bu] 2
The offset, in bytes, to the first byte in the above buffer for the new
view to reference.
If unspecified, the buffer view starts with the first byte.
.RE
.IP \[bu] 2
\f[V]byteLength\f[R] \f[I](optional)\f[R]
.RS 2
.IP \[bu] 2
The number of elements in the byte array.
If unspecified, the view\[cq]s length will match the buffer\[cq]s
length.
.RE
.SS Return value
.PP
A new \f[V]DataView\f[R] object representing the specified data buffer.
.SS Exceptions
.IP \[bu] 2
\f[V]RangeError\f[R]
.RS 2
.IP \[bu] 2
Thrown if the \f[V]byteOffset\f[R] or \f[V]byteLength\f[R] parameter
values result in the view extending past the end of the buffer.
.RS 2
.PP
For example, if the buffer is 16 bytes long, the \f[V]byteOffset\f[R] is
8, and the \f[V]byteLength\f[R] is 10, this error is thrown because the
resulting view tries to extend 2 bytes past the total length of the
buffer.
.RE
.RE
.SH Examples
.SS Using DataView
.IP
.nf
\f[C]
const buffer = new ArrayBuffer(16);
const view = new DataView(buffer, 0);

view.setInt16(1, 42);
view.getInt16(1); // 42
\f[R]
.fi
.SH See also
.IP \[bu] 2
Polyfill of \f[V]DataView\f[R] in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-typed-arrays)
.IP \[bu] 2
\f[V]DataView\f[R]
