.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "AUDIONODE.CHANNELCOUNT" "3JS" "April 6, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
AudioNode.channelCount \- AudioNode: channelCount property
.SH SYNOPSIS
The \f[B]\f[CB]channelCount\f[B]\f[R] property of the
\f[CR]AudioNode\f[R] interface represents an integer used to determine
how many channels are used when up\-mixing and down\-mixing connections
to any inputs to the node.
.PP
\f[CR]channelCount\f[R]\(cqs usage and precise definition depend on the
value of \f[CR]AudioNode.channelCountMode\f[R]:
.IP \(bu 2
It is ignored if the \f[CR]channelCountMode\f[R] value is
\f[CR]max\f[R].
.IP \(bu 2
It is used as a maximum value if the \f[CR]channelCountMode\f[R] value
is \f[CR]clamped\-max\f[R].
.IP \(bu 2
It is used as the exact value if the \f[CR]channelCountMode\f[R] value
is \f[CR]explicit\f[R].
.SH VALUE
An integer.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] audioCtx = \f[B]new\f[R] AudioContext();

\f[B]const\f[R] oscillator = audioCtx.createOscillator();
\f[B]const\f[R] gainNode = audioCtx.createGain();

oscillator.connect(gainNode);
gainNode.connect(audioCtx.destination);

oscillator.channelCount;
.EE
.SH SEE ALSO
.IP \(bu 2
Using the Web Audio API
