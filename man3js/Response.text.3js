.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "RESPONSE.TEXT" "3JS" "March 13, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Response.text \- Response: text() method
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]text()\f[B]\f[R] method of the \f[CR]Response\f[R]
interface takes a \f[CR]Response\f[R] stream and reads it to completion.
It returns a promise that resolves with a \f[CR]String\f[R].
The response is \f[I]always\f[R] decoded using UTF\-8.
.SH SYNTAX
.IP
.EX
text()
.EE
.SS Parameters
None.
.SS Return value
A Promise that resolves with a \f[CR]String\f[R].
.SS Exceptions
.TP
\f[B]DOMException\f[R] \f[B]AbortError\f[R]
The request was aborted.
.TP
\f[B]TypeError\f[R]
Thrown for one of the following reasons:
.RS
.IP \(bu 2
The response body is disturbed or locked.
.IP \(bu 2
There was an error decoding the body content (for example, because the
\f[CR]Content\-Encoding\f[R] header is incorrect).
.RE
.SH EXAMPLES
In our \c
.UR https://github.com/mdn/dom-examples/tree/main/fetch/fetch-text
fetch text example
.UE \c
\ (run \c
.UR https://mdn.github.io/dom-examples/fetch/fetch-text/
fetch text live
.UE \c
), we have an \f[CR]<article>\f[R] element and three links (stored in
the \f[CR]myLinks\f[R] array.)
First, we loop through all of these and give each one an
\f[CR]onclick\f[R] event handler so that the \f[CR]getData()\f[R]
function is run \(em with the link\(cqs \f[CR]data\-page\f[R] identifier
passed to it as an argument \(em when one of the links is clicked.
.PP
When \f[CR]getData()\f[R] is run, we create a new request using the
\f[CR]Request()\f[R] constructor, then use it to fetch a specific
\f[CR].txt\f[R] file.
When the fetch is successful, we read a string out of the response using
\f[CR]text()\f[R], then set the \f[CR]innerText\f[R] of the
\f[CR]<article>\f[R] element equal to the text object.
.IP
.EX
\f[B]const\f[R] myArticle = document.querySelector(\(dqarticle\(dq);
\f[B]const\f[R] myLinks = document.querySelectorAll(\(dqul a\(dq);

\f[B]for\f[R] (\f[B]const\f[R] link \f[B]of\f[R] myLinks) {
  link.onclick = (e) \f[B]=>\f[R] {
    e.preventDefault();
    \f[B]const\f[R] linkData = e.target.getAttribute(\(dqdata\-page\(dq);
    getData(linkData);
  };
}

\f[B]function\f[R] getData(pageId) {
  console.log(pageId);
  \f[B]const\f[R] myRequest = \f[B]new\f[R] Request(\(ga${pageId}.txt\(ga);
  fetch(myRequest)
    .then((response) \f[B]=>\f[R] response.text())
    .then((text) \f[B]=>\f[R] {
      myArticle.innerText = text;
    });
}
.EE
.SH SEE ALSO
.IP \(bu 2
ServiceWorker API
.IP \(bu 2
HTTP access control (CORS)
.IP \(bu 2
HTTP
