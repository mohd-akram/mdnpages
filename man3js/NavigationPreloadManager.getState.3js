.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "NAVIGATIONPRELOADMANAGER.GETSTATE" "3JS" "July 24, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NavigationPreloadManager.getState \- NavigationPreloadManager:
getState() method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]getState()\f[B]\f[R] method of the
\f[CR]NavigationPreloadManager\f[R] interface returns a
\f[CR]Promise\f[R] that resolves to an object with properties that
indicate whether preload is enabled and what value will be sent in the
\f[CR]Service\-Worker\-Navigation\-Preload\f[R] HTTP header.
.SH SYNTAX
.IP
.EX
getState()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]Promise\f[R] that resolves with an object that has the following
properties:
.TP
\f[B]enabled\f[R]
\f[CR]true\f[R] if preloading is enabled, and \f[CR]false\f[R]
otherwise.
.TP
\f[B]headerValue\f[R]
A string containing the value that will be sent in the
\f[CR]Service\-Worker\-Navigation\-Preload\f[R] HTTP header following a
preloading \f[CR]fetch()\f[R].
This defaults to \f[CR]true\f[R] unless the value was changed using
\f[CR]NavigationPreloadManager.setHeaderValue()\f[R].
.SS Exceptions
.TP
\f[B]InvalidStateError\f[R] \f[B]DOMException\f[R]
There is no active worker associated with the registration to which this
\f[CR]NavigationPreloadManager\f[R] belongs.
.SH EXAMPLES
The code below shows a request for the current state, made once the
service worker is ready.
.IP
.EX
navigator.serviceWorker.ready
  .then((registration) \f[B]=>\f[R] registration.navigationPreload.getState())
  .then((state) \f[B]=>\f[R] {
    console.log(state.enabled); \f[I]// boolean\f[R]
    console.log(state.headerValue); \f[I]// string\f[R]
  })
  .catch((e) \f[B]=>\f[R]
    console.error(\(gaNavigationPreloadManager not supported: ${e.message}\(ga),
  );
.EE
