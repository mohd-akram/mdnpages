.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "WeakRef.deref" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]deref()\f[B]\f[R] method returns the \f[V]WeakRef\f[R]
instance\[cq]s target value, or \f[V]undefined\f[R] if the target value
has been garbage-collected.
.SH Syntax
.IP
.nf
\f[C]
deref()
\f[R]
.fi
.SS Return value
.PP
The target value of the WeakRef, which is either an object or a
non-registered symbol.
Returns \f[V]undefined\f[R] if the value has been garbage-collected.
.SH Description
.PP
See the Notes on WeakRefs section of the \f[V]WeakRef\f[R] page for some
important notes.
.SH Examples
.SS Using deref()
.PP
See the Examples section of the \f[V]WeakRef\f[R] page for the complete
example.
.IP
.nf
\f[C]
const tick = () => {
  // Get the element from the weak reference, if it still exists
  const element = this.ref.deref();
  if (element) {
    element.textContent = ++this.count;
  } else {
    // The element doesn\[aq]t exist anymore
    console.log(\[dq]The element is gone.\[dq]);
    this.stop();
    this.ref = null;
  }
};
\f[R]
.fi
.SH See also
.IP \[bu] 2
\f[V]WeakRef\f[R]
