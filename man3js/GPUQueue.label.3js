.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "GPUQUEUE.LABEL" "3JS" "June 18, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
GPUQueue.label \- GPUQueue: label property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]label\f[B]\f[R] read\-only property of the
\f[CR]GPUQueue\f[R] interface is a string providing a label that can be
used to identify the object, for example in \f[CR]GPUError\f[R] messages
or console warnings.
.PP
You can get and set it directly on the \f[CR]GPUQueue\f[R] object.
.SH VALUE
A string.
If no label value has previously been set, getting the label returns an
empty string.
.SH EXAMPLES
Setting and getting a label via \f[CR]GPUQueue.label\f[R]:
.IP
.EX
device.queue.label = \(dqmy_queue\(dq;
console.log(device.queue.label); \f[I]// \(dqmy_queue\(dq\f[R]
.EE
.PP
You can also set the queue\(cqs label at the time you request the
device, like this:
.IP
.EX
\f[B]const\f[R] device = adapter.requestDevice({
  defaultQueue: { label: \(dqmy_queue\(dq },
});
.EE
.SH SEE ALSO
.IP \(bu 2
The WebGPU API
