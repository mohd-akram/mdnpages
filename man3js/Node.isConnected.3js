.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "Node.isConnected" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Node.isConnected \- Node: isConnected property
.SH SYNOPSIS
The read\-only \f[B]\f[CB]isConnected\f[B]\f[R] property of the
\f[CR]Node\f[R] interface returns a boolean indicating whether the node
is connected (directly or indirectly) to a \f[CR]Document\f[R] object.
.SH VALUE
A boolean value that is \f[CR]true\f[R] if the node is connected to its
relevant context object, and \f[CR]false\f[R] if not.
.SH EXAMPLES
.SS Standard DOM
A standard DOM example:
.IP
.EX
\f[B]let\f[R] test = document.createElement(\[dq]p\[dq]);
console.log(test.isConnected); \f[I]// Returns false\f[R]
document.body.appendChild(test);
console.log(test.isConnected); \f[I]// Returns true\f[R]
.EE
.SS Shadow DOM
A shadow DOM example:
.IP
.EX
\f[I]// Create a shadow root\f[R]
\f[B]const\f[R] shadow = \f[B]this\f[R].attachShadow({ mode: \[dq]open\[dq] });

\f[I]// Create some CSS to apply to the shadow DOM\f[R]
\f[B]const\f[R] style = document.createElement(\[dq]style\[dq]);
console.log(style.isConnected); \f[I]// returns false\f[R]

style.textContent = \[ga]
\&.wrapper {
  position: relative;
}

\&.info {
  font\-size: 0.8rem;
  width: 200px;
  display: inline\-block;
  border: 1px solid black;
  padding: 10px;
  background: white;
  border\-radius: 10px;
  opacity: 0;
  transition: 0.6s all;
  positions: absolute;
  bottom: 20px;
  left: 10px;
  z\-index: 3
}
\[ga];

\f[I]// Attach the created style element to the shadow DOM\f[R]

shadow.appendChild(style);
console.log(style.isConnected); \f[I]// Returns true\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
\c
.UR https://gist.github.com/eligrey/f109a6d0bf4efe3461201c3d7b745e8f
Node.prototype.isConnected polyfill
.UE \c
