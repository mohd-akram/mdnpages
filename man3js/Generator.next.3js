.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Generator.next" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Generator.next - Generator.prototype.next()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]next()\f[B]\f[R] method returns an object with two
properties \f[V]done\f[R] and \f[V]value\f[R].
You can also provide a parameter to the \f[V]next\f[R] method to send a
value to the generator.
.SH SYNTAX
.IP
.nf
\f[C]
generatorObject.next(value)
\f[R]
.fi
.SS Parameters
.TP
\f[B]value\f[R]
The value to send to the generator.
.RS
.PP
The value will be assigned as a result of a \f[V]yield\f[R] expression.
For example, in \f[V]variable = yield expression\f[R], the value passed
to the \f[V].next()\f[R] function will be assigned to
\f[V]variable\f[R].
.RE
.SS Return value
.PP
An \f[V]Object\f[R] with two properties:
.TP
\f[B]done\f[R]
A boolean value:
.RS
.IP \[bu] 2
\f[V]true\f[R] if the generator is past the end of its control flow.
In this case \f[V]value\f[R] specifies the \f[I]return value\f[R] of the
generator (which may be undefined).
.IP \[bu] 2
\f[V]false\f[R] if the generator is able to produce more values.
.RE
.TP
\f[B]value\f[R]
Any JavaScript value yielded or returned by the generator.
.SH EXAMPLES
.SS Using next()
.PP
The following example shows a simple generator and the object that the
\f[V]next\f[R] method returns:
.IP
.nf
\f[C]
function* gen() {
  yield 1;
  yield 2;
  yield 3;
}

const g = gen(); // Generator { }
g.next(); // { value: 1, done: false }
g.next(); // { value: 2, done: false }
g.next(); // { value: 3, done: false }
g.next(); // { value: undefined, done: true }
\f[R]
.fi
.SS Using next() with a list
.PP
In this example, \f[V]getPage\f[R] takes a list and \[lq]paginates\[rq]
it into chunks of size \f[V]pageSize\f[R].
Each call to \f[V]next\f[R] will yield one such chunk.
.IP
.nf
\f[C]
function* getPage(list, pageSize = 1) {
  for (let index = 0; index < list.length; index += pageSize) {
    yield list.slice(index, index + pageSize);
  }
}

const list = [1, 2, 3, 4, 5, 6, 7, 8];
const page = getPage(list, 3); // Generator { }

page.next(); // { value: [1, 2, 3], done: false }
page.next(); // { value: [4, 5, 6], done: false }
page.next(); // { value: [7, 8], done: false }
page.next(); // { value: undefined, done: true }
\f[R]
.fi
.SS Sending values to the generator
.PP
In this example, \f[V]next\f[R] is called with a value.
.RS
.PP
\f[B]Note:\f[R] The first call does not log anything, because the
generator was not yielding anything initially.
.RE
.IP
.nf
\f[C]
function* gen() {
  while (true) {
    const value = yield;
    console.log(value);
  }
}

const g = gen();
g.next(1); // Returns { value: undefined, done: false }
// No log at this step: the first value sent through \[ga]next\[ga] is lost
g.next(2); // Returns { value: undefined, done: false }
// Logs 2
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]function*\f[R]
.IP \[bu] 2
Iterators and generators
