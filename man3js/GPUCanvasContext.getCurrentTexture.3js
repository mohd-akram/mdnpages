.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "GPUCANVASCONTEXT.GETCURRENTTEXTURE" "3JS" "June 18, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
GPUCanvasContext.getCurrentTexture \- GPUCanvasContext:
getCurrentTexture() method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]getCurrentTexture()\f[B]\f[R] method of the
\f[CR]GPUCanvasContext\f[R] interface returns the next
\f[CR]GPUTexture\f[R] to be composited to the document by the canvas
context.
.SH SYNTAX
.IP
.EX
getCurrentTexture()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]GPUTexture\f[R] object instance.
.SS Exceptions
.TP
\f[B]InvalidStateError\f[R] \f[B]DOMException\f[R]
Thrown if \f[CR]getCurrentTexture()\f[R] is called on the canvas context
before it is configured (i.e., before
\f[CR]GPUCanvasContext.configure()\f[R] has been called).
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] canvas = document.querySelector(\(dq#gpuCanvas\(dq);
\f[B]const\f[R] context = canvas.getContext(\(dqwebgpu\(dq);

context.configure({
  device,
  format: navigator.gpu.getPreferredCanvasFormat(),
  alphaMode: \(dqpremultiplied\(dq,
});

\f[I]// \&...\f[R]
\f[I]// Later on\f[R]
\f[B]const\f[R] commandEncoder = device.createCommandEncoder();

\f[B]const\f[R] renderPassDescriptor = {
  colorAttachments: [
    {
      clearValue: [0, 0, 0, 1], \f[I]// Opaque black\f[R]
      loadOp: \(dqclear\(dq,
      storeOp: \(dqstore\(dq,
      view: context.getCurrentTexture().createView(),
    },
  ],
};

\f[B]const\f[R] passEncoder = commandEncoder.beginRenderPass(renderPassDescriptor);
.EE
.SH SEE ALSO
.IP \(bu 2
The WebGPU API
