.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "SpeechRecognitionResultList" "JS" "March 3, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SpeechRecognitionResultList \- SpeechRecognitionResultList
.SH SYNOPSIS
The \f[B]\f[CB]SpeechRecognitionResultList\f[B]\f[R] interface of the
Web Speech API represents a list of \f[CR]SpeechRecognitionResult\f[R]
objects, or a single one if results are being captured in
\f[CR]non\-continuous\f[R] mode.
.SH INSTANCE PROPERTIES
.TP
\f[B]SpeechRecognitionResultList.length\f[R] \f[I](read\-only)\f[R]
Returns the length of the \[lq]array\[rq] \[em] the number of
\f[CR]SpeechRecognitionResult\f[R] objects in the list.
.SH INSTANCE METHODS
.TP
\f[B]SpeechRecognitionResultList.item\f[R]
A standard getter that allows \f[CR]SpeechRecognitionResult\f[R] objects
in the list to be accessed via array syntax.
.SH EXAMPLES
This code is excerpted from our \c
.UR https://github.com/mdn/dom-examples/blob/main/web-speech-api/speech-color-changer/script.js
Speech color changer
.UE \c
\ example.
.IP
.EX
recognition.onresult = (event) \f[B]=>\f[R] {
  \f[I]// The SpeechRecognitionEvent results property returns a SpeechRecognitionResultList object\f[R]
  \f[I]// The SpeechRecognitionResultList object contains SpeechRecognitionResult objects.\f[R]
  \f[I]// It has a getter so it can be accessed like an array\f[R]
  \f[I]// The first [0] returns the SpeechRecognitionResult at position 0.\f[R]
  \f[I]// Each SpeechRecognitionResult object contains SpeechRecognitionAlternative objects\f[R]
  \f[I]// that contain individual results.\f[R]
  \f[I]// These also have getters so they can be accessed like arrays.\f[R]
  \f[I]// The second [0] returns the SpeechRecognitionAlternative at position 0.\f[R]
  \f[I]// We then return the transcript property of the SpeechRecognitionAlternative object\f[R]
  \f[B]const\f[R] color = event.results[0][0].transcript;
  diagnostic.textContent = \[ga]Result received: ${color}.\[ga];
  bg.style.backgroundColor = color;
};
.EE
.SH SEE ALSO
.IP \[bu] 2
Web Speech API
