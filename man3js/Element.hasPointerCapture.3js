.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "Element.hasPointerCapture" "JS" "November 28, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Element.hasPointerCapture \- Element: hasPointerCapture() method
.SH SYNOPSIS
The \f[B]\f[CB]hasPointerCapture()\f[B]\f[R] method of the
\f[CR]Element\f[R] interface checks whether the element on which it is
invoked has pointer capture for the pointer identified by the given
pointer ID.
.SH SYNTAX
.IP
.EX
hasPointerCapture(pointerId)
.EE
.SS Parameters
.TP
\f[B]pointerId\f[R]
The \f[CR]pointerId\f[R] of a \f[CR]PointerEvent\f[R] object.
.SS Return value
A boolean value \[em] \f[CR]true\f[R] if the element does have pointer
capture for the pointer identified by the given pointer ID,
\f[CR]false\f[R] if it doesn\[cq]t.
.SH EXAMPLES
.IP
.EX
<html lang=\[dq]en\[dq]>
  <script>
    function downHandler(ev) {
      const el = document.getElementById(\[dq]target\[dq]);
      // Element \[aq]target\[aq] will receive/capture further events
      el.setPointerCapture(ev.pointerId);

      // \&...

      // Check whether element still has pointer capture
      let pointerCap = el.hasPointerCapture(ev.pointerId);
      if (pointerCap) {
        // We\[aq]ve still got pointer capture
      } else {
        // oops, we\[aq]ve lost pointer capture!
      }
    }

    function init() {
      const el = document.getElementById(\[dq]target\[dq]);
      el.onpointerdown = downHandler;
    }
  </script>
  <body onload=\[dq]init();\[dq]>
    <div id=\[dq]target\[dq]>Touch this element with a pointer.</div>
  </body>
</html>
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Element.setPointerCapture()\f[R]
.IP \[bu] 2
\f[CR]Element.releasePointerCapture()\f[R]
.IP \[bu] 2
\f[CR]Pointer Events\f[R]
