.\" Automatically generated by Pandoc 3.10.1
.\"
.TH "DATATRANSFER.GETDATA" "3JS" "August 31, 2026" "JavaScript" "JavaScript Reference Manual"
.SH NAME
DataTransfer.getData \- DataTransfer: getData() method
.SH SYNOPSIS
The \f[B]\f[CB]getData()\f[B]\f[R] method of the \f[CR]DataTransfer\f[R]
interface retrieves drag data (as a string) for the specified type.
If the drag operation does not include data, this method returns an
empty string.
.PP
Example data types are \f[CR]text/plain\f[R] and
\f[CR]text/uri\-list\f[R].
.PP
During a drag operation, this method can only read data in the handlers
for the \f[CR]dragstart\f[R] and \f[CR]drop\f[R] events, because those
are the only times the drag data store is readable.
Calling it from any other drag event returns an empty string.
See Reading the drag data store for details.
.SH SYNTAX
.IP
.EX
getData(format)
.EE
.SS Parameters
.TP
\f[B]format\f[R]
A string representing the type of data to retrieve.
.SS Return value
A string representing the drag data for the specified \f[CR]format\f[R].
If the drag operation has no data or the operation has no data for the
specified \f[CR]format\f[R], this method returns an empty string.
.PP
The items and their formats can still be enumerated during other drag
events using \f[CR]DataTransfer.items\f[R] and
\f[CR]DataTransfer.types\f[R].
.SH EXAMPLES
This example shows the use of the \f[CR]DataTransfer\f[R] object\(cqs
\f[CR]getData()\f[R] and \f[CR]setData()\f[R] methods.
.SS HTML
.IP
.EX
<\f[B]div\f[R] id=\(dqdiv1\(dq>
  <\f[B]span\f[R] id=\(dqdrag\(dq draggable=\(dqtrue\(dq>drag me to the other box</\f[B]span\f[R]>
</\f[B]div\f[R]>
<\f[B]div\f[R] id=\(dqdiv2\(dq></\f[B]div\f[R]>
.EE
.SS CSS
.IP
.EX
div1,
div2 {
  \f[B]width\f[R]: 100px;
  \f[B]height\f[R]: 50px;
  \f[B]padding\f[R]: 10px;
  \f[B]border\f[R]: 1px solid #aaaaaa;
}
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] div1 = document.getElementById(\(dqdiv1\(dq);
\f[B]const\f[R] div2 = document.getElementById(\(dqdiv2\(dq);
\f[B]const\f[R] dragElement = document.getElementById(\(dqdrag\(dq);

dragElement.addEventListener(\(dqdragstart\(dq, drag);
div1.addEventListener(\(dqdragover\(dq, allowDrop);
div2.addEventListener(\(dqdragover\(dq, allowDrop);
div1.addEventListener(\(dqdrop\(dq, drop);
div2.addEventListener(\(dqdrop\(dq, drop);

\f[B]function\f[R] allowDrop(allowDropEvent) {
  allowDropEvent.target.style.color = \(dqblue\(dq;
  allowDropEvent.preventDefault();
}

\f[B]function\f[R] drag(dragEvent) {
  dragEvent.dataTransfer.setData(\(dqtext\(dq, dragEvent.target.id);
  dragEvent.target.style.color = \(dqgreen\(dq;
}

\f[B]function\f[R] drop(dropEvent) {
  dropEvent.preventDefault();
  \f[B]const\f[R] data = dropEvent.dataTransfer.getData(\(dqtext\(dq);
  dropEvent.target.appendChild(document.getElementById(data));
  dragElement.style.color = \(dqblack\(dq;
}
.EE
.SS Result
.SH SEE ALSO
.IP \(bu 2
Drag and drop
.IP \(bu 2
Drag Operations
.IP \(bu 2
Working with the drag data store
