.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "escape" "JS" "August 29, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
escape - escape()
.SH SYNOPSIS
\f[I](deprecated)\f[R]
.RS
.PP
\f[B]Note:\f[R] \f[CR]escape()\f[R] is a non-standard function
implemented by browsers and was only standardized for cross-engine
compatibility.
It is not required to be implemented by all JavaScript engines and may
not work everywhere.
Use \f[CR]encodeURIComponent()\f[R] or \f[CR]encodeURI()\f[R] if
possible.
.RE
.PP
The \f[B]\f[CB]escape()\f[B]\f[R] function computes a new string in
which certain characters have been replaced by hexadecimal escape
sequences.
.SH SYNTAX
.IP
.EX
escape(str)
.EE
.SS Parameters
.TP
\f[B]str\f[R]
A string to be encoded.
.SS Return value
A new string in which certain characters have been escaped.
.SH DESCRIPTION
\f[CR]escape()\f[R] is a function property of the global object.
.PP
The \f[CR]escape()\f[R] function replaces all characters with escape
sequences, with the exception of ASCII word characters (A\[en]Z,
a\[en]z, 0\[en]9, _) and \f[CR]\[at]\[rs]*_+-./\f[R].
Characters are escaped by UTF-16 code units.
If the code unit\[cq]s value is less than 256, it is represented by a
two-digit hexadecimal number in the format \f[CR]%XX\f[R], left-padded
with 0 if necessary.
Otherwise, it is represented by a four-digit hexadecimal number in the
format \f[CR]%uXXXX\f[R], left-padded with 0 if necessary.
.RS
.PP
\f[B]Note:\f[R] This function was used mostly for URL
encoding (https://en.wikipedia.org/wiki/URL_encoding) and is partly
based on the escape format in RFC 1738.
The escape format is \f[I]not\f[R] an escape sequence in string
literals.
You can replace \f[CR]%XX\f[R] with \f[CR]\[rs]xXX\f[R] and
\f[CR]%uXXXX\f[R] with \f[CR]\[rs]uXXXX\f[R] to get a string containing
actual string-literal escape sequences.
.RE
.SH EXAMPLES
.SS Using escape()
.IP
.EX
escape(\[dq]abc123\[dq]); // \[dq]abc123\[dq]
escape(\[dq]äöü\[dq]); // \[dq]%E4%F6%FC\[dq]
escape(\[dq]ć\[dq]); // \[dq]%u0107\[dq]

// special characters
escape(\[dq]\[at]*_+-./\[dq]); // \[dq]\[at]*_+-./\[dq]
.EE
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[CR]escape\f[R] in
\f[CR]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-string-and-regexp)
.IP \[bu] 2
\f[CR]encodeURI\f[R]
.IP \[bu] 2
\f[CR]encodeURIComponent\f[R]
.IP \[bu] 2
\f[CR]unescape\f[R]
