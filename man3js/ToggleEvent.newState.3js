.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "ToggleEvent.newState" "JS" "June 16, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
ToggleEvent.newState \- ToggleEvent: newState property
.SH SYNOPSIS
The \f[B]\f[CB]newState\f[B]\f[R] read\-only property of the
\f[CR]ToggleEvent\f[R] interface is a string representing the state the
element is transitioning to.
.SH VALUE
A string.
Possible values are \f[CR]\[dq]open\[dq]\f[R] (the popover is being
shown) or \f[CR]\[dq]closed\[dq]\f[R] (the popover is being hidden).
.SH EXAMPLES
.IP
.EX
const popover = document.getElementById(\[dq]mypopover\[dq]);

// ...

popover.addEventListener(\[dq]beforetoggle\[dq], (event) => {
  if (event.newState === \[dq]open\[dq]) {
    console.log(\[dq]Popover is being shown\[dq]);
  } else {
    console.log(\[dq]Popover is being hidden\[dq]);
  }
});
.EE
.SH SEE ALSO
.IP \[bu] 2
Popover API
