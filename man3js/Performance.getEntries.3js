.\" Automatically generated by Pandoc 3.2.1
.\"
.TH "Performance.getEntries" "JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Performance.getEntries \- Performance: getEntries() method
.SH SYNOPSIS
The \f[B]\f[CB]getEntries()\f[B]\f[R] method returns an array of all
\f[CR]PerformanceEntry\f[R] objects currently present in the performance
timeline.
.PP
If you are only interested in performance entries of certain types or
that have certain names, see \f[CR]getEntriesByType()\f[R] and
\f[CR]getEntriesByName()\f[R].
.RS
.PP
\f[B]Note:\f[R] This method does not notify you about new performance
entries; you will only get entries that are present in the performance
timeline at the time you call this method.
To receive notifications about entries as they become available, use a
\f[CR]PerformanceObserver\f[R].
.RE
.PP
The following entry types are not supported by this method at all and
won\[cq]t be returned even if entries for these types might exist:
.IP \[bu] 2
\f[CR]\[dq]element\[dq]\f[R] (\f[CR]PerformanceElementTiming\f[R])
.IP \[bu] 2
\f[CR]\[dq]event\[dq]\f[R] (\f[CR]PerformanceEventTiming\f[R])
.IP \[bu] 2
\f[CR]\[dq]largest\-contentful\-paint\[dq]\f[R]
(\f[CR]LargestContentfulPaint\f[R])
.IP \[bu] 2
\f[CR]\[dq]layout\-shift\[dq]\f[R] (\f[CR]LayoutShift\f[R])
.IP \[bu] 2
\f[CR]\[dq]longtask\[dq]\f[R] (\f[CR]PerformanceLongTaskTiming\f[R])
.PP
To access entries of these types, you must use a
\f[CR]PerformanceObserver\f[R] instead.
.SH SYNTAX
.IP
.EX
getEntries()
.EE
.SS Parameters
None.
.SS Return value
An \f[CR]Array\f[R] of \f[CR]PerformanceEntry\f[R] objects.
The items will be in chronological order based on the entries\[cq]
\f[CR]startTime\f[R].
.SH EXAMPLES
.SS Logging all performance markers and measures
Assuming you created your own \f[CR]PerformanceMark\f[R] and
\f[CR]PerformanceMeasure\f[R] objects at appropriate places in your
code, you might want to log all them to the console like this:
.IP
.EX
\f[I]// Example markers/measures\f[R]
performance.mark(\[dq]login\-started\[dq]);
performance.mark(\[dq]login\-finished\[dq]);
performance.mark(\[dq]form\-sent\[dq]);
performance.mark(\[dq]video\-loaded\[dq]);
performance.measure(\[dq]login\-duration\[dq], \[dq]login\-started\[dq], \[dq]login\-finished\[dq]);

\f[B]const\f[R] entries = performance.getEntries();

entries.forEach((entry) \f[B]=>\f[R] {
  \f[B]if\f[R] (entry.entryType === \[dq]mark\[dq]) {
    console.log(\[ga]${entry.name}\[aq]s startTime: ${entry.startTime}\[ga]);
  }
  \f[B]if\f[R] (entry.entryType === \[dq]measure\[dq]) {
    console.log(\[ga]${entry.name}\[aq]s duration: ${entry.duration}\[ga]);
  }
});
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Performance.getEntriesByType()\f[R]
.IP \[bu] 2
\f[CR]Performance.getEntriesByName()\f[R]
