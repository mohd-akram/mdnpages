.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "RTCDTMFSender.tonechange_event" "JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
RTCDTMFSender.tonechange_event \- RTCDTMFSender: tonechange event
.SH SYNOPSIS
The \f[B]\f[CB]tonechange\f[B]\f[R] event is sent to an
\f[CR]RTCDTMFSender\f[R] by the WebRTC API to indicate when DTMF tones
previously queued for sending (by calling
\f[CR]RTCDTMFSender.insertDTMF()\f[R]) begin and end.
.PP
To determine what tone started playing, or if a tone stopped playing,
check the value of the event\[cq]s \f[CR]tone\f[R] property.
.PP
This event is not cancelable and does not bubble.
.SH SYNTAX
Use the event name in methods like \f[CR]addEventListener()\f[R], or set
an event handler property.
.IP
.EX
addEventListener(\[dq]tonechange\[dq], (event) => {});

ontonechange = (event) => {};
.EE
.SH EVENT TYPE
An \f[CR]RTCDTMFToneChangeEvent\f[R].
Inherits from \f[CR]Event\f[R].
.SH EVENT PROPERTIES
\f[I]In addition to the properties of \f[CI]Event\f[I], this interface
offers the following:\f[R]
.TP
\f[B]RTCDTMFToneChangeEvent.tone\f[R] \f[I](read\-only)\f[R]
A string specifying the tone which has begun playing, or an empty string
(\f[CR]\[dq]\[dq]\f[R]) if the previous tone has finished playing.
.SH EXAMPLES
This example establishes a handler for the \f[CR]tonechange\f[R] event
which updates an element to display the currently playing tone in its
content, or, if all tones have played, the string \[lq]<none>\[rq].
.PP
This can be done using \f[CR]addEventListener()\f[R]:
.IP
.EX
dtmfSender.addEventListener(
  \[dq]tonechange\[dq],
  (ev) => {
    let tone = ev.tone;
    if (tone === \[dq]\[dq]) {
      tone = \[dq]&lt;none&gt;\[dq];
    }

    document.getElementById(\[dq]playingTone\[dq]).innerText = tone;
  },
  false,
);
.EE
.PP
You can also just set the \f[CR]ontonechange\f[R] event handler property
directly:
.IP
.EX
dtmfSender.ontonechange = (ev) => {
  let tone = ev.tone;
  if (tone === \[dq]\[dq]) {
    tone = \[dq]&lt;none&gt;\[dq];
  }

  document.getElementById(\[dq]playingTone\[dq]).innerText = tone;
};
.EE
