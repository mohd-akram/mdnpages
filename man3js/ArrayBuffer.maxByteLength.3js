.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "ArrayBuffer.maxByteLength" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
ArrayBuffer.maxByteLength - ArrayBuffer.prototype.maxByteLength
.SH SYNOPSIS
.PP
The \f[B]\f[VB]maxByteLength\f[B]\f[R] accessor property of
\f[V]ArrayBuffer\f[R] instances returns the maximum length (in bytes)
that this array buffer can be resized to.
.SH DESCRIPTION
.PP
The \f[V]maxByteLength\f[R] property is an accessor property whose set
accessor function is \f[V]undefined\f[R], meaning that you can only read
this property.
The value is established when the array is constructed, set via the
\f[V]maxByteLength\f[R] option of the \f[V]ArrayBuffer()\f[R]
constructor, and cannot be changed.
.PP
This property returns 0 if this \f[V]ArrayBuffer\f[R] has been detached.
If this \f[V]ArrayBuffer\f[R] was constructed without specifying a
\f[V]maxByteLength\f[R] value, this property returns a value equal to
the value of the \f[V]ArrayBuffer\f[R]\[cq]s \f[V]byteLength\f[R].
.SH EXAMPLES
.SS Using maxByteLength
.PP
In this example, we create an 8-byte buffer that is resizable to a max
length of 16 bytes, then return its \f[V]maxByteLength\f[R]:
.IP
.nf
\f[C]
const buffer = new ArrayBuffer(8, { maxByteLength: 16 });

buffer.maxByteLength; // 16
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
\f[V]ArrayBuffer\f[R]
.IP \[bu] 2
\f[V]ArrayBuffer.prototype.byteLength\f[R]
.IP \[bu] 2
\f[V]ArrayBuffer.prototype.resize()\f[R]
