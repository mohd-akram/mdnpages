.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "ArrayBuffer.resizable" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]resizable\f[B]\f[R] accessor property of
\f[V]ArrayBuffer\f[R] instances returns whether this array buffer can be
resized or not.
.SH Description
.PP
The \f[V]resizable\f[R] property is an accessor property whose set
accessor function is \f[V]undefined\f[R], meaning that you can only read
this property.
The value is established when the array is constructed.
If the \f[V]maxByteLength\f[R] option was set in the constructor,
\f[V]resizable\f[R] will return \f[V]true\f[R]; if not, it will return
\f[V]false\f[R].
.SH Examples
.SS Using resizable
.PP
In this example, we create a 8-byte buffer that is resizable to a max
length of 16 bytes, then check its \f[V]resizable\f[R] property,
resizing it if \f[V]resizable\f[R] returns \f[V]true\f[R]:
.IP
.nf
\f[C]
const buffer = new ArrayBuffer(8, { maxByteLength: 16 });

if (buffer.resizable) {
  console.log(\[dq]Buffer is resizable!\[dq]);
  buffer.resize(12);
}
\f[R]
.fi
.SH See also
.IP \[bu] 2
\f[V]ArrayBuffer\f[R]
.IP \[bu] 2
\f[V]ArrayBuffer.prototype.maxByteLength\f[R]
.IP \[bu] 2
\f[V]ArrayBuffer.prototype.resize()\f[R]
