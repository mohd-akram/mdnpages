.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "SVGANIMATIONELEMENT.GETCURRENTTIME" "3JS" "January 23, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SVGAnimationElement.getCurrentTime \- SVGAnimationElement:
getCurrentTime() method
.SH SYNOPSIS
The \f[CR]SVGAnimationElement\f[R] method \f[CR]getCurrentTime()\f[R]
returns a float representing the current time in seconds relative to
time zero for the given time container.
.PP
Time zero refers to the moment when the time container begins its
timeline.
It acts as the starting reference point for all animations within that
container.
.PP
A time container is an element or context that defines a local timeline
for one or more animations.
Animations inside the time container are measured relative to its
timeline.
If a time container is delayed, paused, or manipulated, all animations
within it adjust accordingly.
.SH SYNTAX
.IP
.EX
getCurrentTime()
.EE
.SS Parameters
None (\f[CR]undefined\f[R]).
.SS Return value
A float.
.SH EXAMPLES
This example demonstrates how the \f[CR]getCurrentTime()\f[R] method
retrieves the time elapsed since the time container\(cqs time zero.
.IP
.EX
<\f[B]svg\f[R] width=\(dq200\(dq height=\(dq200\(dq viewBox=\(dq0 0 200 200\(dq>
  <\f[B]circle\f[R] cx=\(dq50\(dq cy=\(dq50\(dq r=\(dq20\(dq fill=\(dqrebeccapurple\(dq>
    <\f[B]animate\f[R]
      attributeName=\(dqcx\(dq
      from=\(dq50\(dq
      to=\(dq150\(dq
      dur=\(dq4s\(dq
      repeatCount=\(dqindefinite\(dq />
  </\f[B]circle\f[R]>
</\f[B]svg\f[R]>
.EE
.IP
.EX
\f[B]const\f[R] animationElement = document.querySelector(\(dqanimate\(dq);

setInterval(() \f[B]=>\f[R] {
  \f[B]const\f[R] currentTime = animationElement.getCurrentTime();
  console.log(
    \(gaCurrent time relative to the time container: ${currentTime} seconds\(ga,
  );
}, 1000);
.EE
.PP
The animation starts at \f[CR]time zero = 0\f[R] and runs indefinitely,
and the \f[CR]getCurrentTime()\f[R] value increments continuously within
the context of the time container.
