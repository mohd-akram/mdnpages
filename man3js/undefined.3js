'\" t
.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "undefined" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
undefined - undefined
.SH SYNOPSIS
.PP
The \f[B]\f[VB]undefined\f[B]\f[R] global property represents the
primitive value \f[V]undefined\f[R].
It is one of JavaScript\[cq]s primitive types.
.SH VALUE
.PP
The primitive value \f[V]undefined\f[R].
.PP
.TS
tab(@);
l l.
T{
Writable
T}@T{
no
T}
T{
Enumerable
T}@T{
no
T}
T{
Configurable
T}@T{
no
T}
.TE
.SH DESCRIPTION
.PP
\f[V]undefined\f[R] is a property of the \f[I]global object\f[R].
That is, it is a variable in global scope.
.PP
In all non-legacy browsers, \f[V]undefined\f[R] is a non-configurable,
non-writable property.
Even when this is not the case, avoid overriding it.
.PP
A variable that has not been assigned a value is of type
\f[V]undefined\f[R].
A method or statement also returns \f[V]undefined\f[R] if the variable
that is being evaluated does not have an assigned value.
A function returns \f[V]undefined\f[R] if a value was not
\f[V]returned\f[R].
.RS
.PP
\f[B]Note:\f[R] While you can use \f[V]undefined\f[R] as an identifier
(variable name) in any scope other than the global scope (because
\f[V]undefined\f[R] is not a reserved word), doing so is a very bad idea
that will make your code difficult to maintain and debug.
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
// DON\[aq]T DO THIS

(() => {
  const undefined = \[dq]foo\[dq];
  console.log(undefined, typeof undefined); // foo string
})();

((undefined) => {
  console.log(undefined, typeof undefined); // foo string
})(\[dq]foo\[dq]);
\f[R]
.fi
.RE
.SH EXAMPLES
.SS Strict equality and undefined
.PP
You can use \f[V]undefined\f[R] and the strict equality and inequality
operators to determine whether a variable has a value.
In the following code, the variable \f[V]x\f[R] is not initialized, and
the \f[V]if\f[R] statement evaluates to true.
.IP
.nf
\f[C]
let x;
if (x === undefined) {
  // these statements execute
} else {
  // these statements do not execute
}
\f[R]
.fi
.RS
.PP
\f[B]Note:\f[R] The \f[I]strict equality\f[R] operator (as opposed to
the \f[I]standard equality\f[R] operator) must be used here, because
\f[V]x == undefined\f[R] also checks whether \f[V]x\f[R] is
\f[V]null\f[R], while strict equality doesn\[cq]t.
This is because \f[V]null\f[R] is not equivalent to \f[V]undefined\f[R].
.PP
See Equality comparison and sameness for details.
.RE
.SS typeof operator and undefined
.PP
Alternatively, \f[V]typeof\f[R] can be used:
.IP
.nf
\f[C]
let x;
if (typeof x === \[dq]undefined\[dq]) {
  // these statements execute
}
\f[R]
.fi
.PP
One reason to use \f[V]typeof\f[R] is that it does not throw an error if
the variable has not been declared.
.IP
.nf
\f[C]
// x has not been declared before
// evaluates to true without errors
if (typeof x === \[dq]undefined\[dq]) {
  // these statements execute
}

// Throws a ReferenceError
if (x === undefined) {
}
\f[R]
.fi
.PP
However, there is another alternative.
JavaScript is a statically scoped language, so knowing if a variable is
declared can be read by seeing whether it is declared in an enclosing
context.
.PP
The global scope is bound to the \f[V]global object\f[R], so checking
the existence of a variable in the global context can be done by
checking the existence of a property on the \f[I]global object\f[R],
using the \f[V]in\f[R] operator, for instance:
.IP
.nf
\f[C]
if (\[dq]x\[dq] in window) {
  // These statements execute only if x is defined globally
}
\f[R]
.fi
.SS void operator and undefined
.PP
The \f[V]void\f[R] operator is a third alternative.
.IP
.nf
\f[C]
let x;
if (x === void 0) {
  // these statements execute
}

// y has not been declared before
if (y === void 0) {
  // throws Uncaught ReferenceError: y is not defined
}
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
JavaScript\[cq]s primitive types
.IP \[bu] 2
\f[V]null\f[R]
