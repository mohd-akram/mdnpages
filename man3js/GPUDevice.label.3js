.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "GPUDEVICE.LABEL" "3JS" "June 18, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
GPUDevice.label \- GPUDevice: label property
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]label\f[B]\f[R] read\-only property of the
\f[CR]GPUDevice\f[R] interface is a string providing a label that can be
used to identify the object, for example in \f[CR]GPUError\f[R] messages
or console warnings.
.SH VALUE
A string.
If no label value has previously been set, getting the label returns an
empty string.
.SH EXAMPLES
.IP
.EX
\f[B]async\f[R] \f[B]function\f[R] init() {
  \f[B]if\f[R] (!navigator.gpu) {
    \f[B]throw\f[R] Error(\(dqWebGPU not supported.\(dq);
  }

  \f[B]const\f[R] adapter = \f[B]await\f[R] navigator.gpu.requestAdapter();
  \f[B]if\f[R] (!adapter) {
    \f[B]throw\f[R] Error(\(dqCouldn\(aqt request WebGPU adapter.\(dq);
  }

  \f[I]// Create a GPUDevice\f[R]
  \f[B]const\f[R] device = \f[B]await\f[R] adapter.requestDevice();

  \f[I]// Set a label\f[R]
  device.label = \(dqmy_label\(dq;

  \f[I]// Get a label\f[R]
  console.log(device.label);

  \f[I]// \&...\f[R]
}
.EE
.SH SEE ALSO
.IP \(bu 2
The WebGPU API
