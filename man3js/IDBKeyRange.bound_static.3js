.\" Automatically generated by Pandoc 3.1.12.3
.\"
.TH "IDBKeyRange.bound_static" "JS" "March 6, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
IDBKeyRange.bound_static \- IDBKeyRange: bound() static method
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]bound()\f[B]\f[R] static method of the
\f[CR]IDBKeyRange\f[R] interface creates a new key range with the
specified upper and lower bounds.
The bounds can be open (that is, the bounds exclude the endpoint values)
or closed (that is, the bounds include the endpoint values).
By default, the bounds are closed.
.SH SYNTAX
.IP
.EX
IDBKeyRange.bound(lower, upper)
IDBKeyRange.bound(lower, upper, lowerOpen)
IDBKeyRange.bound(lower, upper, lowerOpen, upperOpen)
.EE
.SS Parameters
.TP
\f[B]lower\f[R]
specifies the lower bound of the new key range.
.TP
\f[B]upper\f[R]
specifies the upper bound of the new key range.
.TP
\f[B]lowerOpen\f[R] \f[I](optional)\f[R]
indicates whether the lower bound excludes the endpoint value.
The default is false.
.TP
\f[B]upperOpen\f[R] \f[I](optional)\f[R]
Indicates whether the upper bound excludes the endpoint value.
The default is false.
.SS Return value
\f[CR]IDBKeyRange\f[R]: The newly created key range.
.SS Exceptions
.TP
\f[B]DataError\f[R] \f[B]DOMException\f[R]
Thrown when one of the following conditions is met:
.RS
.IP \[bu] 2
The lower or upper parameters were not passed a valid key.
.IP \[bu] 2
The lower key is greater than the upper key.
.IP \[bu] 2
The lower key and upper key match and either of the bounds are open.
.RE
.SH EXAMPLES
The following example illustrates how you\[cq]d use a bound key range.
Here we declare a
\f[CR]keyRangeValue = IDBKeyRange.bound(\[dq]A\[dq], \[dq]F\[dq]);\f[R]
\[em] a range between values of \[lq]A\[rq] and \[lq]F\[rq].
We open a transaction (using \f[CR]IDBTransaction\f[R]) and an object
store, and open a Cursor with \f[CR]IDBObjectStore.openCursor\f[R],
declaring \f[CR]keyRangeValue\f[R] as its optional key range value.
This means that the cursor will only retrieve records with keys inside
that range.
This range includes the values \[lq]A\[rq] and \[lq]F\[rq], as we
haven\[cq]t declared that they should be open bounds.
If we used
\f[CR]IDBKeyRange.bound(\[dq]A\[dq], \[dq]F\[dq], true, true);\f[R],
then the range would not include \f[CR]\[dq]A\[dq]\f[R] and
\f[CR]\[dq]F\[dq]\f[R], only the values between them.
.RS
.PP
\f[B]Note:\f[R] For a more complete example allowing you to experiment
with key range, have a look at the idbkeyrange directory in the \c
.UR https://github.com/mdn/dom-examples/tree/main/indexeddb-examples/idbkeyrange
indexeddb\-examples
.UE \c
\ repo.
(View the example \c
.UR https://mdn.github.io/dom-examples/indexeddb-examples/idbkeyrange/
live
.UE \c
\ too.
.RE
.IP
.EX
\f[B]function\f[R] displayData() {
  \f[B]const\f[R] keyRangeValue = IDBKeyRange.bound(\[dq]A\[dq], \[dq]F\[dq]);

  \f[B]const\f[R] transaction = db.transaction([\[dq]fThings\[dq]], \[dq]readonly\[dq]);
  \f[B]const\f[R] objectStore = transaction.objectStore(\[dq]fThings\[dq]);

  objectStore.openCursor(keyRangeValue).onsuccess = (event) \f[B]=>\f[R] {
    \f[B]const\f[R] cursor = event.target.result;
    \f[B]if\f[R] (cursor) {
      \f[B]const\f[R] listItem = document.createElement(\[dq]li\[dq]);
      listItem.textContent = \[ga]${cursor.value.fThing}, ${cursor.value.fRating}\[ga];
      list.appendChild(listItem);

      cursor.continue();
    } \f[B]else\f[R] {
      console.log(\[dq]Entries all displayed.\[dq]);
    }
  };
}
.EE
.SH SEE ALSO
.IP \[bu] 2
Using IndexedDB
.IP \[bu] 2
Starting transactions: \f[CR]IDBDatabase\f[R]
.IP \[bu] 2
Using transactions: \f[CR]IDBTransaction\f[R]
.IP \[bu] 2
Setting a range of keys: \f[CR]IDBKeyRange\f[R]
.IP \[bu] 2
Retrieving and making changes to your data: \f[CR]IDBObjectStore\f[R]
.IP \[bu] 2
Using cursors: \f[CR]IDBCursor\f[R]
.IP \[bu] 2
Reference example: \c
.UR https://github.com/mdn/dom-examples/tree/main/to-do-notifications
To\-do Notifications
.UE \c
\ (\c
.UR https://mdn.github.io/dom-examples/to-do-notifications/
View the example live
.UE \c
).
