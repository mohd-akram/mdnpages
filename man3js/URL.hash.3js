.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "URL.HASH" "3JS" "February 13, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
URL.hash \- URL: hash property
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]hash\f[B]\f[R] property of the \f[CR]URL\f[R] interface
is a string containing a \f[CR]\(dq#\(dq\f[R] followed by the fragment
identifier of the URL.
If the URL does not have a fragment identifier, this property contains
an empty string, \f[CR]\(dq\(dq\f[R].
.PP
This property can be set to change the fragment identifier of the URL.
When setting, a single \f[CR]\(dq#\(dq\f[R] prefix is added to the
provided value, if not already present.
Setting it to \f[CR]\(dq\(dq\f[R] removes the fragment identifier.
.PP
The fragment is percent\-encoded when setting but not percent\-decoded
when reading.
.SH VALUE
A string.
.SH EXAMPLES
.IP
.EX
\f[B]const\f[R] url = \f[B]new\f[R] URL(
  \(dqhttps://developer.mozilla.org/en\-US/docs/Web/API/URL/href#examples\(dq,
);
console.log(url.hash); \f[I]// \(aq#examples\(aq\f[R]
.EE
.SH SEE ALSO
.IP \(bu 2
The \f[CR]URL\f[R] interface it belongs to.
