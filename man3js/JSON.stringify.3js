.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "JSON.stringify" "JS" "2023-06-02" "JavaScript" "JavaScript Reference Manual"
.hy
.SH Synopsis
.PP
The \f[B]\f[VB]JSON.stringify()\f[B]\f[R] static method converts a
JavaScript value to a JSON string, optionally replacing values if a
replacer function is specified or optionally including only the
specified properties if a replacer array is specified.
.SH Syntax
.IP
.nf
\f[C]
JSON.stringify(value)
JSON.stringify(value, replacer)
JSON.stringify(value, replacer, space)
\f[R]
.fi
.SS Parameters
.IP \[bu] 2
\f[V]value\f[R]
.RS 2
.IP \[bu] 2
The value to convert to a JSON string.
.RE
.IP \[bu] 2
\f[V]replacer\f[R] \f[I](optional)\f[R]
.RS 2
.IP \[bu] 2
A function that alters the behavior of the stringification process, or
an array of strings and numbers that specifies properties of
\f[V]value\f[R] to be included in the output.
If \f[V]replacer\f[R] is an array, all elements in this array that are
not strings or numbers (either primitives or wrapper objects), including
\f[V]Symbol\f[R] values, are completely ignored.
If \f[V]replacer\f[R] is anything other than a function or an array
(e.g.\ \f[V]null\f[R] or not provided), all string-keyed properties of
the object are included in the resulting JSON string.
.RE
.IP \[bu] 2
\f[V]space\f[R] \f[I](optional)\f[R]
.RS 2
.IP \[bu] 2
A string or number that\[cq]s used to insert white space (including
indentation, line break characters, etc.)
into the output JSON string for readability purposes.
.RS 2
.PP
If this is a number, it indicates the number of space characters to be
used as indentation, clamped to 10 (that is, any number greater than
\f[V]10\f[R] is treated as if it were \f[V]10\f[R]).
Values less than 1 indicate that no space should be used.
.PP
If this is a string, the string (or the first 10 characters of the
string, if it\[cq]s longer than that) is inserted before every nested
object or array.
.PP
If \f[V]space\f[R] is anything other than a string or number (can be
either a primitive or a wrapper object) \[em] for example, is
\f[V]null\f[R] or not provided \[em] no white space is used.
.RE
.RE
.SS Return value
.PP
A JSON string representing the given value, or undefined.
.SS Exceptions
.IP \[bu] 2
\f[V]TypeError\f[R]
.RS 2
.IP \[bu] 2
Thrown if one of the following is true:
.RS 2
.IP \[bu] 2
\f[V]value\f[R] contains a circular reference.
.IP \[bu] 2
A \f[V]BigInt\f[R] value is encountered.
.RE
.RE
.SH Description
.PP
\f[V]JSON.stringify()\f[R] converts a value to the JSON notation that
the value represents.
Values are stringified in the following manner:
.IP \[bu] 2
\f[V]Boolean\f[R], \f[V]Number\f[R], \f[V]String\f[R], and
\f[V]BigInt\f[R] (obtainable via \f[V]Object()\f[R]) objects are
converted to the corresponding primitive values during stringification,
in accordance with the traditional conversion semantics.
\f[V]Symbol\f[R] objects (obtainable via \f[V]Object()\f[R]) are treated
as plain objects.
.IP \[bu] 2
Attempting to serialize \f[V]BigInt\f[R] values will throw.
However, if the BigInt has a \f[V]toJSON()\f[R] method (through monkey
patching: \f[V]BigInt.prototype.toJSON = ...\f[R]), that method can
provide the serialization result.
This constraint ensures that a proper serialization (and, very likely,
its accompanying deserialization) behavior is always explicitly provided
by the user.
.IP \[bu] 2
\f[V]undefined\f[R], \f[V]Function\f[R], and \f[V]Symbol\f[R] values are
not valid JSON values.
If any such values are encountered during conversion, they are either
omitted (when found in an object) or changed to \f[V]null\f[R] (when
found in an array).
\f[V]JSON.stringify()\f[R] can return \f[V]undefined\f[R] when passing
in \[lq]pure\[rq] values like \f[V]JSON.stringify(() => {})\f[R] or
\f[V]JSON.stringify(undefined)\f[R].
.IP \[bu] 2
The numbers \f[V]Infinity\f[R] and \f[V]NaN\f[R], as well as the value
\f[V]null\f[R], are all considered \f[V]null\f[R].
(But unlike the values in the previous point, they would never be
omitted.)
.IP \[bu] 2
Arrays are serialized as arrays (enclosed by square brackets).
Only array indices between 0 and \f[V]length - 1\f[R] (inclusive) are
serialized; other properties are ignored.
.IP \[bu] 2
For other objects:
.RS 2
.IP \[bu] 2
All \f[V]Symbol\f[R]-keyed properties will be completely ignored, even
when using the \f[V]replacer\f[R] parameter.
.IP \[bu] 2
If the value has a \f[V]toJSON()\f[R] method, it\[cq]s responsible to
define what data will be serialized.
Instead of the object being serialized, the value returned by the
\f[V]toJSON()\f[R] method when called will be serialized.
\f[V]JSON.stringify()\f[R] calls \f[V]toJSON\f[R] with one parameter,
the \f[V]key\f[R], which has the same semantic as the \f[V]key\f[R]
parameter of the \f[V]replacer\f[R] function:
.RS 2
.IP \[bu] 2
if this object is a property value, the property name
.IP \[bu] 2
if it is in an array, the index in the array, as a string
.IP \[bu] 2
if \f[V]JSON.stringify()\f[R] was directly called on this object, an
empty string
.PP
\f[V]Date\f[R] objects implement the \f[V]toJSON()\f[R] method which
returns a string (the same as \f[V]date.toISOString()\f[R]).
Thus, they will be stringified as strings.
.RE
.IP \[bu] 2
Only enumerable own properties are visited.
This means \f[V]Map\f[R], \f[V]Set\f[R], etc.
will become \f[V]\[dq]{}\[dq]\f[R].
You can use the \f[V]replacer\f[R] parameter to serialize them to
something more useful.
.RS 2
.PP
Properties are visited using the same algorithm as
\f[V]Object.keys()\f[R], which has a well-defined order and is stable
across implementations.
For example, \f[V]JSON.stringify\f[R] on the same object will always
produce the same string, and \f[V]JSON.parse(JSON.stringify(obj))\f[R]
would produce an object with the same key ordering as the original
(assuming the object is completely JSON-serializable).
.RE
.RE
.SS The replacer parameter
.PP
The \f[V]replacer\f[R] parameter can be either a function or an array.
.PP
As an array, its elements indicate the names of the properties in the
object that should be included in the resulting JSON string.
Only string and number values are taken into account; symbol keys are
ignored.
.PP
As a function, it takes two parameters: the \f[V]key\f[R] and the
\f[V]value\f[R] being stringified.
The object in which the key was found is provided as the
\f[V]replacer\f[R]\[cq]s \f[V]this\f[R] context.
.PP
The \f[V]replacer\f[R] function is called for the initial object being
stringified as well, in which case the \f[V]key\f[R] is an empty string
(\f[V]\[dq]\[dq]\f[R]).
It is then called for each property on the object or array being
stringified.
Array indices will be provided in its string form as \f[V]key\f[R].
The current property value will be replaced with the
\f[V]replacer\f[R]\[cq]s return value for stringification.
This means:
.IP \[bu] 2
If you return a number, string, boolean, or \f[V]null\f[R], that value
is directly serialized and used as the property\[cq]s value.
(Returning a BigInt will throw as well.)
.IP \[bu] 2
If you return a \f[V]Function\f[R], \f[V]Symbol\f[R], or
\f[V]undefined\f[R], the property is not included in the output.
.IP \[bu] 2
If you return any other object, the object is recursively stringified,
calling the \f[V]replacer\f[R] function on each property.
.RS
.PP
\f[B]Note:\f[R] When parsing JSON generated with \f[V]replacer\f[R]
functions, you would likely want to use the \f[V]reviver\f[R] parameter
to perform the reverse operation.
.RE
.PP
Typically, array elements\[cq] index would never shift (even when the
element is an invalid value like a function, it will become
\f[V]null\f[R] instead of omitted).
Using the \f[V]replacer\f[R] function allows you to control the order of
the array elements by returning a different array.
.SS The space parameter
.PP
The \f[V]space\f[R] parameter may be used to control spacing in the
final string.
.IP \[bu] 2
If it is a number, successive levels in the stringification will each be
indented by this many space characters.
.IP \[bu] 2
If it is a string, successive levels will be indented by this string.
.PP
Each level of indentation will never be longer than 10.
Number values of \f[V]space\f[R] are clamped to 10, and string values
are truncated to 10 characters.
.SH Examples
.SS Using JSON.stringify
.IP
.nf
\f[C]
JSON.stringify({}); // \[aq]{}\[aq]
JSON.stringify(true); // \[aq]true\[aq]
JSON.stringify(\[dq]foo\[dq]); // \[aq]\[dq]foo\[dq]\[aq]
JSON.stringify([1, \[dq]false\[dq], false]); // \[aq][1,\[dq]false\[dq],false]\[aq]
JSON.stringify([NaN, null, Infinity]); // \[aq][null,null,null]\[aq]
JSON.stringify({ x: 5 }); // \[aq]{\[dq]x\[dq]:5}\[aq]

JSON.stringify(new Date(1906, 0, 2, 15, 4, 5));
// \[aq]\[dq]1906-01-02T15:04:05.000Z\[dq]\[aq]

JSON.stringify({ x: 5, y: 6 });
// \[aq]{\[dq]x\[dq]:5,\[dq]y\[dq]:6}\[aq]
JSON.stringify([new Number(3), new String(\[dq]false\[dq]), new Boolean(false)]);
// \[aq][3,\[dq]false\[dq],false]\[aq]

// String-keyed array elements are not enumerable and make no sense in JSON
const a = [\[dq]foo\[dq], \[dq]bar\[dq]];
a[\[dq]baz\[dq]] = \[dq]quux\[dq]; // a: [ 0: \[aq]foo\[aq], 1: \[aq]bar\[aq], baz: \[aq]quux\[aq] ]
JSON.stringify(a);
// \[aq][\[dq]foo\[dq],\[dq]bar\[dq]]\[aq]

JSON.stringify({ x: [10, undefined, function () {}, Symbol(\[dq]\[dq])] });
// \[aq]{\[dq]x\[dq]:[10,null,null,null]}\[aq]

// Standard data structures
JSON.stringify([
  new Set([1]),
  new Map([[1, 2]]),
  new WeakSet([{ a: 1 }]),
  new WeakMap([[{ a: 1 }, 2]]),
]);
// \[aq][{},{},{},{}]\[aq]

// TypedArray
JSON.stringify([new Int8Array([1]), new Int16Array([1]), new Int32Array([1])]);
// \[aq][{\[dq]0\[dq]:1},{\[dq]0\[dq]:1},{\[dq]0\[dq]:1}]\[aq]
JSON.stringify([
  new Uint8Array([1]),
  new Uint8ClampedArray([1]),
  new Uint16Array([1]),
  new Uint32Array([1]),
]);
// \[aq][{\[dq]0\[dq]:1},{\[dq]0\[dq]:1},{\[dq]0\[dq]:1},{\[dq]0\[dq]:1}]\[aq]
JSON.stringify([new Float32Array([1]), new Float64Array([1])]);
// \[aq][{\[dq]0\[dq]:1},{\[dq]0\[dq]:1}]\[aq]

// toJSON()
JSON.stringify({
  x: 5,
  y: 6,
  toJSON() {
    return this.x + this.y;
  },
});
// \[aq]11\[aq]

// Symbols:
JSON.stringify({ x: undefined, y: Object, z: Symbol(\[dq]\[dq]) });
// \[aq]{}\[aq]
JSON.stringify({ [Symbol(\[dq]foo\[dq])]: \[dq]foo\[dq] });
// \[aq]{}\[aq]
JSON.stringify({ [Symbol.for(\[dq]foo\[dq])]: \[dq]foo\[dq] }, [Symbol.for(\[dq]foo\[dq])]);
// \[aq]{}\[aq]
JSON.stringify({ [Symbol.for(\[dq]foo\[dq])]: \[dq]foo\[dq] }, (k, v) => {
  if (typeof k === \[dq]symbol\[dq]) {
    return \[dq]a symbol\[dq];
  }
});
// undefined

// Non-enumerable properties:
JSON.stringify(
  Object.create(null, {
    x: { value: \[dq]x\[dq], enumerable: false },
    y: { value: \[dq]y\[dq], enumerable: true },
  }),
);
// \[aq]{\[dq]y\[dq]:\[dq]y\[dq]}\[aq]

// BigInt values throw
JSON.stringify({ x: 2n });
// TypeError: BigInt value can\[aq]t be serialized in JSON
\f[R]
.fi
.SS Using a function as replacer
.IP
.nf
\f[C]
function replacer(key, value) {
  // Filtering out properties
  if (typeof value === \[dq]string\[dq]) {
    return undefined;
  }
  return value;
}

const foo = {
  foundation: \[dq]Mozilla\[dq],
  model: \[dq]box\[dq],
  week: 45,
  transport: \[dq]car\[dq],
  month: 7,
};
JSON.stringify(foo, replacer);
// \[aq]{\[dq]week\[dq]:45,\[dq]month\[dq]:7}\[aq]
\f[R]
.fi
.PP
If you wish the \f[V]replacer\f[R] to distinguish an initial object from
a key with an empty string property (since both would give the empty
string as key and potentially an object as value), you will have to keep
track of the iteration count (if it is beyond the first iteration, it is
a genuine empty string key).
.IP
.nf
\f[C]
function makeReplacer() {
  let isInitial = true;

  return (key, value) => {
    if (isInitial) {
      isInitial = false;
      return value;
    }
    if (key === \[dq]\[dq]) {
      // Omit all properties with name \[dq]\[dq] (except the initial object)
      return undefined;
    }
    return value;
  };
}

const replacer = makeReplacer();
console.log(JSON.stringify({ \[dq]\[dq]: 1, b: 2 }, replacer)); // \[dq]{\[dq]b\[dq]:2}\[dq]
\f[R]
.fi
.SS Using an array as replacer
.IP
.nf
\f[C]
const foo = {
  foundation: \[dq]Mozilla\[dq],
  model: \[dq]box\[dq],
  week: 45,
  transport: \[dq]car\[dq],
  month: 7,
};

JSON.stringify(foo, [\[dq]week\[dq], \[dq]month\[dq]]);
// \[aq]{\[dq]week\[dq]:45,\[dq]month\[dq]:7}\[aq], only keep \[dq]week\[dq] and \[dq]month\[dq] properties
\f[R]
.fi
.SS Using the space parameter
.PP
Indent the output with one space:
.IP
.nf
\f[C]
console.log(JSON.stringify({ a: 2 }, null, \[dq] \[dq]));
/*
{
 \[dq]a\[dq]: 2
}
*/
\f[R]
.fi
.PP
Using a tab character mimics standard pretty-print appearance:
.IP
.nf
\f[C]
console.log(JSON.stringify({ uno: 1, dos: 2 }, null, \[dq]\[rs]t\[dq]));
/*
{
    \[dq]uno\[dq]: 1,
    \[dq]dos\[dq]: 2
}
*/
\f[R]
.fi
.SS toJSON() behavior
.PP
Defining \f[V]toJSON()\f[R] for an object allows overriding its
serialization behavior.
.IP
.nf
\f[C]
const obj = {
  data: \[dq]data\[dq],

  toJSON(key) {
    return key ? \[ga]Now I am a nested object under key \[aq]${key}\[aq]\[ga] : this;
  },
};

JSON.stringify(obj);
// \[aq]{\[dq]data\[dq]:\[dq]data\[dq]}\[aq]

JSON.stringify({ obj });
// \[aq]{\[dq]obj\[dq]:\[dq]Now I am a nested object under key \[aq]obj\[aq]\[dq]}\[aq]

JSON.stringify([obj]);
// \[aq][\[dq]Now I am a nested object under key \[aq]0\[aq]\[dq]]\[aq]
\f[R]
.fi
.SS Issue with serializing circular references
.PP
Since the JSON format (https://www.json.org/) doesn\[cq]t support object
references (although an IETF draft
exists (https://datatracker.ietf.org/doc/html/draft-pbryan-zyp-json-ref-03)),
a \f[V]TypeError\f[R] will be thrown if one attempts to encode an object
with circular references.
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const circularReference = {};
circularReference.myself = circularReference;

// Serializing circular references throws \[dq]TypeError: cyclic object value\[dq]
JSON.stringify(circularReference);
\f[R]
.fi
.PP
To serialize circular references, you can use a library that supports
them
(e.g.\ cycle.js (https://github.com/douglascrockford/JSON-js/blob/master/cycle.js)
by Douglas Crockford) or implement a solution yourself, which will
require finding and replacing (or removing) the cyclic references by
serializable values.
.PP
If you are using \f[V]JSON.stringify()\f[R] to deep-copy an object, you
may instead want to use \f[V]structuredClone()\f[R], which supports
circular references.
JavaScript engine APIs for binary serialization, such as
\f[V]v8.serialize()\f[R] (https://nodejs.org/api/v8.html#v8serializevalue),
also support circular references.
.SS Using JSON.stringify() with localStorage
.PP
In a case where you want to store an object created by your user and
allow it to be restored even after the browser has been closed, the
following example is a model for the applicability of
\f[V]JSON.stringify()\f[R]:
.IP
.nf
\f[C]
// Creating an example of JSON
const session = {
  screens: [],
  state: true,
};
session.screens.push({ name: \[dq]screenA\[dq], width: 450, height: 250 });
session.screens.push({ name: \[dq]screenB\[dq], width: 650, height: 350 });
session.screens.push({ name: \[dq]screenC\[dq], width: 750, height: 120 });
session.screens.push({ name: \[dq]screenD\[dq], width: 250, height: 60 });
session.screens.push({ name: \[dq]screenE\[dq], width: 390, height: 120 });
session.screens.push({ name: \[dq]screenF\[dq], width: 1240, height: 650 });

// Converting the JSON string with JSON.stringify()
// then saving with localStorage in the name of session
localStorage.setItem(\[dq]session\[dq], JSON.stringify(session));

// Example of how to transform the String generated through
// JSON.stringify() and saved in localStorage in JSON object again
const restoredSession = JSON.parse(localStorage.getItem(\[dq]session\[dq]));

// Now restoredSession variable contains the object that was saved
// in localStorage
console.log(restoredSession);
\f[R]
.fi
.SS Well-formed JSON.stringify()
.PP
Engines implementing the well-formed JSON.stringify
specification (https://github.com/tc39/proposal-well-formed-stringify)
will stringify lone surrogates (any code point from U+D800 to U+DFFF)
using Unicode escape sequences rather than literally (outputting lone
surrogates).
Before this change, such strings could not be encoded in valid UTF-8 or
UTF-16:
.IP
.nf
\f[C]
JSON.stringify(\[dq]\[rs]uD800\[dq]); // \[aq]\[dq]�\[dq]\[aq]
\f[R]
.fi
.PP
But with this change \f[V]JSON.stringify()\f[R] represents lone
surrogates using JSON escape sequences that \f[I]can\f[R] be encoded in
valid UTF-8 or UTF-16:
.IP
.nf
\f[C]
JSON.stringify(\[dq]\[rs]uD800\[dq]); // \[aq]\[dq]\[rs]\[rs]ud800\[dq]\[aq]
\f[R]
.fi
.PP
This change should be backwards-compatible as long as you pass the
result of \f[V]JSON.stringify()\f[R] to APIs such as
\f[V]JSON.parse()\f[R] that will accept any valid JSON text, because
they will treat Unicode escapes of lone surrogates as identical to the
lone surrogates themselves.
\f[I]Only\f[R] if you are directly interpreting the result of
\f[V]JSON.stringify()\f[R] do you need to carefully handle
\f[V]JSON.stringify()\f[R]\[cq]s two possible encodings of these code
points.
.SH See also
.IP \[bu] 2
Polyfill of modern \f[V]JSON.stringify\f[R] behavior (symbol and
well-formed unicode) in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-json)
.IP \[bu] 2
\f[V]JSON.parse()\f[R]
