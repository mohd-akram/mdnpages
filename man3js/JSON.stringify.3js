.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "JSON.STRINGIFY" "3JS" "June 20, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
JSON.stringify \- JSON.stringify()
.SH SYNOPSIS
The \f[B]\f[CB]JSON.stringify()\f[B]\f[R] static method converts a
JavaScript value to a JSON string, optionally replacing values if a
replacer function is specified or optionally including only the
specified properties if a replacer array is specified.
.IP
.EX
console.log(JSON.stringify({ x: 5, y: 6 }));
\f[I]// Expected output: \(aq{\(dqx\(dq:5,\(dqy\(dq:6}\(aq\f[R]

console.log(
  JSON.stringify([\f[B]new\f[R] Number(3), \f[B]new\f[R] String(\(dqfalse\(dq), \f[B]new\f[R] Boolean(\f[B]false\f[R])]),
);
\f[I]// Expected output: \(aq[3,\(dqfalse\(dq,false]\(aq\f[R]

console.log(JSON.stringify({ x: [10, \f[B]undefined\f[R], \f[B]function\f[R] () {}, Symbol(\(dq\(dq)] }));
\f[I]// Expected output: \(aq{\(dqx\(dq:[10,null,null,null]}\(aq\f[R]

console.log(JSON.stringify(\f[B]new\f[R] Date(2006, 0, 2, 15, 4, 5)));
\f[I]// Expected output: \(aq\(dq2006\-01\-02T15:04:05.000Z\(dq\(aq\f[R]
.EE
.SH SYNTAX
.IP
.EX
JSON.stringify(value)
JSON.stringify(value, replacer)
JSON.stringify(value, replacer, space)
.EE
.SS Parameters
.TP
\f[B]value\f[R]
The value to convert to a JSON string.
.TP
\f[B]replacer\f[R] \f[I](optional)\f[R]
A function that alters the behavior of the stringification process, or
an array of strings and numbers that specifies properties of
\f[CR]value\f[R] to be included in the output.
If \f[CR]replacer\f[R] is an array, all elements in this array that are
not strings or numbers (either primitives or wrapper objects), including
\f[CR]Symbol\f[R] values, are completely ignored.
If \f[CR]replacer\f[R] is anything other than a function or an array
(e.g., \f[CR]null\f[R] or not provided), all string\-keyed properties of
the object are included in the resulting JSON string.
.TP
\f[B]space\f[R] \f[I](optional)\f[R]
A string or number that\(cqs used to insert white space (including
indentation, line break characters, etc.)
into the output JSON string for readability purposes.
.RS
.PP
If this is a number, it indicates the number of space characters to be
used as indentation, clamped to 10 (that is, any number greater than
\f[CR]10\f[R] is treated as if it were \f[CR]10\f[R]).
Values less than 1 indicate that no space should be used.
.PP
If this is a string, the string (or the first 10 characters of the
string, if it\(cqs longer than that) is inserted before every nested
object or array.
.PP
If \f[CR]space\f[R] is anything other than a string or number (can be
either a primitive or a wrapper object) \(em for example, is
\f[CR]null\f[R] or not provided \(em no white space is used.
.RE
.SS Return value
A JSON string representing the given value, or undefined.
.SS Exceptions
.TP
\f[B]TypeError\f[R]
Thrown in one of the following cases:
.RS
.IP \(bu 2
\f[CR]value\f[R] contains a circular reference.
.IP \(bu 2
A \f[CR]BigInt\f[R] value is encountered.
.RE
.SH DESCRIPTION
\f[CR]JSON.stringify()\f[R] converts a value to the JSON notation that
the value represents.
Values are stringified in the following manner:
.IP \(bu 2
\f[CR]Boolean\f[R], \f[CR]Number\f[R], \f[CR]String\f[R], and
\f[CR]BigInt\f[R] (obtainable via \f[CR]Object()\f[R]) objects are
converted to the corresponding primitive values during stringification,
in accordance with the traditional conversion semantics.
\f[CR]Symbol\f[R] objects (obtainable via \f[CR]Object()\f[R]) are
treated as plain objects.
.IP \(bu 2
Attempting to serialize \f[CR]BigInt\f[R] values will throw.
However, if the BigInt has a \f[CR]toJSON()\f[R] method (through monkey
patching: \f[CR]BigInt.prototype.toJSON = ...\f[R]), that method can
provide the serialization result.
This constraint ensures that a proper serialization (and, very likely,
its accompanying deserialization) behavior is always explicitly provided
by the user.
.IP \(bu 2
\f[CR]undefined\f[R], \f[CR]Function\f[R], and \f[CR]Symbol\f[R] values
are not valid JSON values.
If any such values are encountered during conversion, they are either
omitted (when found in an object) or changed to \f[CR]null\f[R] (when
found in an array).
\f[CR]JSON.stringify()\f[R] can return \f[CR]undefined\f[R] when passing
in \(lqpure\(rq values like \f[CR]JSON.stringify(() => {})\f[R] or
\f[CR]JSON.stringify(undefined)\f[R].
.IP \(bu 2
The numbers \f[CR]Infinity\f[R] and \f[CR]NaN\f[R], as well as the value
\f[CR]null\f[R], are all considered \f[CR]null\f[R].
(But unlike the values in the previous point, they would never be
omitted.)
.IP \(bu 2
Arrays are serialized as arrays (enclosed by square brackets).
Only array indices between 0 and \f[CR]length \- 1\f[R] (inclusive) are
serialized; other properties are ignored.
.IP \(bu 2
The special raw JSON object created with \f[CR]JSON.rawJSON()\f[R] is
serialized as the raw JSON text it contains (by accessing its
\f[CR]rawJSON\f[R] property).
.IP \(bu 2
For other objects:
.RS 2
.IP \(bu 2
All \f[CR]Symbol\f[R]\-keyed properties will be completely ignored, even
when using the \f[CR]replacer\f[R] parameter.
.IP \(bu 2
If the value has a \f[CR]toJSON()\f[R] method, it\(cqs responsible to
define what data will be serialized.
Instead of the object being serialized, the value returned by the
\f[CR]toJSON()\f[R] method when called will be serialized.
\f[CR]JSON.stringify()\f[R] calls \f[CR]toJSON\f[R] with one parameter,
the \f[CR]key\f[R], which has the same semantic as the \f[CR]key\f[R]
parameter of the \f[CR]replacer\f[R] function:
.RS 2
.IP \(bu 2
if this object is a property value, the property name
.IP \(bu 2
if it is in an array, the index in the array, as a string
.IP \(bu 2
if \f[CR]JSON.stringify()\f[R] was directly called on this object, an
empty string
.PP
All \f[CR]Temporal\f[R] objects implement the \f[CR]toJSON()\f[R]
method, which returns a string (the same as calling
\f[CR]toString()\f[R]).
Thus, they will be serialized as strings.
Similarly, \f[CR]Date\f[R] objects implement \f[CR]toJSON()\f[R], which
returns the same as \f[CR]toISOString()\f[R].
.RE
.IP \(bu 2
Only enumerable own properties are visited.
This means \f[CR]Map\f[R], \f[CR]Set\f[R], etc.
will become \f[CR]\(dq{}\(dq\f[R].
You can use the \f[CR]replacer\f[R] parameter to serialize them to
something more useful.
.RS 2
.PP
Properties are visited using the same algorithm as
\f[CR]Object.keys()\f[R], which has a well\-defined order and is stable
across implementations.
For example, \f[CR]JSON.stringify\f[R] on the same object will always
produce the same string, and \f[CR]JSON.parse(JSON.stringify(obj))\f[R]
would produce an object with the same key ordering as the original
(assuming the object is completely JSON\-serializable).
.RE
.RE
.SS The replacer parameter
The \f[CR]replacer\f[R] parameter can be either a function or an array.
.PP
As an array, its elements indicate the names of the properties in the
object that should be included in the resulting JSON string.
Only string and number values are taken into account; symbol keys are
ignored.
.PP
As a function, it takes two parameters: the \f[CR]key\f[R] and the
\f[CR]value\f[R] being stringified.
The object in which the key was found is provided as the
\f[CR]replacer\f[R]\(cqs \f[CR]this\f[R] context.
.PP
The \f[CR]replacer\f[R] function is called for the initial object being
stringified as well, in which case the \f[CR]key\f[R] is an empty string
(\f[CR]\(dq\(dq\f[R]).
It is then called for each property on the object or array being
stringified.
Array indices will be provided in its string form as \f[CR]key\f[R].
The current property value will be replaced with the
\f[CR]replacer\f[R]\(cqs return value for stringification.
This means:
.IP \(bu 2
If you return a number, string, boolean, or \f[CR]null\f[R], that value
is directly serialized and used as the property\(cqs value.
(Returning a BigInt will throw as well.)
.IP \(bu 2
If you return a \f[CR]Function\f[R], \f[CR]Symbol\f[R], or
\f[CR]undefined\f[R], the property is not included in the output.
.IP \(bu 2
If you return any other object, the object is recursively stringified,
calling the \f[CR]replacer\f[R] function on each property.
.RS
.PP
\f[B]Note:\f[R] When parsing JSON generated with \f[CR]replacer\f[R]
functions, you would likely want to use the \f[CR]reviver\f[R] parameter
to perform the reverse operation.
.RE
.PP
Typically, array elements\(cq index would never shift (even when the
element is an invalid value like a function, it will become
\f[CR]null\f[R] instead of omitted).
Using the \f[CR]replacer\f[R] function allows you to control the order
of the array elements by returning a different array.
.SS The space parameter
The \f[CR]space\f[R] parameter may be used to control spacing in the
final string.
.IP \(bu 2
If it is a number, successive levels in the stringification will each be
indented by this many space characters.
.IP \(bu 2
If it is a string, successive levels will be indented by this string.
.PP
Each level of indentation will never be longer than 10.
Number values of \f[CR]space\f[R] are clamped to 10, and string values
are truncated to 10 characters.
.SH EXAMPLES
.SS Using JSON.stringify
.IP
.EX
JSON.stringify({}); \f[I]// \(aq{}\(aq\f[R]
JSON.stringify(\f[B]true\f[R]); \f[I]// \(aqtrue\(aq\f[R]
JSON.stringify(\(dqfoo\(dq); \f[I]// \(aq\(dqfoo\(dq\(aq\f[R]
JSON.stringify([1, \(dqfalse\(dq, \f[B]false\f[R]]); \f[I]// \(aq[1,\(dqfalse\(dq,false]\(aq\f[R]
JSON.stringify([\f[B]NaN\f[R], \f[B]null\f[R], \f[B]Infinity\f[R]]); \f[I]// \(aq[null,null,null]\(aq\f[R]
JSON.stringify({ x: 5 }); \f[I]// \(aq{\(dqx\(dq:5}\(aq\f[R]

JSON.stringify(\f[B]new\f[R] Date(1906, 0, 2, 15, 4, 5));
\f[I]// \(aq\(dq1906\-01\-02T15:04:05.000Z\(dq\(aq\f[R]

JSON.stringify({ x: 5, y: 6 });
\f[I]// \(aq{\(dqx\(dq:5,\(dqy\(dq:6}\(aq\f[R]
JSON.stringify([\f[B]new\f[R] Number(3), \f[B]new\f[R] String(\(dqfalse\(dq), \f[B]new\f[R] Boolean(\f[B]false\f[R])]);
\f[I]// \(aq[3,\(dqfalse\(dq,false]\(aq\f[R]

\f[I]// String\-keyed array elements are not enumerable and make no sense in JSON\f[R]
\f[B]const\f[R] a = [\(dqfoo\(dq, \(dqbar\(dq];
a[\(dqbaz\(dq] = \(dqquux\(dq; \f[I]// a: [ 0: \(aqfoo\(aq, 1: \(aqbar\(aq, baz: \(aqquux\(aq ]\f[R]
JSON.stringify(a);
\f[I]// \(aq[\(dqfoo\(dq,\(dqbar\(dq]\(aq\f[R]

JSON.stringify({ x: [10, \f[B]undefined\f[R], \f[B]function\f[R] () {}, Symbol(\(dq\(dq)] });
\f[I]// \(aq{\(dqx\(dq:[10,null,null,null]}\(aq\f[R]

\f[I]// Standard data structures\f[R]
JSON.stringify([
  \f[B]new\f[R] Set([1]),
  \f[B]new\f[R] Map([[1, 2]]),
  \f[B]new\f[R] WeakSet([{ a: 1 }]),
  \f[B]new\f[R] WeakMap([[{ a: 1 }, 2]]),
]);
\f[I]// \(aq[{},{},{},{}]\(aq\f[R]

\f[I]// TypedArray\f[R]
JSON.stringify([\f[B]new\f[R] Int8Array([1]), \f[B]new\f[R] Int16Array([1]), \f[B]new\f[R] Int32Array([1])]);
\f[I]// \(aq[{\(dq0\(dq:1},{\(dq0\(dq:1},{\(dq0\(dq:1}]\(aq\f[R]
JSON.stringify([
  \f[B]new\f[R] Uint8Array([1]),
  \f[B]new\f[R] Uint8ClampedArray([1]),
  \f[B]new\f[R] Uint16Array([1]),
  \f[B]new\f[R] Uint32Array([1]),
]);
\f[I]// \(aq[{\(dq0\(dq:1},{\(dq0\(dq:1},{\(dq0\(dq:1},{\(dq0\(dq:1}]\(aq\f[R]
JSON.stringify([\f[B]new\f[R] Float32Array([1]), \f[B]new\f[R] Float64Array([1])]);
\f[I]// \(aq[{\(dq0\(dq:1},{\(dq0\(dq:1}]\(aq\f[R]

\f[I]// toJSON()\f[R]
JSON.stringify({
  x: 5,
  y: 6,
  toJSON() {
    \f[B]return\f[R] \f[B]this\f[R].x + \f[B]this\f[R].y;
  },
});
\f[I]// \(aq11\(aq\f[R]

\f[I]// Symbols:\f[R]
JSON.stringify({ x: \f[B]undefined\f[R], y: Object, z: Symbol(\(dq\(dq) });
\f[I]// \(aq{}\(aq\f[R]
JSON.stringify({ [Symbol(\(dqfoo\(dq)]: \(dqfoo\(dq });
\f[I]// \(aq{}\(aq\f[R]
JSON.stringify({ [Symbol.for(\(dqfoo\(dq)]: \(dqfoo\(dq }, [Symbol.for(\(dqfoo\(dq)]);
\f[I]// \(aq{}\(aq\f[R]
JSON.stringify({ [Symbol.for(\(dqfoo\(dq)]: \(dqfoo\(dq }, (k, v) \f[B]=>\f[R] {
  \f[B]if\f[R] (\f[B]typeof\f[R] k === \(dqsymbol\(dq) {
    \f[B]return\f[R] \(dqa symbol\(dq;
  }
});
\f[I]// undefined\f[R]

\f[I]// Non\-enumerable properties:\f[R]
JSON.stringify(
  Object.create(\f[B]null\f[R], {
    x: { value: \(dqx\(dq, enumerable: \f[B]false\f[R] },
    y: { value: \(dqy\(dq, enumerable: \f[B]true\f[R] },
  }),
);
\f[I]// \(aq{\(dqy\(dq:\(dqy\(dq}\(aq\f[R]

\f[I]// BigInt values throw\f[R]
JSON.stringify({ x: 2n });
\f[I]// TypeError: BigInt value can\(aqt be serialized in JSON\f[R]
.EE
.SS Using a function as replacer
.IP
.EX
\f[B]function\f[R] replacer(key, value) {
  \f[I]// Filtering out properties\f[R]
  \f[B]if\f[R] (\f[B]typeof\f[R] value === \(dqstring\(dq) {
    \f[B]return\f[R] \f[B]undefined\f[R];
  }
  \f[B]return\f[R] value;
}

\f[B]const\f[R] foo = {
  foundation: \(dqMozilla\(dq,
  model: \(dqbox\(dq,
  week: 45,
  transport: \(dqcar\(dq,
  month: 7,
};
JSON.stringify(foo, replacer);
\f[I]// \(aq{\(dqweek\(dq:45,\(dqmonth\(dq:7}\(aq\f[R]
.EE
.PP
If you wish the \f[CR]replacer\f[R] to distinguish an initial object
from a key with an empty string property (since both would give the
empty string as key and potentially an object as value), you will have
to keep track of the iteration count (if it is beyond the first
iteration, it is a genuine empty string key).
.IP
.EX
\f[B]function\f[R] makeReplacer() {
  \f[B]let\f[R] isInitial = \f[B]true\f[R];

  \f[B]return\f[R] (key, value) \f[B]=>\f[R] {
    \f[B]if\f[R] (isInitial) {
      isInitial = \f[B]false\f[R];
      \f[B]return\f[R] value;
    }
    \f[B]if\f[R] (key === \(dq\(dq) {
      \f[I]// Omit all properties with name \(dq\(dq (except the initial object)\f[R]
      \f[B]return\f[R] \f[B]undefined\f[R];
    }
    \f[B]return\f[R] value;
  };
}

\f[B]const\f[R] replacer = makeReplacer();
console.log(JSON.stringify({ \(dq\(dq: 1, b: 2 }, replacer)); \f[I]// \(dq{\(dqb\(dq:2}\(dq\f[R]
.EE
.SS Using an array as replacer
.IP
.EX
\f[B]const\f[R] foo = {
  foundation: \(dqMozilla\(dq,
  model: \(dqbox\(dq,
  week: 45,
  transport: \(dqcar\(dq,
  month: 7,
};

JSON.stringify(foo, [\(dqweek\(dq, \(dqmonth\(dq]);
\f[I]// \(aq{\(dqweek\(dq:45,\(dqmonth\(dq:7}\(aq, only keep \(dqweek\(dq and \(dqmonth\(dq properties\f[R]
.EE
.SS Using the space parameter
Indent the output with one space:
.IP
.EX
console.log(JSON.stringify({ a: 2 }, \f[B]null\f[R], \(dq \(dq));
\f[I]/*\f[R]
\f[I]{\f[R]
\f[I] \(dqa\(dq: 2\f[R]
\f[I]}\f[R]
\f[I]*/\f[R]
.EE
.PP
Using a tab character mimics standard pretty\-print appearance:
.IP
.EX
console.log(JSON.stringify({ uno: 1, dos: 2 }, \f[B]null\f[R], \(dq\(rst\(dq));
\f[I]/*\f[R]
\f[I]{\f[R]
\f[I]    \(dquno\(dq: 1,\f[R]
\f[I]    \(dqdos\(dq: 2\f[R]
\f[I]}\f[R]
\f[I]*/\f[R]
.EE
.SS toJSON() behavior
Defining \f[CR]toJSON()\f[R] for an object allows overriding its
serialization behavior.
.IP
.EX
\f[B]const\f[R] obj = {
  data: \(dqdata\(dq,

  toJSON(key) {
    \f[B]return\f[R] key ? \(gaNow I am a nested object under key \(aq${key}\(aq\(ga : \f[B]this\f[R];
  },
};

JSON.stringify(obj);
\f[I]// \(aq{\(dqdata\(dq:\(dqdata\(dq}\(aq\f[R]

JSON.stringify({ obj });
\f[I]// \(aq{\(dqobj\(dq:\(dqNow I am a nested object under key \(aqobj\(aq\(dq}\(aq\f[R]

JSON.stringify([obj]);
\f[I]// \(aq[\(dqNow I am a nested object under key \(aq0\(aq\(dq]\(aq\f[R]
.EE
.SS Issue with serializing circular references
Since the \c
.UR https://www.json.org/
JSON format
.UE \c
\ doesn\(cqt support object references (although an \c
.UR https://datatracker.ietf.org/doc/html/draft-pbryan-zyp-json-ref-03
IETF draft exists
.UE \c
), a \f[CR]TypeError\f[R] will be thrown if one attempts to encode an
object with circular references.
.PP
\f[B]Bad\f[R]
.IP
.EX
\f[B]const\f[R] circularReference = {};
circularReference.myself = circularReference;

\f[I]// Serializing circular references throws \(dqTypeError: cyclic object value\(dq\f[R]
JSON.stringify(circularReference);
.EE
.PP
To serialize circular references, you can use a library that supports
them (e.g., \c
.UR https://github.com/douglascrockford/JSON-js/blob/master/cycle.js
cycle.js
.UE \c
\ by Douglas Crockford) or implement a solution yourself, which will
require finding and replacing (or removing) the cyclic references by
serializable values.
.PP
If you are using \f[CR]JSON.stringify()\f[R] to deep\-copy an object,
you may instead want to use \f[CR]structuredClone()\f[R], which supports
circular references.
JavaScript engine APIs for binary serialization, such as \c
.UR https://nodejs.org/api/v8.html#v8serializevalue
\f[CR]v8.serialize()\f[R]
.UE \c
, also support circular references.
.SS Using JSON.stringify() with localStorage
In a case where you want to store an object created by your user and
allow it to be restored even after the browser has been closed, the
following example is a model for the applicability of
\f[CR]JSON.stringify()\f[R]:
.IP
.EX
\f[I]// Creating an example of JSON\f[R]
\f[B]const\f[R] session = {
  screens: [],
  state: \f[B]true\f[R],
};
session.screens.push({ name: \(dqscreenA\(dq, width: 450, height: 250 });
session.screens.push({ name: \(dqscreenB\(dq, width: 650, height: 350 });
session.screens.push({ name: \(dqscreenC\(dq, width: 750, height: 120 });
session.screens.push({ name: \(dqscreenD\(dq, width: 250, height: 60 });
session.screens.push({ name: \(dqscreenE\(dq, width: 390, height: 120 });
session.screens.push({ name: \(dqscreenF\(dq, width: 1240, height: 650 });

\f[I]// Converting the JSON string with JSON.stringify()\f[R]
\f[I]// then saving with localStorage in the name of session\f[R]
localStorage.setItem(\(dqsession\(dq, JSON.stringify(session));

\f[I]// Example of how to transform the String generated through\f[R]
\f[I]// JSON.stringify() and saved in localStorage in JSON object again\f[R]
\f[B]const\f[R] restoredSession = JSON.parse(localStorage.getItem(\(dqsession\(dq));

\f[I]// Now restoredSession variable contains the object that was saved\f[R]
\f[I]// in localStorage\f[R]
console.log(restoredSession);
.EE
.SS Well\-formed JSON.stringify()
Engines implementing the \c
.UR https://github.com/tc39/proposal-well-formed-stringify
well\-formed JSON.stringify specification
.UE \c
\ will stringify lone surrogates (any code point from U+D800 to U+DFFF)
using Unicode escape sequences rather than literally (outputting lone
surrogates).
Before this change, such strings could not be encoded in valid UTF\-8 or
UTF\-16:
.IP
.EX
JSON.stringify(\(dq\(rsuD800\(dq); \f[I]// \(aq\(dq�\(dq\(aq\f[R]
.EE
.PP
But with this change \f[CR]JSON.stringify()\f[R] represents lone
surrogates using JSON escape sequences that \f[I]can\f[R] be encoded in
valid UTF\-8 or UTF\-16:
.IP
.EX
JSON.stringify(\(dq\(rsuD800\(dq); \f[I]// \(aq\(dq\(rs\(rsud800\(dq\(aq\f[R]
.EE
.PP
This change should be backwards\-compatible as long as you pass the
result of \f[CR]JSON.stringify()\f[R] to APIs such as
\f[CR]JSON.parse()\f[R] that will accept any valid JSON text, because
they will treat Unicode escapes of lone surrogates as identical to the
lone surrogates themselves.
\f[I]Only\f[R] if you are directly interpreting the result of
\f[CR]JSON.stringify()\f[R] do you need to carefully handle
\f[CR]JSON.stringify()\f[R]\(cqs two possible encodings of these code
points.
.SH SEE ALSO
.IP \(bu 2
\c
.UR https://github.com/zloirock/core-js#ecmascript-json
Polyfill of modern \f[CR]JSON.stringify\f[R] behavior (symbol,
well\-formed unicode, raw JSON) in \f[CR]core\-js\f[R]
.UE \c
.IP \(bu 2
\f[CR]JSON.parse()\f[R]
.IP \(bu 2
\f[CR]JSON.rawJSON()\f[R]
