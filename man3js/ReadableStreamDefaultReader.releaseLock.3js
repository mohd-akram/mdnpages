.\" Automatically generated by Pandoc 3.4
.\"
.TH "ReadableStreamDefaultReader.releaseLock" "JS" "April 22, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
ReadableStreamDefaultReader.releaseLock \- ReadableStreamDefaultReader:
releaseLock() method
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]releaseLock()\f[B]\f[R] method of the
\f[CR]ReadableStreamDefaultReader\f[R] interface releases the
reader\[cq]s lock on the stream.
.PP
If the associated stream is errored when the lock is released, the
reader will appear errored in that same way subsequently; otherwise, the
reader will appear closed.
.PP
If the reader\[cq]s lock is released while it still has pending read
requests then the promises returned by the reader\[cq]s
\f[CR]ReadableStreamDefaultReader.read()\f[R] method are immediately
rejected with a \f[CR]TypeError\f[R].
Unread chunks remain in the stream\[cq]s internal queue and can be read
later by acquiring a new reader.
.SH SYNTAX
.IP
.EX
releaseLock()
.EE
.SS Parameters
None.
.SS Return value
None (\f[CR]undefined\f[R]).
.SS Exceptions
.TP
\f[B]TypeError\f[R]
Thrown if the source object is not a
\f[CR]ReadableStreamDefaultReader\f[R].
.SH EXAMPLES
.IP
.EX
\f[B]function\f[R] fetchStream() {
  \f[B]const\f[R] reader = stream.getReader();

  \f[I]// ...\f[R]

  reader.releaseLock();

  \f[I]// ...\f[R]
}
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]ReadableStreamDefaultReader()\f[R] constructor
.IP \[bu] 2
Using readable streams
