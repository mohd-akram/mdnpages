'\" t
.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Array.reduce" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Array.reduce - Array.prototype.reduce()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]reduce()\f[B]\f[R] method executes a user-supplied
\[lq]reducer\[rq] callback function on each element of the array, in
order, passing in the return value from the calculation on the preceding
element.
The final result of running the reducer across all elements of the array
is a single value.
.PP
The first time that the callback is run there is no \[lq]return value of
the previous calculation\[rq].
If supplied, an initial value may be used in its place.
Otherwise the array element at index 0 is used as the initial value and
iteration starts from the next element (index 1 instead of index 0).
.PP
Perhaps the easiest-to-understand case for \f[V]reduce()\f[R] is to
return the sum of all the elements in an array:
.PP
The reducer walks through the array element-by-element, at each step
adding the current array value to the result from the previous step
(this result is the running sum of all the previous steps) \[em] until
there are no more elements to add.
.SH SYNTAX
.IP
.nf
\f[C]
reduce(callbackFn)
reduce(callbackFn, initialValue)
\f[R]
.fi
.SS Parameters
.TP
\f[B]callbackFn\f[R]
A function to execute for each element in the array.
Its return value becomes the value of the \f[V]accumulator\f[R]
parameter on the next invocation of \f[V]callbackFn\f[R].
For the last invocation, the return value becomes the return value of
\f[V]reduce()\f[R].
The function is called with the following arguments:
.RS
.TP
\f[B]accumulator\f[R]
The value resulting from the previous call to \f[V]callbackFn\f[R].
On first call, \f[V]initialValue\f[R] if specified, otherwise the value
of \f[V]array[0]\f[R].
.TP
\f[B]currentValue\f[R]
The value of the current element.
On first call, the value of \f[V]array[0]\f[R] if an
\f[V]initialValue\f[R] was specified, otherwise the value of
\f[V]array[1]\f[R].
.TP
\f[B]currentIndex\f[R]
The index position of \f[V]currentValue\f[R] in the array.
On first call, \f[V]0\f[R] if \f[V]initialValue\f[R] was specified,
otherwise \f[V]1\f[R].
.TP
\f[B]array\f[R]
The array \f[V]reduce()\f[R] was called upon.
.RE
.TP
\f[B]initialValue\f[R] \f[I](optional)\f[R]
A value to which \f[V]accumulator\f[R] is initialized the first time the
callback is called.
If \f[V]initialValue\f[R] is specified, \f[V]callbackFn\f[R] starts
executing with the first value in the array as \f[V]currentValue\f[R].
If \f[V]initialValue\f[R] is \f[I]not\f[R] specified,
\f[V]accumulator\f[R] is initialized to the first value in the array,
and \f[V]callbackFn\f[R] starts executing with the second value in the
array as \f[V]currentValue\f[R].
In this case, if the array is empty (so that there\[cq]s no first value
to return as \f[V]accumulator\f[R]), an error is thrown.
.SS Return value
.PP
The value that results from running the \[lq]reducer\[rq] callback
function to completion over the entire array.
.SS Exceptions
.TP
\f[B]TypeError\f[R]
Thrown if the array contains no elements and \f[V]initialValue\f[R] is
not provided.
.SH DESCRIPTION
.PP
The \f[V]reduce()\f[R] method is an iterative method.
It runs a \[lq]reducer\[rq] callback function over all elements in the
array, in ascending-index order, and accumulates them into a single
value.
Every time, the return value of \f[V]callbackFn\f[R] is passed into
\f[V]callbackFn\f[R] again on next invocation as \f[V]accumulator\f[R].
The final value of \f[V]accumulator\f[R] (which is the value returned
from \f[V]callbackFn\f[R] on the final iteration of the array) becomes
the return value of \f[V]reduce()\f[R].
.PP
\f[V]callbackFn\f[R] is invoked only for array indexes which have
assigned values.
It is not invoked for empty slots in sparse arrays.
.PP
Unlike other iterative methods, \f[V]reduce()\f[R] does not accept a
\f[V]thisArg\f[R] argument.
\f[V]callbackFn\f[R] is always called with \f[V]undefined\f[R] as
\f[V]this\f[R], which gets substituted with \f[V]globalThis\f[R] if
\f[V]callbackFn\f[R] is non-strict.
.PP
\f[V]reduce()\f[R] is a central concept in functional
programming (https://en.wikipedia.org/wiki/Functional_programming),
where it\[cq]s not possible to mutate any value, so in order to
accumulate all values in an array, one must return a new accumulator
value on every iteration.
This convention propagates to JavaScript\[cq]s \f[V]reduce()\f[R]: you
should use spreading or other copying methods where possible to create
new arrays and objects as the accumulator, rather than mutating the
existing one.
If you decided to mutate the accumulator instead of copying it, remember
to still return the modified object in the callback, or the next
iteration will receive undefined.
.PP
\f[V]reduce()\f[R] does not mutate the array on which it is called, but
the function provided as \f[V]callbackFn\f[R] can.
Note, however, that the length of the array is saved \f[I]before\f[R]
the first invocation of \f[V]callbackFn\f[R].
Therefore:
.IP \[bu] 2
\f[V]callbackFn\f[R] will not visit any elements added beyond the
array\[cq]s initial length when the call to \f[V]reduce()\f[R] began.
.IP \[bu] 2
Changes to already-visited indexes do not cause \f[V]callbackFn\f[R] to
be invoked on them again.
.IP \[bu] 2
If an existing, yet-unvisited element of the array is changed by
\f[V]callbackFn\f[R], its value passed to the \f[V]callbackFn\f[R] will
be the value at the time that element gets visited.
Deleted elements are not visited.
.RS
.PP
\f[B]Warning:\f[R] Concurrent modifications of the kind described above
frequently lead to hard-to-understand code and are generally to be
avoided (except in special cases).
.RE
.PP
The \f[V]reduce()\f[R] method is generic.
It only expects the \f[V]this\f[R] value to have a \f[V]length\f[R]
property and integer-keyed properties.
.SS Edge cases
.PP
If the array only has one element (regardless of position) and no
\f[V]initialValue\f[R] is provided, or if \f[V]initialValue\f[R] is
provided but the array is empty, the solo value will be returned
\f[I]without\f[R] calling \f[V]callbackFn\f[R].
.PP
If \f[V]initialValue\f[R] is provided and the array is not empty, then
the reduce method will always invoke the callback function starting at
index 0.
.PP
If \f[V]initialValue\f[R] is not provided then the reduce method will
act differently for arrays with length larger than 1, equal to 1 and 0,
as shown in the following example:
.IP
.nf
\f[C]
const getMax = (a, b) => Math.max(a, b);

// callback is invoked for each element in the array starting at index 0
[1, 100].reduce(getMax, 50); // 100
[50].reduce(getMax, 10); // 50

// callback is invoked once for element at index 1
[1, 100].reduce(getMax); // 100

// callback is not invoked
[50].reduce(getMax); // 50
[].reduce(getMax, 1); // 1

[].reduce(getMax); // TypeError
\f[R]
.fi
.SH EXAMPLES
.SS How reduce() works without an initial value
.PP
The code below shows what happens if we call \f[V]reduce()\f[R] with an
array and no initial value.
.IP
.nf
\f[C]
const array = [15, 16, 17, 18, 19];

function reducer(accumulator, currentValue, index) {
  const returns = accumulator + currentValue;
  console.log(
    \[ga]accumulator: ${accumulator}, currentValue: ${currentValue}, index: ${index}, returns: ${returns}\[ga],
  );
  return returns;
}

array.reduce(reducer);
\f[R]
.fi
.PP
The callback would be invoked four times, with the arguments and return
values in each call being as follows:
.PP
.TS
tab(@);
lw(13.5n) lw(16.0n) lw(17.2n) lw(8.6n) lw(14.7n).
T{
T}@T{
\f[V]accumulator\f[R]
T}@T{
\f[V]currentValue\f[R]
T}@T{
\f[V]index\f[R]
T}@T{
Return value
T}
_
T{
First call
T}@T{
\f[V]15\f[R]
T}@T{
\f[V]16\f[R]
T}@T{
\f[V]1\f[R]
T}@T{
\f[V]31\f[R]
T}
T{
Second call
T}@T{
\f[V]31\f[R]
T}@T{
\f[V]17\f[R]
T}@T{
\f[V]2\f[R]
T}@T{
\f[V]48\f[R]
T}
T{
Third call
T}@T{
\f[V]48\f[R]
T}@T{
\f[V]18\f[R]
T}@T{
\f[V]3\f[R]
T}@T{
\f[V]66\f[R]
T}
T{
Fourth call
T}@T{
\f[V]66\f[R]
T}@T{
\f[V]19\f[R]
T}@T{
\f[V]4\f[R]
T}@T{
\f[V]85\f[R]
T}
.TE
.PP
The \f[V]array\f[R] parameter never changes through the process \[em]
it\[cq]s always \f[V][15, 16, 17, 18, 19]\f[R].
The value returned by \f[V]reduce()\f[R] would be that of the last
callback invocation (\f[V]85\f[R]).
.SS How reduce() works with an initial value
.PP
Here we reduce the same array using the same algorithm, but with an
\f[V]initialValue\f[R] of \f[V]10\f[R] passed as the second argument to
\f[V]reduce()\f[R]:
.IP
.nf
\f[C]
[15, 16, 17, 18, 19].reduce(
  (accumulator, currentValue) => accumulator + currentValue,
  10,
);
\f[R]
.fi
.PP
The callback would be invoked five times, with the arguments and return
values in each call being as follows:
.PP
.TS
tab(@);
lw(13.5n) lw(16.0n) lw(17.2n) lw(8.6n) lw(14.7n).
T{
T}@T{
\f[V]accumulator\f[R]
T}@T{
\f[V]currentValue\f[R]
T}@T{
\f[V]index\f[R]
T}@T{
Return value
T}
_
T{
First call
T}@T{
\f[V]10\f[R]
T}@T{
\f[V]15\f[R]
T}@T{
\f[V]0\f[R]
T}@T{
\f[V]25\f[R]
T}
T{
Second call
T}@T{
\f[V]25\f[R]
T}@T{
\f[V]16\f[R]
T}@T{
\f[V]1\f[R]
T}@T{
\f[V]41\f[R]
T}
T{
Third call
T}@T{
\f[V]41\f[R]
T}@T{
\f[V]17\f[R]
T}@T{
\f[V]2\f[R]
T}@T{
\f[V]58\f[R]
T}
T{
Fourth call
T}@T{
\f[V]58\f[R]
T}@T{
\f[V]18\f[R]
T}@T{
\f[V]3\f[R]
T}@T{
\f[V]76\f[R]
T}
T{
Fifth call
T}@T{
\f[V]76\f[R]
T}@T{
\f[V]19\f[R]
T}@T{
\f[V]4\f[R]
T}@T{
\f[V]95\f[R]
T}
.TE
.PP
The value returned by \f[V]reduce()\f[R] in this case would be
\f[V]95\f[R].
.SS Sum of values in an object array
.PP
To sum up the values contained in an array of objects, you
\f[B]must\f[R] supply an \f[V]initialValue\f[R], so that each item
passes through your function.
.IP
.nf
\f[C]
const objects = [{ x: 1 }, { x: 2 }, { x: 3 }];
const sum = objects.reduce(
  (accumulator, currentValue) => accumulator + currentValue.x,
  0,
);

console.log(sum); // 6
\f[R]
.fi
.SS Function sequential piping
.PP
The \f[V]pipe\f[R] function takes a sequence of functions and returns a
new function.
When the new function is called with an argument, the sequence of
functions are called in order, which each one receiving the return value
of the previous function.
.IP
.nf
\f[C]
const pipe =
  (...functions) =>
  (initialValue) =>
    functions.reduce((acc, fn) => fn(acc), initialValue);

// Building blocks to use for composition
const double = (x) => 2 * x;
const triple = (x) => 3 * x;
const quadruple = (x) => 4 * x;

// Composed functions for multiplication of specific values
const multiply6 = pipe(double, triple);
const multiply9 = pipe(triple, triple);
const multiply16 = pipe(quadruple, quadruple);
const multiply24 = pipe(double, triple, quadruple);

// Usage
multiply6(6); // 36
multiply9(9); // 81
multiply16(16); // 256
multiply24(10); // 240
\f[R]
.fi
.SS Running promises in sequence
.PP
Promise sequencing is essentially function piping demonstrated in the
previous section, except done asynchronously.
.IP
.nf
\f[C]
// Compare this with pipe: fn(acc) is changed to acc.then(fn),
// and initialValue is ensured to be a promise
const asyncPipe =
  (...functions) =>
  (initialValue) =>
    functions.reduce((acc, fn) => acc.then(fn), Promise.resolve(initialValue));

// Building blocks to use for composition
const p1 = async (a) => a * 5;
const p2 = async (a) => a * 2;
// The composed functions can also return non-promises, because the values are
// all eventually wrapped in promises
const f3 = (a) => a * 3;
const p4 = async (a) => a * 4;

asyncPipe(p1, p2, f3, p4)(10).then(console.log); // 1200
\f[R]
.fi
.PP
\f[V]asyncPipe\f[R] can also be implemented using
\f[V]async\f[R]/\f[V]await\f[R], which better demonstrates its
similarity with \f[V]pipe\f[R]:
.IP
.nf
\f[C]
const asyncPipe =
  (...functions) =>
  (initialValue) =>
    functions.reduce(async (acc, fn) => fn(await acc), initialValue);
\f[R]
.fi
.SS Using reduce() with sparse arrays
.PP
\f[V]reduce()\f[R] skips missing elements in sparse arrays, but it does
not skip \f[V]undefined\f[R] values.
.IP
.nf
\f[C]
console.log([1, 2, , 4].reduce((a, b) => a + b)); // 7
console.log([1, 2, undefined, 4].reduce((a, b) => a + b)); // NaN
\f[R]
.fi
.SS Calling reduce() on non-array objects
.PP
The \f[V]reduce()\f[R] method reads the \f[V]length\f[R] property of
\f[V]this\f[R] and then accesses each integer index.
.IP
.nf
\f[C]
const arrayLike = {
  length: 3,
  0: 2,
  1: 3,
  2: 4,
};
console.log(Array.prototype.reduce.call(arrayLike, (x, y) => x + y));
// 9
\f[R]
.fi
.SS When to not use reduce()
.PP
Multipurpose higher-order functions like \f[V]reduce()\f[R] can be
powerful but sometimes difficult to understand, especially for
less-experienced JavaScript developers.
If code becomes clearer when using other array methods, developers must
weigh the readability tradeoff against the other benefits of using
\f[V]reduce()\f[R].
.PP
Note that \f[V]reduce()\f[R] is always equivalent to a
\f[V]for...of\f[R] loop, except that instead of mutating a variable in
the upper scope, we now return the new value for each iteration:
.IP
.nf
\f[C]
const val = array.reduce((acc, cur) => update(acc, cur), initialValue);

// Is equivalent to:
let val = initialValue;
for (const cur of array) {
  val = update(val, cur);
}
\f[R]
.fi
.PP
As previously stated, the reason why people may want to use
\f[V]reduce()\f[R] is to mimic functional programming practices of
immutable data.
Therefore, developers who uphold the immutability of the accumulator
often copy the entire accumulator for each iteration, like this:
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const names = [\[dq]Alice\[dq], \[dq]Bob\[dq], \[dq]Tiff\[dq], \[dq]Bruce\[dq], \[dq]Alice\[dq]];
const countedNames = names.reduce((allNames, name) => {
  const currCount = Object.hasOwn(allNames, name) ? allNames[name] : 0;
  return {
    ...allNames,
    [name]: currCount + 1,
  };
}, {});
\f[R]
.fi
.PP
This code is ill-performing, because each iteration has to copy the
entire \f[V]allNames\f[R] object, which could be big, depending how many
unique names there are.
This code has worst-case \f[V]O(N\[ha]2)\f[R] performance, where
\f[V]N\f[R] is the length of \f[V]names\f[R].
.PP
A better alternative is to \f[I]mutate\f[R] the \f[V]allNames\f[R]
object on each iteration.
However, if \f[V]allNames\f[R] gets mutated anyway, you may want to
convert the \f[V]reduce()\f[R] to a simple \f[V]for\f[R] loop instead,
which is much clearer:
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const names = [\[dq]Alice\[dq], \[dq]Bob\[dq], \[dq]Tiff\[dq], \[dq]Bruce\[dq], \[dq]Alice\[dq]];
const countedNames = names.reduce((allNames, name) => {
  const currCount = allNames[name] ?? 0;
  allNames[name] = currCount + 1;
  // return allNames, otherwise the next iteration receives undefined
  return allNames;
}, Object.create(null));
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const names = [\[dq]Alice\[dq], \[dq]Bob\[dq], \[dq]Tiff\[dq], \[dq]Bruce\[dq], \[dq]Alice\[dq]];
const countedNames = Object.create(null);
for (const name of names) {
  const currCount = countedNames[name] ?? 0;
  countedNames[name] = currCount + 1;
}
\f[R]
.fi
.PP
Therefore, if your accumulator is an array or an object and you are
copying the array or object on each iteration, you may accidentally
introduce quadratic complexity into your code, causing performance to
quickly degrade on large data.
.PP
Some of the acceptable use cases of \f[V]reduce()\f[R] are given above
(most notably, summing an array, promise sequencing, and function
piping).
There are other cases where better alternatives than \f[V]reduce()\f[R]
exist.
.IP \[bu] 2
Flattening an array of arrays.
Use \f[V]flat()\f[R] instead.
.RS 2
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const flattened = array.reduce((acc, cur) => acc.concat(cur), []);
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const flattened = array.flat();
\f[R]
.fi
.RE
.IP \[bu] 2
Grouping objects by a property.
Use \f[V]group()\f[R] instead.
.RS 2
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const groups = array.reduce((acc, obj) => {
  const key = obj.name;
  const curGroup = acc[key] ?? [];
  return { ...acc, [key]: [...curGroup, obj] };
}, {});
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const groups = array.group((obj) => obj.name);
\f[R]
.fi
.RE
.IP \[bu] 2
Concatenating arrays contained in an array of objects.
Use \f[V]flatMap()\f[R] instead.
.RS 2
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const friends = [
  { name: \[dq]Anna\[dq], books: [\[dq]Bible\[dq], \[dq]Harry Potter\[dq]] },
  { name: \[dq]Bob\[dq], books: [\[dq]War and peace\[dq], \[dq]Romeo and Juliet\[dq]] },
  { name: \[dq]Alice\[dq], books: [\[dq]The Lord of the Rings\[dq], \[dq]The Shining\[dq]] },
];
const allBooks = friends.reduce((acc, cur) => [...acc, ...cur.books], []);
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const allBooks = friends.flatMap((person) => person.books);
\f[R]
.fi
.RE
.IP \[bu] 2
Removing duplicate items in an array.
Use \f[V]Set\f[R] and \f[V]Array.from()\f[R] instead.
.RS 2
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const uniqArray = array.reduce(
  (acc, cur) => (acc.includes(cur) ? acc : [...acc, cur]),
  [],
);
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const uniqArray = Array.from(new Set(array));
\f[R]
.fi
.RE
.IP \[bu] 2
Eliminating or adding elements in an array.
Use \f[V]flatMap()\f[R] instead.
.RS 2
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
// Takes an array of numbers and splits perfect squares into its square roots
const roots = array.reduce((acc, cur) => {
  if (cur < 0) return acc;
  const root = Math.sqrt(cur);
  if (Number.isInteger(root)) return [...acc, root, root];
  return [...acc, cur];
}, []);
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const roots = array.flatMap((val) => {
  if (val < 0) return [];
  const root = Math.sqrt(val);
  if (Number.isInteger(root)) return [root, root];
  return [val];
});
\f[R]
.fi
.PP
If you are only eliminating elements from an array, you also can use
\f[V]filter()\f[R].
.RE
.IP \[bu] 2
Searching for elements or testing if elements satisfy a condition.
Use \f[V]find()\f[R] and \f[V]findIndex()\f[R], or \f[V]some()\f[R] and
\f[V]every()\f[R] instead.
These methods have the additional benefit that they return as soon as
the result is certain, without iterating the entire array.
.RS 2
.PP
\f[B]Bad\f[R]
.IP
.nf
\f[C]
const allEven = array.reduce((acc, cur) => acc && cur % 2 === 0, true);
\f[R]
.fi
.PP
\f[B]Good\f[R]
.IP
.nf
\f[C]
const allEven = array.every((val) => val % 2 === 0);
\f[R]
.fi
.RE
.PP
In cases where \f[V]reduce()\f[R] is the best choice, documentation and
semantic variable naming can help mitigate readability drawbacks.
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[V]Array.prototype.reduce\f[R] in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-array)
.IP \[bu] 2
Indexed collections
.IP \[bu] 2
\f[V]Array\f[R]
.IP \[bu] 2
\f[V]Array.prototype.group()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.groupToMap()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.map()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.flat()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.flatMap()\f[R]
.IP \[bu] 2
\f[V]Array.prototype.reduceRight()\f[R]
.IP \[bu] 2
\f[V]TypedArray.prototype.reduce()\f[R]
