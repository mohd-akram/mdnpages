.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "HighlightRegistry.delete" "JS" "December 2, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HighlightRegistry.delete \- HighlightRegistry: delete() method
.SH SYNOPSIS
The \f[B]\f[CB]delete()\f[B]\f[R] method of the
\f[CR]HighlightRegistry\f[R] interface removes a the named
\f[CR]Highlight\f[R] object from the \f[CR]HighlightRegistry\f[R].
.PP
\f[CR]HighlightRegistry\f[R] is a \f[CR]Map\f[R]\-like object, so this
is similar to using \f[CR]Map.delete()\f[R].
.SH SYNTAX
.IP
.EX
delete(customHighlightName)
.EE
.SS Parameters
.TP
\f[B]customHighlightName\f[R]
The name, as a \f[CR]String\f[R], of the \f[CR]Highlight\f[R] object to
remove from the \f[CR]HighlightRegistry\f[R].
.SS Return value
Returns \f[CR]true\f[R] if a \f[CR]Highlight\f[R] object under the
provided name was in the \f[CR]HighlightRegistry\f[R]; otherwise
\f[CR]false\f[R].
.SH EXAMPLES
The following code sample registers a highlight in the registry, and
then deletes it:
.IP
.EX
\f[B]const\f[R] myHighlight = \f[B]new\f[R] Highlight(range1, range2);

CSS.highlights.set(\[dq]my\-highlight\[dq], myHighlight);

CSS.highlights.delete(\[dq]foo\[dq]); \f[I]// false\f[R]
CSS.highlights.delete(\[dq]my\-highlight\[dq]); \f[I]// true\f[R]
.EE
.SH SEE ALSO
.IP \[bu] 2
The CSS Custom Highlight API
.IP \[bu] 2
\c
.UR https://css-tricks.com/css-custom-highlight-api-early-look/
CSS Custom Highlight API: The Future of Highlighting Text Ranges on the
Web
.UE \c
