.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "HTMLELEMENT.COMMAND_EVENT" "3JS" "May 2, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLElement.command_event \- HTMLElement: command event
.SH SYNOPSIS
The \f[B]\f[CB]command\f[B]\f[R] event of the \f[CR]HTMLElement\f[R]
interface fires on an element that is controlled via a \f[CR]button\f[R]
with valid \f[CR]commandForElement\f[R] and \f[CR]command\f[R] values,
whenever the button is interacted with (e.g., it is clicked).
.SH SYNTAX
Use the event name in methods like \f[CR]addEventListener()\f[R], or set
an event handler property.
.IP
.EX
addEventListener(\(dqcommand\(dq, (event) => { })

oncommand = (event) => { }
.EE
.SH EVENT TYPE
A \f[CR]CommandEvent\f[R].
Inherits from \f[CR]Event\f[R].
.SH EXAMPLES
.SS Basic example
.IP
.EX
\f[B]const\f[R] popover = document.getElementById(\(dqmypopover\(dq);

\f[I]// \&...\f[R]

popover.addEventListener(\(dqcommand\(dq, (event) \f[B]=>\f[R] {
  \f[B]if\f[R] (event.action === \(dqshow\-popover\(dq) {
    console.log(\(dqPopover is about to be shown\(dq);
  }
});
.EE
.SS Event dispatch and cancellation
It is worth pointing out that \f[CR]command\f[R] events fire on the
element being invoked.
If the button is clicked, it will first dispatch a \f[CR]click\f[R]
event which, if cancelled, then the \f[CR]command\f[R] event will not
fire and the default behavior will not be run.
In addition to cancelling the \f[CR]click\f[R] event on the button, it
is also possible to cancel the \f[CR]command\f[R] event.
.PP
For example:
.IP
.EX
button.addEventListener(\(dqclick\(dq, (event) \f[B]=>\f[R] {
  event.preventDefault(); \f[I]// the \(gacommand\(ga event will never fire\f[R]
});
.EE
.IP
.EX
element.addEventListener(\(dqcommand\(dq, (event) \f[B]=>\f[R] {
  event.preventDefault(); \f[I]// the \(gacommand\(ga event fires but the default behavior is cancelled\f[R]
});
.EE
.SH SEE ALSO
.IP \(bu 2
Invoker Commands API
.IP \(bu 2
\f[CR]HTMLButtonElement.command\f[R]
.IP \(bu 2
\f[CR]HTMLButtonElement.commandForElement\f[R]
