.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "TEXTTRACK.ADDCUE" "3JS" "July 26, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
TextTrack.addCue \- TextTrack: addCue() method
.SH SYNOPSIS
The \f[B]\f[CB]addCue()\f[B]\f[R] method of the \f[CR]TextTrack\f[R]
interface adds a new cue to the list of cues.
.SH SYNTAX
.IP
.EX
addCue(cue)
.EE
.SS Parameters
.TP
\f[B]cue\f[R]
A \f[CR]TextTrackCue\f[R].
.RS
.PP
\f[B]Note:\f[R] The \f[CR]TextTrackCue\f[R] interface is an abstract
class used as the parent for other cue interfaces such as
\f[CR]VTTCue\f[R].
Therefore, when adding a cue you will be using one of the cue types that
inherit from \f[CR]TextTrackCue\f[R].
.RE
.SS Return value
Undefined.
.SS Exceptions
.TP
\f[B]InvalidStateError\f[R] \f[B]DOMException\f[R]
Thrown if the rules for this \f[CR]TextTrackList\f[R] do not match those
that are appropriate for the incoming \f[CR]TextTrackCue\f[R].
.SH EXAMPLES
In the following example two cues are added to a video text track using
\f[CR]addCue()\f[R].
.IP
.EX
\f[B]let\f[R] video = document.querySelector(\(dqvideo\(dq);
\f[B]let\f[R] track = video.addTextTrack(\(dqcaptions\(dq, \(dqCaptions\(dq, \(dqen\(dq);
track.mode = \(dqshowing\(dq;
track.addCue(\f[B]new\f[R] VTTCue(0, 0.9, \(dqHildy!\(dq));
track.addCue(\f[B]new\f[R] VTTCue(1, 1.4, \(dqHow are you?\(dq));
.EE
