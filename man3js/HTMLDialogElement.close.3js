.\" Automatically generated by Pandoc 3.8
.\"
.TH "HTMLDIALOGELEMENT.CLOSE" "3JS" "January 26, 2026" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLDialogElement.close \- HTMLDialogElement: close() method
.SH SYNOPSIS
The \f[B]\f[CB]close()\f[B]\f[R] method of the
\f[CR]HTMLDialogElement\f[R] interface closes the \f[CR]<dialog>\f[R].
An optional string may be passed as an argument, updating the
\f[CR]returnValue\f[R] of the dialog.
.PP
The \f[CR]close\f[R] event is fired after the dialog has closed.
Unlike when calling \f[CR]HTMLDialogElement.requestClose()\f[R], the
close operation cannot be cancelled.
.SH SYNTAX
.IP
.EX
close()
close(returnValue)
.EE
.SS Parameters
.TP
\f[B]returnValue\f[R] \f[I](optional)\f[R]
A string that replaces the existing value of
\f[CR]HTMLDialogElement.returnValue\f[R].
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.SS Closing a dialog
The following example shows a button that, when clicked, opens a
\f[CR]<dialog>\f[R] via the \f[CR]showModal()\f[R] method.
From there you can click the either \f[I]Close\f[R] button to close the
dialog (via the \f[CR]close()\f[R] method).
.PP
The \f[I]Close\f[R] button closes the dialog without a
\f[CR]returnValue\f[R], while the \f[I]Close w/ return value\f[R] button
closes the dialog with a \f[CR]returnValue\f[R].
.SS HTML
.IP
.EX
<\f[B]dialog\f[R] id=\(dqdialog\(dq>
  <\f[B]button\f[R] type=\(dqbutton\(dq id=\(dqclose\(dq>Close</\f[B]button\f[R]>
  <\f[B]button\f[R] type=\(dqbutton\(dq id=\(dqclose\-w\-value\(dq>Close w/ return value</\f[B]button\f[R]>
</\f[B]dialog\f[R]>

<\f[B]button\f[R] id=\(dqopen\(dq>Open dialog</\f[B]button\f[R]>
.EE
.SS JavaScript
.IP
.EX
\f[B]const\f[R] dialog = document.getElementById(\(dqdialog\(dq);
\f[B]const\f[R] openButton = document.getElementById(\(dqopen\(dq);
\f[B]const\f[R] closeButton = document.getElementById(\(dqclose\(dq);
\f[B]const\f[R] closeWithValueButton = document.getElementById(\(dqclose\-w\-value\(dq);

\f[I]// Update button opens a modal dialog\f[R]
openButton.addEventListener(\(dqclick\(dq, () \f[B]=>\f[R] {
  \f[I]// Reset the return value\f[R]
  dialog.returnValue = \(dq\(dq;
  \f[I]// Show the dialog\f[R]
  dialog.showModal();
});

\f[I]// Close button closes the dialog box\f[R]
closeButton.addEventListener(\(dqclick\(dq, () \f[B]=>\f[R] {
  dialog.close();
});

\f[I]// Close button closes the dialog box with a return value\f[R]
closeWithValueButton.addEventListener(\(dqclick\(dq, () \f[B]=>\f[R] {
  dialog.close(\(gaClosed at ${\f[B]new\f[R] Date().toLocaleTimeString()}\(ga);
});

\f[I]// Form close button closes the dialog box\f[R]
dialog.addEventListener(\(dqclose\(dq, () \f[B]=>\f[R] {
  log(\(gaDialog closed. Return value: \(dq${dialog.returnValue}\(dq\(ga);
});
.EE
.RS
.PP
\f[B]Note:\f[R]
.PP
You know you can also automatically close a \f[CR]<dialog>\f[R] by
submitting a \f[CR]<form>\f[R] element with a
\f[CR]method=\(dqdialog\(dq\f[R] attribute.
.RE
.SS Result
.SH SEE ALSO
.IP \(bu 2
HTML \f[CR]<dialog>\f[R] element
.IP \(bu 2
The \f[CR]close\f[R] event
.IP \(bu 2
\f[CR]HTMLDialogElement.requestClose()\f[R]
