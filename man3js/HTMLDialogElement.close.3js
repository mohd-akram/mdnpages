.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "HTMLDialogElement.close" "JS" "April 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
HTMLDialogElement.close \- HTMLDialogElement: close() method
.SH SYNOPSIS
The \f[B]\f[CB]close()\f[B]\f[R] method of the
\f[CR]HTMLDialogElement\f[R] interface closes the \f[CR]<dialog>\f[R].
An optional string may be passed as an argument, updating the
\f[CR]returnValue\f[R] of the dialog.
.SH SYNTAX
.IP
.EX
close()
close(returnValue)
.EE
.SS Parameters
.TP
\f[B]returnValue\f[R] \f[I](optional)\f[R]
A string representing an updated value for the
\f[CR]HTMLDialogElement.returnValue\f[R] of the dialog.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
The following example shows a simple button that, when clicked, opens a
\f[CR]<dialog>\f[R] containing a form via the \f[CR]showModal()\f[R]
method.
From there you can click the \f[I]X\f[R] button to close the dialog (via
the \f[CR]HTMLDialogElement.close()\f[R] method), or submit the form via
the submit button.
.IP
.EX
<!\-\- Simple pop\-up dialog box, containing a form \-\->
<dialog id=\[dq]favDialog\[dq]>
  <form method=\[dq]dialog\[dq]>
    <button id=\[dq]close\[dq] aria\-label=\[dq]close\[dq] formnovalidate>X</button>
    <section>
      <p>
        <label for=\[dq]favAnimal\[dq]>Favorite animal:</label>
        <select id=\[dq]favAnimal\[dq] name=\[dq]favAnimal\[dq]>
          <option></option>
          <option>Brine shrimp</option>
          <option>Red panda</option>
          <option>Spider monkey</option>
        </select>
      </p>
    </section>
    <menu>
      <button type=\[dq]reset\[dq]>Reset</button>
      <button type=\[dq]submit\[dq]>Confirm</button>
    </menu>
  </form>
</dialog>

<menu>
  <button id=\[dq]updateDetails\[dq]>Update details</button>
</menu>

<script>
  (() => {
    const updateButton = document.getElementById(\[dq]updateDetails\[dq]);
    const closeButton = document.getElementById(\[dq]close\[dq]);
    const dialog = document.getElementById(\[dq]favDialog\[dq]);
    dialog.returnValue = \[dq]favAnimal\[dq];

    function openCheck(dialog) {
      if (dialog.open) {
        console.log(\[dq]Dialog open\[dq]);
      } else {
        console.log(\[dq]Dialog closed\[dq]);
      }
    }

    // Update button opens a modal dialog
    updateButton.addEventListener(\[dq]click\[dq], () => {
      dialog.showModal();
      openCheck(dialog);
    });

    // Form close button closes the dialog box
    closeButton.addEventListener(\[dq]click\[dq], () => {
      dialog.close(\[dq]animalNotChosen\[dq]);
      openCheck(dialog);
    });
  })();
</script>
.EE
.PP
If the \[lq]X\[rq] button was of \f[CR]type=\[dq]submit\[dq]\f[R], the
dialog would have closed without requiring JavaScript.
A form submission closes the \f[CR]<dialog>\f[R] it is nested within if
the form\[cq]s method is \f[CR]dialog\f[R], so no \[lq]close\[rq] button
is required.
.SS Result
.SH SEE ALSO
.IP \[bu] 2
The HTML element implementing this interface: \f[CR]<dialog>\f[R].
