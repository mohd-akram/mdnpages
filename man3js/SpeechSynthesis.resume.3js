.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "SpeechSynthesis.resume" "JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
SpeechSynthesis.resume \- SpeechSynthesis: resume() method
.SH SYNOPSIS
The \f[B]\f[CB]resume()\f[B]\f[R] method of the
\f[CR]SpeechSynthesis\f[R] interface puts the \f[CR]SpeechSynthesis\f[R]
object into a non\-paused state: resumes it if it was already paused.
.SH SYNTAX
.IP
.EX
resume()
.EE
.SS Parameters
None.
.SS Return value
None (\f[CR]undefined\f[R]).
.SH EXAMPLES
.IP
.EX
let synth = window.speechSynthesis;

let utterance1 = new SpeechSynthesisUtterance(
  \[dq]How about we say this now? This is quite a long sentence to say.\[dq],
);
let utterance2 = new SpeechSynthesisUtterance(
  \[dq]We should say another sentence too, just to be on the safe side.\[dq],
);

synth.speak(utterance1);
synth.speak(utterance2);

synth.pause(); // pauses utterances being spoken
synth.resume(); // resumes speaking
.EE
.SH SEE ALSO
.IP \[bu] 2
Web Speech API
