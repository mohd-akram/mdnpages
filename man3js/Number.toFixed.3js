.\" Automatically generated by Pandoc 3.1.8
.\"
.TH "Number.toFixed" "JS" "April 12, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Number.toFixed - Number.prototype.toFixed()
.SH SYNOPSIS
The \f[B]\f[CB]toFixed()\f[B]\f[R] method formats a number using
fixed-point notation.
.SH SYNTAX
.IP
.EX
toFixed()
toFixed(digits)
.EE
.SS Parameters
.TP
\f[B]digits\f[R] \f[I](optional)\f[R]
The number of digits to appear after the decimal point; should be a
value between \f[CR]0\f[R] and \f[CR]100\f[R], inclusive.
If this argument is omitted, it is treated as \f[CR]0\f[R].
.SS Return value
A string representing the given number using fixed-point notation.
.SS Exceptions
.TP
\f[B]RangeError\f[R]
Thrown if \f[CR]digits\f[R] is not between \f[CR]1\f[R] and
\f[CR]100\f[R] (inclusive).
.TP
\f[B]TypeError\f[R]
Thrown if this method is invoked on an object that is not a
\f[CR]Number\f[R].
.SH DESCRIPTION
The \f[CR]toFixed()\f[R] method returns a string representation of
\f[CR]numObj\f[R] that does not use exponential notation and has exactly
\f[CR]digits\f[R] digits after the decimal place.
The number is rounded if necessary, and the fractional part is padded
with zeros if necessary so that it has the specified length.
.PP
If the absolute value of \f[CR]numObj\f[R] is greater or equal to 1021,
this method uses the same algorithm as
\f[CR]Number.prototype.toString()\f[R] and returns a string in
exponential notation.
\f[CR]toFixed()\f[R] returns \f[CR]\[dq]Infinity\[dq]\f[R],
\f[CR]\[dq]NaN\[dq]\f[R], or \f[CR]\[dq]-Infinity\[dq]\f[R] if the value
of \f[CR]numObj\f[R] is non-finite.
.PP
The output of \f[CR]toFixed()\f[R] may be more precise than
\f[CR]toString()\f[R] for some values, because \f[CR]toString()\f[R]
only prints enough significant digits to distinguish the number from
adjacent number values.
For example:
.IP
.EX
(1000000000000000128).toString(); // \[aq]1000000000000000100\[aq]
(1000000000000000128).toFixed(0); // \[aq]1000000000000000128\[aq]
.EE
.PP
However, choosing a \f[CR]digits\f[R] precision that\[cq]s too high can
return unexpected results, because decimal fractional numbers cannot be
represented precisely in floating point.
For example:
.IP
.EX
(0.3).toFixed(50); // \[aq]0.29999999999999998889776975374843459576368331909180\[aq]
.EE
.SH EXAMPLES
.SS Using toFixed()
.IP
.EX
const numObj = 12345.6789;

numObj.toFixed(); // \[aq]12346\[aq]; rounding, no fractional part
numObj.toFixed(1); // \[aq]12345.7\[aq]; it rounds up
numObj.toFixed(6); // \[aq]12345.678900\[aq]; additional zeros
(1.23e20).toFixed(2); // \[aq]123000000000000000000.00\[aq]
(1.23e-10).toFixed(2); // \[aq]0.00\[aq]
(2.34).toFixed(1); // \[aq]2.3\[aq]
(2.35).toFixed(1); // \[aq]2.4\[aq]; it rounds up
(2.55).toFixed(1); // \[aq]2.5\[aq]
// it rounds down as it can\[aq]t be represented exactly by a float and the
// closest representable float is lower
(2.449999999999999999).toFixed(1); // \[aq]2.5\[aq]
// it rounds up as it\[aq]s less than NUMBER.EPSILON away from 2.45.
// This literal actually encodes the same number value as 2.45

(6.02 * 10 ** 23).toFixed(50); // 6.019999999999999e+23; large numbers still use exponential notation
.EE
.SS Using toFixed() with negative numbers
Because member access has higher precedence than unary minus, you need
to group the negative number expression to get a string.
.IP
.EX
-2.34.toFixed(1); // -2.3, a number
(-2.34).toFixed(1); // \[aq]-2.3\[aq]
.EE
.SH SEE ALSO
.IP \[bu] 2
\f[CR]Number.prototype.toExponential()\f[R]
.IP \[bu] 2
\f[CR]Number.prototype.toPrecision()\f[R]
.IP \[bu] 2
\f[CR]Number.prototype.toString()\f[R]
.IP \[bu] 2
\f[CR]Number.EPSILON\f[R]
