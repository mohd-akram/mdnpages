.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "WindowClient.focused" "JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
WindowClient.focused \- WindowClient: focused property
.SH SYNOPSIS
The \f[B]\f[CB]focused\f[B]\f[R] read\-only property of the
\f[CR]WindowClient\f[R] interface is a boolean value that indicates
whether the current client has focus.
.SH VALUE
A boolean value.
.SH EXAMPLES
.IP
.EX
self.addEventListener(\[dq]notificationclick\[dq], (event) => {
  console.log(\[dq]On notification click: \[dq], event.notification.tag);
  event.notification.close();

  // This looks to see if the current is already open and
  // focuses if it is
  event.waitUntil(
    clients
      .matchAll({
        type: \[dq]window\[dq],
      })
      .then((clientList) => {
        for (const client of clientList) {
          if (client.url === \[dq]/\[dq] && \[dq]focus\[dq] in client) {
            if (!client.focused) return client.focus();
          }
        }

        if (clients.openWindow) return clients.openWindow(\[dq]/\[dq]);
      }),
  );
});
.EE
