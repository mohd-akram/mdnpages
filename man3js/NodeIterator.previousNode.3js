.\" Automatically generated by Pandoc 3.1.11
.\"
.TH "NodeIterator.previousNode" "JS" "July 7, 2023" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NodeIterator.previousNode \- NodeIterator: previousNode() method
.SH SYNOPSIS
The \f[B]\f[CB]NodeIterator.previousNode()\f[B]\f[R] method returns the
previous node in the set represented by the \f[CR]NodeIterator\f[R] and
moves the position of the iterator backwards within the set.
.PP
This method returns \f[CR]null\f[R] when the current node is the first
node in the set.
.PP
In old browsers, as specified in old versions of the specifications, the
method may throws the \f[CR]INVALID_STATE_ERR\f[R]
\f[CR]DOMException\f[R] if this method is called after the
\f[CR]NodeIterator.detach()\f[R]method.
Recent browsers never throw.
.SH SYNTAX
.IP
.EX
previousNode()
.EE
.SS Parameters
None.
.SS Return value
A \f[CR]Node\f[R] representing the node before the current node in the
set represented by this \f[CR]NodeIterator\f[R], or \f[CR]null\f[R] if
the current node is the first node in the set.
.SH EXAMPLES
.IP
.EX
const nodeIterator = document.createNodeIterator(
  document.body,
  NodeFilter.SHOW_ELEMENT,
  {
    acceptNode(node) {
      return NodeFilter.FILTER_ACCEPT;
    },
  },
);
currentNode = nodeIterator.nextNode(); // returns the next node
previousNode = nodeIterator.previousNode(); // same result, since we backtracked to the previous node
.EE
.SH SEE ALSO
.IP \[bu] 2
The interface it belongs to: \f[CR]NodeIterator\f[R].
