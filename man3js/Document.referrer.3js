.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "DOCUMENT.REFERRER" "3JS" "March 13, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Document.referrer \- Document: referrer property
.SH SYNOPSIS
The \f[B]\f[CB]Document.referrer\f[B]\f[R] property returns the \c
.UR https://www.w3.org/Addressing/#background
URI
.UE \c
\ of the page that linked to this page.
.SH VALUE
The value is an empty string if the user navigated to the page directly
(not through a link, but, for example, by using a bookmark).
Because this property returns only a string, it doesn\(cqt give you
document object model (DOM) access to the referring page.
.PP
Inside an \f[CR]<iframe>\f[R], the \f[CR]Document.referrer\f[R] will
initially be set to the \f[CR]href\f[R] of the parent\(cqs
\f[CR]Window.location\f[R] in same\-origin requests.
In cross\-origin requests, it\(cqs the \f[CR]origin\f[R] of the
parent\(cqs \f[CR]Window.location\f[R] by default.
For more information, see the Referrer\-Policy:
strict\-origin\-when\-cross\-origin documentation.
.SH EXAMPLES
The following will log a string containing the document\(cqs referrer.
.IP
.EX
console.log(document.referrer);
.EE
.PP
If the user navigated to the page via a link like
\f[CR]<a href=\(dqhttps://www.w3.org/\(dq>W3</a>\f[R], then it will
output the previous domain like \f[CR]developer.mozilla.org\f[R].
If the user navigated to the page directly, it will output an empty
string.
