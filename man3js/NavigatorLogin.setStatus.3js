.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "NAVIGATORLOGIN.SETSTATUS" "3JS" "April 3, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
NavigatorLogin.setStatus \- NavigatorLogin: setStatus() method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
The \f[B]\f[CB]setStatus()\f[B]\f[R] method of the
\f[CR]NavigatorLogin\f[R] interface sets the login status of a federated
identity provider (IdP), when called from the IdP\(cqs origin.
By this, we mean \(lqwhether any users are logged into the IdP on the
current browser or not\(rq.
This should be called by the IdP site following a user login or logout.
.PP
The browser stores this state for each IdP; the FedCM API then uses it
to reduce the number of requests it makes to the IdP (because it does
not need to waste time requesting accounts when there are no users
logged in to the IdP).
It also mitigates \c
.UR https://github.com/w3c-fedid/FedCM/issues/447
potential timing attacks
.UE \c
\&.
.PP
See Update login status using the Login Status API for more information
about FedCM login status.
.SH SYNTAX
.IP
.EX
setStatus(status)
.EE
.SS Parameters
.TP
\f[B]status\f[R]
A string representing the login status to set for the IdP.
Possible values are:
.RS
.IP \(bu 2
\f[CR]\(dqlogged\-in\(dq\f[R]: The IdP has at least one user account
signed in.
.IP \(bu 2
\f[CR]\(dqlogged\-out\(dq\f[R]: All IdP user accounts are currently
signed out.
.RE
.SS Return value
A \f[CR]Promise\f[R] that fulfills with \f[CR]undefined\f[R].
.SS Exceptions
.TP
\f[B]SecurityError\f[R] \f[B]DOMException\f[R]
Thrown if the calling domain is not in a frame where all of the nesting
hierarchy is same\-origin.
Whether called from the main frame, an \f[CR]<iframe>\f[R] nested inside
the main frame, or another \f[CR]<iframe>\f[R] nested one or more levels
deep inside the first \f[CR]<iframe>\f[R], \f[I]all\f[R] levels of the
nesting hierarchy must be same\-origin for the call to be successful.
.SH EXAMPLES
.IP
.EX
\f[I]/* Set logged\-in status */\f[R]
navigator.login.setStatus(\(dqlogged\-in\(dq);

\f[I]/* Set logged\-out status */\f[R]
navigator.login.setStatus(\(dqlogged\-out\(dq);
.EE
.SH SEE ALSO
.IP \(bu 2
Federated Credential Management (FedCM) API
