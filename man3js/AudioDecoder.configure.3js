.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "AUDIODECODER.CONFIGURE" "3JS" "May 28, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
AudioDecoder.configure \- AudioDecoder: configure() method
.SH SYNOPSIS
\f[B]Secure context:\f[R] This feature is available only in secure
contexts (HTTPS).
.PP
\f[B]Note:\f[R] This feature is available in Dedicated Web Workers.
.PP
The \f[B]\f[CB]configure()\f[B]\f[R] method of the
\f[CR]AudioDecoder\f[R] interface enqueues a control message to
configure the audio decoder for decoding chunks.
.SH SYNTAX
.IP
.EX
configure(config)
.EE
.SS Parameters
.TP
\f[B]config\f[R]
A dictionary object containing the following members:
.RS
.TP
\f[B]codec\f[R]
A string containing a \c
.UR https://w3c.github.io/webcodecs/codec_registry.html#audio-codec-registry
valid codec string
.UE \c
\&.
See \(lqcodecs\(rq parameter for details on codec string construction.
.TP
\f[B]sampleRate\f[R]
An integer representing the number of frame samples per second.
.TP
\f[B]numberOfChannels\f[R]
An integer representing the number of audio channels.
.TP
\f[B]description\f[R] \f[I](optional)\f[R]
An \f[CR]ArrayBuffer\f[R], a \f[CR]TypedArray\f[R], or a
\f[CR]DataView\f[R] containing a sequence of codec specific bytes,
commonly known as extradata.
.RE
.RS
.PP
\f[B]Note:\f[R] The registrations in the \c
.UR https://w3c.github.io/webcodecs/codec_registry.html#audio-codec-registry
WebCodecs Codec Registry
.UE \c
\ link to a specification detailing whether and how to populate the
optional \f[CR]description\f[R] member.
.RE
.SS Return value
None (\f[CR]undefined\f[R]).
.SS Exceptions
.TP
\f[B]TypeError\f[R]
Thrown if the provided \f[CR]config\f[R] is invalid.
.TP
\f[B]InvalidStateError\f[R] \f[B]DOMException\f[R]
Thrown if the \f[CR]state\f[R] is \f[CR]\(dqclosed\(dq\f[R].
.TP
\f[B]NotSupportedError\f[R] \f[B]DOMException\f[R]
Thrown if the provided \f[CR]config\f[R] is valid but the user agent
cannot provide a codec that can decode this profile.
.SH EXAMPLES
The following example configures the \f[CR]audioDecoder\f[R] with the
\f[CR]opus\f[R] codec.
.IP
.EX
audioDecoder.configure({
  codec: \(dqopus\(dq,
  sampleRate: 44100,
  numberOfChannels: 2,
});
.EE
