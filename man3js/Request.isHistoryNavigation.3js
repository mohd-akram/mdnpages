.\" Automatically generated by Pandoc 3.7.0.1
.\"
.TH "REQUEST.ISHISTORYNAVIGATION" "3JS" "May 23, 2025" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Request.isHistoryNavigation \- Request: isHistoryNavigation property
.SH SYNOPSIS
\f[B]Note:\f[R] This feature is available in Web Workers.
.PP
The \f[B]\f[CB]isHistoryNavigation\f[B]\f[R] read\-only property of the
\f[CR]Request\f[R] interface is a boolean indicating whether the request
is a history navigation.
.PP
A history navigation is a navigation within the browser\(cqs history,
made by calling \f[CR]History.go()\f[R], \f[CR]History.back()\f[R],
\f[CR]History.forward()\f[R], \f[CR]Navigation.traverseTo()\f[R],
\f[CR]Navigation.back()\f[R], \f[CR]Navigation.forward()\f[R], or
directly by clicking the browser\(cqs back or forward navigation button.
.SH VALUE
A boolean value.
.SH EXAMPLES
This example executes in a service worker.
It listens for the \f[CR]fetch\f[R] event.
In the event handler, the service worker checks the
\f[CR]isHistoryNavigation\f[R] property to know whether the request
happened because of a history navigation.
If so, it attempts to respond with a cached response.
If the cache does not contain a response for this request, the service
worker fetches a response from the network, caches a clone of it, and
responds with the network response.
.IP
.EX
self.addEventListener(\(dqrequest\(dq, (event) \f[B]=>\f[R] {
  \f[I]// \&...\f[R]

  \f[B]if\f[R] (event.request.isHistoryNavigation) {
    event.respondWith(
      caches.match(event.request).then((response) \f[B]=>\f[R] {
        \f[B]if\f[R] (response !== \f[B]undefined\f[R]) {
          \f[B]return\f[R] response;
        }
        \f[B]return\f[R] fetch(event.request).then((response) \f[B]=>\f[R] {
          \f[B]const\f[R] responseClone = response.clone();

          caches
            .open(\(dqv1\(dq)
            .then((cache) \f[B]=>\f[R] cache.put(event.request, responseClone));

          \f[B]return\f[R] response;
        });
      }),
    );
  }

  \f[I]// \&...\f[R]
});
.EE
.SH SEE ALSO
.IP \(bu 2
History API
.IP \(bu 2
Navigation API
.IP \(bu 2
Service Worker API
