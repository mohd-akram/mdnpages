.\" Automatically generated by Pandoc 3.6.2
.\"
.TH "Touch.pageY" "JS" "July 25, 2024" "JavaScript" "JavaScript Reference Manual"
.SH NAME
Touch.pageY \- Touch: pageY property
.SH SYNOPSIS
The \f[B]\f[CB]Touch.pageY\f[B]\f[R] read\-only property returns the Y
coordinate of the touch point relative to the viewport, including any
scroll offset.
.SH VALUE
A \f[CR]double\f[R] floating point value representing the Y coordinate
of the touch point relative to the viewport, including any scroll
offset.
.SH EXAMPLES
This example illustrates how to access the \f[CR]Touch\f[R] object\[cq]s
\f[CR]Touch.pageX\f[R] and \f[CR]Touch.pageY\f[R] properties.
The \f[CR]Touch.pageX\f[R] property is the horizontal coordinate of a
touch point relative to the viewport (in CSS pixels), including any
scroll offset.
The \f[CR]Touch.pageY\f[R] property is the vertical coordinate of a
touch point relative to the viewport (in CSS pixels), including any
scroll offset.
.PP
In following simple code snippet, we assume the user initiates one or
more touch contacts on the \f[CR]source\f[R] element, moves the touch
points and then releases all contacts with the surface.
When the \f[CR]touchmove\f[R] event handler is invoked, each touch
point\[cq]s \f[CR]Touch.pageX\f[R] and \f[CR]Touch.pageY\f[R]
coordinates are accessed via the event\[cq]s
\f[CR]TouchEvent.changedTouches\f[R] list.
.IP
.EX
\f[I]// Register a touchmove listeners for the \[aq]source\[aq] element\f[R]
\f[B]const\f[R] src = document.getElementById(\[dq]source\[dq]);

src.addEventListener(
  \[dq]touchmove\[dq],
  (e) \f[B]=>\f[R] {
    \f[I]// Iterate through the touch points that have moved and log each\f[R]
    \f[I]// of the pageX/Y coordinates. The unit of each coordinate is CSS pixels.\f[R]
    \f[B]for\f[R] (\f[B]let\f[R] i = 0; i < e.changedTouches.length; i++) {
      console.log(\[ga]touchpoint[${i}].pageX = ${e.changedTouches[i].pageX}\[ga]);
      console.log(\[ga]touchpoint[${i}].pageY = ${e.changedTouches[i].pageY}\[ga]);
    }
  },
  \f[B]false\f[R],
);
.EE
