.\" Automatically generated by Pandoc 3.1.1
.\"
.\" Define V font for inline verbatim, using C font in formats
.\" that render this, and otherwise B font.
.ie "\f[CB]x\f[]"x" \{\
. ftr V B
. ftr VI BI
. ftr VB B
. ftr VBI BI
.\}
.el \{\
. ftr V CR
. ftr VI CI
. ftr VB CB
. ftr VBI CBI
.\}
.TH "Object.is" "JS" "June 2, 2023" "JavaScript" "JavaScript Reference Manual"
.hy
.SH NAME
.PP
Object.is - Object.is()
.SH SYNOPSIS
.PP
The \f[B]\f[VB]Object.is()\f[B]\f[R] static method determines whether
two values are the same value.
.SH SYNTAX
.IP
.nf
\f[C]
Object.is(value1, value2)
\f[R]
.fi
.SS Parameters
.TP
\f[B]value1\f[R]
The first value to compare.
.TP
\f[B]value2\f[R]
The second value to compare.
.SS Return value
.PP
A boolean indicating whether or not the two arguments are the same
value.
.SH DESCRIPTION
.PP
\f[V]Object.is()\f[R] determines whether two values are the same value.
Two values are the same if one of the following holds:
.IP \[bu] 2
both \f[V]undefined\f[R]
.IP \[bu] 2
both \f[V]null\f[R]
.IP \[bu] 2
both \f[V]true\f[R] or both \f[V]false\f[R]
.IP \[bu] 2
both strings of the same length with the same characters in the same
order
.IP \[bu] 2
both the same object (meaning both values reference the same object in
memory)
.IP \[bu] 2
both BigInts with the same numeric value
.IP \[bu] 2
both symbols that reference the same symbol value
.IP \[bu] 2
both numbers and
.RS 2
.IP \[bu] 2
both \f[V]+0\f[R]
.IP \[bu] 2
both \f[V]-0\f[R]
.IP \[bu] 2
both \f[V]NaN\f[R]
.IP \[bu] 2
or both non-zero, not \f[V]NaN\f[R], and have the same value
.RE
.PP
\f[V]Object.is()\f[R] is not equivalent to the \f[V]==\f[R] operator.
The \f[V]==\f[R] operator applies various coercions to both sides (if
they are not the same type) before testing for equality (resulting in
such behavior as \f[V]\[dq]\[dq] == false\f[R] being \f[V]true\f[R]),
but \f[V]Object.is()\f[R] doesn\[cq]t coerce either value.
.PP
\f[V]Object.is()\f[R] is also \f[I]not\f[R] equivalent to the
\f[V]===\f[R] operator.
The only difference between \f[V]Object.is()\f[R] and \f[V]===\f[R] is
in their treatment of signed zeros and \f[V]NaN\f[R] values.
The \f[V]===\f[R] operator (and the \f[V]==\f[R] operator) treats the
number values \f[V]-0\f[R] and \f[V]+0\f[R] as equal, but treats
\f[V]NaN\f[R] as not equal to each other.
.SH EXAMPLES
.SS Using Object.is()
.IP
.nf
\f[C]
// Case 1: Evaluation result is the same as using ===
Object.is(25, 25); // true
Object.is(\[dq]foo\[dq], \[dq]foo\[dq]); // true
Object.is(\[dq]foo\[dq], \[dq]bar\[dq]); // false
Object.is(null, null); // true
Object.is(undefined, undefined); // true
Object.is(window, window); // true
Object.is([], []); // false
const foo = { a: 1 };
const bar = { a: 1 };
const sameFoo = foo;
Object.is(foo, foo); // true
Object.is(foo, bar); // false
Object.is(foo, sameFoo); // true

// Case 2: Signed zero
Object.is(0, -0); // false
Object.is(+0, -0); // false
Object.is(-0, -0); // true

// Case 3: NaN
Object.is(NaN, 0 / 0); // true
Object.is(NaN, Number.NaN); // true
\f[R]
.fi
.SH SEE ALSO
.IP \[bu] 2
Polyfill of \f[V]Object.is\f[R] in
\f[V]core-js\f[R] (https://github.com/zloirock/core-js#ecmascript-object)
.IP \[bu] 2
Equality comparisons and sameness \[em] a comparison of all three
built-in sameness facilities
